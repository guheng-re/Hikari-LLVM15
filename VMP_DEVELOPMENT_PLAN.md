# VMP（虚拟机保护）开发计划

## 1. 目标

在当前 Hikari-LLVM15（Hikari 的 LLVM 15 移植版）中增加可选择启用的 VMP（虚拟机保护）编译遍（Pass）。受保护的函数将由原始 LLVM（编译器框架）IR（中间表示）控制流转换为私有字节码、虚拟寄存器状态和调度循环，同时保持函数对外 ABI（应用二进制接口）与可观察行为不变。

首个可交付版本以“正确性优先、覆盖面可控”为原则；不把 VMP（虚拟机保护）加入 `enable-allobf`，避免无差别扩大编译时间、二进制体积和运行开销。

## 2. 当前工程接入点

现有混淆调度器已经位于以下位置：

- `llvm/lib/Transforms/Obfuscation/Obfuscation.cpp`（混淆调度器与开关）；
- `llvm/lib/Transforms/Obfuscation/Utils.cpp`（函数标记读取与筛选）；
- `llvm/lib/Transforms/Obfuscation/CMakeLists.txt`（混淆组件构建清单）；
- `llvm/lib/Passes/PassBuilderPipelines.cpp`（新版编译遍流程）；
- `llvm/lib/Transforms/IPO/PassManagerBuilder.cpp`（旧版编译遍流程）。

现有调度器在优化流程起点执行。VMP（虚拟机保护）必须改为优化后期执行，或从现有调度器中拆出一个后期调度入口；否则常量传播、控制流简化等后续优化可能显著削弱虚拟机调度结构。

## 3. 功能边界

### 第一阶段支持

- 整数与指针的常用算术、位运算、比较和类型转换；
- 条件跳转、无条件跳转与返回；
- 栈变量、普通内存读写和全局变量访问；
- 标量与地址空间零指针的原子加载、存储，以及内存序和同步域保留；
- 固定参数的直接函数调用；
- `vmp`（启用标记）与 `novmp`（禁用标记）进行逐函数筛选；
- 编译器开关 `-mllvm -enable-vmpobf` 与环境变量 `VMPOBF`（启用环境变量）。

### 第一阶段明确跳过

- EH（异常处理）相关指令与着陆块；
- 协程、超出保守 C（编程语言）`va_start`/`va_arg`/`va_end` 子集的可变参数、`musttail`（强制尾调用）、超出受限直接 `CallInst` 表面的内联汇编，以及超出受限直接 `InlineAsm` `CallBr` 表面的 `callbr`（带标签调用）；
- 原子读改写、比较交换、栅栏、线程局部特殊语义、Swift（苹果语言）错误参数和垃圾回收状态；其中原子读改写、比较交换与栅栏已按受限类型、地址空间、内存序与同步域子集实现支持，地址空间零的 `thread_local` GlobalValue 已作为普通指针常量接受，其余形态仍安全跳过；
- 超出受限规则子集的间接调用、递归边界不清晰的调用图，以及无法安全降低的向量或聚合类型。

被跳过的函数应保留原始实现，输出明确的诊断信息；绝不能生成语义不完整的虚拟化结果。

## 4. 实现架构

1. **资格检查**：复用 `toObfuscate`（函数筛选函数），新增 `vmp` 属性；在转换前扫描函数属性、指令类型和异常边，建立“可虚拟化 / 跳过原因”结果。
2. **规范化**：降低多路分支；将跨基本块的 SSA（静态单赋值）值安全地保存到虚拟帧；保留调试位置和调用属性中与语义相关的部分。
3. **字节码生成**：为支持的 LLVM（编译器框架）IR（中间表示）指令建立操作码、操作数索引、常量池和基本块地址映射。操作码编号、基本块编号及常量编码使用现有随机数工具派生的种子。
4. **虚拟帧**：在函数内分配虚拟程序计数器、虚拟寄存器槽和临时状态。对于指针值，保留原始指针宽度与地址空间；不把原生指针截断为固定宽度整数。
5. **调度器与处理器**：生成主调度循环，根据解码后的操作码分派至处理器；处理器更新虚拟状态，再回到调度器。控制流跳转仅通过虚拟程序计数器改变。
6. **收尾**：替换原函数体、删除已迁移的基本块、运行 LLVM（编译器框架）验证器，并为调度器加入必要的防优化约束。

虚拟机解释器优先内联到受保护函数，不依赖外部运行时库；这可避免链接步骤、跨平台部署和符号暴露带来的额外问题。

## 5. 文件与接口改动

- 新增 `llvm/lib/Transforms/Obfuscation/Virtualization.cpp`（VMP 实现）；
- 新增 `llvm/include/llvm/Transforms/Obfuscation/Virtualization.h`（VMP 接口）；
- 更新 `llvm/lib/Transforms/Obfuscation/CMakeLists.txt`（纳入构建）；
- 更新 `llvm/lib/Transforms/Obfuscation/Obfuscation.h` 与 `Obfuscation.cpp`（开关、工厂函数、逐函数调度）；
- 更新新版和旧版流程入口，使 VMP（虚拟机保护）在优化后期运行；
- 新增 `llvm/test/Transforms/Obfuscation/` 下的回归用例。

开关默认关闭；`enable-allobf` 保持不包含 VMP（虚拟机保护）。只有显式选择的函数或显式全局开关才会进入转换。

## 6. 执行顺序

建议的顺序如下：

1. 正常优化与内联；
2. 字符串加密、调用点混淆等与函数主体独立的变换；
3. 对未选择 VMP（虚拟机保护）的函数执行常规 OLLVM（混淆 LLVM）函数体变换；
4. 对已选择函数执行 VMP（虚拟机保护），生成字节码表、虚拟寄存器和解释器外壳；
5. 对同时选择 `fla`（控制流平坦化标记）的已虚拟化函数执行专用后期 CFF（控制流平坦化）；
6. 运行验证器并直接进入代码生成，不再执行会还原或大幅简化调度结构的常规优化。

叠加顺序固定为 VMP（虚拟机保护）→ OLLVM（混淆 LLVM）。早期调度器继续排除已选择 VMP（虚拟机保护）的函数体变换，避免形成 OLLVM（混淆 LLVM）→ VMP（虚拟机保护）的反向顺序。第一步只开放后期 CFF（控制流平坦化）；基本块拆分、伪控制流、指令替换和间接分支需分别完成兼容性验证后才能加入后处理链。

## 7. 里程碑与验收

### 里程碑一：骨架与筛选

- 建立编译遍（Pass）、开关、标记与构建注册；
- 对不支持函数可靠跳过并记录原因；
- 完成空转换和 LLVM（编译器框架）验证器测试。

**验收条件**：全量编译成功；未启用开关时产物不变；支持与跳过路径均有回归测试。

### 里程碑二：最小指令集

- 完成整数、比较、分支、返回和虚拟寄存器；
- 完成基本块地址映射、随机操作码与调度循环；
- 建立原始函数与虚拟化函数的输出对比测试。

**验收条件**：在 `O0`（无优化）和 `O2`（常规优化）下通过语义测试，且验证器无错误。

### 里程碑三：内存与调用

- 增加地址空间零指针的加载、存储、静态栈分配、指针合流、`GEP`（获取元素指针）和普通直接调用；
- 通过独立指针虚拟寄存器帧保留原始指针来源，支持标量、数组和结构体字段寻址；
- 保留调用约定、调用属性、返回值、对齐、易失访问与内存元数据；验证递归、互递归和跨模块调用；
- 仅转换 AArch64（64 位 ARM 架构）模块。动态栈分配、非零地址空间、尾调用和复杂 ABI（应用二进制接口）调用安全跳过；指针整数转换（地址空间零的受限子集）、原子（受限子集）、常用标量内建（受限子集）与间接调用（受限规则子集）已按各自边界实现支持，其余形态仍安全跳过。

**验收条件**：在 `O0`（无优化）和 `O2`（常规优化）下通过验证器与宿主解释器行为测试；以 AArch64（64 位 ARM 架构）生成对象文件并确认目标架构。X86（x86 架构）仅用于转换后修改目标三元组的宿主解释器测试，不执行 VMP（虚拟机保护）转换。

### 里程碑四：增强与性能控制

- 每个成功转换的函数从 `aesSeed`（随机种子）驱动的随机源取得独立的 64 位密钥；字节码表的全部字段与载荷按“密钥、绝对字偏移、轮转”派生掩码进行异或白化。解释器使用 `llvm.fshl`（LLVM 漏斗左移内建）在线解码，因此私有表中不直接保留操作码、寄存器编号或跳转目标。
- 在每个非终结虚拟指令后按比例插入伪指令。伪指令只写入已初始化的专用整数虚拟寄存器，不参与真实数据流、控制流、内存或返回值；处理器创建与调度案例按随机顺序排列。
- 提供 `-vmp-fake-op-rate`（伪指令比例，默认 25%）、`-vmp-max-fake-ops`（最大伪指令数，默认 64）、`-vmp-max-source-insts`（最大源指令数，默认 1024）、`-vmp-max-bytecode-words`（最大字节码字数，默认 4096）和默认关闭的 `-vmp-report-stats`（统计报告）。基础字节码或源函数超过预算时保留原函数和已选择属性；仅伪指令超预算时确定性缩减伪指令数。
- `-vmp-report-stats`（统计报告）输出源指令数、字节码字数、伪指令数和生成 IR（中间表示）指令数。宿主解释器仅用于差分正确性；AArch64（64 位 ARM（精简指令集）架构）运行时性能需要在目标设备上另行测量。
- 支持标量与地址空间零指针原子加载、存储，保留对齐、易失性、内存元数据、内存序和同步域；并支持受限子集的 `fence`（栅栏）、`atomicrmw`（原子读改写）与 `cmpxchg`（比较交换）（按类型、地址空间、内存序与同步域边界），继续拒绝聚合 SSA（静态单赋值）值。

**验收条件**：固定 `aesSeed`（随机种子）时增强布局可复现，不同种子产生不同表或处理器布局；预算回退与统计报告均有回归；原子加载、存储在 `O0`（无优化）与 `O2`（常规优化）下通过验证器、宿主解释器和 AArch64（64 位 ARM（精简指令集）架构）对象生成验证。

### 里程碑五：可靠性、安全边界与目标设备验证

- `CryptoUtils`（随机数工具）统一持有 `aesSeed`（随机种子）和幂等初始化入口。所有随机读取都会先初始化：非默认种子得到可复现布局，默认种子继续使用时间熵。新版 `vmpobf`（VMP 混淆）函数编译遍可独立运行，不再依赖早期混淆调度器。
- 每个函数密钥由随机字与函数 GUID（全局唯一标识）经可逆混合派生，且强制非零；不再以 `Module`（模块）指针保存静态密钥集合，因此转换结束后没有跨模块密钥状态。
- 新增默认开启的 `-vmp-integrity-check`（字节码完整性校验）。生成阶段对完整的已白化字节码表计算密钥化滚动标签；解释器入口以易失读取逐字重算，比较受掩码保护的期望值，失败时调用 `llvm.trap`（LLVM 陷阱内建）并进入不可达路径。关闭开关时不生成校验循环。
- 伪指令扩展为加法、异或和轮转三种无副作用处理器，仍只读写已初始化的专用虚拟寄存器槽。比例、上限、字节码预算和确定性缩减规则保持不变。
- `llvm/test/lit.cfg.py`（LLVM 测试配置）接受可选的 `VMP_AARCH64_RUNNER`（VMP AArch64 执行器）环境变量。存在时公开 `vmp-aarch64-runner`（VMP AArch64 执行器）特性及 `%vmp_aarch64_runner`（VMP AArch64 执行器）替换；缺失时目标设备用例以跳过处理。执行器接收 `--object`（对象文件）、`--expect`（预期退出码）、`--iterations`（迭代次数）和 `--metrics`（指标文件）。
- `llvm/test/CMakeLists.txt`（LLVM 测试构建清单）增加选择性 `check-vmp-obfuscation`（VMP 混淆检查）目标；该目标依赖常规 CMake（构建系统）生成的站点配置，不能直接对源目录调用 `llvm-lit`（LLVM 测试运行器）。
- `vmp-aarch64-metrics.py`（VMP AArch64 指标脚本）读取执行器产生的 JSON（JavaScript 对象表示法）指标、可选的 `llvm-size`（LLVM 尺寸工具）结果与 `-vmp-report-stats`（统计报告）。默认限制为受保护函数文本段不超过参考函数的 256 倍、运行时间不超过 512 倍；可通过 `VMP_AARCH64_MAX_TEXT_RATIO`（VMP AArch64 最大文本比例）和 `VMP_AARCH64_MAX_RUNTIME_RATIO`（VMP AArch64 最大运行时间比例）覆盖，超限即失败。
- 明确安全边界：按密钥、偏移和轮转进行的异或白化及完整性校验只提高篡改和静态分析成本，不是密码学加密或保密机制；具备二进制分析能力的攻击者仍可能恢复密钥和解释器状态。

**执行器指标契约**：执行器写入的 JSON（JavaScript 对象表示法）必须包含 `reference.runtime_ns`（参考运行时间）、`reference.text_bytes`（参考文本字节数）、`protected.runtime_ns`（受保护运行时间）和 `protected.text_bytes`（受保护文本字节数），且四个值均为正数。执行器负责目标对象的链接、部署与运行；仓库不绑定 QEMU（处理器模拟器）、SSH（安全外壳协议）或 ADB（安卓调试桥）。已文档的对象负载是 C ABI：`vmp-aarch64-runner.ll` 只有 `main`、整数递归、局部／全局原子，CLI 仅为 `--object`／`--expect`／`--iterations`／`--metrics`。契约未规定以 `clang++`／`g++` 链接，也未规定 `libstdc++`／`libc++`＋`libc++abi`、`libgcc_s`／`libunwind`，或解析 `__gxx_personality_v0`、`__cxa_allocate_exception`、`__cxa_throw`、`__cxa_begin_catch`、`__cxa_end_catch`、`_Unwind_Resume`、`_ZTIi`。不得把“执行器负责链接”解读成 Itanium C++ 异常运行时可用。

**验收条件**：独立运行 `vmpobf`（VMP 混淆）编译遍时随机源安全可用；固定 `aesSeed`（随机种子）时输出一致、变更种子时密钥和布局变化；完整性校验可检测篡改且关闭时不发射校验循环；三类伪指令均保持语义；`check-vmp-obfuscation`（VMP 混淆检查）在经过 CMake（构建系统）配置的构建目录中可运行选择性回归。配置目标执行器后，纯整数、内存、递归和原子运行用例记录行为、尺寸及性能并执行阈值检查；未配置时该组用例跳过而非失败。

### 里程碑六：VMP（虚拟机保护）→ CFF（控制流平坦化）后处理叠加

- 新增默认关闭的 `-enable-vmp-post-cff`（启用 VMP 后控制流平坦化）开关。函数同时具有 `vmp`（虚拟机保护标记）与 `fla`（控制流平坦化标记）时选择叠加；该专用全局开关才会将后期 CFF（控制流平坦化）扩展到所有成功虚拟化函数。普通 `-enable-cffobf`（启用控制流平坦化）不会改变已选择 VMP（虚拟机保护）函数的早期排他行为；`novmp`（禁用虚拟机保护标记）与 `nofla`（禁用控制流平坦化标记）优先级最高。
- 在早期标记清理前，将后期平坦化意图保存为 `hikari.vmp.post.cff`（VMP 后控制流平坦化选择属性）。后期转换只接受同时具有 `hikari.vmp.virtualized`（VMP 已虚拟化属性）和该选择属性的函数；成功后添加 `hikari.vmp.post.cff.applied`（VMP 后控制流平坦化完成属性），保证重复运行幂等。
- 新增专用后期 OLLVM（混淆 LLVM）调度入口，严格放置在 VMP（虚拟机保护）编译遍之后。新版流程覆盖 `O0`（无优化）、`O2`（常规优化）和链接后流程；旧版流程在 `createVirtualizationPass`（创建虚拟化编译遍）之后运行。链接前流程继续不执行 VMP（虚拟机保护）或后期 CFF（控制流平坦化）。
- 保持现有早期排他逻辑：已选择 VMP（虚拟机保护）的原函数不运行早期 CFF（控制流平坦化）。后期 CFF（控制流平坦化）只处理虚拟机解释器外壳，因此叠加结果不会改变 VMP（虚拟机保护）的资格分析和字节码规划。
- 扩展 CFF（控制流平坦化）的终结指令检查：在降低解释器调度 `switch`（多路分支）之后，允许 `br`（分支）、`ret`（返回）和 `unreachable`（不可达）终结块。调度操作码先写入专用的非易失局部槽，再在降低后的比较块中重新读取，避免跨块 SSA（静态单赋值）值；完整性失败块、无效操作码块和 `llvm.trap`（LLVM 陷阱内建）路径作为真实案例保留，任何分支目标都不得被重定向到默认案例。
- 后期 CFF（控制流平坦化）不得对虚拟寄存器、程序计数器、字节码读取和完整性状态的 `volatile`（易失）访问进行提升、合并或删除，且禁止直接调用当前无边界的 `fixStack`（栈修复函数）。解释器外壳仍含有无法安全局部化的跨块 SSA（静态单赋值）值、Φ（合流）或不支持控制流时，统一跳过后处理。
- 转换采用事务式回退：先在函数克隆上完成多路分支降低、平坦化和验证，成功后替换原虚拟机外壳；任何资格、预算或验证失败都保留原有已虚拟化函数，不允许留下仅降低多路分支或部分平坦化的中间状态。`deleteBody` 替换外壳时必须保存并写回 Function 级状态：`personality`、prefix／prologue、函数 metadata、原 linkage；不得把无 EH 控制流的 personality 或 `internal` 链接丢掉。`vmp-cxx-personality-no-eh-semantic.ll` 对 VMP 与 VMP→CFF 覆盖 `prefix i32`／`prologue i32`（只 FileCheck 虚拟化后的 IR；`main` 不调用它们，宿主 `lli`／ORC 会把 prefix／prologue 当入口字节；AArch64 `llc` 只做模块冒烟）、`!annotation` 的 live 写回与 `internal` 链接写回。
- 增加 `-vmp-post-cff-max-blocks`（VMP 后控制流平坦化最大基本块数，默认 256）与 `-vmp-post-cff-max-insts`（VMP 后控制流平坦化最大指令数，默认 8192）。超过预算时保留原 VMP（虚拟机保护）结果并输出跳过原因；该回退不移除 `hikari.vmp.virtualized`（VMP 已虚拟化属性）。
- 里程碑六只开放 VMP（虚拟机保护）→ CFF（控制流平坦化）。BCF（伪控制流）、基本块拆分、指令替换和间接分支继续保持排除，直到分别具备事务式回退、行为测试和目标性能数据。

**验收条件**：同一函数可按 VMP（虚拟机保护）→ CFF（控制流平坦化）顺序转换；只启用其中一个标记时保持单独功能；`nofla`（禁用控制流平坦化标记）可阻止后处理；`-enable-vmp-post-cff`（启用 VMP 后控制流平坦化）可扩展选择范围；固定 `aesSeed`（随机种子）时组合输出可复现；完整性校验在平坦化后仍可检测字节码篡改；预算和转换失败只回退到有效的 VMP（虚拟机保护）结果。组合流程须在 `O0`（无优化）、`O2`（常规优化）、链接后流程、宿主解释器和 AArch64（64 位 ARM（精简指令集）架构）对象生成中通过验证。

### 里程碑六之后的增量：地址空间零指针整数转换

- 在保持整数虚拟寄存器与指针虚拟寄存器分离的前提下，支持地址空间零的 `ptrtoint`（指针转整数）与 `inttoptr`（整数转指针）。
- 仅接受精确 `ptrtoint` `addrspace(0) ptr -> i64` 与 `inttoptr` `i64 -> addrspace(0) ptr`。成对 `VMOpcode::PtrToInt`／`IntToPtr` 解释器处理器分别 `CreatePtrToInt`／`CreateIntToPtr`，不把 provenance 折成 `bitcast`，也不把指针帧与整数帧混用。
- `i1`／`i8`／`i16`／`i32`／`i128`、向量、聚合、非零地址空间、非整数地址空间、`addrspacecast` 以及其它转换形态保持既有安全跳过。
- 不扩展复杂 ABI、尾调用语义、动态栈分配、浮点或聚合转换；VMP → CFF 安全回退路径保持不变。
- 回归覆盖 `O0`/`O2` × `aesSeed` 97/7 宿主解释器差分、FileCheck、非 AArch64 跳过，以及 AArch64 `llc`／`readobj`。见下一节实际边界。

**当前实际 AArch64 地址空间零 `ptrtoint`／`inttoptr` 支持边界**

- 接受：精确标量 `ptrtoint`（`addrspace(0) ptr` → `i64`）与 `inttoptr`（`i64` → `addrspace(0) ptr`）。指针操作数／结果走既有指针虚拟寄存器帧（`1<<63`），整数走 `i64` 整数帧。处理器按 LLVM 原样重放转换，结果可进入既有 `GEP`、非原子 `load`／`store`、指针 `icmp`（`eq`／`ne`／无符号关系）、`select`、`phi`、以及已放行的直接／间接 C 调用。参数、静态 `alloca`、全局与 `GEP` 四种指针来源均覆盖。`vmp-ptrtoint-semantic.ll`（O0/O2×97/7）做参考／保护宿主 `lli` 差分、FileCheck、AArch64 `llc`／`readobj`，以及把源三元组改成 `x86_64` 的 `only AArch64 targets are supported` 负例。
- 拒绝：非 `i64` 宽度（含 `i1`／`i8`／`i16`／`i32`／`i128`）、非零地址空间、DataLayout 非整数地址空间、向量 `ptrtoint`／`inttoptr`、聚合、`addrspacecast`、以及其它 `CastInst`。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

### 里程碑七：指令覆盖治理与增量扩展

目标是把“覆盖所有指令”拆分为可安全验收的指令族，不承诺一次性覆盖全部 LLVM（编译器框架）IR（中间表示）语法。每个指令族独立经过资格检查、行为测试与目标验证后才宣告支持；未支持形态一律保持稳定“安全跳过”，绝不生成语义不完整的虚拟化结果。

**当前已验证的覆盖矩阵**

| 指令族 | 已支持形态 |
| --- | --- |
| 标量算术与位运算 | 整数算术、位运算、比较与类型转换（含受限标量 `i128` 独立虚拟寄存器帧）；标量 `half`/`f32`/`f64` 的 `fadd`/`fsub`/`fmul`/`fdiv`/`frem` 与 `fneg`（浮点二元/一元）、`fcmp`、`select`、`phi`、栈/全局 `load`/`store`、`bitcast` 与整数互转、`fpext`/`fptrunc`；标量 IEEE `fp128` 基础 SSA 走独立类型化 `fp128` 虚拟寄存器帧（`1<<57`，`vmp.fp128.regs`，不得与 `i128` 整数帧或 `half`/`f32`/`f64`/`bfloat` 浮点帧混用，见独立表面）；标量 `bfloat` 基础 SSA 与非原子地址空间零 `load`/`store` 走同一 16 位浮点虚拟寄存器帧，但函数 `target-features` 最后令牌须为精确 `+bf16`（见独立表面） |
| SVE 基础整数 SSA | 最后令牌 `+sve` 的全寄存器整数可伸缩 SSA：`<vscale x 16 x i8>`／`<vscale x 8 x i16>`／`<vscale x 4 x i32>`／`<vscale x 2 x i64>`；同型 `add`／`sub`／`mul`／`shl`／`lshr`／`ashr`／`udiv`／`sdiv`／`urem`／`srem`／`and`／`or`／`xor`（`add`／`sub`／`mul`／`shl` 重放 `nuw`／`nsw`；`lshr`／`ashr`／`udiv`／`sdiv` 重放 `exact`；`rem`／逻辑无 BinaryFlag）、标量 `i1` `select`、`phi`、`freeze`、非原子地址空间零 `load`／`store`、函数实参／返回；独立 `nxv16i8` 虚拟寄存器帧（`1<<56`，`vmp.sve.regs`），见 `vmp-sve-integer-ssa-semantic.ll`。向量 `icmp`／谓词见下一行。无 extract／insert／shuffle。受限直接 C 的 SVE 数据调用见独立行 |
| SVE 谓词 SSA | 最后令牌 `+sve`。仅上述四宽同型整数 `icmp`（十种整数谓词），结果谓词仅 `nxv16i1`／`nxv8i1`／`nxv4i1`／`nxv2i1`；车道精确匹配的 data `select`；同型谓词 `and`／`or`／`xor`、`freeze`（普通／`undef`／`poison`）、谓词／谓词／谓词 `select`；谓词 `phi`；本函数谓词实参／返回。独立 `nxv16i1` 帧（`1<<55`，`vmp.sve.preds`），解包谓词先 `convert.from.svbool` 到正确视图再运算再打包。见 `vmp-sve-predicate-ssa-semantic.ll`。无谓词内存、无标量 `i1` 谓词 `select`、无 `nxv1i1`、无经调用传递谓词 |
| SVE masked.load／store | 最后令牌 `+sve`。四宽整数或全寄存器浮点（`<vscale x 4 x float>`／`<vscale x 2 x double>`；`<vscale x 8 x half>` 另需最后令牌 `+fullfp16`）＋匹配谓词＋AS0 标量指针＋`i32` 二幂对齐 ImmArg＋同型 passthru。独立 `ScalableMaskedLoad`／`Store` 重建 `CreateMaskedLoad`／`Store`（汇编 `ld1*`／`st1*` 谓词＋`mov` 合并 passthru），不走 `CallDescriptor`、不降成无条件访存。见 `vmp-sve-masked-memory-semantic.ll` 与 `vmp-sve-float-masked-memory-semantic.ll`。无非 AS0、无其它可伸缩宽。gather／scatter 见下一行 |
| SVE masked.gather／scatter | 最后令牌 `+sve`。四宽整数或全寄存器浮点（`nxv4f32`／`nxv2f64`；`nxv8f16` 另需最后令牌 `+fullfp16`）＋匹配谓词＋同车道 AS0 `nxv16/8/4/2 x ptr`＋`i32` 二幂对齐 ImmArg＋同型 passthru。地址来自受限可伸缩 GEP（标量 AS0 基址＋匹配四宽整数向量下标，以及与固定 gather 相同的 2／3 下标 AoS／Wrap 形态）或这些地址向量的 `phi`。独立 `nxv2ptr` 分块帧（`1<<54`，`vmp.sve.pvregs`），`vector.extract`／`insert` 打包，禁止 `ptrtoint`。操作码 `ScalableMaskedGather`／`Scatter` 重建 `CreateMaskedGather`／`Scatter`（汇编谓词 `ld1*`／`st1*` gather，宽类型可 `punpk` 拆分，不是逐车道循环）。见 `vmp-sve-gather-scatter-semantic.ll` 与 `vmp-sve-float-gather-scatter-semantic.ll`。无指针向量函数 ABI、无常量／poison 地址向量 |
| SVE expandload／compressstore | **拒绝。** LLVM 15 AArch64 把可伸缩 `llvm.masked.expandload`／`compressstore` 选成普通谓词 `ld1*`／`st1*`（与 masked.load／store 相同），丢失紧凑序列语义。不得用普通 masked 访存或逐车道循环冒充。跳过 `unsupported masked memory instruction`。见 gather lit `@unsupported_expand` |
| SVE 基础浮点 SSA | 最后令牌 `+sve` 的全寄存器浮点可伸缩 SSA：`<vscale x 4 x float>`／`<vscale x 2 x double>`；`<vscale x 8 x half>` 另需最后令牌 `+fullfp16`。同型 `fadd`／`fsub`／`fmul`／`fdiv`／`fneg`（重放 FastMathFlags）、`fcmp`（写入既有谓词帧）、标量 `i1` 与车道匹配谓词 `select`、`phi`、`freeze`、非原子地址空间零 `load`／`store`、函数实参／返回。复用整数 `nxv16i8` 虚拟寄存器帧（`1<<56`，`vmp.sve.regs`）的同宽 bitcast，不新开浮点可伸缩帧。**拒绝** `frem`：LLVM 15 AArch64 ISel 对可伸缩 `frem` 以 `Invalid size request on a scalable vector` 中止，不得用 `fdiv`/`fmul` 冒充。无 extract／insert／shuffle、无 `bfloat` 可伸缩、无经 `CallDescriptor` 的 SVE 浮点调用。浮点 masked 访存与同车道整数互转见独立行。见 `vmp-sve-float-ssa-semantic.ll`。 |
| SVE 同车道整数↔浮点转换 | 最后令牌 `+sve`。仅全寄存器同车道且元素位宽相等：`nxv4i32`↔`nxv4f32`、`nxv2i64`↔`nxv2f64`、`nxv8i16`↔`nxv8f16`（半精度另需最后令牌 `+fullfp16`）的 `sitofp`／`uitofp`／`fptosi`／`fptoui`。独立 `ScalableCast` 在 `nxv16i8` 帧上 `CreateCast`。**拒绝** `fpext`／`fptrunc`：改变元素宽会让一侧不再是全寄存器 128 位颗粒（`nxv4f64`／`nxv2f32`／`nxv8f32`），不能复用现有槽。见 `vmp-sve-int-fp-cast-semantic.ll`。 |
| SVE 浮点数学内建 | 最后令牌 `+sve`。全寄存器 `nxv4f32`／`nxv2f64`；`nxv8f16` 另需最后令牌 `+fullfp16`。`llvm.sqrt`／`fabs`／`fma`／`fmuladd`／`minnum`／`maxnum`／`minimum`／`maximum`／`ceil`／`floor`／`trunc`／`round`／`roundeven`／`rint`／`nearbyint`／`copysign`。独立 `ScalableFPMath` 在 `nxv16i8` 帧上 `CreateIntrinsic`（不走 `CallDescriptor`），重放 FastMathFlags。**拒绝** `sin`／`pow`：LLVM 15 ISel 对可伸缩形态 `Invalid size request on a scalable vector`。**拒绝** `canonicalize`：`Cannot select fcanonicalize`。见 `vmp-sve-float-math-semantic.ll`。 |
| SVE 整数数学内建 | 最后令牌 `+sve`。全寄存器 `nxv16i8`／`nxv8i16`／`nxv4i32`／`nxv2i64`。`llvm.abs`／`smin`／`smax`／`umin`／`umax`／`ctpop`／`ctlz`／`cttz`／`bswap`（元素 ≥16 位）／`bitreverse`／`sadd.sat`／`ssub.sat`／`uadd.sat`／`usub.sat`／`fshl`／`fshr`（三同型向量，含旋转 `fshl(a,a,n)`）。独立 `ScalableIntMath` 在 `nxv16i8` 帧上 `CreateIntrinsic`（不走 `CallDescriptor`）。`abs` 的 `is_int_min_poison` 与 `ctlz`／`cttz` 的 `is_zero_undef` 为 `i1` ImmArg，写入 Variant `[8]`。漏斗第三实参走 Source2。**拒绝** `sshl.sat`／`ushl.sat`：LLVM 15 ISel `UnrollVectorOp` 对可伸缩 `Invalid size request`。见 `vmp-sve-int-math-semantic.ll`。 |
| SVE 向量归约 | 最后令牌 `+sve`。全寄存器整数 `add`／`and`／`or`／`xor`／`smin`／`smax`／`umin`／`umax`（标量元素结果）；全寄存器浮点 `fadd`（标量 start＋向量）／`fmin`／`fmax`；`nxv8f16` 另需最后令牌 `+fullfp16`。独立 `ScalableReduce`：向量走 `nxv16i8` 帧，结果走既有整数／浮点标量帧，`CreateIntrinsic` 重建同一 ID。**拒绝** `mul`／`fmul`：LLVM 15 ISel `Expanding reductions for scalable vectors is undefined`。无 `+sve` 的既有哨兵仍是 `unsupported vector reduce instruction`。见 `vmp-sve-reduce-semantic.ll`。 |
| SVE is.fpclass | 最后令牌 `+sve`。全寄存器 `nxv4f32`／`nxv2f64`；`nxv8f16` 另需最后令牌 `+fullfp16`。结果为同车道谓词 `nxv4i1`／`nxv2i1`／`nxv8i1`。独立 `ScalableFPClass`：源走 `nxv16i8` 帧，结果走 `nxv16i1` 谓词帧，`i32` ImmArg 测试掩码写入 Variant `[39:8]`。`CreateIntrinsic` 重建同一 ID。半寄存器 `nxv4f16` 不开放。无 `+sve` 的既有哨兵仍是 `unsupported return type`。见 `vmp-sve-fpclass-semantic.ll`。 |
| SVE 饱和浮点转整数 | 最后令牌 `+sve`。仅同车道且元素位宽相等：`nxv4f32`→`nxv4i32`、`nxv2f64`→`nxv2i64`、`nxv8f16`→`nxv8i16`（半精度另需最后令牌 `+fullfp16`）的 `llvm.fptosi.sat`／`llvm.fptoui.sat`。独立 `ScalableSatCast` 在 `nxv16i8` 帧上 `CreateIntrinsic`，不是普通 `fptosi`／`fptoui`。改变元素宽（如 `nxv4f16`→`nxv4i32`）拒绝。无 `+sve` 的既有哨兵仍是 `unsupported return type`。见 `vmp-sve-sat-convert-semantic.ll`。 |
| SVE 直接 C 调用 | 最后令牌 `+sve`。普通直接非内建 `CallingConv::C`、非可变参数，至少一侧为全寄存器 SVE 数据（四宽整数或 `nxv4f32`／`nxv2f64`／`nxv8f16`，半精度另需 `+fullfp16`），可与既有标量混用。经既有 `CallDescriptor` 与 `nxv16i8` 帧重放，`TCK_None`。**不开放**谓词／指针向量／间接／`musttail`／`invoke`。既有谓词调用哨兵仍是 `unsupported call return type`。见 `vmp-sve-direct-call-semantic.ll`。 |
| SVE bitcast／stepvector／reverse／active.lane.mask | 最后令牌 `+sve`。全寄存器数据之间的同颗粒 `bitcast`（复用 `ScalableCast`）；`llvm.experimental.stepvector`（仅整数）与 `llvm.experimental.vector.reverse`（`ScalableVecHelper`）；`llvm.get.active.lane.mask` 与基础 SVE `whilelo`／`whilelt`／`whilele`／`whilels` 到匹配全寄存器谓词（`ScalableActiveLaneMask`，`i32`／`i64` 起止）。`nxv8f16` 另需 `+fullfp16`。ISel：`bitcast` 为空操作、`index #0,#1`、`rev z`、`whilelo`／`whilelt`／`whilele`／`whilels`。SVE2 `whilege`／`gt`／`hs`／`hi` 拒绝。见 `vmp-sve-vec-helper-semantic.ll`／`vmp-sve-while-semantic.ll`。 |
| SVE vector.splice | 最后令牌 `+sve`。全寄存器数据上的 `llvm.experimental.vector.splice`（两同型向量＋`i32` ImmArg）。复用 `ScalableVecHelper`，ImmArg 写入 Variant。仅接受 ISel 安全下标：`idx>=0` 且 `< minElts`（`ext`）；`idx<0` 且 `abs(idx)` 是已知 SVE 谓词图案（1..8／16／32／64／128／256）且 `<= minElts`（`ptrue`＋`rev p`＋`splice`）。`nxv16i8` 的 `-9..-15` 验证器合法但无图案，拒绝。`nxv8f16` 另需 `+fullfp16`。见 `vmp-sve-splice-semantic.ll`。 |
| SVE 谓词 helper | 最后令牌 `+sve`。全寄存器谓词 `nxv16i1`／`nxv8i1`／`nxv4i1`／`nxv2i1`。`llvm.experimental.vector.reverse` 与 `llvm.aarch64.sve.ptrue`（`i32` ImmArg 图案 0..31）写谓词帧；`llvm.aarch64.sve.cntp`（两同型谓词→`i64`）与 `ptest.any`／`first`／`last`（两同型谓词→`i1`）写整数帧。独立 `ScalablePredHelper`。ISel：`rev p`／`ptrue`／`cntp`／`ptest`＋`cset`。谓词 `splice` 仍拒绝（ISel 走整数 `ext`）。见 `vmp-sve-pred-helper-semantic.ll`。 |
| SVE 常见 ACLE | 最后令牌 `+sve`。独立 `ScalableACLE`。谓词算术 `add`／`sub`／`mul`／`smax`／`smin`／`umax`／`umin`／`and`／`orr`／`eor`／`sel`／`fadd`／`fsub`／`fmul`／`fdiv`（`pred,a,b`）；`sdiv`／`udiv` 仅 `.s`／`.d`；merged `abs`／`neg`；谓词 `and_z`／`orr_z`／`eor_z`／`bic_z`；`convert.to/from.svbool`；`cntb`／`cnth`／`cntw`／`cntd`；`lasta`／`lastb`；`dup.x`／`index`；`zip1`／`zip2`；`compact` 仅 `.s`／`.d`；`cmpeq`／`cmpne`／`cmpge`／`cmpgt`／`cmphi`／`cmphs`。ISel 为原生谓词 `add`／`sel`／`fadd`／`cntb`／`lasta`／`index`／`zip1`／`compact`／`cmpeq`。见 `vmp-sve-acle-common-semantic.ll`。 |
| 固定向量 | 总原始宽度 1..128 的固定向量，元素 `i1`/`i8`/`i16`/`i32`/`i64`/`half`/`f32`/`f64`（`bfloat` 元素见独立 last-token `+bf16` 表面），独立 `i128` 向量虚拟寄存器帧；整数向量算术／比较、浮点向量 `fadd`/`fsub`/`fmul`/`fdiv`/`frem`/`fneg`（含 `half`，保留 FastMathFlags）、`fcmp`、`select`、`phi`、非原子 `load`/`store`、`bitcast`、`extractelement`/`insertelement`、常量 `shufflevector`、受限固定向量 `llvm.vector.insert`／`llvm.vector.extract`（ImmArg 下标，降为既有 `VectorShuffle`，见独立表面）、同车道 `f32`↔`f64` 与 `half`↔`float` 的 `fpext`/`fptrunc`、同车道整数向量与 `half`/`f32`/`f64` 向量的 `sitofp`/`uitofp`/`fptosi`/`fptoui`（复用既有 `VectorSIToFP`／`VectorUIToFP`／`VectorFPToSI`／`VectorFPToUI`）、以及普通直接 C 调用与受限间接 C 调用的向量实参／返回。受限 AArch64 NEON `tbl`/`tbx` 见下。listed 固定 `half` 向量数学内建见下。固定 `f32`/`f64` 向量严格 `constrained.fadd`／`fsub`／`fmul`／`fdiv`／`frem`／`fma`／`fmuladd`／`sqrt`／`fcmp`／`fcmps`、同类型 `constrained.ceil`／`floor`／`trunc`／`round`／`roundeven`／`rint`／`nearbyint` 与 `constrained.fptosi`／`fptoui`／`sitofp`／`uitofp`／`fptrunc`／`fpext` 以及 `constrained.minnum`／`maxnum`／`minimum`／`maximum` 与 `constrained.sin`／`cos`／`exp`／`exp2`／`log`／`log2`／`log10` 以及 `constrained.pow`／`powi` 见独立表面。固定 `half` 向量严格 `constrained.fadd`／`fsub`／`fmul`／`fdiv`／`frem`／`fma`／`fmuladd`／`sqrt`／`fcmp`／`fcmps`、同类型 `constrained.ceil`／`floor`／`trunc`／`round`／`roundeven`／`rint`／`nearbyint`、`constrained.fptosi`／`fptoui`／`sitofp`／`uitofp`／`fptrunc`／`fpext`、`constrained.minnum`／`maxnum`／`minimum`／`maximum` 与 `constrained.sin`／`cos`／`exp`／`exp2`／`log`／`log2`／`log10` 以及 `constrained.pow`／`powi` 见独立 last-token `+fullfp16` 表面。通用固定 `f32`/`f64` 向量 `llvm.fma`/`llvm.fmuladd`、`llvm.sqrt`/`llvm.fabs`、`llvm.minnum`/`maxnum`/`minimum`/`maximum`、舍入／`copysign`／`pow`／`powi`、`sin`／`cos`／`exp`／`exp2`／`log`／`log2`／`log10` 与同车道 `fptosi.sat`／`fptoui.sat`（固定 `f32`／`f64` 源到 `i8`／`i16`／`i32`／`i64` 目的，两侧各自总原始宽度 1..128）、`llvm.canonicalize`（降为 `VectorFMul x, 1.0`）与 `llvm.is.fpclass`（同车道 `<N x i1>`，`i32` ImmArg），以及固定 `i8`／`i16`／`i32`／`i64` 向量 `llvm.ctpop`／`bswap`／`bitreverse`／`ctlz`／`cttz` 与普通双操作数 `llvm.fshl`／`fshr`、`llvm.abs` 与 `sadd`／`uadd`／`ssub`／`usub`／`sshl`／`ushl.sat` 见下（不经过 `half` 门控）。`vmp-half-vector-semantic.ll` 覆盖 `half` 基础 SSA；`vmp-half-vector-masked-reduce-semantic.ll` 覆盖受限 `half` 掩码内存与浮点归约；`vmp-half-vector-math-semantic.ll` 覆盖 listed `half` 向量数学；`vmp-fp-vector-fma-semantic.ll` 覆盖 `f32`/`f64` 向量 FMA；`vmp-fp-vector-sqrt-fabs-semantic.ll` 覆盖 `f32`/`f64` 向量 `sqrt`/`fabs`；`vmp-fp-vector-minmax-semantic.ll` 覆盖 `f32`/`f64` 向量 `minnum`/`maxnum`/`minimum`/`maximum`；`vmp-fp-vector-rounding-sign-power-semantic.ll` 覆盖 `f32`/`f64` 向量舍入族、`copysign`、`pow` 与 `powi`（严格 `i32` 指数）；`vmp-fp-vector-transcendental-semantic.ll` 覆盖 `f32`/`f64` 向量 `sin`／`cos`／`exp`／`exp2`／`log`／`log2`／`log10`；`vmp-fp-vector-sat-convert-semantic.ll` 覆盖同车道 `f32`／`f64` 源到 `i8`／`i16`／`i32`／`i64` 目的的 `fptosi.sat`／`fptoui.sat`；`vmp-fp-vector-canonicalize-semantic.ll` 覆盖 `f32`／`f64` 向量 `canonicalize`（`VectorFMul x, 1.0`）；`vmp-fp-vector-fpclass-semantic.ll` 覆盖 `f32`／`f64` 向量 `llvm.is.fpclass`（同车道 `<N x i1>`，`i32` ImmArg）；`vmp-half-vector-fpclass-semantic.ll` 覆盖固定 `half` 向量 `is.fpclass`（最后令牌 `+fullfp16`）；`vmp-half-sat-convert-semantic.ll` 覆盖标量与固定 `half` 向量同车道 `fptosi.sat`／`fptoui.sat`（`i8`／`i16`／`i32`／`i64`，最后令牌 `+fullfp16`）；`vmp-int-vector-bit-semantic.ll` 覆盖固定 `i8`／`i16`／`i32`／`i64` 向量 `ctpop`／`bswap`／`bitreverse`／`ctlz`／`cttz` 与普通双操作数 `fshl`／`fshr`；`vmp-int-vector-abs-sat-semantic.ll` 覆盖固定整数向量 `abs` 与 `sadd`／`uadd`／`ssub`／`usub`／`sshl`／`ushl.sat`；`vmp-int-vector-fixpoint-semantic.ll` 覆盖固定整数向量 `smul`／`umul`／`sdiv`／`udiv.fix` 与 `.fix.sat`（仅总宽 64／128） |
| 标量浮点数学内建 | 标量 `f32`/`f64` 的 `sqrt`、`fma`/`fmuladd`、`sin`、`cos`、`exp`、`exp2`、`log`、`log2`、`log10`、`fabs`、`pow`、`powi`（严格 `i32` 指数）、`minnum`/`maxnum`、`minimum`/`maximum`、`ceil`、`floor`、`trunc`、`round`、`rint`、`nearbyint`、`roundeven`、`copysign`、`canonicalize`（降级为带标志的 `fmul x, 1.0`）、`is.fpclass`（`i1` 结果，`i32` ImmArg）；标量 `half` 覆盖同一组纯内建以及 listed 超越内建（`sin`/`cos`/`exp`/`exp2`/`log`/`log2`/`log10`/`pow`/`powi` 精确 `i32` 指数），且函数 `target-features` 最后令牌须为 `+fullfp16`。标量 `half` `is.fpclass`、标量 `half` `fptosi.sat`／`fptoui.sat`（结果仅 `i8`／`i16`／`i32`／`i64`）与标量 `half` `lround`／`llround`／`lrint`／`llrint`（结果仅 `i64`）是独立表面，同样要求最后令牌 `+fullfp16`。标量 `f32`／`f64` `llvm.experimental.constrained.fadd`／`fsub`／`fmul`／`fdiv`／`frem`／`fma`／`sqrt`／`fcmp`／`fcmps`、`fptosi`／`fptoui`／`sitofp`／`uitofp`／`fptrunc`／`fpext`、`ceil`／`floor`／`trunc`／`round`／`roundeven`／`rint`／`nearbyint` 以及 `lrint`／`lround`（结果 `i32`／`i64`）／`llrint`／`llround`（结果仅 `i64`）／`minnum`／`maxnum`／`minimum`／`maximum`／`sin`／`cos`／`exp`／`exp2`／`log`／`log2`／`log10`／`pow`／`powi`（指数为普通 `i32`）／`fmuladd` 是独立严格浮点表面。标量 `half` `constrained.fadd`／`fsub`／`fmul`／`fdiv`／`frem`／`fma`／`fmuladd`／`sqrt`／`fcmp`／`fcmps` 与同类型 `constrained.ceil`／`floor`／`trunc`／`round`／`roundeven`／`rint`／`nearbyint`、`constrained.lrint`／`lround`（`i32`／`i64`）／`llrint`／`llround`（仅 `i64`）以及 `constrained.fptosi`／`fptoui`／`sitofp`／`uitofp`／`fptrunc`／`fpext` 与 `constrained.minnum`／`maxnum`／`minimum`／`maximum` 以及 `constrained.sin`／`cos`／`exp`／`exp2`／`log`／`log2`／`log10` 与 `constrained.pow`／`powi`（指数为普通 `i32`）是独立 last-token `+fullfp16` 严格表面，经 `CallDescriptor` MetadataArguments 原样重发射 rounding／exception／predicate MDString，不带 FastMathFlags。带 FastMathFlags（快速数学标志）的调用经 CallDescriptor（调用描述符）掩码精确保存并恢复 |
| 半精度向量数学内建 | 总宽 1..128 的固定 IEEE `half` 向量上的 listed 内建，经既有 `CallDescriptor` 重放：`sqrt`、`fma`/`fmuladd`、`fabs`、`minnum`/`maxnum`、`minimum`/`maximum`、舍入族、`copysign`、`sin`/`cos`/`exp`/`exp2`/`log`/`log2`/`log10`/`pow`，以及 LangRef 明确的向量 `powi`（匹配 `half` 向量 + 标量 `i32` 指数）。函数 `target-features` 最后令牌须为 `+fullfp16`。本表面不开放 `f32`/`f64` 向量数学或其它未列入内建。固定 `half` 向量 `llvm.is.fpclass`、`llvm.canonicalize`（`VectorFMul x, 1.0`）与同车道 `fptosi.sat`／`fptoui.sat`（固定 `half` 源到 `i8`／`i16`／`i32`／`i64` 目的，两侧各自总原始宽度 1..128）是独立表面，同样要求最后令牌 `+fullfp16`，但不并入 listed 数学 ID。标量 `half` `llvm.is.fpclass` 与标量 `half` 饱和转换见独立表面。通用固定 `f32`/`f64` 向量 `fma`/`fmuladd`、`sqrt`/`fabs`、`minnum`/`maxnum`/`minimum`/`maximum`、舍入／`copysign`／`pow`／`powi`、`sin`／`cos`／`exp`／`exp2`／`log`／`log2`／`log10`、同车道 `fptosi.sat`／`fptoui.sat` 、`canonicalize`（`VectorFMul`）与 `is.fpclass` 见独立表面，不经过本门控 |
| 控制流 | `br`、`switch`（多路分支降低）、静态同函数 `indirectbr`（`select`／`phi`／`bitcast` 的 `blockaddress`，以及仅本函数使用的本地链接常量 `[1..16 x ptr]` 跳表 `load`，见 `vmp-indirectbr-jumptable-semantic.ll`）、受限直接 `InlineAsm` `CallBr`（LLVM `asm goto`，见 `vmp-inline-asm-callbr-semantic.ll`）、`ret`、`unreachable`、`llvm.trap`（专用终结操作码） |
| 内存 | 标量与地址空间零指针的 `load`/`store`/`alloca`/`GEP`（获取元素指针）、`memcpy`/`memmove`/`memset`、全局变量访问（含地址空间零 `thread_local` GlobalValue，作为普通指针常量写入指针虚拟寄存器，见独立表面）；保留对齐、易失性与内存元数据 |
| 调用 | 普通固定参数直接调用（调用约定与属性保留；已支持表面上的普通 `tail` 是优化提示，解释器按非 tail `CreateCall`／`TCK_None` 重放，见 `vmp-direct-call-tail-eligibility-semantic.ll`／`vmp-tail-call-semantic.ll`；`musttail` 仍拒绝）、受限直接 `InlineAsm` `CallInst`（ATT、非抛、非 `alignstack`、`CallingConv::C`、非可变参数、`void`／`i1`..`i64`／地址空间零指针、约束仅 `r`／`{memory}`／`{cc}`，经既有 `CallDescriptor` 重放 `InlineAsm*`，普通 `tail` 按 `TCK_None`，见 `vmp-inline-asm-semantic.ll`；`musttail`／`InvokeInst` 仍拒绝；受限 `CallBr` 见独立表面）、受限语义上不抛出的直接 `invoke`（调用点／被调 `nounwind`，双方 `CallingConv::C` 且 `FunctionType` 精确同一，展开边为不可达最小 landingpad 岛；另接受既有保守直接 C void `sret`，见独立表面；事务式降为既有 `CallDescriptor` 重放再把虚拟机 PC 转到正常后继，从不生成或执行展开边）、可达 Itanium `{ptr,i32}` 直接 `invoke`／`landingpad`／`resume`（`__gxx_personality_v0`／`__gcc_personality_v0`，解释器发射原生 `InvokeInst`＋条款完整的 `LandingPadInst`，聚合进既有扁平聚合虚拟寄存器，`resume` 原样重放；post-CFF 见到 EH 外壳则事务式保留 VMP，见 `vmp-itanium-invoke-semantic.ll`）、受限规则子集的间接调用（混合标量 `void`/`i1`..`i64`/地址空间零指针/`half`/`f32`/`f64`、既有定长向量以及既有小型扁平聚合实参／返回，经既有 `CallDescriptor` 与整数／指针／浮点／向量／聚合虚拟寄存器重放，不另加 `+fullfp16`）、受限普通直接 `noreturn`（不返回）void C 调用（专用终结操作码：重放后 `unreachable`，不推进程序计数器）、同函数保守 C `va_start`/`va_arg`/`va_end`／受限一次 `va_copy`（专用 `VAArg` 操作码从指针帧恢复 `va_list` 地址并原生取值；`va_copy` 经既有 `CallDescriptor` 重放）、受限直接 C void `sret`（第 0 参数为地址空间零指针且调用点与被调形式参数带匹配 `StructRet`，经既有 `CallDescriptor` 重放；函数自身的 `sret` 参数仍只是指针虚拟寄存器）、受限直接 C `byval`（至少一个地址空间零指针实参带匹配 `ByVal` 指向类型，经既有 `CallDescriptor` 重放；函数自身的 `byval` 参数仍只是指针虚拟寄存器）、受限直接 C `byref`（至少一个地址空间零指针实参带匹配 `ByRef` 指向类型，经既有 `CallDescriptor` 重放；函数自身的 `byref` 参数仍只是指针虚拟寄存器）、`*with.overflow`（仅经 `extractvalue` 使用）、`llvm.expect`／`llvm.expect.with.probability`（标量 `i1`／`i8`／`i16`／`i32`／`i64`／受限 `i128`，降为 `Move` 操作数 0，概率为有限 `double` `ConstantFP` 且位于闭区间 `[0.0, 1.0]`（IEEE 比较接受 `-0.0`）并丢弃，见独立表面） |
| 扁平聚合 | 具名已定大小结构体与固定数组：1..8 个直接字段、保守分配大小 1..64 字节；每槽为 `[4 x i128]`（64 字节），原生 `AggTy` load/store，不新操作码。字段为既有标量（整数 / 地址空间零指针 / `half`/`f32`/`f64`）、总位宽仅为 8/16/32/64/128 的受支持固定向量（含 `half` 元素），或一层叶子聚合（结构体套结构体／数组套结构体／结构体套数组）。`insertvalue`/`extractvalue` 允许 1 或 2 个下标；聚合 `load`/`store`/`select`/`phi`、普通直接 C 调用与受限间接 C 调用的聚合实参与返回。`half` 字段不引入 `+fullfp16` 门控。`vmp-flat-aggregate-semantic.ll` 覆盖原小型基线；`vmp-large-flat-aggregate-semantic.ll` 覆盖 20／24／32／64 字节形态；`vmp-nested-aggregate-semantic.ll` 覆盖一层嵌套（`{ {i32,i32}, i32 }`／`[2 x {i32,i32}]`／`{ [2 x i32], i32 }` 与双下标）；`vmp-half-aggregate-semantic.ll` 覆盖 `{half,half}`；`vmp-indirect-call-aggregate-semantic.ll` 覆盖受限间接聚合 |
| 掩码内存 | 地址空间零的 `llvm.masked.load`/`llvm.masked.store`／`llvm.masked.expandload`／`llvm.masked.compressstore`／`llvm.masked.gather`／`llvm.masked.scatter`（固定数据向量含 `half`/`f32`/`f64` 与整数元素、匹配的掩码与 passthru/value；load/store/gather/scatter 另有 `i32` 对齐立即数，expand/compress 无对齐操作数）。gather/scatter 指针为受限 `<1..8 x ptr>` 地址空间零向量，独立指针向量虚拟寄存器帧（非通用 pointer-vector ABI）。地址 GEP：标量 AS0 基址 + 同车道整数向量下标，或再加一个范围内的 `ConstantInt` 结构体字段（AoS）或数组元素，或三下标 Wrap 形态（向量下标 + `ConstantInt` 结构体字段 + 范围内数组元素），或 clang 常见 `getelementptr [N x %Pair], ptr, i32 0／i64 0, <N x iK>, i32 field`。全 false 掩码且不访问内存的形态已在宿主 `lli` 回归中验证。掩码 `half` 不要求 `+fullfp16`。固定 1..128 `bfloat` 的 `llvm.masked.load`／`llvm.masked.store`／`llvm.masked.gather`／`llvm.masked.scatter`／`llvm.masked.expandload`／`llvm.masked.compressstore` 是独立 last-token `+bf16` 逐通道合法化表面，不得重放原生 `llvm.masked.*.vNbf16`（见独立节） |
| 向量归约 | 既有 allowlist 的 `llvm.vector.reduce.add`/`mul`/`and`/`or`/`xor`/`smin`/`smax`/`umin`/`umax` 与 `fadd`/`fmul`/`fmin`/`fmax`；标量结果、C 调用约定、固定向量总宽 1..128。`half` 浮点归约另要求函数 `target-features` 最后令牌为 `+fullfp16`；`f32`/`f64` 与整数归约不受该门控。固定 1..128 `bfloat` 的 `fadd`/`fmul`/`fmin`/`fmax` 是独立 last-token `+bf16` 合法化表面，不得重放原生 `llvm.vector.reduce.*.bf16`（见独立节） |
| 冻结 | 标量整数 / 地址空间零指针 / 标量 `half`/`f32`/`f64` 与最后令牌 `+bf16` 标量 `bfloat` 的 `freeze`；标量 IEEE `fp128` 的 `FP128Freeze`（独立类型化帧）；受支持固定向量（总位宽 1..128，元素 i1/i8/i16/i32/i64/`half`/`f32`/`f64`，以及最后令牌 `+bf16` 的 `bfloat`）的 `VectorFreeze`（原生 `CreateFreeze`，`undef`/`poison` 仅在冻结处物化，不新增操作码） |
| 原子 | 原子 `load`/`store`、受限子集 `atomicrmw`（原子读改写）与 `cmpxchg`（比较交换）、受限 `fence`（栅栏）、`memcpy`/`memmove`/`memset.element.unordered.atomic`；内存序与同步域保留。独立 `atomicrmw xchg` 整数锁定见 `vmp-atomicrmw-xchg-integer-semantic.ll`；独立整数 `add`／`sub` 锁定见 `vmp-atomicrmw-add-sub-semantic.ll`；独立整数 `and`／`or`／`xor` 锁定见 `vmp-atomicrmw-and-or-xor-semantic.ll`；独立整数 `max`／`min`／`umax`／`umin` 锁定见 `vmp-atomicrmw-minmax-semantic.ll`；独立整数 `nand` 锁定见 `vmp-atomicrmw-nand-semantic.ll`；独立标量 `f32`／`f64` `fadd`／`fsub`／`fmax`／`fmin` 锁定见 `vmp-atomicrmw-float-ops-semantic.ll`；独立整数 `cmpxchg` 锁定见 `vmp-cmpxchg-integer-semantic.ll`；独立 `fence` 锁定见 `vmp-fence-ordering-semantic.ll`（原样 `CreateFence`，仅 LLVM 15 合法序）；指针／`f32`/`f64` `xchg` 见既有独立 lit |
| 其它内建 | 位内建（标量 `i1`..`i64` 与受限标量 `i128` 的 `bitreverse`/`bswap`/`ctpop`/`ctlz`/`cttz`；固定 `i8`／`i16`／`i32`／`i64` 向量见独立表面）、饱和（标量 `i1`..`i64` 与受限标量 `i128` 的 `sadd`/`uadd`/`ssub`/`usub`/`sshl`/`ushl.sat`；固定整数向量见独立表面）、`min`/`max`（含受限标量 `i128` 与定长整数向量）、`fshl`/`fshr`（标量 `i1`..`i64` 普通漏斗、受限标量 `i128` 普通漏斗；固定整数向量普通漏斗见独立表面）、定点乘除（标量 `i1`..`i64` 与受限标量 `i128`；固定 `i8`／`i16`／`i32`／`i64` 向量仅总宽 64／128，见独立表面）、`abs`（标量 `i1`..`i64` 与受限标量 `i128`；固定整数向量见独立表面）、`assume`（含受限知识操作数捆绑 `align`／`nonnull`／`noundef`／`dereferenceable`／`dereferenceable_or_null`／`cold`，见独立表面）、`llvm.is.constant`（受限标量／`i128`／固定向量，降为 `Move`，见独立表面）、受限通用 `llvm.ptrauth.sign`／`llvm.ptrauth.sign_generic`（见独立表面）、`llvm.stackprotector`（受限，见独立表面）、`llvm.experimental.noalias.scope.decl`（按 ID 丢弃，见独立表面）、`llvm.invariant.start`／`llvm.invariant.end`（仅形态良好的标量 AS0 配对、令牌不逃逸时按 lifetime 丢弃，见独立表面）、`lround`/`llround`/`lrint`/`llrint`（`i64` 结果；`f32`／`f64` 无 `+fullfp16` 门控，标量 `half` 要求最后令牌 `+fullfp16`，见 `vmp-half-lround-semantic.ll`）、`fptosi`/`fptoui.sat`（标量 `f32`／`f64`→`i8`／`i16`／`i32`／`i64`）、`llvm.objectsize`（仅静态可证明的标量 AS0 `i32`／`i64`，规划期 `getObjectSize` 后 `Move` 常量，见独立表面）、`ptrmask`、`llvm.ptr.annotation`（标量 AS0，降为 `PointerMove` 操作数 0，仅丢弃编译期注解常量，见独立表面）、`llvm.var.annotation`（AS0 void 标记，仅编译期注解操作数时按 lifetime 丢弃，见独立表面）、`llvm.annotation`（标量 `i1`..`i64`／受限 `i128`，降为 `Move` 操作数 0，仅丢弃编译期注解常量，见独立表面）、`launder`/`strip.invariant.group`（标量地址空间零指针，降为 `PointerMove` 操作数 0，不经 `CallDescriptor` 重放，见独立表面）、`clear_cache`、`thread.pointer`、`vscale`、受限 AArch64（64 位 ARM 架构）内建（`barrier`、`crc32*`、`clrex`、`hint`、`cls`、`sdiv`/`udiv`、`frint32z`/`frint64z`、`frint32x`/`frint64x`、NEON `tbl1`..`tbl4`/`tbx1`..`tbx4`、NEON `st1x2`/`st1x3`/`st1x4` 与 `st2`/`st3`/`st4`、NEON `st2lane`/`st3lane`/`st4lane`（车道立即数）、NEON `ld1x2`/`ld1x3`/`ld1x4` 与 `ld2`/`ld3`/`ld4`（仅 `extractvalue` 展开，不进 64 字节聚合帧）、NEON `ld2lane`/`ld3lane`/`ld4lane`（同样展开；车道立即数）、NEON `vcopy.lane`、NEON `sqadd`/`uqadd`/`sqsub`/`uqsub`（仅 ISel 整数向量）、NEON 混符号饱和累加 `suqadd`／`usqadd`（ISel BHSD 绑定累加器第 0 实参，不得降为 `add`／`min`／`max`）、NEON 减半加 `shadd`／`uhadd`／`srhadd`／`urhadd`（仅 ISel BHS：`v8i8`／`v16i8`／`v4i16`／`v8i16`／`v2i32`／`v4i32`，不得降为 `add`／移位）、NEON 减半减 `shsub`／`uhsub`（同一 BHS，ISel 直接匹配内建，不得降为 `sub`／移位）、NEON 饱和绝对值／取负 `sqabs`／`sqneg`（同一 ISel 整数向量，不得降为 `llvm.abs`／`sub`）、NEON 整数绝对值 `abs`（同一 ISel BHSD，clang 整数 `vabs`；标量仅 ISel `i64`，`INT_MIN` 保持，不得降为 `llvm.abs`／`sqabs`）、NEON 前导符号位计数 `cls`（仅 ISel BHS：`v8i8`／`v16i8`／`v4i16`／`v8i16`／`v2i32`／`v4i32`，不得降为 `ctlz`／标量 `cls`）、NEON `fmin`/`fmax`/`fminnm`/`fmaxnm`（仅 ISel `f32`/`f64` 向量；标量 `f32`／`f64`／`half` 走 `TwoOperandFPData`，`half` 另需最后令牌 `+fullfp16`）、NEON `fminp`/`fmaxp`/`fminnmp`/`fmaxnmp`（仅 ISel `f32`/`f64` 成对向量）、NEON `fminv`/`fmaxv`/`fminnmv`/`fmaxnmv`（仅 ISel `f32`/`f64` 跨车道归约）、NEON `frecpe`/`frsqrte`/`frecps`/`frsqrts`（仅 ISel `f32`/`f64` 倒数／倒平方根估算与迭代）、NEON `pmull`／`pmull64`／`pmul`、NEON `sqdmulh`／`sqrdmulh`（及 `.lane`／`.laneq`，仅 ISel `i16`／`i32` 向量；标量仅 ISel `i32`）、NEON `sqrdmlah`／`sqrdmlsh`（仅 ISel `i16`／`i32` 向量，最后令牌 `+rdm`）、NEON `smull`／`umull`（仅 ISel 低半长乘：`v8i8`→`v8i16`／`v4i16`→`v4i32`／`v2i32`→`v2i64`）、NEON `sqdmull`（仅 ISel HS 低半：`v4i16`→`v4i32`／`v2i32`→`v2i64`）、NEON 标量饱和加倍长乘 `sqdmulls.scalar`（仅 `i64(i32, i32)`，clang `vqdmulls_s32`，不得降为 `mul`／`smull`／向量 `sqdmull`）、NEON 饱和加倍长乘累加／减 `sqdmlal`／`sqdmlsl`（LLVM 15 无对应 IR ID，ISel 匹配 `sqadd`／`sqsub`+`sqdmull`）、NEON 加宽加减 `saddl`／`uaddl`／`ssubl`／`usubl`（LLVM 15 无对应 IR ID，ISel 匹配 `sext`／`zext`+`add`／`sub`）、NEON 变窄高半 `addhn`／`subhn`／`raddhn`／`rsubhn`（仅 ISel 低半：`v8i16`→`v8i8`／`v4i32`→`v4i16`／`v2i64`→`v2i32`）、NEON 成对加宽 `saddlp`／`uaddlp`、NEON 同宽成对加 `addp`、NEON 同宽成对浮点加 `faddp`（ISel `v2f32`／`v4f32`／`v2f64`，`half` 另需最后令牌 `+fullfp16`，不得降为元素级 `fadd`）、NEON 跨车道浮点加 `faddv`（仅 ISel `f32`←`v2f32`／`v4f32` 与 `f64`←`v2f64`，不得降为 `vector.reduce.fadd`／`faddp`）、NEON 加宽跨车道加 `saddlv`／`uaddlv`（`i32`←`v8i8`／`v16i8`／`v4i16`／`v8i16`，`i64`←`v2i32`／`v4i32`，不得降为 `saddv`／`saddlp`／`vector.reduce.add`）、NEON 元素级整数最值 `smax`／`smin`／`umax`／`umin`（ISel BHS：`v8i8`／`v16i8`／`v4i16`／`v8i16`／`v2i32`／`v4i32`，clang `vmax`／`vmin`，不得降为 `llvm.smax`／`smaxp`）、NEON 绝对浮点比较 `facge`／`facgt`（整数掩码：`v2i32`←`v2f32`／`v4i32`←`v4f32`／`v2i64`←`v2f64`，`half` 另需最后令牌 `+fullfp16`，不得降为 `fabs`+`fcmp`；标量 `i32(f32)`／`i64(f64)`／`i32(f16)` 走 `SIMDThreeScalarFPCmp`）、NEON 成对整数极值 `smaxp`／`umaxp`／`sminp`／`uminp`（仅 ISel BHS：`v8i8`／`v16i8`／`v4i16`／`v8i16`／`v2i32`／`v4i32`，不得降为元素级 `smax`／`smin`／洗牌／通用归约）、NEON 元素级整数最值 `smax`／`smin`／`umax`／`umin`（独立表面）、NEON 同宽绝对差 `sabd`／`uabd`、NEON 浮点绝对差 `fabd`（ISel `v2f32`／`v4f32`／`v2f64`，`half` 另需最后令牌 `+fullfp16`，不得降为 `fabs(fsub)`）、NEON 标量绝对差 `sisd.fabd`（ISel `SIMDFPThreeScalar` `f32`／`f64`，`half` 另需最后令牌 `+fullfp16`，不得降为 `fabs(fsub)`）、NEON 绝对差累加 `saba`／`uaba`（LLVM 15 无对应 IR ID，ISel 匹配 `add`+`sabd`／`uabd`）、NEON 加宽绝对差 `sabdl`／`uabdl`（LLVM 15 无对应 IR ID，ISel 匹配 `zext`(`sabd`／`uabd`)）、NEON 整数跨车道归约 `saddv`／`uaddv`／`sminv`／`uminv`／`smaxv`／`umaxv`、NEON 饱和可变移位 `sqshl`／`uqshl`／`sqrshl`／`uqrshl`（向量 ISel BHSD；标量仅 ISel `i32`／`i64`）、NEON 立即有符号转无符号饱和左移 `sqshlu`（ISel 仅立即 `SQSHLU_I`，splat 走 ImmediateArguments；标量仅 ISel `i32`／`i64` 立即，不得降为 `sqshl`／`ushl`／`shl`）、NEON 非饱和可变移位 `sshl`／`ushl`／`srshl`／`urshl`（向量 ISel BHSD；标量仅 ISel `i64`）、NEON 饱和变窄移位 `sqshrn`／`uqshrn`／`sqrshrn`／`uqrshrn`／`sqshrun`／`sqrshrun`（仅 ISel 低半 `<8 x i8>`←`<8 x i16>`／`<4 x i16>`←`<4 x i32>`／`<2 x i32>`←`<2 x i64>`；标量仅 ISel `i32(i64)`＋`vecshiftR32` 1..32，`i32` 立即数走 ImmediateArguments）、NEON 非饱和舍入变窄移位 `rshrn`（同一三宽与 ImmLeaf，无 `shrn`／`rshrn2` IR ID）、NEON 加宽左移 `shll`／`sshll`／`ushll`（仅低半 `v8i16`←`v8i8`／`v4i32`←`v4i16`／`v2i64`←`v2i32`；`shll` 按元素宽；`sshll`／`ushll` 的 `i32` 走 ImmediateArguments，`vecshiftL*` 0..7／15／31；高半为 extract＋同一 ID，不得降为 `sext`／`zext`＋`shl`）、NEON 移位插入 `vsli`／`vsri`（同宽整数向量，立即数 `vecshiftL`／`vecshiftR` 走 ImmediateArguments）、NEON 饱和变窄 `sqxtn`／`uqxtn`／`sqxtun`（仅 ISel 低半三对，无 `sqxtn2` IR ID）、NEON 标量饱和变窄 `scalar.sqxtn`／`scalar.uqxtn`／`scalar.sqxtun`（仅 ISel `i32(i64)`，clang `vqmovnd`，不得降为 `trunc`／向量 `sqxtn`）、NEON 扩展浮点乘 `fmulx`（`f32`／`f64` 向量基线 HasNEON，`half` 另需最后令牌 `+fullfp16`，无 `.lane` IR ID；标量 `f16`／`f32`／`f64` 走同一 ID 的 `SIMDFPThreeScalar`）、NEON 显式舍入浮点转整数 `fcvtas`／`fcvtau`／`fcvtms`／`fcvtmu`／`fcvtns`／`fcvtnu`／`fcvtps`／`fcvtpu`（不得降为 `fptosi`／`fptoui`；`half` 另需最后令牌 `+fullfp16`；标量 `i32`／`i64`←`f16`／`f32`／`f64` 走 `FPToIntegerUnscaled`，汇编 `fcvtas w/x, s/d/h`，`i16(f16)` 无 Pat）、NEON 向零饱和浮点转整数 `fcvtzs`／`fcvtzu`（同一向量 ISel 配对，不得降为 `fptosi`／`fptoui`；`half` 另需最后令牌 `+fullfp16`；标量 `i32`／`i64`←`f16`／`f32`／`f64` 走 `FPToIntegerIntPats`，汇编 `fcvtzs w/x, s/d/h`，`i16(f16)` 无 Pat）、NEON 定点转换 `vcvtfp2fxs`／`vcvtfp2fxu`／`vcvtfxs2fp`／`vcvtfxu2fp`（`i32` 小数位走 ImmediateArguments，`half` 另需最后令牌 `+fullfp16`；标量 `i32`↔`f32`／`i64`↔`f64` 与 `i32`／`i64`↔`f16` 走 `SIMDFPScalarRShift`，汇编 `fcvtzs s/d/h, #n`）、NEON 不精确变窄 `fcvtxn`／`sisd.fcvtxn`（仅 `v2f32`←`v2f64` 与标量 `f32`←`f64`，不得降为 `fptrunc`）、NEON 半精度／单精度转换 `vcvtfp2hf`／`vcvthf2fp`（固定 `v4i16`↔`v4f32`，汇编 `fcvtn`／`fcvtl`，不得降为 `fptrunc`／`fpext`）、NEON `frint32z`／`frint32x`／`frint64z`／`frint64x`（`v2f32`／`v4f32`／`v2f64`，最后令牌 `+fptoint`，不得降为 `ceil`／`floor`／`rint`）、NEON 倒数指数 `frecpx`（仅标量 `f32`／`f64`，标量 `half` 另需最后令牌 `+fullfp16`，无向量 ISel）、NEON 整数倒数估算 `urecpe`／`ursqrte`（仅 `<2 x i32>`／`<4 x i32>`）、NEON 点积 `sdot`／`udot`（仅 ISel `<2 x i32>`＋`<8 x i8>`／`<4 x i32>`＋`<16 x i8>`，最后令牌 `+dotprod`）、NEON 混符号点积 `usdot`（同一两宽，最后令牌 `+i8mm`，不得降为 `sdot`／`udot`）、NEON i8mm 矩阵乘累加 `smmla`／`ummla`／`usmmla`（仅 ISel `<4 x i32>`＋两个 `<16 x i8>`，最后令牌 `+i8mm`）、NEON BF16 点积／矩阵乘累加 `bfdot`／`bfmmla`（仅 ISel `<2 x float>`＋`<4 x bfloat>`／`<4 x float>`＋`<8 x bfloat>`，`bfmmla` 仅 128 位，最后令牌 `+bf16`）、NEON BF16 融合乘加长 `bfmlalb`／`bfmlalt`（仅 ISel `<4 x float>`＋两个 `<8 x bfloat>`，最后令牌 `+bf16`；LLVM 15 AArch64 无 `+bf16fml`）、NEON BF16 转换 `bfcvt`／`bfcvtn`／`bfcvtn2`（仅非重载 `bfloat(float)`／`<8 x bfloat>(<4 x float>)`／`<8 x bfloat>(<8 x bfloat>, <4 x float>)`，最后令牌 `+bf16`，`bfcvtn2` 绑定低半，不得降为 `fptrunc`／`fpext`）、NEON FP16 融合乘加／减长 `fmlal`／`fmlsl`／`fmlal2`／`fmlsl2`（仅 ISel `<2 x float>`＋`<4 x half>`／`<4 x float>`＋`<8 x half>`，最后令牌 `+fp16fml`）、NEON 复数融合乘加 `vcmla.rot0`／`rot90`／`rot180`／`rot270`（汇编 `fcmla`；仅 ISel `<4 x half>`／`<8 x half>`／`<2 x float>`／`<4 x float>`／`<2 x double>`，旋转在 ID 上，最后令牌 `+complxnum`，半精度另需 `+fullfp16`）、NEON 复数加 `vcadd.rot90`／`rot270`（汇编 `fcadd`；同一五布局，仅 90／270，最后令牌 `+complxnum`，半精度另需 `+fullfp16`）、NEON 字节反转／重排 `vrev16`／`vrev32`／`vrev64`（LLVM 15 无 `llvm.aarch64.neon.rev*` IR ID；clang 前端是常量掩码 `shufflevector`，ISel `isREVMask` 降为汇编 `rev16`／`rev32`／`rev64`，见独立审查节）、AArch64 crypto AES `aese`／`aesd`／`aesmc`／`aesimc`（仅非重载 `<16 x i8>` 状态，最后令牌 `+aes`）、AArch64 crypto SHA-1 `sha1h`／`sha1c`／`sha1p`／`sha1m`／`sha1su0`／`sha1su1`（非重载 `i32`／`<4 x i32>` 状态，最后令牌 `+sha2`）、AArch64 crypto SHA-256 `sha256h`／`sha256h2`／`sha256su0`／`sha256su1`（非重载 `<4 x i32>` 状态，最后令牌 `+sha2`）、AArch64 crypto SHA-512 `sha512h`／`sha512h2`／`sha512su0`／`sha512su1`（非重载 `<2 x i64>` 状态，最后令牌 `+sha3`）、AArch64 crypto SHA-3 `eor3s`／`eor3u`／`bcaxs`／`bcaxu`／`rax1`／`xar`（仅 `<2 x i64>` 状态，`xar` 的 `i64` ImmArg `timm0_63`，最后令牌 `+sha3`）、AArch64 crypto SM4 `sm4e`／`sm4ekey`（非重载 `<4 x i32>` 状态／密钥，最后令牌 `+sm4`）、AArch64 crypto SM3 `sm3ss1`／`sm3partw1`／`sm3partw2`／`sm3tt1a`／`sm3tt1b`／`sm3tt2a`／`sm3tt2b`（非重载 `<4 x i32>` 状态，`tt*` 的 `i64` ImmArg 车道 `[0,4)`，最后令牌 `+sm4`）） |

**当前安全跳过的语义边界**

- EH（异常处理）相关指令与着陆块：Windows funclet（`catchswitch`／`catchpad`／`cleanuppad`）、间接／可变参数／捆绑／`musttail`／`noreturn`／复杂 ABI 的 `invoke`、非 Itanium personality、畸形或共享不合格着陆块、超出受限直接 `InlineAsm` 表面的 `callbr`（跳过原因 `callbr`，见独立表面）、协程与 `llvm.coro.*`。带 `personality` 且函数体无 EH 控制流的形态已接受（不按符号名过滤）：`deleteBody` 会清掉 hung-off personality，解释器发射后必须把原 Constant 写回。语义上不抛出、展开目的为不可达最小 landingpad 岛的直接 `invoke` 仍降为普通直接调用重放（见下）。**可达** Itanium `{ptr,i32}` `invoke`／`landingpad`／`resume` 是独立表面（见下），不再整族跳过。见 `vmp-cxx-personality-no-eh-semantic.ll`、`vmp-nounwind-invoke-semantic.ll` 与 `vmp-itanium-invoke-semantic.ll`；
- 超出保守同函数 C `va_start`/`va_arg`/`va_end`／一次 `va_copy` 子集的可变参数（含重复或链式 `va_copy`、非入口或动态 `va_list`、vector/aggregate/`half`/`bfloat` `va_arg`、可变参数函数再发起可变参数尾部调用、非 C 调用约定）、`musttail`（强制尾调用）或间接的 `noreturn`（不返回）调用（已支持表面上的普通直接 `tail` 由通用接受门收下，解释器按 `TCK_None` 重放，见独立普通 tail 节；`musttail` 仍走既有早期诊断；`sret`／`byval`／`byref`／专用 `noreturn` 与其它形状助手不得再因普通 `tail` 拒绝；`llvm.trap`／`ubsantrap`／`aarch64.break` 等专用终结路径同样只拒绝 `musttail`，普通 `tail` 按 `TCK_None` 重放）、`noreturn` 内建（仍走 `llvm.trap` 等既有专用终结路径，不并入普通 noreturn 调用）、以及复杂 ABI（应用二进制接口）调用（含非该受限形态的 `sret`/`byval`/`byref`，以及 `preallocated`/`inalloca`/`nest`/`swifterror`/`swiftasync`/`elementtype`）；
- 超出小型扁平聚合边界的聚合与向量字段：可伸缩向量字段、指针向量字段、超过 64 字节、超过 8 个直接字段或两层及以上嵌套的聚合（一层叶子嵌套已支持）、以及总位宽不是 8/16/32/64/128 的固定向量字段（含 `<3 x half>`）；无最后令牌 `+bf16` 的标量 `bfloat` 以及 `ppc_fp128` 及更宽浮点、非零地址空间；标量 IEEE `fp128` 基础 SSA 见独立表面；独立可伸缩向量、指针向量与超过 128 位的固定向量仍拒绝；可伸缩、指针、聚合、`ppc_fp128` 或超过 128 位／`fp128` 向量上的 `freeze` 仍安全跳过；标量 IEEE `fp128` `freeze` 见独立表面；缺少或最后令牌为 `-bf16` 的形态良好 `bfloat` 向量 `freeze` 以 `unsupported target feature` 跳过（见独立 `bfloat` 向量表面）；标量 `half` 不得进入原子 `load`/`store`/`atomicrmw`、缺少或最后令牌为 `-fullfp16` 的 listed 数学／超越内建／`is.fpclass`／饱和转换／`lround`／`llround`／`lrint`／`llrint`（`i64`）、constrained／`bfloat`/`fp128` 数学内建、非 `i32` 指数的 `powi`，或既有仅 `f32`/`f64` 的其它目标／取整到整数内建（`i32` 结果的 `lround` 等仍拒绝）；`half` 向量不得进入缺少或最后令牌为 `-fullfp16` 的 listed 向量数学／浮点归约／`is.fpclass`／`canonicalize`／饱和转换、未列入的向量数学、`llvm.vp.reduce.*`、`half`↔`double` 宽度转换、可变参数或原子访问；标量 `i128` 不得进入原子、`insertvalue`／整体使用的 `*with.overflow` 聚合、`va_arg`、`switch` 条件、GEP 下标、动态 `alloca` 长度或 `i128` 元素、`ptrtoint`/`inttoptr`、可变参数具名／尾部实参、间接调用、非 C 约定的直接调用、向量元素、扁平聚合字段、或既有仅 `i1`..`i64` 的其它未列入整数内建；
- 非入口、或以 `load`/`store`/`call`/`alloca`/`freeze`/`phi`/原子/内建/指针/浮点/向量/聚合/`undef`/`poison`/循环依赖算出长度的动态栈分配；入口动态 `alloca` 与同一函数内的 `llvm.stacksave` / `llvm.stackrestore` / `llvm.get.dynamic.area.offset` 组合（跳过原因 `dynamic stack state`）；超出规则子集的原子形态、超出规则子集的间接调用，以及不受支持的调用属性或调用约定；
- 上述边界外的一切指令形态。

**当前实际 llvm.objectsize 支持边界**

- 接受：精确非 constrained 标量 `llvm.objectsize`，结果仅 `i32`／`i64`，指针为地址空间零，三个 `i1` ImmArg（`min`／`nullunknown`／`dynamic`），`dynamic` 必须为 false，`CallingConv::C`、非可变参数。必须在规划期对**原指针**调用 LLVM 15 `getObjectSize`（`ObjectSizeOpts::EvalMode` 按 `min` 取 Min／Max，`NullIsUnknownSize` 取 `nullunknown`，`RoundToAlign` 保持 false，与 `lowerObjectSizeCall` 一致；`TLI` 为空，不做 malloc 族）。成功且尺寸能放入结果宽度则降为把该 `ConstantInt` 写入整数虚拟寄存器的 `Move`。AS0 `null` 按 LangRef：`nullunknown=false` 为 0；`nullunknown=true` 时按 `min` 为 0 或全 1。覆盖静态 `alloca`、有确定初值的全局、指向它们的常量 GEP、两个已知对象的 `select`（检验 min／max）、以及 `null`。**不得** `CallDescriptor` 重放：解释器从指针虚拟寄存器 load 后不再是原对象，已知尺寸会变成未知 0／-1。宿主 `IntrinsicLowering` 无 `objectsize` 分支（`lli` fatal），参考函数写 DataLayout 分配尺寸。不新增操作码。`vmp-objectsize-intrinsic-semantic.ll` 用 AArch64 `datalayout`（`{i8,i32}` 分配 8，不是 store size 5）覆盖上述正例与参数／`inttoptr`／`dynamic=true`／`poison`／`undef`／AS1／`i128`／`i8`／`musttail`／bundle／`fastcc`／汇编／间接／`sret` 负例。
- 拒绝：分析失败（函数参数、`inttoptr`、load、无 TLI 的 malloc 族）、`dynamic=true`（`ObjectSizeOffsetEvaluator` 可能插入运行时 IR）、非零地址空间、`i1`／`i8`／`i16`／`i128` 结果、`poison`／`undef`、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。失败时不把未知折成 0／-1。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 与内联汇编除外）。

**当前实际 llvm.thread.pointer 支持边界**

- 接受：精确 `ptr @llvm.thread.pointer()`（零实参，AS0 指针结果）。LangRef：当前线程 TLS 区指针，目标相关。AArch64 ISel 降为 `AArch64ISD::THREAD_POINTER`／`MRS TPIDR_EL0`。**不得**折成常量或全局指针。经既有 `CallDescriptor` 重放：原 `Function*`、`FunctionType` `ptr()`、`CallingConv::C`、`AttributeList`、`DebugLoc`，结果写入指针虚拟寄存器。普通 `tail` 降为普通 call。不新增操作码。宿主 `IntrinsicLowering` 无此分支（`lli` fatal），且 x86／AArch64 TLS ABI 不同，故 `vmp-thread-pointer-intrinsic-semantic.ll` 只走 FileCheck 与 AArch64 `llc`／`readobj`／汇编（确认 `TPIDR_EL0`）。覆盖两次调用、与 `@g`／`thread_local` 比较、把 TP 存入／load／GEP、分支、回边循环、普通 `tail`；负例为 `musttail`／bundle／`fastcc`／汇编／间接／`sret`／AS1 形参、与 `poison` 的指针 icmp（零实参内建无 poison 操作数形态）、以及调用点 `noreturn`／`returns_twice`（畸形）。
- 拒绝：`musttail`、操作数捆绑、`fastcc`／非 C、可变参数、`noreturn`／`returns_twice`／复杂 ABI。非零地址空间结果无法声明（固定 `llvm_ptr_ty`）。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 与内联汇编除外）。

**当前实际 llvm.returnaddress 支持边界**

- 接受：精确 `ptr @llvm.returnaddress(i32 0)`，深度必须是带 `ImmArg` 的 `i32` `ConstantInt` 0，结果为地址空间零指针，`CallingConv::C`，非可变参数。LangRef：当前函数（深度 0）或调用者（深度 > 0）的返回地址；非 0 深度通常不可靠。AArch64 `LowerRETURNADDR` 深度 0 读入站 `LR`／`x30` 再 `XPACLRI`（`hint #7`）；深度 > 0 沿 `FP` 链走。VMP 就地改写为本函数解释器，post-CFF 也仍是同一 `Function`，故深度 0 仍是本函数保存的 `LR`，必须按原内建重放，**不得**折成常量／`null`／合成指针。`i32 0` 经 `isCallImmediateArgument` 留在 `CallDescriptor::ImmediateArguments`，不进虚拟寄存器（否则 `cast<ConstantSDNode>` 失败）。结果写入指针虚拟寄存器。普通 `tail` 降为普通 call。不新增操作码。宿主 `IntrinsicLowering`／JIT 对两次读取并不忠实（已探测 `lli` 不可比），故 `vmp-returnaddress-intrinsic-semantic.ll` 只走 FileCheck 与 AArch64 `llc`／`readobj`／汇编（`hint #7`）。覆盖同函数两次读取、回边循环、普通 `tail`；负例为深度 1、与 `poison` 的指针 icmp（`ImmArg` 不允许 poison／动态深度，verifier 会拒）、AS1 形参、`musttail`／bundle／`fastcc`／`sret`+`byval`。
- 拒绝：深度 > 0（VMP／CFF 改帧后沿 `FP` 走不可信）、`poison`／`undef`／非常量深度、非 C、可变参数、非 AS0 结果、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 llvm.frameaddress 支持边界**

- 接受：精确 `ptr @llvm.frameaddress.p0(i32 0)`，深度必须是带 `ImmArg` 的 `i32` `ConstantInt` 0，结果为地址空间零指针，`FunctionType` 精确 `ptr(i32)` 非可变参数，`CallingConv::C`。LangRef：指定栈帧的帧指针；非 0 深度通常不可靠。AArch64 `LowerFRAMEADDR` 深度 0 复制 `FP`／`x29`；深度 > 0 沿 `FP` 链 load。VMP 就地改写为本函数解释器，post-CFF 也仍是同一 `Function`，故深度 0 仍是本函数的 `FP`，必须按原内建重放，**不得**折成常量／`null`／合成指针。`i32 0` 经 `isCallImmediateArgument` 留在 `CallDescriptor::ImmediateArguments`，不进虚拟寄存器（否则 `cast<ConstantSDNode>` 失败）。结果写入指针虚拟寄存器。普通 `tail` 接受并按非 tail `CreateCall`／`TCK_None` 重放（资格与非 tail 相同；见独立普通 tail 节）。不新增操作码。宿主 `IntrinsicLowering`／JIT 对帧地址不忠实，且 VMP 改帧后指针值本身不可与原生函数交叉比较，故 `vmp-frameaddress-intrinsic-semantic.ll` 只走 FileCheck 与 AArch64 `llc`／`readobj`／汇编（`x29`）。覆盖同函数两次读取、回边循环；负例为深度 1、`.p1` 非 AS0、与 `poison` 的指针 icmp、调用点 `noreturn`（畸形；非 `ptr(i32)`／可变参数 `FunctionType` 无法与规范声明同模块通过 verifier）、`musttail`／bundle／`fastcc`／`sret`+`byval`。
- 拒绝：深度 > 0、`musttail`、`poison`／`undef`／非常量深度、非 C、可变参数或非 `ptr(i32)` 函数类型、非 AS0 结果、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 llvm.addressofreturnaddress 支持边界**

- 接受：精确 `ptr @llvm.addressofreturnaddress.p0()`，零实参，结果为地址空间零指针，`FunctionType` 精确 `ptr()` 非可变参数，`CallingConv::C`。LangRef：当前函数返回地址槽的目标相关指针（仅 x86／AArch64）。AArch64 `LowerADDROFRETURNADDR` 为 `FP+8`。VMP 就地改写为本函数解释器，槽仍是本函数保存的 `LR` 槽，必须按原内建重放，**不得**折成常量／`null`／合成指针。结果写入指针虚拟寄存器。普通 `tail` 接受并按非 tail `CreateCall`／`TCK_None` 重放。不新增操作码。宿主 `IntrinsicLowering`／JIT 对槽地址不忠实，故 `vmp-addressofreturnaddress-intrinsic-semantic.ll` 只走 FileCheck 与 AArch64 `llc`／`readobj`／汇编（`x29`）。覆盖同函数两次读取；负例为调用点 `noreturn`（畸形）、`.p1` 非 AS0、`musttail`／bundle／`fastcc`。可变参数或非 `ptr()` `FunctionType` 无法与规范声明同模块通过 verifier。
- 拒绝：`musttail`、操作数捆绑、`fastcc`／非 C、可变参数、非 AS0 结果、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 llvm.sponentry 支持边界**

- 接受：精确 `ptr @llvm.sponentry.p0()`，零实参，结果为地址空间零指针，`FunctionType` 精确 `ptr()` 非可变参数，`CallingConv::C`。LangRef：当前函数入口栈指针，仅在 AArch64／ARM 上验证。AArch64 `LowerSPONENTRY` 为偏移 0 的固定帧对象。VMP 就地改写为本函数解释器，值仍是本函数入口 `SP`，必须按原内建重放，**不得**预计算或折成常量／`null`。结果写入指针虚拟寄存器。普通 `tail` 接受并按非 tail `CreateCall`／`TCK_None` 重放。不新增操作码。宿主未验证该内建，故 `vmp-sponentry-intrinsic-semantic.ll` 只走 FileCheck 与 AArch64 `llc`／`readobj`／汇编（`sp`）。覆盖同函数两次读取与回边循环中保存入口 SP；负例为调用点 `noreturn`（畸形）、`.p1` 非 AS0、`musttail`／bundle／`fastcc`／`sret`+`byval`。可变参数或非 `ptr()` `FunctionType` 无法与规范声明同模块通过 verifier。
- 拒绝：`musttail`、操作数捆绑、`fastcc`／非 C、可变参数、非 AS0 结果、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 llvm.stackguard 支持边界**

- 接受：精确 `ptr @llvm.stackguard()`，零实参，结果为地址空间零指针，`FunctionType` 精确 `ptr()` 非可变参数，`CallingConv::C`。LangRef：系统栈金丝雀（常为 `@__stack_chk_guard`）。VMP **不**插入该符号或 ssp 属性；经既有 `CallDescriptor` 重放，让 AArch64 `LOAD_STACK_GUARD` 仍看见原内建，**不得**预计算或折成常量／`null`。结果写入指针虚拟寄存器。普通 `tail` 接受并按非 tail `CreateCall`／`TCK_None` 重放。不新增操作码。宿主 JIT 无法提供 `__stack_chk_guard`（会崩溃），故 `vmp-stackguard-intrinsic-semantic.ll` 只走 FileCheck 与 AArch64 `llc`／`readobj`／汇编（模块须保留外部 `@__stack_chk_guard` 与 `llvm.compiler.used`，否则 Post-RA 会崩）。覆盖同函数两次读取与回边循环；负例为调用点 `noreturn`（畸形）、AS1 形参（固定 `llvm_ptr_ty`，无 `.p1`）、`musttail`／bundle／`fastcc`／`sret`+`byval`。
- 拒绝：`musttail`、操作数捆绑、`fastcc`／非 C、可变参数、非 AS0 结果、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 llvm.stacksave／llvm.stackrestore 支持边界**

- 接受：精确 `ptr @llvm.stacksave()`（`FunctionType` `ptr()`）与 `void @llvm.stackrestore(ptr)`（`FunctionType` `void(ptr)`，实参类型等于形参），均为地址空间零、`CallingConv::C`、非可变参数。LangRef：保存／恢复当前函数栈状态（C99 VLA）。经既有 `CallDescriptor` 重放，保存的指针走指针虚拟寄存器，**不得**预计算或折成常量／`null`。可与静态 `alloca` 以及嵌套 save／restore 共存。同一函数内的入口动态 `alloca` 仍以 `dynamic stack state` 拒绝（序言提前分配会打乱栈令牌顺序）。普通 `tail` 接受并按非 tail `CreateCall`／`TCK_None` 重放。不新增操作码。宿主 `IntrinsicLowering` 把 `stacksave` 降为 `null`、`stackrestore` 为空操作，故 `vmp-stacksave-stackrestore-intrinsic-semantic.ll` 在 triple-swap 后走 `lli -force-interpreter` 做参考／保护差分，并用 AArch64 `llc`／`readobj`。覆盖成对 save／restore、嵌套令牌加静态 `alloca`；负例为入口动态 `alloca` 组合、调用点 `noreturn`、`stackrestore(poison)`、AS1 形参、`musttail`／bundle／`fastcc`／`sret`+`byval`。
- 拒绝：入口动态 `alloca` 同函数、`musttail`、操作数捆绑、`fastcc`／非 C、可变参数、非 AS0、`poison`／`undef` 恢复指针、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 llvm.localaddress 支持边界**

- 接受：精确 `ptr @llvm.localaddress()`，零实参，结果为地址空间零指针，`FunctionType` 精确 `ptr()` 非可变参数，`CallingConv::C`。LangRef：当前函数局部变量区地址。AArch64 为 `getLocalAddressRegister` 的 `CopyFromReg`（无 EH funclet 且无变长对象时为 `SP`）。经既有 `CallDescriptor` 重放，结果写入指针虚拟寄存器，**不得**合成或折成常量。`localescape`／`localrecover`／`eh.recoverfp` 仍拒绝。普通 `tail` 接受并按非 tail `CreateCall`／`TCK_None` 重放。不新增操作码。宿主 `IntrinsicLowering` 无此分支（纯解释器 fatal），故无 `lli`。`vmp-localaddress-intrinsic-semantic.ll` 覆盖两次读取、静态 `alloca`／`GEP`，以及调用点 `noreturn`、AS1 形参、`musttail`／bundle／`fastcc`／`sret`，并保留 `localescape`／`localrecover`／`eh.recoverfp` 负例。AArch64 `llc`／`readobj`／汇编确认 `sp`。
- 拒绝：`musttail`、操作数捆绑、`fastcc`／非 C、可变参数、非 AS0、`noreturn`／`returns_twice`／复杂 ABI、`localescape`／`localrecover`／`eh.recoverfp`。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 与可变参数 `localescape` 除外）。

**当前实际 llvm.readcyclecounter／llvm.flt.rounds／llvm.set.rounding 支持边界**

- 接受：精确 `i64 @llvm.readcyclecounter()`（`FunctionType` `i64()`）、`i32 @llvm.flt.rounds()`（`FunctionType` `i32()`）、`void @llvm.set.rounding(i32)`（`FunctionType` `void(i32)`，实参类型等于形参），均为 `CallingConv::C`、非可变参数。经既有 `CallDescriptor` 重放，计数器／舍入模式走整数虚拟寄存器，**不得**预折成常量或删除。AArch64：`readcyclecounter` 在 `+perfmon` 下为 `MRS PMCCNTR_EL0`，否则 0；`flt.rounds`／`set.rounding` 读写 `FPCR[23:22]`。普通 `tail` 接受并按非 tail `CreateCall`／`TCK_None` 重放。不新增操作码。宿主 `IntrinsicLowering` 把 `readcyclecounter` 换成 0（带警告）、`flt.rounds` 换成常量 1，且无 `set.rounding` 分支，故无 `lli`。`vmp-readcyclecounter-intrinsic-semantic.ll`／`vmp-flt-rounds-intrinsic-semantic.ll`／`vmp-set-rounding-intrinsic-semantic.ll` 覆盖两次读取、alloca／掩码使用、以及 `set.rounding` 与 `flt.rounds` 成对读写；负例为调用点 `noreturn`、AS1 形参、`poison` 模式（仅 set.rounding）、`musttail`／bundle／`fastcc`／`sret`+`byval`。AArch64 `llc`／`readobj`／汇编确认 `PMCCNTR_EL0`／`FPCR`。
- 拒绝：`musttail`、操作数捆绑、`fastcc`／非 C、可变参数、非固定宽度、`poison`／`undef` 模式、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 llvm.prefetch／llvm.sideeffect／llvm.donothing／llvm.aarch64.{dmb,dsb,isb} 支持边界**

- 接受：精确 `void @llvm.prefetch.p0(ptr, i32, i32, i32)`（`FunctionType` `void(ptr, i32, i32, i32)`，AS0 普通指针，实参类型等于形参，`rw`∈{0,1}、`locality`∈{0..3}、`cache`∈{0,1} 的 `ConstantInt`，1/2 为 `ImmArg`，3 仍作立即数进描述符）；精确 `void @llvm.sideeffect()`／`void @llvm.donothing()`（`FunctionType` `void()`）；精确 `void @llvm.aarch64.{dmb,dsb,isb}(i32)`（`FunctionType` `void(i32)`，CRm 为描述符立即数：dmb/dsb ∈ {1,2,3,5,6,7,9,10,11,13,14,15}，isb 仅 15）。均为 `CallingConv::C`、非可变参数。经既有 `CallDescriptor` 重放，**不得**折叠或删除。普通 `tail` 接受并按非 tail `CreateCall`／`TCK_None` 重放。不新增操作码。宿主 `IntrinsicLowering` 可剥掉 `prefetch`，但无 `sideeffect`／`donothing` 分支且无法选择 AArch64 屏障，故无 `lli`。`vmp-prefetch-intrinsic-semantic.ll`／`vmp-sideeffect-intrinsic-semantic.ll`／`vmp-donothing-intrinsic-semantic.ll`／`vmp-aarch64-barrier-intrinsic.ll` 覆盖合法重放与 `noreturn`／AS1／`poison`／越界立即数／`musttail`／bundle／`fastcc`／`sret`+`byval` 负例。AArch64 `llc`／`readobj`／汇编确认 `prfm`／`dmb sy`／`dsb ish`／`isb`。
- 拒绝：`musttail`、操作数捆绑、`fastcc`／非 C、可变参数、非 AS0／`poison` 指针、越界或动态 CRm／prefetch 立即数、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 llvm.aarch64.clrex／hint／cls／sdiv／udiv／crc32*／frint*z/x.f32 支持边界**

- 接受：精确 `void @llvm.aarch64.clrex()`（`void()`）；`void @llvm.aarch64.hint(i32)`（立即数 0..127，走描述符立即数）；`i32 @llvm.aarch64.cls(i32)`／`cls64(i64)`；`i32/i64 @llvm.aarch64.{s,u}div.i{32,64}`（结果与两实参同宽）；`i32 @llvm.aarch64.crc32{b,h,w,cb,ch,cw}(i32,i32)` 与 `crc32{x,cx}(i32,i64)`（函数最后令牌 `+crc`）；标量 `float @llvm.aarch64.frint{32,64}{z,x}.f32(float)`（最后令牌 `+fptoint`；FMF 仍走通用浮点调用门）。均为 `CallingConv::C`、非可变参数、实参类型等于形参。经既有 `CallDescriptor` 重放，**不得**折叠或删除。普通 `tail` 接受并按非 tail `CreateCall`／`TCK_None` 重放。不新增操作码，不放宽宽度。`vmp-aarch64-target-helpers-intrinsic-semantic.ll`（O0/O2×97/7）覆盖重放与 `noreturn`／`musttail`／bundle／`fastcc`／`poison`／动态／越界 hint／`sret`；既有 `vmp-aarch64-crc32w-intrinsic.ll` 仍覆盖全套 ID 与 `+crc`／`+fptoint` 门。AArch64 `llc`／`readobj`／汇编确认 `clrex`／`cls`／`sdiv`／`udiv`／`crc32w`／`frint32z`。
- 拒绝：`musttail`、操作数捆绑、`fastcc`／非 C、可变参数、`poison`、动态／越界 hint、缺／关 `+crc` 或 `+fptoint`（原因仍为 `unsupported target feature`）、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 llvm.memcpy／memmove／memset／memcpy.inline／element.unordered.atomic 支持边界**

- 接受：精确 `void @llvm.{memcpy,memmove}.p0.p0.i{32,64}(ptr, ptr, iN, i1)`、`void @llvm.memset.p0.i{32,64}(ptr, i8, iN, i1)`、`void @llvm.memcpy.inline.p0.p0.i{32,64}(...)`（长度 `ImmArg` 常量）、以及 `memcpy/memmove/memset.element.unordered.atomic`（AS0、显式 `align >= elementsize`、正常量长度且为 2 的幂 `elementsize` 的倍数）。均为 `CallingConv::C`、非可变参数、实参类型等于形参。`isvolatile` 仍为描述符立即数。经既有 `CallDescriptor` 重放，**不得**折叠。普通 `tail` 接受并按非 tail `CreateCall`／`TCK_None` 重放。不新增操作码，不放宽到 i8 长度或非 AS0。`vmp-memory-intrinsics-eligibility-semantic.ll`（O0/O2×97/7）覆盖重放、宿主普通 mem 差分，以及 `noreturn`／`musttail`／bundle／`fastcc`／`sret`／AS1；既有 dynamic／volatile／inline／atomic 语义 lit 仍覆盖长度与对齐规则。
- 拒绝：`musttail`、操作数捆绑、`fastcc`／非 C、可变参数、非 AS0、非 i32/i64 长度、动态／零／非倍数 atomic 长度、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AtomicRMW xchg 支持边界**

- 接受：仅 AArch64。既有 `VMOpcode::AtomicRMW`＋`AtomicRMWDescriptor` 对 `atomicrmw xchg` 原样 `CreateAtomicRMW`，**不是** load／store 对，也不是调用／ABI。结果是旧值，新值写入同一地址；解释器复制对齐、`volatile`、内存序、同步域、调试位置与内存元数据（`applyMetadata` 仍剥离 `!alias.scope`／`!noalias`／`!invariant.load`／`!invariant.group`，保留 `!tbaa` 等）。最小与当前帧兼容的子集：AS0 标量 `i8`／`i16`／`i32`／`i64`（整数虚拟寄存器，`fromSlot`／`toSlot`）；另已存在、本轮不改动的旁路为 AS0 指针载荷（指针帧，`BitWidth=0`）与标量 `f32`／`f64`（浮点帧）。合法内存序仅 `monotonic`／`acquire`／`release`／`acq_rel`／`seq_cst`。本轮**不**收窄也不拓宽 `add`／`sub`／`and`／`or`／`xor`／`nand`／`max`／`min`／`umax`／`umin`／`fadd`／`fsub`／`fmax`／`fmin`、`cmpxchg` 或 `fence`。不新增操作码。`vmp-atomicrmw-xchg-integer-semantic.ll`（O0/O2×97/7）覆盖四宽、五序、`volatile`＋`singlethread`、自然对齐、`!tbaa` 重放、独立旧值／终值观察（不用 XOR 混折）、宿主 `lli` 与 AArch64 `llc`／`readobj`；指针／浮点 `xchg` 仍由 `vmp-atomicrmw-xchg-pointer-semantic.ll`／`vmp-atomicrmw-xchg-float-semantic.ll` 覆盖；混合整数 RMW 遗留路径仍是 `vmp-atomicrmw-xchg.ll`。
- 拒绝：`i128`／非 `i8`..`i64` 整数宽、`half`／`bfloat`／`fp128`／聚合／向量载荷、非 AS0、非 xchg 的指针 `atomicrmw`（LLVM 15 验证器本身也不接受）、`unordered`（LLVM 15 验证器禁止 `atomicrmw` 使用该序，故无 skip 哨兵）、非 AArch64 模块。资格失败保持原函数与 `hikari.vmp.selected`，跳过原因 `unsupported atomicrmw instruction`（向量返回等先被类型门挡住的除外）。本轮不把已接受的 `add`／`sub`／…改成负例。

**当前实际 AtomicRMW 整数 add／sub 支持边界**

- 接受：仅 AArch64。既有 `VMOpcode::AtomicRMW`＋`AtomicRMWDescriptor` 对 `atomicrmw add`／`sub` 原样 `CreateAtomicRMW`，**不是** load／store 对，也不是调用／ABI。结果是旧值；单元写入 `old+delta`／`old-delta` 的二进制补码回绕（有符号与无符号同一比特语义）。解释器复制对齐、`volatile`、内存序、同步域、调试位置与内存元数据（`applyMetadata` 仍剥离 scoped-AA／invariant，保留 `!tbaa`）。子集仅 AS0 标量 `i8`／`i16`／`i32`／`i64`（整数虚拟寄存器）。合法内存序仅 `monotonic`／`acquire`／`release`／`acq_rel`／`seq_cst`。本轮**不**纳入 `and`／`or`／`xor`／`nand`／`max`／`min`／`umax`／`umin`、浮点 `fadd`／`fsub`／`fmax`／`fmin`、`xchg`、`cmpxchg` 或 `fence`（既有表面保持不变）。不新增操作码。`vmp-atomicrmw-add-sub-semantic.ll`（O0/O2×97/7）覆盖四宽加减、回绕、五序、`volatile`＋`singlethread`、自然对齐、`!tbaa`、独立旧值／终值观察、宿主 `lli` 与 AArch64 `llc`／`readobj`。混合整数 RMW 遗留路径仍是 `vmp-atomicrmw-xchg.ll`。
- 拒绝：`i128` `add`／`sub`、非 `i8`..`i64` 整数宽、指针／浮点／聚合／向量载荷（LLVM 15 对非 xchg 的非整数 `atomicrmw` 直接验证失败，故无指针／浮点 add 哨兵）、非 AS0、`unordered`（验证器禁止）、非 AArch64 模块。资格失败保持原函数与 `hikari.vmp.selected`，跳过原因 `unsupported atomicrmw instruction`（超宽向量返回等先被类型门挡住的除外）。本轮不把已接受的 `xchg`／位运算／最值／浮点 RMW 改成负例。

**当前实际 AtomicRMW 整数 and／or／xor 支持边界**

- 接受：仅 AArch64。既有 `VMOpcode::AtomicRMW`＋`AtomicRMWDescriptor` 对 `atomicrmw and`／`or`／`xor` 原样 `CreateAtomicRMW`，**不是** load／store 对，也不是调用／ABI。结果是旧值；单元写入 `old&mask`／`old|bits`／`old^bits`。解释器复制对齐、`volatile`、内存序、同步域、调试位置与内存元数据（`applyMetadata` 仍剥离 scoped-AA／invariant，保留 `!tbaa`）。子集仅 AS0 标量 `i8`／`i16`／`i32`／`i64`（整数虚拟寄存器）。合法内存序仅 `monotonic`／`acquire`／`release`／`acq_rel`／`seq_cst`。本轮**不**纳入 `nand`、`max`／`min`／`umax`／`umin`、浮点 RMW、`xchg`、`add`／`sub`、`cmpxchg` 或 `fence`（既有表面保持不变）。不新增操作码。`vmp-atomicrmw-and-or-xor-semantic.ll`（O0/O2×97/7）覆盖四宽与三运算、五序、`volatile`＋`singlethread`、自然对齐、`!tbaa`、独立旧值／终值观察、宿主 `lli` 与 AArch64 `llc`／`readobj`。混合整数 RMW 遗留路径仍是 `vmp-atomicrmw-xchg.ll`。
- 拒绝：`i128` `and`／`or`／`xor`、非 `i8`..`i64` 整数宽、指针／浮点／聚合／向量载荷（LLVM 15 对非 xchg 的非整数 `atomicrmw` 直接验证失败）、非 AS0、`unordered`（验证器禁止）、非 AArch64 模块。资格失败保持原函数与 `hikari.vmp.selected`，跳过原因 `unsupported atomicrmw instruction`（超宽向量返回等先被类型门挡住的除外）。本轮不把已接受的 `nand`／最值／加减／`xchg`／浮点 RMW 改成负例。

**当前实际 AtomicRMW 整数 max／min／umax／umin 支持边界**

- 接受：仅 AArch64。既有 `VMOpcode::AtomicRMW`＋`AtomicRMWDescriptor` 对 `atomicrmw max`／`min`／`umax`／`umin` 原样 `CreateAtomicRMW`，**不是** load／store 对，也不是调用／ABI。结果是旧值；单元写入有符号（`max`／`min`）或无符号（`umax`／`umin`）比较后的极值。解释器复制对齐、`volatile`、内存序、同步域、调试位置与内存元数据（`applyMetadata` 仍剥离 scoped-AA／invariant，保留 `!tbaa`）。子集仅 AS0 标量 `i8`／`i16`／`i32`／`i64`（整数虚拟寄存器）。合法内存序仅 `monotonic`／`acquire`／`release`／`acq_rel`／`seq_cst`。本轮**不**纳入 `nand`、浮点 `fmax`／`fmin`、`xchg`、`add`／`sub`、`and`／`or`／`xor`、`cmpxchg` 或 `fence`（既有表面保持不变）。不新增操作码。`vmp-atomicrmw-minmax-semantic.ll`（O0/O2×97/7）覆盖四宽与四运算、有符号／无符号分歧（`-20`/`5` 对 `-1`）、五序、`volatile`＋`singlethread`、自然对齐、`!tbaa`、独立旧值／终值观察；先在 AArch64 下变换，再只替换变换结果三元组后走宿主 `lli`，并用 AArch64 `llc`／`readobj`。混合整数 RMW 遗留路径仍是 `vmp-atomicrmw-xchg.ll`。
- 拒绝：`i128` `max`／`min`／`umax`／`umin`、非 `i8`..`i64` 整数宽、指针／浮点／聚合／向量载荷（LLVM 15 对非 xchg 的非整数 `atomicrmw` 直接验证失败）、非 AS0、`unordered`（验证器禁止）、非 AArch64 模块。资格失败保持原函数与 `hikari.vmp.selected`，跳过原因 `unsupported atomicrmw instruction`（超宽向量返回等先被类型门挡住的除外）。本轮不把已接受的 `nand`／加减／位运算／`xchg`／浮点 RMW 改成负例。

**当前实际 AtomicRMW 整数 nand 支持边界**

- 接受：仅 AArch64。既有 `VMOpcode::AtomicRMW`＋`AtomicRMWDescriptor` 对 `atomicrmw nand` 原样 `CreateAtomicRMW`，**不是** load／store 对、也不是 `and` 后再 `xor -1` 的手写降级。结果是旧值；单元写入 `~(old & rhs)`（与 `LowerAtomic` 一致）。解释器复制对齐、`volatile`、内存序、同步域、调试位置与内存元数据（`applyMetadata` 仍剥离 scoped-AA／invariant，保留 `!tbaa`）。子集仅 AS0 标量 `i8`／`i16`／`i32`／`i64`（整数虚拟寄存器）。合法内存序仅 `monotonic`／`acquire`／`release`／`acq_rel`／`seq_cst`。本轮**不**纳入 `and`／`or`／`xor`、最值、浮点 RMW、`xchg`、`add`／`sub`、`cmpxchg` 或 `fence`（既有表面保持不变）。不新增操作码。`vmp-atomicrmw-nand-semantic.ll`（O0/O2×97/7）覆盖四宽、与 `and` 可区分的 `~(x&y)` 序列、五序、`volatile`＋`singlethread`、自然对齐、`!tbaa`、独立旧值／终值观察；先在 AArch64 下变换，再 `internalize`／`globaldce` 只留 `main`，然后只替换变换结果三元组走宿主 `lli`，避免被拒的 `i128 nand` 在宿主上拉起不可用的原子辅助函数；并用 AArch64 `llc`／`readobj`。混合整数 RMW 遗留路径仍是 `vmp-atomicrmw-xchg.ll`。
- 拒绝：`i128` `nand`、非 `i8`..`i64` 整数宽、指针／浮点／聚合／向量载荷（LLVM 15 对非 xchg 的非整数 `atomicrmw` 直接验证失败）、非 AS0、`unordered`（验证器禁止）、非 AArch64 模块。资格失败保持原函数与 `hikari.vmp.selected`，跳过原因 `unsupported atomicrmw instruction`（超宽向量返回等先被类型门挡住的除外）。本轮不把已接受的 `and`／最值／加减／`xchg`／浮点 RMW 改成负例。

**当前实际 AtomicRMW 浮点 fadd／fsub／fmax／fmin 支持边界**

- 接受：仅 AArch64。既有 `VMOpcode::AtomicRMW`＋`AtomicRMWDescriptor` 对 `atomicrmw fadd`／`fsub`／`fmax`／`fmin` 原样 `CreateAtomicRMW`，**不是** load／算术／store 对。结果是旧值；单元写入浮点加／减，或 LLVM 15 LangRef 规定的 `maxnum`／`minnum`（一侧为 NaN 时取另一侧有限值；两端均为 NaN 才得 NaN；`fmax(+/-0,+/-0)` 可返回任一零，故不锁该比特）。解释器走浮点虚拟寄存器帧，复制对齐、`volatile`、内存序、同步域、调试位置与内存元数据（保留 `!tbaa`）。子集仅 AS0 标量 `f32`／`f64`。合法内存序仅 `monotonic`／`acquire`／`release`／`acq_rel`／`seq_cst`。本轮**不**纳入整数 RMW、`xchg`、`cmpxchg` 或 `fence`。不新增操作码。`vmp-atomicrmw-float-ops-semantic.ll`（O0/O2×97/7）覆盖两宽四运算、有限值、`(-0)+(+0)`、`maxnum`／`minnum` 对 qNaN、五序、`volatile`＋`singlethread`、`!tbaa`、独立比特观察；先在 AArch64 下变换，再 `internalize`／`globaldce` 只留 `main`，然后只替换 live 三元组走宿主 `lli`；并用 AArch64 `llc`／`readobj`。既有 `vmp-atomicrmw-float-semantic.ll`／`vmp-atomicrmw-double-semantic.ll`（种子 93／94）仍保留。
- 拒绝：`half`／`bfloat`／`fp128`／聚合／向量／指针载荷、非 AS0、`unordered`（验证器禁止）、非 AArch64 模块。资格失败保持原函数与 `hikari.vmp.selected`，跳过原因 `unsupported atomicrmw instruction`（超宽向量返回等先被类型门挡住的除外）。本轮不把已接受的整数 RMW／`xchg` 改成负例。

**当前实际 AtomicCmpXchg 整数支持边界**

- 接受：仅 AArch64。既有 `VMOpcode::AtomicCmpXchg`＋`AtomicCmpXchgDescriptor` 对标量 AS0 整数／AS0 指针 `cmpxchg` 原样 `CreateAtomicCmpXchg`，**不是** load／icmp／store，也不把 `{ty,i1}` 放进聚合虚拟寄存器。每个使用必须是单下标 `extractvalue` 0（旧值，整数或指针帧）或 1（`i1` 成功，整数帧），至多各一次；零使用（丢弃结果）亦可。解释器复制弱／强、`volatile`、成功／失败内存序、同步域、对齐、调试位置与内存元数据（保留 `!tbaa`）。整数载荷仅 `i8`／`i16`／`i32`／`i64`。成功序 `monotonic`／`acquire`／`release`／`acq_rel`／`seq_cst`；失败序仅 `monotonic`／`acquire`／`seq_cst`（LangRef）。本轮**不**纳入 `fence` 或放宽浮点／向量／`i128`。不新增操作码。`vmp-cmpxchg-integer-semantic.ll`（O0/O2×97/7）覆盖四宽成功与失败、`weak`、`volatile`、`singlethread`、`!tbaa`、独立旧值／成功／单元观察；先在 AArch64 下变换，再 `internalize`／`globaldce` 只留 `main`，然后只替换 live 三元组走宿主 `lli`；并用 AArch64 `llc`／`readobj`。AS0 指针与丢弃结果仍由既有 `vmp-cmpxchg-semantic.ll`（种子 37）覆盖。
- 拒绝：把 `{ty,i1}` 当聚合 SSA（`ret`／`insertvalue`／二次 `extractvalue` 同一字段）、`i128`／向量／浮点载荷、非 AS0、失败序 `release`／`acq_rel`／`unordered`、成功序 `unordered`、非 AArch64 模块。LLVM 15 对非法失败／成功序在解析期拒绝，故无 skip 哨兵。资格失败保持原函数与 `hikari.vmp.selected`，跳过原因 `unsupported cmpxchg instruction`（超宽向量返回等先被类型门挡住的除外）。本轮不把已接受的 `atomicrmw`／`fence` 改成负例。

**当前实际 FenceInst 支持边界**

- 接受：仅 AArch64。既有 `VMOpcode::Fence`＋`FenceDescriptor` 对 `fence` 原样 `CreateFence`，复制内存序与同步域及元数据。LLVM 15 验证器只允许 `acquire`／`release`／`acq_rel`／`seq_cst`（**没有** `monotonic`／`unordered`）。解释器不得删除栅栏（`CreateFence` 有副作用，不是空操作降级）。本轮**不**放宽原子类型或 RMW／cmpxchg。不新增操作码。`vmp-fence-ordering-semantic.ll`（O0/O2×97/7）覆盖四合法序、`singlethread` 的 `acquire`／`seq_cst`、夹在原子 `load`／`store` 之间的整数载荷；先在 AArch64 下变换，再 `internalize`／`globaldce` 只留 `main`，然后只替换 live 三元组走宿主 `lli`（顺序解释下栅栏无额外可见效果，只验证载荷）；并用 AArch64 `llc`／`readobj`。既有 `vmp-fence-semantic.ll`（种子 29）仍保留；其中已虚拟化的指针 `cmpxchg` 负例已删，改指向 `vmp-cmpxchg-semantic.ll`。
- 拒绝：`monotonic`／`unordered` 栅栏（LLVM 15 解析期拒绝，故无 skip 哨兵）、非 AArch64 模块、以及与本表面无关的不兼容控制流／ABI（如 `musttail`）。资格失败保持原函数与 `hikari.vmp.selected`。本轮不把已接受的 `atomicrmw`／`cmpxchg` 改成负例。

**当前实际 llvm.get.dynamic.area.offset 支持边界**

- 接受：精确 `i64 @llvm.get.dynamic.area.offset.i64()`（零实参，`CallingConv::C`，非可变参数）。LangRef：从原生栈指针到最近一次动态 `alloca` 的偏移；多数目标（含 AArch64／x86_64）降为 0，PowerPC 为编译期常量。**不得**在 VMP 里预折成 `0`／`Move`。经既有 `CallDescriptor` 重放：原 `Function*`、`FunctionType` `i64()`、`CallingConv::C`、`AttributeList`、`DebugLoc`，`i64` 结果写入整数虚拟寄存器，由目标 lowering 决定值。可与静态 `alloca` 以及同一函数内的 `llvm.stacksave`／`llvm.stackrestore` 共存。不新增操作码。宿主 `IntrinsicLowering` 与 AArch64／x86 SelectionDAG Expand 均降为 0，故 `vmp-dynamic-area-offset-intrinsic-semantic.ll` 在 triple-swap 后走 `lli -force-interpreter` 做参考／保护整数差分，并用 AArch64 `llc`／`readobj`／汇编确认目标零 lowering（`xzr`）。覆盖把偏移加到种子、ASan 式 `add`／`sub`／`GEP i8`／load／store、回边循环中重复读取，以及与 `stacksave`／`stackrestore` 共存；负例为入口动态 `alloca` 组合（`dynamic stack state`）、`i32`／`i128` 宽度、`musttail`／bundle／`fastcc`／`sret`+`byval`。
- 拒绝：入口动态 `alloca` 与本内建同函数（序言提前分配会改变观察到的动态区，跳过原因 `dynamic stack state`）、非 `i64` 结果、`musttail`、操作数捆绑、`fastcc`／非 C、可变参数、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 llvm.clear_cache 支持边界**

- 接受：精确 `void @llvm.clear_cache(ptr, ptr)`。LangRef：使 `[begin, end)` 对执行单元可见；AArch64 指令／数据缓存不统一，ISel 降为运行库 `__clear_cache`，**不是**空操作（x86 覆盖名为空故为 nop）。经既有 `CallDescriptor` 重放：原 `Function*`、`FunctionType` `void(ptr, ptr)`、`CallingConv::C`、`AttributeList`、`DebugLoc` 与其余元数据，两个区间指针从指针虚拟寄存器 load。普通 `tail` 降为普通 call（解释器 `CreateCall` 为 `TCK_None`）。不新增操作码，不得丢弃或改成 `Move`。`vmp-clear-cache-intrinsic-semantic.ll` 覆盖静态区间 GEP、指针形参区间、回边循环、普通 `tail`，FileCheck 重放调用的两个指针来自 volatile 指针帧，并用 AArch64 `llc` 汇编确认 `__clear_cache`；负例为 `poison`／`undef`、`musttail`／bundle／`fastcc`／汇编／间接／`sret`／AS1 形参。宿主 `lli` 在 x86 上对该内建为 nop，只验证其后 store／load 差分。
- 拒绝：`poison`／`undef`、非零地址空间（内建固定 AS0；AS1 形参在参数类型门被拒）、`musttail`、操作数捆绑、`fastcc`／非 C、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 与内联汇编除外）。

**当前实际 llvm.annotation 支持边界**

- 接受：精确标量 `llvm.annotation`（四实参：同宽整数、注解串、编译单元串、`i32` 行号）。LangRef：结果就是操作数 0；变换可换成第一实参；ISel 与 `IntrinsicLowering` 丢弃调用并转发整数。结果与 arg0 为同一 VMP 标量整数：`i1`／`i8`／`i16`／`i32`／`i64` 或受限 `i128`。注解串／单元串须为编译期 AS0 指针常量（与 `ptr.annotation` 相同的 `isCompileTimeAnnotationPointer`），行号须为 `i32` `ConstantInt`。解释器发既有 `Move`，把运行时值写入对应整数或 `i128` 虚拟寄存器，不经 `CallDescriptor`。`CallingConv::C`、非可变参数。`musttail` 拒绝；普通 `tail` 接受并按 `TCK_None` 重放。不把 `i128` 并入 `isSupportedIntegerType`。不改 `ptr.annotation`／`var.annotation`。`vmp-annotation-intrinsic-semantic.ll` 覆盖六种整数宽度、分支、钻石 `phi`、回边循环，以及动态串／行号、`poison`／`undef`、`i2`、`musttail`／bundle／`fastcc`／汇编／间接／可变参数／`sret`／`noreturn`／`returns_twice` 负例。
- 拒绝：指针／浮点／向量、`i2` 或其它非 VMP 整数宽度、非常量或逃逸的注解串／单元串、非常量行号、`poison`／`undef`、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／非 C／可变参数／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail`、内联汇编与非 C 可变参数除外）。

**当前实际 llvm.var.annotation 支持边界**

- 接受：精确 LLVM 15 五实参 `void @llvm.var.annotation(ptr, ptr, ptr, i32, ptr)`。LangRef：给局部变量挂字符串，无其它定义用途，代码生成与优化忽略。ISel／IRTranslator／`IntrinsicLowering` 直接丢弃（void，不转发值）。对晚于优化的 VMP 是无观察语义的标记，按 `lifetime` 消费：不经 `CallDescriptor` 重放，不占虚拟寄存器，不新增操作码。被注解的数据指针须为受支持的 AS0 操作数（通常是 `alloca`）。注解串／单元串／额外参数须为编译期 AS0 指针常量（与 `ptr.annotation` 相同的 `isCompileTimeAnnotationPointer`，含前端函数内常量下标 GEP），行号须为 `i32` `ConstantInt`。这样丢弃调用不会丢掉带运行时副作用的注解表达式。`CallingConv::C`、非可变参数。`musttail` 拒绝；普通 `tail` 接受并按 `TCK_None` 重放。不开放 `llvm.annotation`／`llvm.codeview.annotation`。`vmp-var-annotation-intrinsic-semantic.ll` 覆盖注解后的 `load`／`store`、钻石 `phi`、回边循环，以及动态串／行号／额外参数、`poison`／`undef`、`musttail`／bundle／`fastcc`／汇编／间接／可变参数／`sret`／`noreturn`／`returns_twice`／`codeview.annotation` 负例。
- 拒绝：非常量或逃逸的注解串／单元串／额外参数、非常量行号、`poison`／`undef` 数据指针、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／非 C／可变参数／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail`、内联汇编与非 C 可变参数除外）。

**当前实际 llvm.ptr.annotation 支持边界**

- 接受：精确标量地址空间零 `llvm.ptr.annotation`（LLVM 15 五实参：数据指针、注解串、编译单元串、`i32` 行号、额外参数指针）。LangRef：结果就是操作数 0；变换可换成第一实参；ISel 与 `IntrinsicLowering` 丢弃调用并转发指针。注解串／单元串／额外参数必须是编译期 AS0 指针常量（`null`、全局、其 GEP／bitcast；前端 `unnamed_addr` 字符串与可选常量 args 结构），行号必须是 `i32` `ConstantInt`。只丢弃这些无运行时副作用的常量操作数，把数据指针经既有 `PointerMove` 写入指针虚拟寄存器。不得 `CallDescriptor` 重放。`CallingConv::C`、非可变参数。普通 `tail` 允许。不开放 `llvm.annotation`／`llvm.codeview.annotation`。`llvm.var.annotation` 见独立表面。`vmp-ptr-annotation-intrinsic-semantic.ll` 覆盖通过结果的 `load`／`store`、GEP、普通调用、回边循环、普通 `tail`，以及动态串／行号／额外参数、`poison`／`undef`、非零地址空间、`musttail`／bundle／`fastcc`／汇编／间接／可变参数／`sret`／`noreturn`／`returns_twice` 负例。宿主 `lli` 能降低 `ptr.annotation`；保护函数不再重放该内建。
- 拒绝：非常量或逃逸的注解串／单元串／额外参数、非常量行号、非零地址空间、`poison`／`undef`、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail`、内联汇编与非 C 可变参数除外）。

**当前实际 llvm.invariant.start／llvm.invariant.end 支持边界**

- 接受：形态良好的标量地址空间零 `llvm.invariant.start`／`llvm.invariant.end` 配对。LangRef：二者只声明内存在区间内不变／重新可变，不是运行时访存。ISel 与 `IntrinsicLowering` 丢弃它们（start 的令牌变成 `undef`）。对晚于优化的 VMP 解释器无观察语义，按 `lifetime` 丢弃：不经 `CallDescriptor` 重放，不占虚拟寄存器，不新增操作码。start 的令牌只能被同函数形态良好的 `invariant.end` 使用（`dbg` 用户忽略）；`end` 的 scope 必须是这样的 start。`i64` 尺寸为 `ConstantInt`（含 `-1` 可变长）。普通 `tail` 允许。`applyMetadata` 剥离重建后的 `!invariant.load`／`!invariant.group`／`!alias.scope`／`!noalias`，避免共用 load／store／调用处理器带上没有对应动态标记的不变／别名承诺。`vmp-invariant-start-end-intrinsic-semantic.ll` 覆盖标记前后的 store／load、双对象配对、钻石 `phi`、回边循环、普通 `tail`、带 `!invariant.load`／`!invariant.group` 的 load，以及令牌逃逸／无配对 end／`poison`／`undef`／非零地址空间／`musttail`／bundle／`fastcc`／汇编／间接／可变参数／`sret` 负例。
- 拒绝：令牌逃逸（返回／存入／传入其它调用／`phi`）、无配对 start 的 end、`poison`／`undef` 数据指针、非零地址空间、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。与 `noalias.scope.decl` 不同，不按 ID 无条件忽略 `musttail`／bundle。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail`、内联汇编与非 C 可变参数除外）。

**当前实际 llvm.launder.invariant.group／llvm.strip.invariant.group 支持边界**

- 接受：精确标量地址空间零 `llvm.launder.invariant.group` 与 `llvm.strip.invariant.group`。恰好一个非 `poison`／`undef` 指针操作数，结果与实参同一 AS0 指针类型，匹配 `FunctionType`，`CallingConv::C`、非可变参数。LangRef：结果与实参互为 alias，对 `load`/`store` 的 `!invariant.group` 视为不同（launder）或不再关联（strip）；不读可访问内存，可推测。LLVM 15 `Intrinsics.td` 给 launder 标 `inaccessiblememonly`（避免对同一指针的两次 launder 做 CSE），strip 为 `readnone`；`DefaultAttrs` 带 `nocallback`，这是该版本的规范签名，拒绝它会关掉本表面。ISel／CodeGenPrepare／FastISel 把两者降为恒等。宿主 `IntrinsicLowering` 无对应分支（`lli` 会 fatal），故不得 `CallDescriptor` 重放。解释器发既有 `PointerMove`，把运行时指针写入指针虚拟寄存器，不新增操作码。共享 `PointerMove` 处理器没有按指令的调试位置（与整数 `Move` 相同）；后续 `load`/`store` 仍经 `MemoryDescriptor` 保留 `DebugLoc` 与元数据。`vmp-invariant-group-intrinsic-semantic.ll` 覆盖通过结果的 `load`/`store`／`!invariant.group`、GEP、普通调用、钻石 `phi`、回边循环、普通 `tail`，以及非零地址空间实参／调用、`poison`／`undef`、`musttail`、operand bundle、`fastcc`、内联汇编、间接、可变参数、`noreturn`、`returns_twice`、`sret` 负例。参考函数用原指针。同时走 FileCheck 与 AArch64 `llc`／`readobj`。
- 拒绝：非零地址空间、向量／畸形签名、`poison`／`undef`、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、内联汇编、间接调用。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail`、内联汇编与非 C 可变参数除外）。`invariant.start`／`end` 见独立表面。

**当前实际 llvm.expect／expect.with.probability 支持边界**

- 接受：精确非 constrained 标量 `llvm.expect`（两实参）与 `llvm.expect.with.probability`（三实参）。结果与前两实参为同一 VMP 标量整数：`i1`／`i8`／`i16`／`i32`／`i64` 或受限 `i128`。LangRef 语义是操作数 0 的恒等；解释器发既有 `Move`，把运行时值写入对应整数或 `i128` 虚拟寄存器，不经 `CallDescriptor`，不重放内建。期望值实参丢弃。`expect.with.probability` 的第三实参必须是有限 `double` `ConstantFP` 且位于闭区间 `[0.0, 1.0]`（本表面的立即数合同；LangRef 未标 ImmArg，动态 `double` 不进浮点虚拟寄存器）。`isFinite` 拒绝 NaN 与正负无穷；IEEE `APFloat::compare` 把 `-0.0` 视为等于 `+0.0`，故接受该端点；小于 0 或大于 1 的有限值拒绝。该常量丢弃。调用点须为 `CallingConv::C`、非可变参数。普通 `tail` 不拒绝。不新增操作码。`vmp-expect-intrinsic-semantic.ll` 覆盖六种整数宽度、概率 0／0.5／0.8／1／`-0.0`、分支／钻石 `phi`／回边循环／普通 `tail`，以及 `poison`／`undef`／向量／`i2`／动态概率／NaN／`+Inf`／`-Inf`／负概率／大于 1 的概率／`musttail`／operand bundle／`fastcc`／内联汇编／间接／`sret` 负例。既有 `vmp-expect-semantic.ll`／`vmp-expect-probability-semantic.ll` 仍覆盖 `i1` 分支差分。宿主 `lli` 能降低 `llvm.expect`；不能降低 `expect.with.probability`，故后者参考函数用原值。同时走 FileCheck 与 AArch64 `llc`／`readobj`。
- 拒绝：向量整数、`i2` 或其它非 VMP 整数宽度、浮点、动态或非 `double` 概率、非有限或越出 `[0.0, 1.0]` 的概率（NaN、正负无穷、负有限值、大于 1）、`poison`／`undef`、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。不把 `i128` 并入 `isSupportedIntegerType`。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 与内联汇编除外）。

**当前实际向量 poison/undef 构造支持边界**

- 接受：整值 `poison`／`undef` 作为 `insertelement` 的向量基（数据向量走既有 1..128 帧，含 `i8`／`i16`／`i32`／`i64`／`half`／`f32`／`f64` 与 last-token `+bf16` 的 `bfloat`；指针向量走既有 `<1..8 x ptr>` 帧）以及作为常量掩码 `shufflevector` 的任一操作数。这是 Clang／LLVM 的 splat 与逐元素构造序列（`insertelement poison, %x, 0` 再 `shufflevector %v, poison, zeroinitializer`）。基不进虚拟寄存器，按既有 `FreezeSourceKind` 在处理器里物化；插入元素、下标、`extractelement` 源、算术与调用实参仍走 `isSupportedOperand`（`poison`／`undef` 仍拒绝）。不新增操作码。`vmp-vector-construct-poison-semantic.ll` 覆盖 i8／i16／i32／i64／float／half 构造与 splat、指针向量 insert、钻石 `phi` 与回边循环，以及 poison 插入元素／extract poison／算术 poison／可伸缩／超过 128 位／`fp128`／`ppc_fp128` 负例。宿主 `lli` 对参考／保护做整数差分；同时走 FileCheck 与 AArch64 `llc`／`readobj`。
- 拒绝：作为插入元素或 `extractelement` 源的 `poison`／`undef`、向量算术上的 `poison`／`undef`、可伸缩、超过 128 位、`fp128`／`ppc_fp128` 向量。不放宽通用 `isSupportedOperand`。资格失败保持原函数与 `hikari.vmp.selected`。

**后续优先级**

1. 先补齐对 ABI（应用二进制接口）和解释器状态影响小、可由原生重发射保持语义的标量指令族与内建（例如其余标量浮点与整数内建的受限子集）；
2. 再评估非自然宽度向量字段、非零地址空间、EH（异常处理）等对解释器状态与调用约定影响更大的方向；
3. 每一项扩展都必须先做事务式资格检查（不支持的形态保持原函数并记录原因）、`O0`（无优化）与 `O2`（常规优化）验证、宿主 `lli`（LLVM 即时执行器）差分与 AArch64（64 位 ARM（精简指令集）架构）对象生成，全部通过后才宣告支持。

**当前实际聚合支持边界**

- 接受：具名已定大小结构体与固定数组；1..8 个直接字段；整值保守分配大小不超过 64 字节。每个聚合虚拟寄存器是独立的 `[4 x i128]` 槽（64 字节、16 字节对齐）；解释器仍对原 `AggTy` 做 volatile load/store，不新操作码。20／24／32／64 字节形态（`{i32 x 5}`／`{i64,i64,i64}`／`{i64 x 4}`／`[8 x i64]`）走该槽。另接受一层嵌套：顶层字段可以是叶子聚合（其字段只能是既有标量／指针／浮点／自然宽度向量），覆盖 `{ {i32,i32}, i32 }`、`[2 x {i32,i32}]`、`{ [2 x i32], i32 }`。`insertvalue`／`extractvalue` 允许 1 个下标（叶子字段或整个叶子聚合字段），或恰好 2 个下标且仅当第一下标是叶子聚合、第二下标是该叶子的叶子字段；双下标不得再落到聚合上。`vmp-large-flat-aggregate-semantic.ll` 覆盖大扁平正向形态；`vmp-nested-aggregate-semantic.ll` 覆盖一层嵌套与合法双下标，以及两层嵌套（含 `{ [2 x {i32,i32}], i32 }`／`[2 x { {i32,i32}, i32 }]`）／对两层做双下标／三下标／9 字段／超 64 字节／`<3 x i32>` 字段／`i128` 字段／NEON 元组负例。
- 字段：既有标量类型（含 IEEE `half`，ABI 大小 2、对齐 2）或总位宽仅为 8/16/32/64/128 的受支持固定向量（含 `half` 元素；因此 `<2 x i32>` 与 `{ <2 x half> }` 允许，`<3 x i32>`／`<3 x half>` 拒绝）。一层嵌套字段的分配大小按同一保守 AArch64 式布局计入顶层。非 packed 按字段大小取不大于 16 的自然对齐并在末尾对齐到最大字段对齐；packed 只累加字段大小。`half` 向量字段沿既有 `getFixedVectorBitWidth` 字节数路径。
- 调用：既有普通直接 C 调用与受限间接 C 调用均可携带上述聚合实参／返回（经既有聚合虚拟寄存器与 `CallDescriptor` 重放）。间接路径不打开 `sret`/`byval`/`byref`。单纯聚合搬运／位模式／内存／C ABI 不要求 `+fullfp16`。
- 拒绝：两层及以上嵌套、可伸缩向量字段、指针向量、`bfloat`/`fp128` 字段、超过 64 字节或超过 8 个直接字段、三个及以上 `extractvalue`／`insertvalue` 下标、聚合原子 `load`/`store`，以及直接调用上非受限的 `sret`/`byval`/`byref`（`sret`+`byval`/`sret`+`byref`/`byval`+`byref`、类型或位置不匹配、非 C、`musttail`、间接等）。保留聚合生产者（overflow／cmpxchg／ld64b／rndr／NEON ld 元组）仍展开，不进 64 字节聚合帧；`{i64 x 8}` 的 ld64b 结果即使恰好 64 字节也不得占用该槽。
- 地址空间零的 `llvm.masked.load`/`store`／`expandload`／`compressstore`／`gather`／`scatter` 在全 false 掩码且不访问内存时，宿主 `lli`（LLVM 即时执行器）回归已验证。受支持固定 `half` 元素向量现已接入同一掩码表面，不要求 `+fullfp16`。

**当前实际 half 向量支持边界**

- 接受：固定向量、总原始宽度 1..128、元素为 IEEE `half`；走既有独立 `i128` 向量虚拟寄存器帧（整向量 bitcast 后放入槽，元素种类编码新增 `F16`）。覆盖 `fadd`/`fsub`/`fmul`/`fdiv`/`frem`/`fneg`（保留 FastMathFlags）、`fcmp`、`select`、`phi`、非原子地址空间零 `load`/`store`、`bitcast`、`extractelement`/`insertelement`、常量掩码 `shufflevector`、同车道 `half`↔`float` 的 `fpext`/`fptrunc`、同车道既有整数固定向量（元素 `i1`/`i8`/`i16`/`i32`/`i64`，总宽 1..128）与 `half` 向量的 `sitofp`/`uitofp`/`fptosi`/`fptoui`（复用既有 `VectorSIToFP`／`VectorUIToFP`／`VectorFPToSI`／`VectorFPToUI` 描述符与处理器），以及既有普通直接 C 调用与受限间接 C 调用的 `half` 向量实参与返回（不另加 `+fullfp16`）。另经既有 `CallDescriptor` 接入地址空间零 `llvm.masked.load`/`llvm.masked.store`／`llvm.masked.expandload`／`llvm.masked.compressstore`／`llvm.masked.gather`／`llvm.masked.scatter`（匹配 `<N x i1>`、匹配 passthru/value；load/store/gather/scatter 另有 `i32` 常量幂二对齐；gather/scatter 指针为受限 `<1..8 x ptr>` AS0；inactive lane 不访问）以及 listed `llvm.vector.reduce.fadd`/`fmul`/`fmin`/`fmax`（标量 `half` 结果、C 约定）。`half` 浮点归约要求所在函数 `target-features` 最后一次出现的精确 `+fullfp16`／`-fullfp16` 令牌为 `+fullfp16`（不读取命令行 `-mattr`）；普通 `half` 向量基础 SSA、掩码内存、`VectorFreeze` 与既有 `f32`/`f64` 归约不受该门控。受支持 `half` 固定向量的 `freeze` 走既有 `VectorFreeze`（原生 `CreateFreeze`；普通源经向量虚拟寄存器读取，直接 `undef`/`poison` 仅在冻结处按既有 Variant 物化）。`vmp-half-vector-semantic.ll` 覆盖基础 SSA 与 `half` 向量 `freeze`；`vmp-half-vector-masked-reduce-semantic.ll` 覆盖掩码／归约、全 false `null`、循环／`phi` 与 feature gate。
- 拒绝：`bfloat`/`fp128` 元素、可伸缩向量、超过 128 位（如 `<16 x half>`）、`half` 向量原子访问、缺少或最后令牌为 `-fullfp16` 的 `half` 浮点归约或 listed 向量数学（跳过原因 `unsupported target feature`）、`llvm.vp.reduce.*`、非自然宽度半精度向量聚合字段、`half`↔`double` 宽度转换、可变参数、未列入的 `half` 向量数学 intrinsic，以及 `sret`/`byval`/`byref` 等调用 ABI 子集的扩大。通用 pointer-vector ABI（函数实参／返回、普通调用、load/store）仍拒绝；受限 gather/scatter 地址向量见独立表面。可伸缩／指针／聚合／超过 128 位或类型不匹配的 `freeze` 仍以 `unsupported freeze instruction` 跳过，保留 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。无最后令牌 `+bf16` 的形态良好 `bfloat` 向量 `freeze` 见独立表面。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际 half 向量掩码内存与归约支持边界**

- 接受：与既有整数／`f32`／`f64` 掩码内存同一组限制的固定 `half` 向量 `llvm.masked.load`／`llvm.masked.store`／`llvm.masked.expandload`／`llvm.masked.compressstore`／`llvm.masked.gather`／`llvm.masked.scatter`（地址空间零、受支持总宽度、匹配 `<N x i1>`、匹配 passthru/value；load/store/gather/scatter 另有 `i32` 常量幂二对齐；gather/scatter 指针为受限 `<1..8 x ptr>` AS0；经 `CallDescriptor` 重放，不另造原生旁路）。listed `llvm.vector.reduce.fadd`／`fmul`（LLVM 15 start + vec）与 `fmin`／`fmax`（仅 vec），标量 `half` 结果、C 调用约定、固定 1..128；`half` 浮点归约复用既有精确最后 `+fullfp16`／`-fullfp16` 令牌检查。AArch64 `llc` 不传全局 `-mattr`。宿主 `lli` 无法为 `llvm.vector.reduce.fmin`／`fmax.f16` 做 x86 指令选择，故这两项只走 FileCheck 与函数属性上的 AArch64 `llc`。
- 拒绝：可伸缩、`>128`、非零地址空间、不匹配掩码／对齐／passthru、`llvm.vp.reduce.*`、`fp128`、不匹配签名或非 C 约定。缺少或最后令牌为 `-fullfp16` 的形态良好 `half` 浮点归约跳过原因为 `unsupported target feature`，保留 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。无 `+bf16` 的形态良好固定 `bfloat` 浮点归约改走独立 last-token 表面，跳过原因同样为 `unsupported target feature`（见 bfloat vector.reduce 节）。通用 pointer-vector ABI 仍拒绝。

**当前实际 llvm.masked.expandload／compressstore 支持边界**

- 接受：精确 `llvm.masked.expandload`／`llvm.masked.compressstore`，固定向量总原始宽度 1..128，元素 `i1`／`i8`／`i16`／`i32`／`i64`／`half`／`f32`／`f64`。指针为地址空间零普通指针，掩码为同车道 `<N x i1>`，passthru／value 与结果／存值同型。无对齐立即数。经既有 `CallDescriptor` 重放，不新增操作码。普通 `tail` 接受并按非 tail `CreateCall`／`TCK_None` 重放。`vmp-masked-expand-compress-semantic.ll` 覆盖 `<4 x i32>` 混合／全真／全假 expand、compress 打包写入、`f32`／`i8` expand，以及 `i16`／`i64`／`half`／`f64`／`i1` 的 live `protected_*` expand 与 compress（经 `vmp-drop-unsupported.py` 保留到对象／`lli`）、钻石 `phi`、回边循环、全假掩码 + `ptr null`，以及 gather／超宽／对其它函数的真实 `musttail`／`poison` passthru 负例。`vmp-masked-memory-semantic.ll` 中原 expand／compress／half expand 负例已改名为 `protected_masked_*`，同一表面正向重放且不被 drop 脚本剥掉。宿主 `lli` 对参考／保护做差分；同时走 FileCheck 与 AArch64 `llc`／`readobj`。
- 拒绝：可伸缩、总宽超过 128、非零地址空间、`poison`／`undef`、`musttail`、operand bundle、`fastcc`／非 C、形参类型不等、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（真实 `musttail` 仍走既有早期 `musttail call` 诊断）。gather／scatter 见下一节。

**当前实际 llvm.masked.gather／scatter 支持边界**

- 接受：精确 `llvm.masked.gather`／`llvm.masked.scatter`。数据为既有固定向量总原始宽度 1..128，元素 `i1`／`i8`／`i16`／`i32`／`i64`／`half`／`f32`／`f64`。指针为地址空间零的 `<N x ptr>`，车道数与数据相同且 `N` 为 1..8，走独立 `[8 x ptr]` 指针向量虚拟寄存器帧（`1<<58`），按指针值拷贝（`extractelement`／`insertvalue`），**不得** `ptrtoint`／`inttoptr`，以免丢掉 LLVM 指针 provenance。不是通用 pointer-vector ABI：函数参数／返回、普通直接／间接调用实参、指针向量 `load`／`store`／`freeze`／`shufflevector` 仍拒绝。地址可由标量基址 + 一个同车道整数向量下标的受限 `getelementptr`，或该下标再加一个范围内的 `ConstantInt` 结构体字段（常见 AoS，例如 `getelementptr %Pair, ptr %base, <4 x i32> %idx, i32 1`）或范围内的 `ConstantInt` 数组元素（例如 `getelementptr [2 x i32], ptr %base, <4 x i32> %idx, i32 1`），或三下标 Wrap 形态 `getelementptr %Wrap, ptr %base, <4 x i32> %idx, i32 0, i32 1`（`%Wrap = { [2 x i32] }`），或 clang 常见 `getelementptr [4 x %Pair], ptr %base, i32 0, <4 x i32> %idx, i32 1`（首下标必须是值为 0 的 `ConstantInt`，live 覆盖 `i32 0` 与 `i64 0`，向量在中间；既有 `addGEPDescriptor`／`emitGEPHandler` 已能重放 ConstantIndex／BitWidth + VectorIndexType 混合列表），指针向量常量（无 `undef`／`poison` 车道）或标量 AS0 指针的 `insertelement`／`extractelement`／`phi`／`select` 产生。对齐为 `i32` 幂二 ImmArg。经既有 `CallDescriptor` 重放，不新增 gather／scatter 操作码。普通 `tail` 接受并按非 tail `CreateCall`／`TCK_None` 重放。`vmp-masked-gather-scatter-semantic.ll` 对声明的全部数据元素 `i1`／`i8`／`i16`／`i32`／`i64`／`half`／`f32`／`f64` 都有 live `protected_*` gather 与 scatter（经 `vmp-drop-unsupported.py` 保留到对象／`lli`，`N<=8` 且数据总宽 1..128）。另覆盖 `<4 x i32>` 混合／全真／全假 gather、insertelement 建址、指针向量 `extractelement`（`PointerVectorExtractElement`）、标量／掩码 `select`（`PointerVectorSelect`）、钻石与回边 `<4 x ptr>` `phi`（`PointerVectorMove`）、`%Pair` 字段 1 的 live gather／scatter AoS 两下标 GEP、`[2 x i32]` 列 1 的 live gather／scatter 数组两下标 GEP、`%Wrap` 字段 0 元素 1 的 live gather／scatter 三下标 GEP、`[4 x %Pair]` 首下标 `i32 0` 的 live gather／scatter 三下标 GEP、同一形态首下标 `i64 0` 的 live gather、全假掩码 + `zeroinitializer` 指针，解释器 IR 不得对地址向量做 `ptrtoint`／`inttoptr`，以及可伸缩／AS1／`N>8`／超宽数据／`musttail`／`poison`／`fastcc`／指针向量形参／动态首下标／非零首下标三下标／`i32 0`+向量+数组元素三下标／数组+数组三下标／四下标／动态第二下标／越界常量数组下标／向量基址 GEP 负例。既有 masked-memory／half-reduce lit 中形态良好的 gather／scatter 负例已改名为 `protected_*`。宿主 `lli` 对参考／保护做差分；同时走 FileCheck 与 AArch64 `llc`／`readobj`。
- 拒绝：可伸缩、非零地址空间、`N>8`、数据总宽超过 128、向量基址 GEP、4+ GEP 下标、非 Wrap／`[N x %Pair], i32 0, vec, field` 的三下标、非零首下标、动态首／第二／第三下标、越界常量数组下标、动态结构体字段、指针向量作函数 ABI、`poison`／`undef`、`musttail`、operand bundle、`fastcc`／非 C、形参类型不等、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量 bfloat 基础 SSA／内存支持边界**

- 接受：标量 `bfloat` 的基础 SSA 与非原子地址空间零内存，复用既有 16 位浮点虚拟寄存器帧（`1<<62`），不新增操作码。覆盖无 FastMathFlags 的 `fadd`／`fsub`／`fmul`／`fdiv`／`frem`／`fneg`、`fcmp`、`select`、`phi`、`freeze`、非原子 AS0 `load`／`store`、`bfloat`↔`i16` `bitcast`、标量 `bfloat`↔`float` 的 `fpext`／`fptrunc`、标量 `bfloat`↔`i1`／`i8`／`i16`／`i32`／`i64` 的 `sitofp`／`uitofp`／`fptosi`／`fptoui`、常量、被虚拟化函数自身的标量 `bfloat` 形参／返回，以及普通直接 C 调用（含普通 `tail`：通用接受门收下，解释器按 `TCK_None` 重放）上的标量 `bfloat` 实参／返回（既有 `CallDescriptor` 与浮点虚拟寄存器，不新增操作码；调用点与被调均须 `CallingConv::C` 且均非可变参数；仅浮点结果调用点是 `FPMathOperator`，须无 FastMathFlags；`void`／整数结果加 `bfloat` 实参不得调用 `getFastMathFlags()`）。所在函数 `target-features` 最后一次出现的精确 `+bf16`／`-bf16` 令牌须为 `+bf16`（逗号分隔列表；`+bf16fml`／`+fullfp16` 等不算；不读取命令行 `-mattr`）。16 位浮点操作码用 Variant 第 7 位（`fcmp` 为第 11 位，`fpext`／`fptrunc` 为第 15 位）区分 `bfloat` 与 `half`。LLVM 15 AArch64 指令选择不能选择标量 `bf16` 算术／比较／`select`／`fpext`／`fptrunc`：解释器把算术与 `fcmp` 升为精确的 `f32`（高 16 位插入）再 `fadd`／…／`fcmp` `float`，写回时对有限 `f32` 做 round-to-nearest-even 收成 `bfloat`。`fpext bfloat`→`float` 复用同一精确提升；`fptrunc float`→`bfloat` 复用同一 RNE（含 Inf／NaN 短路）。`sitofp`／`uitofp` 先到 `f32` 再 RNE 收成 `bfloat`；`fptosi`／`fptoui` 先精确提升再对 `f32` 取整。指数全 1 的 Inf／NaN 不得走 bias-add：直接截断高 16 位，且若 `f32` 尾数非零则置 `0x0040` 安静位，避免低位 payload 的 sNaN 变成 Inf 或 `0x7FFFFFFF` 包成 -0（与 `APFloat::convert` 一致）。`fneg`／`select` 在 `i16` 位模式上完成。`load`／`store`／`bitcast`／`freeze`／返回仍是 `bfloat` 类型。`vmp-bfloat-scalar-semantic.ll` 覆盖上述正例、最后令牌 `+bf16`、`fpext`+`fadd float`+`fptrunc` 交叉、`bfloat`↔`float`↔`half` 与既有 half 宽度转换交叉、`sitofp`／`uitofp`／`fptosi`／`fptoui` 与宽度转换交叉、直接 C `bfloat` 调用与 `fpext` 交叉，以及无特性／最终 `-bf16`／仅宽度转换上的最终 `-bf16`／仅 `+bf16fml`／仅 `+fullfp16`／`bfloat`↔`double`／`i128`／向量整数转换／带 FMF 或 `fastcc`／`musttail`／具名 `bfloat` C 可变参数的 `bfloat` 调用。普通 `tail` 直接 C `bfloat` 调用（含自递归）已改为正例；FileCheck 证明解释器不得保留 `tail call`。宿主 `lli` 不能假定会选择标量 `bfloat`，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。不放带 `fadd bfloat` 的原生参考函数（`llc` 无法选择）。
- 拒绝：缺少或最后令牌为 `-bf16` 的形态良好标量 `bfloat` SSA／内存／`bfloat`↔`float` 宽度转换／`bfloat`↔`i1`..`i64` 整数转换／普通直接 C `bfloat` 调用（跳过原因 `unsupported target feature`，保留 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`）；带任何 FastMathFlags 的标量 `bfloat` `fadd`／`fsub`／`fmul`／`fdiv`／`frem`／`fneg`／`fcmp`／`select`／`phi`／浮点结果直接调用（不得把 FMF 套到 f32 降级、在 i16 位路径上丢掉或写回 `call nnan bfloat`；`+bf16` 下跳过原因为对应 `unsupported float …`；`void`／整数结果加 `bfloat` 实参不是 `FPMathOperator`，不得对它们调用 `getFastMathFlags()`）；`fastcc` 或其它非 C 的 `bfloat` 直接调用（调用点与被调均须为 C）、`musttail` 的 `bfloat` 调用（既有早期资格仍报 `musttail call` 并不加 `hikari.vmp.selected`，形状仍拒绝 `isMustTailCall`；普通 `tail` 由通用接受门收下并按 `TCK_None` 重放）、间接 `bfloat` 调用（标量 lit 中间接哨兵改为 `fastcc`）、可变参数上的 `bfloat`（调用点 `FunctionType` 或被调任一侧为可变参数即拒绝，含具名 `bfloat` 的 C 可变参数调用）、`bfloat`↔`double`／`half` 宽度转换、`i128` 或向量整数转换、未列入的饱和转换（listed `llvm.fptosi.sat`／`llvm.fptoui.sat` 见独立表面）、未列入的数学／constrained 内建（listed `llvm.fabs`／`llvm.sqrt`／`llvm.fma`／最值／`copysign`／舍入族／`sin`..`log10`／`pow`／`powi` 见独立数学表面）、聚合字段、其它通用调用 ABI、原子 `load`／`store`、SVE。固定 `bfloat` 向量见下一节独立表面。既有以 `bfloat` 作函数形参／返回且无 `+bf16` 令牌的负例仍报告 `unsupported argument type`／`unsupported return type`（如 `vmp-half-scalar-semantic.ll` 的 `unsupported_bfloat`）。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量 IEEE fp128 支持边界**

- 接受：标量 IEEE `fp128` 基础 SSA 与非原子地址空间零内存，走独立类型化 `fp128` 虚拟寄存器帧（`1<<57`，解释器 `alloca [N x fp128]` 名为 `vmp.fp128.regs`，对齐 16）。**不得**占用 `i128` 整数帧（`1<<59`）或 `half`/`f32`/`f64`/`bfloat` 的 `i64` 浮点帧（`1<<62`）；解释器发射带类型的 `fadd`/`fsub`/`fmul`/`fdiv`/`frem`/`fneg`/`fcmp`/`select`/`freeze` `fp128`，不是整数位模式算术。覆盖函数自身的 `fp128` 形参／返回、`ConstantFP` 精确位初始化、入口静态 `alloca fp128`、非原子 AS0 `load`/`store`（保留对齐／volatile／调试／内存元数据）、带既有 FastMathFlags 的算术、`fcmp`（有序／无序）、标量 `select` 与 `phi`（含并行拷贝：`FP128Move` 临时槽再写回，最终边可带 FMF `select i1 true`）、`freeze`（`undef`/`poison` 仅在 `FreezeInst` 按既有 source-kind 物化），以及语义转换：`fpext` `half`/`float`/`double`→`fp128`、`fptrunc` `fp128`→`half`/`float`/`double`、`sitofp`/`uitofp` `i1`/`i8`/`i16`/`i32`/`i64`→`fp128`、`fptosi`/`fptoui` `fp128`→`i1`/`i8`/`i16`/`i32`/`i64`。复用既有 `FAdd`…/`FCmp`/`FPExt`/`SIToFP` 操作码，以 `BitWidth==128` 与 `VMPScalarFP128` 选择该帧；`phi`/`select`/`freeze`/`ret` 用专用 `FP128Move`/`FP128Select`/`FP128Freeze`/`FP128Ret`。不新增数学操作码。另接受普通直接非内建、非可变参数 `CallInst`，其实参／返回为既有标量整数／地址空间零指针／`half`／`float`／`double`／标量 `fp128`（可混合；至少一侧为 `fp128`）。经既有 `CallDescriptor`：`fp128` 实参从类型化 `fp128` 帧取出，结果写回该帧，**不得** `bitcast` 到 `i128` 或走整数／`i64` 浮点帧。调用约定／合法属性／元数据／`DebugLoc` 保留；`fp128` 结果上的 FastMathFlags 保存并恢复。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放（`unsupported call instruction`），不得静默降为解释器普通 call。非 `tail` 递归与跨函数走原 `Function*`。另接受形态良好、非 constrained 的标量 IEEE `fp128` listed 数学内建：精确 `llvm.sqrt.f128`／`llvm.fabs.f128`（一个同型非 `poison`／`undef` 实参）与 `llvm.fma.f128`／`llvm.fmuladd.f128`（三个同型非 `poison`／`undef` 实参）。另接受 listed 最值 `llvm.minnum.f128`／`llvm.maxnum.f128`／`llvm.minimum.f128`／`llvm.maximum.f128`（两个同型非 `poison`／`undef` 实参）与 listed 舍入 `llvm.ceil.f128`／`llvm.floor.f128`／`llvm.trunc.f128`／`llvm.round.f128`／`llvm.roundeven.f128`／`llvm.rint.f128`／`llvm.nearbyint.f128`（一个同型非 `poison`／`undef` 实参）。另接受 `llvm.copysign.f128`（两个同型 `fp128`）、`llvm.pow.f128`（两个同型 `fp128`）、`llvm.powi.f128.i32`（`fp128` 底数加精确 `i32` 指数）以及一元 `llvm.sin.f128`／`llvm.cos.f128`／`llvm.exp.f128`／`llvm.exp2.f128`／`llvm.log.f128`／`llvm.log2.f128`／`llvm.log10.f128`。另接受精确非 constrained 标量 IEEE `llvm.canonicalize.f128`（一个同型非 `poison`／`undef` 实参）。另接受精确 `llvm.is.fpclass.f128`：结果 `i1`，第一实参为标量 IEEE `fp128`（非 `poison`／`undef`），第二实参为 `i32` `ConstantInt` ImmArg 分类掩码且仅 LangRef 位 0..9（取值 0..1023）。经既有 `CallDescriptor` 重放原 `Function*`：`fp128` 实参走类型化帧，掩码保持 ImmediateArguments 真常量，**不得** `bitcast` 到 `i128`。LLVM 15 AArch64 与宿主 x86 均可选择该内建。另接受精确非 constrained 标量 IEEE `fp128` 取整到整数：`llvm.lround`／`llvm.lrint` 结果为 `i32` 或 `i64`，`llvm.llround`／`llvm.llrint` 结果仅为 `i64`（本仓库 AArch64 目的宽度约定；不得并入既有仅 `f32`／`f64` 的 `isSupportedFloatToI64RoundIntrinsic`），以及 `llvm.fptosi.sat`／`llvm.fptoui.sat` 结果为 `i8`／`i16`／`i32`／`i64`（不得并入既有 `isSupportedFloatToIntSatIntrinsic`）。单一非 `poison`／`undef` 标量 `fp128` 实参，无 ImmArg。经既有 `CallDescriptor` 重放原 `Function*`：`fp128` 实参走类型化帧，整数结果写入 `i32`／`i64`（饱和另含 `i8`／`i16`）虚拟寄存器，**不得** `bitcast` 到 `i128`。LLVM 15 AArch64 经 libcall（`lroundl`／`llroundl`／`lrintl`／`llrintl`／`__fixtf*`／`__fixunstf*`）选择上述形态。LLVM 15 AArch64 与宿主 x86 均无法选择 `llvm.canonicalize.f128`（`ISD::FCANONICALIZE` `f128`），故按 LangRef 降为类型化 `fmul` `x, 1.0`（既有 `FMul`、`BitWidth==128`、专用 `fp128` 帧），**不得**重放该内建、不得 `bitcast` 到 `i128`。合法 FastMathFlags 写到该 `fmul`。经既有 `CallDescriptor` 与类型化 `fp128` 帧原样重放原 `Function*` 内建声明（canonicalize 除外），**不得** `bitcast` 到 `i128` 或走整数／`i64` 浮点帧，也不得把未列入的 `fp128` 内建改写成普通外部调用，不得把最值降为 `select`／`fcmp`，不得把舍入降为通用整数转换，不得把 `pow` 改写为 `powi` 或相反。`fma` 保持融合（重放 `llvm.fma.f128`）；`fmuladd` 保持原内建语义（重放 `llvm.fmuladd.f128`，不得改写为 `fma`）；`minimum`／`maximum` 保持与 `minnum`／`maxnum` 不同的 IEEE 语义。合法 FastMathFlags、调用约定、属性、元数据与 `DebugLoc` 保留。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放，不得按既有通用内建路径降为普通非 `tail` `CallInst`。不新增操作码，不把 `fp128` 并入 `isSupportedScalarFPType`／`isSupportedFMAIntrinsic`／`isSupportedSqrtIntrinsic`／`isSupportedFAbsIntrinsic`／`isSupportedMinNumIntrinsic`／`isSupportedMinimumMaximumIntrinsic`／`isSupportedCeilIntrinsic`／`isSupportedCopySignIntrinsic`／`isSupportedPowIntrinsic`／`isSupportedPowiIntrinsic`／`isSupportedSinIntrinsic` 等既有 `f16`／`f32`／`f64` 助手。`vmp-fp128-scalar-semantic.ll` 覆盖算术／FMF、有序与无序比较、分支／`phi`／回边、内存、常量、转换、形参／返回，以及向量／聚合／原子／`canonicalize`／constrained／可变参数直接调用／间接调用／`bitcast` 到 `i128`／`ppc_fp128`／冻结外 `poison` 负例。`vmp-fp128-direct-call-semantic.ll` 覆盖 ident／加法／混合标量／`void` sink／整数结果／FMF／`fastcc`／普通 `tail` 负例／跨函数／自递归／互递归，以及 `musttail`／间接／可变参数／`canonicalize`／constrained／operand bundle／`noreturn`／`sret`／`poison`／向量负例。`vmp-fp128-math-semantic.ll` 覆盖四 ID、代表 FastMathFlags、分支／`phi`／回边、普通 `tail`，以及 constrained `sqrt`／`fma`／`ceil`／`sin`／`pow`、`canonicalize`、向量与 `ppc_fp128`、`poison`／`undef`、`musttail`、operand bundle、内联汇编与无效 ABI／调用形状负例。`vmp-fp128-minmax-round-semantic.ll` 覆盖四最值 ID 与七舍入 ID、代表 FastMathFlags、分支／`phi`／回边、普通 `tail`，以及 constrained `minnum`／`ceil`／`sin`／`pow`、`canonicalize`、非 `i32` `powi`、向量与 `ppc_fp128`、`poison`／`undef`、`musttail`、operand bundle、内联汇编与无效 ABI／调用形状负例。`vmp-fp128-copy-pow-transcendental-semantic.ll` 覆盖 `copysign`／`pow`／`powi.i32` 与七个超越 ID、代表 FastMathFlags、分支／`phi`／回边、普通 `tail`，以及 `is.fpclass`、constrained `sin`／`cos`／`pow`、`powi.i64`、向量与 `ppc_fp128`、`poison`／`undef`、`musttail`、operand bundle、内联汇编与无效 ABI／调用形状负例。`vmp-fp128-canonicalize-semantic.ll` 覆盖 `canonicalize.f128` 的类型化 `fmul x, 1.0` 降低、FMF、符号零、分支／`phi`／回边、普通 `tail`，以及 constrained `fmul`、`lround`、向量／`ppc_fp128`、`poison`／`undef`、`musttail`、operand bundle、内联汇编与无效 ABI 负例；FileCheck 证明解释器发 `fmul fp128` 且不再保留 `llvm.canonicalize.f128`。`vmp-fp128-fpclass-semantic.ll` 覆盖 NaN／无穷／零／正规／次正规掩码、分支／`phi`／回边、普通 `tail`，以及 constrained、向量／`ppc_fp128`、越界掩码、`poison`／`undef`、`musttail`、operand bundle、内联汇编与无效 ABI 负例；FileCheck 证明重放 `call i1 @llvm.is.fpclass.f128` 且掩码为常量。`vmp-fp128-int-round-semantic.ll` 覆盖 `lround`／`lrint` 的 `i32`／`i64`、`llround`／`llrint` 的 `i64`、有符号／无符号饱和 `i8`／`i16`／`i32`／`i64`、普通 `tail`、钻石 `phi` 与回边循环，以及 constrained `lround`、`llround`／`llrint` 的 `i32`、`lround.i16`、饱和 `i1`／`i128`、向量／`ppc_fp128`、`poison`／`undef`、`musttail`、operand bundle、内联汇编与无效 ABI 负例；FileCheck 证明解释器发类型化 `fp128` 帧并重放原内建。宿主 `lli` 对 `minnum`／`maxnum`／舍入族、listed `copysign`／`pow`／`powi.i32`／超越、canonicalize 的 `fmul` 降低以及 `is.fpclass` 做参考／保护位差分；`copysign`／`pow`／超越／`is.fpclass` 在宿主 x86 与 AArch64 上均可选择。`minimum`／`maximum` 仍仅 FileCheck（LLVM 15 AArch64 与宿主 x86 均无法选择 `fminimum`／`fmaximum` `fp128`）。`lround`／`llround`／`lrint`／`llrint`／饱和转换仅 FileCheck 与 AArch64 `llc`／`readobj`：宿主 x86 的 `lroundl` 是 `f80` long double，探得 `llvm.lround.i64.f128(1.5)` 在宿主 `lli` 上返回 0，不得作语义神谕。其余走 AArch64 `llc`／`readobj`。
- 拒绝：向量 `fp128`、聚合字段、原子、指针转换、`bitcast` `fp128`↔`i128`、`ppc_fp128`、可变参数、间接调用、`musttail`、operand bundle、`noreturn`／`returns_twice`、复杂 ABI、未列入的数学／constrained 内建（constrained `lround`／`llround.i32`／`llrint.i32`／`lround.i16`／饱和 `i1`／`i128`／constrained／非 `i32` `powi`／越界 `is.fpclass` 掩码；listed `sqrt`／`fabs`／`fma`／`fmuladd`／最值／舍入／`copysign`／`pow`／`powi.i32`／超越／`canonicalize`／`is.fpclass`／`lround`／`lrint`／`llround`／`llrint`／`fptosi.sat`／`fptoui.sat` 见接受项）、`i128` 整数转换。不把 `fp128` 并入 `isSupportedScalarFPType`／`isSupportedScalarFPFrameType`／`isSupportedOrdinaryCallValueType`／`isSupportedIndirectValueType`／`isSupportedFloatToI64RoundIntrinsic`／`isSupportedFloatToIntSatIntrinsic`。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。既有以标量 `fp128` 作仍关闭内建形参的负例（constrained）跳过原因仍为 `unsupported call instruction`（形参类型已接受）。`vmp-half-vector-semantic.ll` 的 `unsupported_fp128` 已改为 `ppc_fp128`。`vmp-fp128-scalar-semantic.ll` 的 `unsupported_direct_call` 已改为可变参数哨兵。`vmp-fp128-scalar-semantic.ll`／`vmp-fp128-direct-call-semantic.ll`／`vmp-fp128-math-semantic.ll`／`vmp-fp128-minmax-round-semantic.ll`／`vmp-fp128-copy-pow-transcendental-semantic.ll`／`vmp-fp128-canonicalize-semantic.ll` 的原 `unsupported_lround` 已改为仍关闭的 `llvm.experimental.constrained.lround.i64.f128`。`vmp-half-lround-semantic.ll` 的 `unsupported_half_lround_fp128` 已改为同一 constrained 哨兵。`vmp-half-sat-convert-semantic.ll` 的 `unsupported_half_sat_fp128` 已改为仍关闭的 `llvm.fptosi.sat.i128.f128`。`vmp-half-scalar-fpclass-semantic.ll` 的 `unsupported_half_fpclass_fp128` 已改为 `ppc_fp128`。

**当前实际固定 bfloat 向量支持边界**

- 接受：固定向量、总原始宽度 1..128、元素为 `bfloat`（含 `<4 x bfloat>` 64 位与 `<8 x bfloat>` 128 位），走既有独立 `i128` 向量虚拟寄存器帧，不新增操作码。元素种类编码新增 `BF16`（Variant 第 24..27 位为 8；0..7 仍是原 3 位编码）。所在函数 `target-features` 最后一次出现的精确 `+bf16`／`-bf16` 令牌须为 `+bf16`。覆盖无 FastMathFlags 的 `fadd`／`fsub`／`fmul`／`fdiv`／`frem`／`fneg`、`fcmp`、`select`、`phi`、`freeze`（复用既有 `VectorFreeze` 与向量虚拟寄存器：定义源从向量帧取出，`undef`／`poison` 仅在冻结处按既有 source-kind 物化；解释器发射带类型的 `freeze <N x bfloat>` 并写回向量帧，无浮点数学／FMF，不新增操作码）、非原子地址空间零 `load`／`store`、`extractelement`／`insertelement`、常量掩码 `shufflevector`（源与结果均为 1..128 的 `bfloat` 向量；解释器先 `bitcast` 到同车道 `<N x i16>` 再 shuffle，不得把原生 `shufflevector <N x bfloat>` 交给指令选择）、同总宽 `bitcast`（`bfloat` 向量与既有整数固定向量或标量 `i1`..`i64` 帧，复用既有 `VectorBitCast`）、同车道 `N=1..4` 的 `fpext` `<N x bfloat>`→`<N x float>` 与 `fptrunc` `<N x float>`→`<N x bfloat>`（两侧向量虚拟寄存器均不超过 128 位；复用既有 `VectorFPExt`／`VectorFPTrunc` 与 `VectorCastDescriptor.LegalizeBFloatFloat` 显式标志，不得与既有 `half`↔`float`／`f32`↔`f64` 原生 `CreateCast` 路径碰撞；解释器按车道复用既有精确提升与 RNE，不得发射原生 `fpext`／`fptrunc` `bfloat` 向量）、同车道 `sitofp`／`uitofp`／`fptosi`／`fptoui`（既有 `i1`／`i8`／`i16`／`i32`／`i64` 固定整数向量与受支持 `bfloat` 向量之间，车道数相同，且源、目的各自总原始宽度独立为 1..128；复用既有 `VectorSIToFP`／`VectorUIToFP`／`VectorFPToSI`／`VectorFPToUI` 与 `VectorCastDescriptor.LegalizeBFloatInt` 显式标志，不得与既有 `half`／`f32`／`f64` 原生 `CreateCast` 路径碰撞；解释器按车道走标量 `sitofp`／`uitofp`→`f32` 再 RNE，或精确提升后再 `fptosi`／`fptoui`，保留 LLVM 有符号／无符号语义，含 `i1`：`sitofp` true 为 `-1.0`，`uitofp` true 为 `1.0`；不得发射原生 `sitofp`／`fptosi` `bfloat` 向量）、常量、以及被虚拟化函数自身的 `bfloat` 向量形参／返回。普通直接 C 调用（含普通 `tail`）上的固定 `bfloat` 向量实参／返回见下一节独立 ABI 表面。LLVM 15 AArch64 不能选择原生 `bfloat` 向量算术／比较／`fpext`／`fptrunc`／整数转换：解释器把每个车道精确提升为 `f32`（高 16 位插入）再做 `fadd`／…／`fcmp` `float`，写回时对每个车道做与标量相同的 RNE（含 Inf／NaN 短路：`icmp` `2139095040`，NaN `or` 64）。`fneg`／`select` 在 `<N x i16>` 位模式上完成。不得把 `fadd <N x bfloat>` 或原生 `fpext`／`fptrunc`／`sitofp`／`fptosi` `bfloat` 向量交给指令选择。LLVM 15 的 `CastInst` 不是 `FPMathOperator`；若 FastMathFlags 可表示则拒绝。`vmp-bfloat-vector-semantic.ll` 覆盖 64／128 位正例、钻石 `phi`、回边循环、内存与 extract／insert、shuffle 与 `bfloat` 向量↔`<4 x i16>`／`i64` 往返 bitcast、`N=2`／`N=4` `fpext`／`fptrunc` 与混合 `f32` 算术、`i1`／`i8`／`i16`／`i32`／`i64` 有符号／无符号整数转换与混合 `bfloat` 算术，以及无特性／最终 `-bf16`／FMF／原子／内建／`fastcc` 直接调用／超过 128 位／可伸缩／无特性 shuffle／bitcast／`bfloat`↔`float` 向量 bitcast／`bfloat`↔`half` bitcast／`bfloat`↔`double` 宽度转换／`i128` 元素／超宽整数转换／`N>4` `f32` 转换负例。`vmp-bfloat-vector-freeze-semantic.ll` 覆盖定义／`undef`／`poison` 的 `<1 x bfloat>`／`<2 x bfloat>`／`<4 x bfloat>`／`<8 x bfloat>` `freeze`、常量源、`phi`／`select`／回边、最后令牌 `+bf16`，以及无特性／最终 `-bf16`／仅 `+bf16fml`／超宽／可伸缩／指针向量／聚合／冻结外 `poison` 负例；FileCheck 证明解释器发射带类型的 `freeze <N x bfloat>`，原 `hikari_vmp` 函数体不再保留。宿主 `lli` 不能假定会选择 `bfloat` 向量，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。
- 拒绝：缺少或最后令牌为 `-bf16` 的形态良好 `bfloat` 向量 SSA／内存／`freeze`／shuffle／整数 bitcast／`N=1..4` `bfloat`↔`float` 宽度转换／同车道 `bfloat`↔`i1`..`i64` 数值转换（跳过原因 `unsupported target feature`，保留 `hikari.vmp.selected`）；可伸缩／超过 128 位／聚合／指针向量或类型不匹配的 `freeze` 仍以 `unsupported freeze instruction` 跳过；冻结之外的 `poison`／`undef` 仍拒绝；带任何 FastMathFlags 的 `bfloat` 向量 `fadd`／`fsub`／`fmul`／`fdiv`／`frem`／`fneg`／`fcmp`／`select`／`phi`（`+bf16` 下跳过原因为对应 `unsupported float …` 或既有向量 select／phi／比较字符串），以及若 FastMathFlags 可表示则同样拒绝 `bfloat`↔`float`／`bfloat`↔整数向量转换；原子 `load`／`store`；未列入的向量内建（listed `llvm.fabs`／`llvm.sqrt`／`llvm.fma`／`llvm.fmuladd`／最值／`copysign`／舍入族／`sin`..`log10`／`pow`／`powi` 见独立数学表面；listed `llvm.vector.reduce.fadd`／`fmul`／`fmin`／`fmax` 见独立归约表面；listed `llvm.masked.load`／`llvm.masked.store`／`llvm.masked.gather`／`llvm.masked.scatter`／`llvm.masked.expandload`／`llvm.masked.compressstore` 见独立掩码表面）；constrained；超过 128 位（如 `<16 x bfloat>`）；可伸缩；聚合 `bfloat` 字段；非零地址空间；`bfloat`↔`half`／`double`／`fp128` 宽度转换、`N>4` 的 `bfloat`↔`float` 转换、`i128` 元素或任一侧超过 128 位的数值整数转换、车道数不同的转换（助手仍检查；LLVM 15 验证器不允许 `sitofp`／`fptosi` 车道数不匹配，故 lit 不含非法 IR）；与 `half`／`f32`／`f64` 向量的 bitcast。不把 `bfloat` 向量并入 `isSupportedFixedVectorType` 或 `isSupportedOrdinaryCallValueType`，以免泄漏到间接／可变参数／聚合字段或其它向量族。普通直接 C 调用见独立 ABI 表面。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量与固定 bfloat 向量数学内建支持边界**

- 接受：精确 `llvm.fabs`／`llvm.sqrt`／`llvm.fma`／`llvm.fmuladd`／`llvm.minnum`／`llvm.maxnum`／`llvm.minimum`／`llvm.maximum`／`llvm.copysign`／`llvm.ceil`／`llvm.floor`／`llvm.trunc`／`llvm.round`／`llvm.roundeven`／`llvm.rint`／`llvm.nearbyint`／`llvm.sin`／`llvm.cos`／`llvm.exp`／`llvm.exp2`／`llvm.log`／`llvm.log2`／`llvm.log10`／`llvm.pow`／`llvm.powi`。`pow` 的结果与两个实参为同一标量 `bfloat` 或同一受支持固定 `bfloat` 向量；`powi` 的底数为同一标量／向量 `bfloat`，指数必须是标量 `i32`（LangRef：向量 `powi` 的指数保持标量整数；非 `i32` 与向量指数拒绝）。总原始宽度 1..128（含 `<4 x bfloat>`／`<8 x bfloat>`）。所在函数 `target-features` 最后令牌须为精确 `+bf16`。调用点须为 `CallingConv::C`、非可变参数、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。任何可表示的 FastMathFlags 均拒绝。不新增操作码。经既有 `CallDescriptor.LegalizeBFloatMath`：`fabs`／`copysign` 仍是 `i16` 位运算；`fmuladd` 把每个 `bfloat` 实参精确提升为 `f32` 后调用 `llvm.fma.f32`（不得重放 `llvm.fmuladd.bf16`／`llvm.fmuladd.f32`，也不得拆成两步 `bfloat` 乘加），再按既有 RNE 收回 `bfloat`，从而选取源语义允许的单次融合舍入；其余把每个 `bfloat` 实参精确提升为 `f32`，调用对应 `f32` 内建，再按既有 RNE 收回 `bfloat`。`powi` 的 `i32` 指数走整数虚拟寄存器，不提升、不按车道拆开。向量按车道降低，因此 `<8 x bfloat>` 不会物化 `<8 x float>`。不得把 `llvm.pow.bf16`／`llvm.powi.v4bf16.i32`／`llvm.fmuladd.bf16` 等原生 `bfloat` 内建交给 AArch64 指令选择。不改变既有 `f32`／`f64`／`half` 数学。`vmp-bfloat-math-semantic.ll` 覆盖 `fabs`／`sqrt`／`fma`；`vmp-bfloat-fmuladd-semantic.ll` 覆盖标量／`<2 x bfloat>`／`<4 x bfloat>`／`<8 x bfloat>` `fmuladd`、与 `fma` 同降为 `f32` `llvm.fma` 的区别、运行时 NaN／负零／无穷、普通 `tail`，以及无特性／最终 `-bf16`／FMF／`fastcc`／`musttail`／operand bundle／`poison`／`undef`／constrained／超宽／可伸缩负例；`vmp-bfloat-minmax-semantic.ll` 覆盖四最值与 `copysign`；`vmp-bfloat-round-transcendental-semantic.ll` 覆盖舍入与 `sin`..`log10`；`vmp-bfloat-pow-semantic.ll` 覆盖标量／向量 `pow`／`powi`、混合 `pow`+`powi`+`fadd`，以及无特性／最终 `-bf16`／FMF／`i64` 指数／constrained／`poison`／超过 128 位负例。LLVM 15 不能表示向量指数的 `powi`，助手仍要求标量 `i32`。既有以 `fmuladd` 作跳过哨兵的测试已改为仍关闭的 constrained 形态。宿主 `lli` 不能假定会选择 `bfloat` 数学，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。
- 拒绝：缺少或最后令牌为 `-bf16` 的形态良好 listed 调用（跳过原因 `unsupported target feature`，保留 `hikari.vmp.selected`）；带任何 FastMathFlags 的 listed 调用（`+bf16` 下跳过原因为 `unsupported float call instruction`）；constrained 形态、非 `i32` 或向量指数的 `powi`、`poison`／`undef`、可伸缩、超过 128 位、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。`llvm.is.fpclass`／`llvm.canonicalize` 见独立表面，不并入 listed 数学 ID。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量与固定 bfloat 向量饱和转换支持边界**

- 接受：精确 `llvm.fptosi.sat`／`llvm.fptoui.sat`。源为标量 `bfloat` 或受支持固定 `bfloat` 向量（总原始宽度 1..128）；目的为标量 `i8`／`i16`／`i32`／`i64`，或同车道固定 `i8`／`i16`／`i32`／`i64` 向量且目的总宽独立为 1..128（含 `<4 x bfloat>`→`<4 x i8>`／`<4 x i32>`、`<2 x bfloat>`→`<2 x i64>`、`<8 x bfloat>`→`<8 x i16>`；`<4 x bfloat>`→`<4 x i64>` 因目的 256 位拒绝）。所在函数 `target-features` 最后令牌须为精确 `+bf16`。调用点须为 `CallingConv::C`、非可变参数、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。整数结果不是 `FPMathOperator`；若 FastMathFlags 可表示则拒绝。不新增操作码。经既有 `CallDescriptor.LegalizeBFloatMath`：源精确提升为 `f32`，调用匹配的 `llvm.fptosi.sat.iN.f32`／`llvm.fptoui.sat.iN.f32`，结果写入整数或整数向量虚拟寄存器。向量按车道降低。不得把 `llvm.fptosi.sat.i32.bf16` 等原生 `bfloat` 饱和内建交给 AArch64 指令选择。不改变普通非饱和 `fptosi`／`fptoui` 或 `f32`／`f64`／`half` 饱和表面。`vmp-bfloat-sat-convert-semantic.ll` 覆盖有符号／无符号标量宽度、负值与上溢形状、64／128 位向量，以及无特性／最终 `-bf16`／`i1`／`i128`／`poison`／超宽目的负例。宿主 `lli` 不能假定会选择 `bfloat` 饱和转换，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。
- 拒绝：缺少或最后令牌为 `-bf16` 的形态良好 listed 调用（跳过原因 `unsupported target feature`，保留 `hikari.vmp.selected`）；`i1`／`i128` 或其它目的宽度；车道数不匹配（助手仍检查；形态良好 LLVM 饱和内建通常不能表示不匹配车道）；任一侧超过 128 位；constrained；`poison`／`undef`；可伸缩；非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际普通直接 C 调用上的标量与固定 bfloat 向量 ABI 支持边界**

- 接受：普通非内建直接 C 调用（含普通 `tail`；解释器按原 `FunctionType`／调用约定／属性／元数据发射普通非 `tail` `CallInst`，不恢复 tail kind，可观察语义不变），其既有直接调用实参／返回表面扩展为标量 `bfloat` 与总原始宽度 1..128 的固定 `bfloat` 向量。复用既有 `CallDescriptor`、向量虚拟寄存器帧与直接调用重建：`bfloat` 向量实参从向量帧 `load`，结果按原类型写回向量帧，不丢失调用约定／属性／`FunctionType`。`bfloat` 向量参与时，所在函数 `target-features` 最后一次出现的精确 `+bf16`／`-bf16` 令牌须为 `+bf16`（不读取命令行 `-mattr`）。调用点与被调均须 `CallingConv::C` 且均非可变参数。既有标量／地址空间零指针／`half`／`f32`／`f64`／定长向量／小型扁平聚合实参仅在已通过普通直接调用安全规则时一并保留；标量 `bfloat` 可与 `bfloat` 向量混合。直接递归与跨函数调用走原 `Function*` 符号，不改写为合成被调。不经 `CallDescriptor.LegalizeBFloatMath`，不得把 listed `bfloat` 数学内建当作本表面重放。标量 `bfloat` 仅调用仍走既有标量表面（浮点虚拟寄存器；`void`／整数结果不得调用 `getFastMathFlags()`）。`vmp-bfloat-vector-call-semantic.ll` 覆盖标量与 `<4 x bfloat>`／`<8 x bfloat>` 实参／返回、`void` sink、整数结果、混合既有 ABI、标量+向量混合、递归与跨函数、普通 `tail`（含自递归；解释器不得保留 `tail call`）、最后令牌 `+bf16`，以及无特性／最终 `-bf16`／仅 `+bf16fml`／FMF／`fastcc`／`musttail`／可变参数／间接（哨兵改为 `fastcc`）／operand bundle／`noreturn`／`returns_twice`／`poison`／`undef`／超宽／可伸缩／`bfloat` 聚合／指针向量／内联汇编／`sret` 负例。FileCheck 证明解释器发射带类型的普通 `call` 与向量帧 `bitcast`／`load`／`store`。宿主 `lli` 不能假定会选择 `bfloat`，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。
- 拒绝：可变参数、非 C、`musttail`（早期资格仍报 `musttail call` 并不加 `hikari.vmp.selected`）、操作数捆绑、`noreturn`／`returns_twice`、内联汇编（早期资格报 `inline assembly` 并不加 `hikari.vmp.selected`）、复杂 ABI（`sret`／`byval`／`byref` 等）、指针向量、可伸缩／超过 128 位的 `bfloat` 向量、`bfloat` 聚合、`poison`／`undef`、缺少或最后令牌为 `-bf16` 的形态良好直接调用（跳过原因 `unsupported target feature`，保留 `hikari.vmp.selected`）、带任何 FastMathFlags 的浮点结果调用（`+bf16` 下跳过原因为 `unsupported float call instruction`）。受限间接 C 见下一节独立表面。不把 `bfloat` 向量并入 `isSupportedOrdinaryCallValueType`。资格失败保持原函数与 `hikari.vmp.selected`（`musttail`／内联汇编除外）。

**当前实际受限间接 C 调用上的标量与固定 bfloat 向量 ABI 支持边界**

- 接受：受限间接 `CallInst`（`getCalledFunction() == null`），`CallingConv::C`、非可变参数、固定原型、0..8 个参数。被调是地址空间零指针虚拟寄存器（函数指针形参、全局函数表 `load`、`select` 或 `phi`）。实参／返回可为标量 `bfloat` 或总原始宽度 1..128 的固定 `bfloat` 向量，并可与既有间接表面的整数／地址空间零指针／`half`／`f32`／`f64`／定长向量／小型扁平聚合实参混合（聚合仅作实参；本表面拒绝聚合结果）。所在函数 `target-features` 最后令牌须为精确 `+bf16`。复用既有 `CallDescriptor`、浮点虚拟寄存器（标量 `bfloat`）与向量虚拟寄存器（`bfloat` 向量）以及普通间接发射路径：先从指针帧取被调，再按类型从浮点／向量帧取出实参，按原 `FunctionType` 发射 `call`，结果写回对应帧。**不得**把 `bfloat` 实参降成整数位模式或走整数虚拟寄存器。不新增操作码。不经 `LegalizeBFloatMath`。任何可表示的 FastMathFlags 均拒绝。普通 `tail` 接受并重放为普通非 `tail` `CallInst`（与直接 `bfloat` C 一致；含经函数指针／回调全局的自递归）。`vmp-bfloat-indirect-call-semantic.ll` 覆盖标量／`<4 x bfloat>`／`<8 x bfloat>` 经函数指针形参、全局表、`select`／`phi`、返回值与 `void` 调用、普通 `tail`（含函数指针递归；解释器不得保留 `tail call`），以及无特性／最终 `-bf16`／FMF／`musttail`／可变参数／`fastcc`／operand bundle／`noreturn`／`returns_twice`／`sret`／超宽／可伸缩／聚合结果／`poison` 负例。FileCheck 证明解释器发射带类型的间接 `call` 与浮点／向量帧传送。宿主 `lli` 不能假定会选择 `bfloat`，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。既有标量／直接调用 lit 中形态良好的间接哨兵已改为 `fastcc`（不得再用普通 `tail`，以免被本表面打开）。
- 拒绝：缺少或最后令牌为 `-bf16` 的形态良好间接调用（跳过原因 `unsupported target feature`，保留 `hikari.vmp.selected`）；`musttail`（间接 `musttail` 晚资格报 `indirect call`，保留 `hikari.vmp.selected`；普通 `tail` 已接受并重放为非 `tail`）；可变参数、非 C、操作数捆绑、`noreturn`／`returns_twice`、内联汇编、复杂 ABI、可伸缩／超过 128 位、聚合结果、指针向量、`poison`／`undef`、带 FastMathFlags 的浮点结果。不把 `bfloat` 并入 `isSupportedIndirectValueType`。资格失败保持原函数（直接 `musttail` 与内联汇编除外）。

**当前实际标量与固定 bfloat 向量 is.fpclass／canonicalize 支持边界**

- 接受：精确 `llvm.is.fpclass` 与 `llvm.canonicalize`，标量 `bfloat` 或受支持固定 `bfloat` 向量（总原始宽度 1..128）。`is.fpclass` 的结果为标量 `i1`，或同车道 `<N x i1>`；第二参数必须是 `i32` `ConstantInt` 且保持 `ImmArg`，经既有 `CallDescriptor` ImmediateArguments 重发射，不进虚拟寄存器。解释器把每个 `bfloat` 精确提升为 `f32`（高 16 位插入）再调用 `llvm.is.fpclass.f32`，按车道写回整数或 `<N x i1>` 向量虚拟寄存器，因此 NaN／无穷／零／次正规／符号分类与提升后的 `f32` 一致；不得把 `llvm.is.fpclass.bf16`／`llvm.is.fpclass.vNbf16` 交给 AArch64 指令选择。`canonicalize` 同样精确提升为 `f32`，复用仓库已验证的 `f32` 正规化降低（`fmul x, 1.0`，不得重放 `llvm.canonicalize.f32`／`llvm.canonicalize.bf16`），再按既有 RNE 收回 `bfloat`（Inf／NaN 短路：指数全 1 时截断高 16 位，非零尾数置 `0x0040` 安静位，与 `APFloat`／既有 bfloat 写回一致）。所在函数 `target-features` 最后令牌须为精确 `+bf16`。调用点须为 `CallingConv::C`、非可变参数、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。任何可表示的 FastMathFlags 均拒绝。整数／`i1` 结果不是 `FPMathOperator`，不得对它们调用 `getFastMathFlags()`。不新增操作码。经既有 `CallDescriptor.LegalizeBFloatMath`。不改变既有 `f32`／`f64`／`half` `is.fpclass`／`canonicalize`，也不改变 listed bfloat 数学、饱和转换或普通直接 C 调用。`vmp-bfloat-fpclass-canonicalize-semantic.ll` 覆盖标量普通／零／无穷／NaN／次正规／负类掩码、普通 `tail`、64／128 位向量、`canonicalize` 与运行时负零／NaN 选择形状、混合 `canonicalize`+`is.fpclass`，以及无特性／最终 `-bf16`／仅 `+bf16fml`／FMF／`fastcc`／`musttail`／operand bundle／`poison`／`undef`／超宽／可伸缩／constrained／`fmuladd` 负例。FileCheck 证明解释器发射 `llvm.is.fpclass.f32` 与 `fmul float`／RNE，且无原生 `bfloat` 内建。宿主 `lli` 不能假定会选择 `bfloat`，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。既有数学／舍入 lit 中用 `canonicalize.bf16` 作跳过哨兵的测试已改为仍关闭的 `fmuladd`。
- 拒绝：缺少或最后令牌为 `-bf16` 的形态良好 listed 调用（跳过原因 `unsupported target feature`，保留 `hikari.vmp.selected`）；带任何 FastMathFlags 的 `canonicalize`（`+bf16` 下跳过原因为 `unsupported float call instruction`）；结果不是 `i1`／同车道 `<N x i1>` 的 `is.fpclass`；动态或非 `i32` mask（形态助手拒绝；`ImmArg` 下非法 IR 不进 lit）；constrained；`poison`／`undef`；可伸缩；超过 128 位；非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（`musttail` 除外）。

**当前实际固定 bfloat 向量 vector.reduce.fadd／fmul／fmin／fmax 支持边界**

- 接受：精确 `llvm.vector.reduce.fadd`／`fmul`／`fmin`／`fmax`，源为总原始宽度 1..128 的固定 `bfloat` 向量（含 `<1 x bfloat>`／`<2 x bfloat>`／`<4 x bfloat>`／`<8 x bfloat>`），结果为标量 `bfloat`。`fadd`／`fmul` 遵循 LLVM 15 `elt(elt start, vec)`：第一个实参必须是匹配的标量 `bfloat` 起始值。`fmin`／`fmax` 仅为 `elt(vec)`。所在函数 `target-features` 最后一次出现的精确 `+bf16`／`-bf16` 令牌须为 `+bf16`（不读取命令行 `-mattr`；`+bf16fml` 不算）。调用点须为 `CallingConv::C`、非可变参数、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。任何可表示的 FastMathFlags（含 `reassoc`／`fast`／`nnan`）均拒绝。不新增操作码。不把 `bfloat` 并入 `isSupportedVectorReduceIntrinsic`／`isSupportedFixedVectorType`，因此既有 `f32`／`f64`／`half`／整数 `vector.reduce` 仍按原路径重放原生内建。经既有 `CallDescriptor.LegalizeBFloatMath`：无 `reassoc` 的 `fadd`／`fmul` 从标量 `bfloat` 起始值起按车道 0..N-1 递增，每步精确提升两端为 `f32`、做一次 `fadd`／`fmul` `float`，再按既有 RNE 收回 `bfloat`（Inf／NaN 短路：`icmp` `2139095040`，NaN `or` 64）；不得先以 `f32` 累积到最后才舍入，也不得重放 `llvm.vector.reduce.fadd/fmul.vNbf16` 或 `llvm.vector.reduce.fadd/fmul.vNf32`。`fmin`／`fmax` 从车道 0 起，将其余车道交给既有 `bfloat` `minnum`／`maxnum` 合法化（`llvm.minnum.f32`／`llvm.maxnum.f32` + RNE），因此 NaN 与有符号零的已定义／未定义边界与该表面一致；`N=1` 仅为 `extractelement`。不得把原生 `llvm.vector.reduce.fmin/fmax.vNbf16` 交给 AArch64 指令选择。`vmp-bfloat-vector-reduce-semantic.ll` 覆盖四 ID、16／32／64／128 位、动态起始值、运行时 NaN／负零、最后令牌 `+bf16`，以及无特性／最终 `-bf16`／仅 `+bf16fml`／`reassoc`／`fast`／`nnan`／`musttail`／operand bundle／`noreturn`／`returns_twice`／`fastcc`／`poison`／`undef`／constrained／`vp.reduce`／超宽／可伸缩负例。FileCheck 证明解释器发射逐通道 `fadd`／`fmul` `float` 或 `llvm.minnum.f32`／`llvm.maxnum.f32` 与每步 RNE，且无原生 `bfloat`／`f32` vector-reduce。宿主 `lli` 不能假定会选择 `bfloat`，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。`vmp-half-vector-masked-reduce-semantic.ll` 中无 `+bf16` 的形态良好 `reduce.fmin.v4bf16` 哨兵现为 `unsupported target feature`。
- 拒绝：缺少或最后令牌为 `-bf16` 的形态良好 listed 调用（跳过原因 `unsupported target feature`，保留 `hikari.vmp.selected`）；带任何 FastMathFlags（含 `reassoc`）的 listed 调用（`+bf16` 下跳过原因为 `unsupported float call instruction`）；`musttail`／操作数捆绑／`noreturn`／`returns_twice`／非 C／可变参数／复杂 ABI／`poison`／`undef`／可伸缩／超过 128 位／`llvm.vp.reduce.*`／constrained／非 `bfloat`／起始值或结果类型不匹配（助手仍检查；LLVM 15 验证器通常不能表示不匹配签名，故 lit 不含非法 IR）。资格失败保持原函数与 `hikari.vmp.selected`（`musttail` 除外）。

**当前实际固定 bfloat 向量 llvm.masked.load／llvm.masked.store 支持边界**

- 接受：精确 `llvm.masked.load`／`llvm.masked.store`，数据为总原始宽度 1..128 的固定 `bfloat` 向量（含 `<1 x bfloat>`／`<2 x bfloat>`／`<4 x bfloat>`／`<8 x bfloat>`）。指针必须是地址空间零标量指针。对齐必须是 `i32` 幂二 `ConstantInt` 且保持 ImmArg。掩码必须是同车道 `<N x i1>`。`masked.load` 的 passthru 必须与结果同型。所在函数 `target-features` 最后一次出现的精确 `+bf16`／`-bf16` 令牌须为 `+bf16`（不读取命令行 `-mattr`；`+bf16fml` 不算）。调用点须为 `CallingConv::C`、非可变参数、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。不新增操作码。不把 `bfloat` 并入 `isSupportedMaskedLoadIntrinsic`／`isSupportedFixedVectorType`，因此既有整数／`half`／`f32`／`f64` 掩码内存仍按原路径重放原生内建。经既有 `CallDescriptor.LegalizeBFloatMaskedMemory`：解释器先从指针／向量虚拟寄存器取出全部实参（含 ImmArg 对齐），再按车道 0..N-1 发射 `condbr`；取路径做一次标量 `bfloat` `load`／`store`，关闭路径不访问该地址。加载关闭车道经 `phi` 保留 passthru。逐通道对齐为 `commonAlignment(向量对齐, 车道×2)`（`bfloat` 两字节），因此 `align 8` 的四车道为 8／2／4／2。源调用上可安全保留的内存元数据（`applyMetadata` 既有规则：复制除 `alias_scope`／`noalias` 外的节点）与 `DebugLoc` 写到每个标量访存。处理器外壳的 volatile 字节码／虚拟寄存器帧访问仍在 handler 入口、第一次 `condbr` 之前完成；新增 PHI／跨块 SSA 走既有 VMP 后 CFF 安全回退（`hasUnsupportedVMPPostCFFSSA` 跳过 flatten，不改写 volatile 帧）。不得重放 `llvm.masked.load/store.vNbf16`，也不得先做无保护整向量访存再 `select`。`vmp-bfloat-masked-memory-semantic.ll` 覆盖动态掩码、全真／全假、passthru、对齐 1／2／8、加载／存储、16／32／64／128 位、运行时 NaN／负零、`!nontemporal` 元数据、最后令牌 `+bf16`，以及无特性／最终 `-bf16`／仅 `+bf16fml`／`musttail`／operand bundle／`noreturn`／`returns_twice`／`fastcc`／`poison`／`undef`／非 AS0／超宽／可伸缩负例。C 约定 gather／scatter／expand／compress 哨兵已改为 `fastcc`。FileCheck 证明解释器发射逐通道 `br i1` 与标量 `load`／`store bfloat`，且无原生 `bfloat` masked 调用或整向量 `load`／`store <N x bfloat>`。宿主 `lli` 不能假定会选择 `bfloat`，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。
- 拒绝：缺少或最后令牌为 `-bf16` 的形态良好 listed 调用（跳过原因 `unsupported target feature`，保留 `hikari.vmp.selected`）；`musttail`／操作数捆绑／`noreturn`／`returns_twice`／非 C／可变参数／复杂 ABI；`poison`／`undef`；可伸缩；超过 128 位；非 AS0；非立即数或非幂二对齐（助手仍检查；ImmArg 下非法非立即数不进 lit）；掩码／passthru 形状不匹配（助手仍检查；LLVM 15 验证器通常不能表示，故 lit 不含非法 IR）；原子形态（LLVM 15 无原子 masked.load／store）。listed `llvm.masked.gather`／`scatter`／`expandload`／`compressstore` 见后续独立表面。资格失败保持原函数与 `hikari.vmp.selected`（`musttail` 除外）。

**当前实际固定 bfloat 向量 llvm.masked.gather／llvm.masked.scatter 支持边界**

- 接受：精确 `llvm.masked.gather`／`llvm.masked.scatter`，数据为总原始宽度 1..128 的固定 `bfloat` 向量（含 `<1 x bfloat>`／`<2 x bfloat>`／`<4 x bfloat>`／`<8 x bfloat>`）。指针必须是同车道地址空间零 `<N x ptr>`（N=1..8，既有指针向量帧；不是通用 pointer-vector ABI）。对齐必须是 `i32` 幂二 `ConstantInt` 且保持 ImmArg。掩码必须是同车道 `<N x i1>`。`masked.gather` 的 passthru 必须与结果同型。所在函数 `target-features` 最后一次出现的精确 `+bf16`／`-bf16` 令牌须为 `+bf16`。调用点须为 `CallingConv::C`、非可变参数、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。不新增操作码。不把 `bfloat` 并入 `isSupportedMaskedGatherIntrinsic`／`isSupportedFixedVectorType`。经既有 `CallDescriptor.LegalizeBFloatMaskedMemory`：解释器先从指针向量／数据向量虚拟寄存器取出全部实参，再按车道 0..N-1 发射 `condbr`；取路径从该车道指针做一次标量 `bfloat` `load`／`store`，关闭路径完全不解引用。gather 关闭车道经 `phi` 保留 passthru。逐通道对齐直接使用 ImmArg（地址彼此独立），因此 `align 8` 的四车道均为 8。源调用上可安全保留的内存元数据与 `DebugLoc` 写到每个标量访存。volatile 字节码／虚拟寄存器帧访问仍在 handler 入口、第一次 `condbr` 之前完成；新增 PHI／跨块 SSA 走既有 VMP 后 CFF 安全回退。不得重放 `llvm.masked.gather/scatter.vNbf16`，也不得无条件访问任何通道地址。指针向量由既有标量基址 + 整数向量下标 GEP、或标量指针 `insertelement` 构成，函数形参仍不得是指针向量。`vmp-bfloat-gather-scatter-semantic.ll` 覆盖动态指针向量与掩码、全真／全假、passthru、对齐 1／2／8、gather／scatter、16／32／64／128 位、`insertelement` 指针向量、运行时 NaN／负零、`!nontemporal`、最后令牌 `+bf16`，以及无特性／最终 `-bf16`／仅 `+bf16fml`／`musttail`／operand bundle／`noreturn`／`returns_twice`／`fastcc`／`poison`／`undef`／非 AS0／超宽／可伸缩负例。FileCheck 证明解释器发射逐通道 `br i1` 与标量 `load`／`store bfloat`，且无原生 `bfloat` gather／scatter 或整向量访存。原先 load／store lit 中 C 约定 gather／scatter 哨兵已改为 `fastcc`，仍为 `unsupported masked memory instruction`。宿主 `lli` 不能假定会选择 `bfloat`，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。
- 拒绝：缺少或最后令牌为 `-bf16` 的形态良好 listed 调用（跳过原因 `unsupported target feature`，保留 `hikari.vmp.selected`）；`musttail`／操作数捆绑／`noreturn`／`returns_twice`／非 C／可变参数／复杂 ABI；`poison`／`undef`；可伸缩；超过 128 位或指针向量 N>8；非 AS0；指针／掩码／值车道数不匹配（助手仍检查；验证器通常不能表示，故 lit 不含非法 IR）；非立即数或非幂二对齐。listed `llvm.masked.expandload`／`compressstore` 见下一节。资格失败保持原函数与 `hikari.vmp.selected`（`musttail` 除外）。

**当前实际固定 bfloat 向量 llvm.masked.expandload／llvm.masked.compressstore 支持边界**

- 接受：精确 `llvm.masked.expandload`／`llvm.masked.compressstore`，数据为总原始宽度 1..128 的固定 `bfloat` 向量（含 `<1 x bfloat>`／`<2 x bfloat>`／`<4 x bfloat>`／`<8 x bfloat>`）。指针必须是地址空间零标量指针。无对齐 ImmArg。掩码必须是同车道 `<N x i1>`。`expandload` 的 passthru 必须与结果同型。所在函数 `target-features` 最后一次出现的精确 `+bf16`／`-bf16` 令牌须为 `+bf16`。调用点须为 `CallingConv::C`、非可变参数、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。不新增操作码。不把 `bfloat` 并入 `isSupportedMaskedExpandLoadIntrinsic`／`isSupportedFixedVectorType`。经既有 `CallDescriptor.LegalizeBFloatMaskedMemory`：解释器先从指针／向量虚拟寄存器取出全部实参，再按车道 0..N-1 发射 `condbr`。取路径对当前打包游标做一次标量 `bfloat` `load`／`store`（`align 1`），再用非 `inbounds` `getelementptr bfloat, ptr, i64 1` 把游标前进一步；关闭路径不解引用、不前进游标。`expandload` 关闭车道经 `phi` 保留 passthru。源调用上可安全保留的内存元数据与 `DebugLoc` 写到每个标量访存。volatile 字节码／虚拟寄存器帧访问仍在 handler 入口、第一次 `condbr` 之前完成；新增 PHI／跨块 SSA 走既有 VMP 后 CFF 安全回退。不得重放 `llvm.masked.expandload/compressstore.vNbf16`，也不得先做无保护整向量访存再 `select`。`vmp-bfloat-expand-compress-semantic.ll` 覆盖混合掩码、全真／全假、动态掩码、passthru、指针前进步序、16／32／64／128 位、运行时 NaN／负零、`!nontemporal`、最后令牌 `+bf16`，以及无特性／最终 `-bf16`／仅 `+bf16fml`／`musttail`／operand bundle／`noreturn`／`returns_twice`／`fastcc`／`poison`／`undef`／非 AS0／超宽／可伸缩负例。FileCheck 证明解释器发射逐通道 `br i1`、标量 `load`／`store bfloat, align 1` 与非 `inbounds` `getelementptr bfloat` 前进一步，且无原生 `bfloat` expand／compress 或整向量访存。原先 load／store／gather lit 中 C 约定 expand／compress 哨兵已改为 `fastcc`。宿主 `lli` 不能假定会选择 `bfloat`，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。
- 拒绝：缺少或最后令牌为 `-bf16` 的形态良好 listed 调用（跳过原因 `unsupported target feature`，保留 `hikari.vmp.selected`）；`musttail`／操作数捆绑／`noreturn`／`returns_twice`／非 C／可变参数／复杂 ABI；`poison`／`undef`；可伸缩；超过 128 位；非 AS0；掩码／passthru 形状不匹配（助手仍检查；验证器通常不能表示，故 lit 不含非法 IR）。资格失败保持原函数与 `hikari.vmp.selected`（`musttail` 除外）。

**当前实际标量 half 数学内建支持边界**

- 接受：标量 IEEE `half` 的既有纯内建族与 listed 超越内建，经既有 `CallDescriptor` 原样重发射并保存／恢复 FastMathFlags：`llvm.sqrt`、`llvm.fma`/`llvm.fmuladd`、`llvm.fabs`、`llvm.minnum`/`llvm.maxnum`、`llvm.minimum`/`llvm.maximum`、`llvm.ceil`/`llvm.floor`/`llvm.trunc`/`llvm.round`/`llvm.rint`/`llvm.nearbyint`/`llvm.roundeven`、`llvm.copysign`、`llvm.sin`/`llvm.cos`、`llvm.exp`/`llvm.exp2`、`llvm.log`/`llvm.log2`/`llvm.log10`、`llvm.pow`、`llvm.powi`（精确 `i32` 指数）。`llvm.canonicalize` 仍按既有路径降级为带相同 FastMathFlags 的 `fmul x, 1.0`。调用必须是严格 `CallInst` 形状：无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。标量 `half` 上述内建还要求所在函数 `target-features` 以最后一次出现的精确 `+fullfp16`／`-fullfp16` 令牌启用完整半精度（逗号分隔列表；`+fullfp16fml` 等不算；不读取命令行 `-mattr`）。`f32`/`f64` 既有路径、普通标量 `half` 算术与已支持的 `half` 向量基础 SSA 不受该门控。`vmp-half-scalar-semantic.ll` 对纯数学的 NaN、正负零、无穷大、负数、舍入边界与 FastMathFlags 做参考／保护位模式差分；超越内建只对有限正常、域内输入做宿主 `lli` 位差分，不对 NaN／无穷／域错误做跨实现比较。正向数学函数自带 `+fullfp16`，AArch64 `llc` 不再传全局 `-mattr`。宿主 `lli` 无法为 `llvm.minimum`/`llvm.maximum.f16` 做 x86 指令选择，故这两项只走 FileCheck 与函数属性上的 AArch64 `llc`。
- 拒绝：缺少或最后令牌为 `-fullfp16` 的上述标量 `half` 纯数学／超越内建（跳过原因 `unsupported target feature`，保留 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`）；constrained 形态、非 `i32` 指数的标量 `powi`、未列入列表的内建、`half` 原子、`bfloat`/`fp128`、间接或可变参数调用。listed 固定 `half` 向量数学见下一节。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际 half 向量数学内建支持边界**

- 接受：总原始宽度 1..128 的固定 IEEE `half` 向量上的 listed 内建，经既有 `CallDescriptor` 原样重发射（向量虚拟寄存器、`F16` 编码、FastMathFlags 掩码）：`llvm.sqrt`、`llvm.fma`/`llvm.fmuladd`、`llvm.fabs`、`llvm.minnum`/`llvm.maxnum`、`llvm.minimum`/`llvm.maximum`、`llvm.ceil`/`llvm.floor`/`llvm.trunc`/`llvm.round`/`llvm.rint`/`llvm.nearbyint`/`llvm.roundeven`、`llvm.copysign`、`llvm.sin`/`llvm.cos`、`llvm.exp`/`llvm.exp2`、`llvm.log`/`llvm.log2`/`llvm.log10`、`llvm.pow`、`llvm.powi`（LangRef：向量浮点底数、第二参数保持标量整数；仅接受精确 `i32` 指数）。调用必须是直接 `CallInst`、C、非可变参数、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI，且参数／返回类型匹配。所在函数 `target-features` 最后一次出现的精确 `+fullfp16`／`-fullfp16` 令牌须为 `+fullfp16`（不读取命令行 `-mattr`）。`f32`/`f64` 向量数学、标量 `half` 既有路径与基础 `half` 向量 SSA 不受该扩展收紧。`vmp-half-vector-math-semantic.ll` 覆盖有限域内参考／保护位差分、分支／`phi`、FMF 结构；`minimum`/`maximum` 仅 FileCheck 与函数属性上的 AArch64 `llc`（宿主 x86 无法选择）。
- 拒绝：缺少或最后令牌为 `-fullfp16` 的形态良好 listed 调用（`unsupported target feature`，保留 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`）；constrained、`bfloat`/`fp128`、可伸缩、指针向量、超过 128 位、其它未列入内建、非 `i32` 指数的向量 `powi`、不匹配签名。固定 `half` 向量 `is.fpclass` 与 `canonicalize`（`VectorFMul`）见独立表面，不并入本 listed 数学表。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际固定 half 向量 is.fpclass 支持边界**

- 接受：精确 `llvm.is.fpclass`，源为固定 IEEE `half` 向量（总原始宽度 1..128，含 `<3 x half>`／`<4 x half>`／`<8 x half>`），结果为同车道 `<N x i1>`。第二参数必须是 `i32` `ConstantInt` 且保持 `ImmArg`，经既有 `CallDescriptor` ImmediateArguments 重发射，不进虚拟寄存器。调用点必须是 `CallingConv::C`、非可变参数、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。所在函数 `target-features` 最后一次出现的精确 `+fullfp16`／`-fullfp16` 令牌须为 `+fullfp16`（不读取命令行 `-mattr`）。不带 FastMathFlags。经既有 `CallDescriptor` 与向量虚拟寄存器重放，不新增操作码。不改变标量 `is.fpclass`、`f32`／`f64` 向量 `is.fpclass` 或 listed half 向量数学。`vmp-half-vector-fpclass-semantic.ll` 覆盖 48／64／128 位形态与普通／零／无穷／NaN 掩码的 FileCheck 及 AArch64 `llc`／`readobj`；宿主 `lli` 不能假定会选择 half 向量 `is.fpclass`，故不做宿主语义差分。
- 拒绝：缺少或最后令牌为 `-fullfp16` 的形态良好调用（`unsupported target feature`，保留 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`）；`bfloat`／`fp128`、可伸缩、超过 128 位、结果不是同车道 `<N x i1>`、动态或非 `i32` mask、constrained、非 C（如 `fastcc`）、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量 half is.fpclass 支持边界**

- 接受：精确 `llvm.is.fpclass`，结果 `i1`，第一参数为标量 IEEE `half`，第二参数为 `i32` `ConstantInt` 且保持 `ImmArg`，经既有 `CallDescriptor` ImmediateArguments 重发射，不进虚拟寄存器。调用点必须是 `CallingConv::C`、非可变参数、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。所在函数 `target-features` 最后一次出现的精确 `+fullfp16`／`-fullfp16` 令牌须为 `+fullfp16`（不读取命令行 `-mattr`）。不带 FastMathFlags。不新增操作码。不改变标量 `f32`／`f64` `is.fpclass` 或固定 `half` 向量 `is.fpclass`。`vmp-half-scalar-fpclass-semantic.ll` 覆盖普通／零／无穷／NaN 掩码、立即数重发射、普通 `tail`、无特性与最终 `-fullfp16`，以及 `bfloat`／`fp128`／`fastcc`／`musttail`／operand bundle／constrained 负例；宿主 `lli` 不能假定会选择 `llvm.is.fpclass.f16`，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。
- 拒绝：缺少或最后令牌为 `-fullfp16` 的形态良好调用（`unsupported target feature`，保留 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`）；`bfloat`／`fp128`、动态或非 `i32` mask、constrained、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际固定 half 向量 canonicalize 支持边界**

- 接受：精确 `llvm.canonicalize`，结果与唯一实参为同一固定 IEEE `half` 向量（总原始宽度 1..128，含 `<2 x half>`／`<3 x half>`／`<4 x half>`／`<8 x half>`）。规划时不得经 `CallDescriptor` 重发射 `llvm.canonicalize`；完全复用既有 `VectorFMul` 降为每车道 `fmul x, 1.0`，同型向量 `1.0` 常量进向量虚拟寄存器，`packVectorVariant` 携带精确 FastMathFlags。调用点必须是 `CallingConv::C`、非可变参数、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。所在函数 `target-features` 最后一次出现的精确 `+fullfp16`／`-fullfp16` 令牌须为 `+fullfp16`（不读取命令行 `-mattr`）。不新增操作码。不改变标量 `half` `canonicalize`（仍走既有标量 `FMul` 降级）或 `f32`／`f64` 向量 `canonicalize`。`vmp-half-vector-canonicalize-semantic.ll` 覆盖 32／48／64／128 位、`fast` 标志降级结构、普通 `tail`、无特性与最终 `-fullfp16`，以及 `bfloat`／可伸缩／超过 128 位／`fastcc`／`musttail`／operand bundle／constrained 负例；参考函数使用同一 `fmul x, 1.0` 展开。宿主 `lli` 不能假定会选择半精度向量乘法，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。
- 拒绝：缺少或最后令牌为 `-fullfp16` 的形态良好调用（`unsupported target feature`，保留 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`）；`bfloat`／`fp128`、可伸缩、超过 128 位、constrained、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量与固定 half 向量饱和转换支持边界**

- 接受：精确 `llvm.fptosi.sat`／`llvm.fptoui.sat` 的两类统一 last-token `+fullfp16` 表面，经既有 `CallDescriptor` 与整数／浮点／向量虚拟寄存器原样重放，不新增操作码。标量：单一 IEEE `half` 实参，结果仅 `i8`／`i16`／`i32`／`i64`。向量：源为固定 IEEE `half` 向量，目的为同车道固定 `i8`／`i16`／`i32`／`i64` 向量；两端各自总原始宽度独立为 1..128，车道数完全相同（含 `<4 x half>`→`<4 x i8>`／`<8 x half>`→`<8 x i16>`／`<4 x half>`→`<4 x i32>`／`<2 x half>`→`<2 x i64>`／`<3 x half>`→`<3 x i8>`／`<3 x half>`→`<3 x i32>` 等；`<3 x half>`→`<3 x i64>` 与 `<4 x half>`→`<4 x i64>` 因目的超过 128 位而拒绝）。调用点必须是 `CallingConv::C`、非可变参数、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放（`CallDescriptor` 重放为普通 call）。所在函数 `target-features` 最后一次出现的精确 `+fullfp16`／`-fullfp16` 令牌须为 `+fullfp16`（不读取命令行 `-mattr`）。不改变既有 `f32`／`f64` 标量／向量饱和转换、普通非饱和 `fptosi`／`fptoui`、half 基础 SSA 或其它 half 数学门控。`vmp-half-sat-convert-semantic.ll` 覆盖 half→`i8`／`i16`／`i32`／`i64` 的标量与向量有符号／无符号代表、非二幂车道、负值 unsigned 归零、上下溢饱和、普通 `tail`、钻石 `phi` 与回边循环，以及无特性／最终 `-fullfp16`／`i1`／`bfloat`／`fp128`／可伸缩／超过 128 位／`fastcc`／`musttail`／operand bundle／constrained／`poison` 负例；宿主 `lli` 不能假定会选择半精度饱和转换，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。
- 拒绝：缺少或最后令牌为 `-fullfp16` 的形态良好调用（`unsupported target feature`，保留 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`）；`i1` 或其它目的宽度、`bfloat`／`fp128`、可伸缩、任一侧超过 128 位、车道数不匹配、constrained、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量 half 取整到 i64 支持边界**

- 接受：精确 `llvm.lround.i64.f16`／`llvm.llround.i64.f16`／`llvm.lrint.i64.f16`／`llvm.llrint.i64.f16`。结果必须是标量 `i64`，唯一实参必须是标量 IEEE `half`。经既有 `CallDescriptor` 与整数／浮点虚拟寄存器原样重放，不新增操作码。调用点必须是 `CallingConv::C`、非可变参数、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放（`CallDescriptor` 重放为普通 call）。所在函数 `target-features` 最后一次出现的精确 `+fullfp16`／`-fullfp16` 令牌须为 `+fullfp16`（不读取命令行 `-mattr`）。不改变既有 `f32`／`f64` `lround`／`llround`／`lrint`／`llrint`、half 普通数学、half 饱和转换或 half 向量路径。`vmp-half-lround-semantic.ll` 覆盖四 ID、普通 `tail`、钻石 `phi` 与回边循环，以及无特性／最终 `-fullfp16`／`i32` 结果／`bfloat`／`fp128`／向量／`fastcc`／`musttail`／operand bundle／constrained／`poison` 负例；宿主 `lli` 不能假定会选择半精度取整到整数，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。
- 拒绝：缺少或最后令牌为 `-fullfp16` 的形态良好调用（`unsupported target feature`，保留 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`）；`i32` 或其它结果宽度、`bfloat`／`fp128`、向量、constrained、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量 f32/f64 严格 constrained 浮点支持边界**

- 接受：精确 `llvm.experimental.constrained.fadd`／`fsub`／`fmul`／`fdiv`／`frem`／`fma`／`sqrt`／`fcmp`／`fcmps`，标量 IEEE `f32`／`f64` 仅。`fadd`／`fsub`／`fmul`／`fdiv`／`frem`：两个同型浮点实参加 rounding／exception 两个 `MDString` 元数据实参。`fma`：三个同型浮点实参加两个元数据实参。`sqrt`：一个同型浮点实参加两个元数据实参。`fcmp`／`fcmps`：两个同型浮点实参、`i1` 结果、predicate／exception 两个元数据实参。合法 LLVM 15 字符串仅为 `round.dynamic`／`round.tonearest`／`round.downward`／`round.upward`／`round.towardzero`，`fpexcept.ignore`／`fpexcept.maytrap`／`fpexcept.strict`，以及 LangRef 有序／无序比较谓词 `oeq`／`ogt`／`oge`／`olt`／`ole`／`one`／`ord`／`ueq`／`ugt`／`uge`／`ult`／`ule`／`une`／`uno`（不含 `true`／`false`）。元数据以 `TrackingMDNodeRef` 保存（`MDString` 包一层单操作数 `MDNode` 以便跟踪），源调用删除后按原 `MetadataAsValue` `MDString` 重发射，不伪造、不默认、不折叠。`read_register`／`read_volatile_register` 仍只允许 `!{!"pc"}`。调用点必须是 `CallingConv::C`、非可变参数、无 FastMathFlags、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。不新增操作码。不改变普通非 constrained 标量数学、half 门控或向量路径。`vmp-constrained-fp-semantic.ll` 覆盖九 ID 的 `f32`／`f64`、各类合法元数据、普通 `tail`、钻石 `phi` 与回边循环，以及 `half`／`bfloat`／`fp128`／向量／其它 constrained ID／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不是严格浮点环境，不能作为语义判据，故只走 FileCheck 与 AArch64 `llc`／`readobj`。既有用 C 约定 `constrained.fadd.f32`／`.f64` 作跳过哨兵的测试已改为同类型 `fastcc` 调用。
- 拒绝：`half`／`bfloat`／`fp128`、向量、其它 constrained ID（含转换族以外的 ID；转换族见下一节）、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量 f32/f64 严格 constrained 转换支持边界**

- 接受：精确 `llvm.experimental.constrained.fptosi`／`fptoui`／`sitofp`／`uitofp`／`fptrunc`／`fpext`。`fptosi`／`fptoui`：标量 `f32`／`f64` 源、标量 `i1`／`i8`／`i16`／`i32`／`i64` 结果、一个浮点实参加一个合法 `fpexcept` `MDString`。`sitofp`／`uitofp`：标量 `i1`／`i8`／`i16`／`i32`／`i64` 源、标量 `f32`／`f64` 结果、一个整数实参加合法 `round` 与 `fpexcept` `MDString`。`fptrunc` 仅 `f64`→`f32`，带合法 `round` 与 `fpexcept`。`fpext` 仅 `f32`→`f64`，带合法 `fpexcept`。合法 token 与既有严格算术相同：`round.dynamic`／`round.tonearest`／`round.downward`／`round.upward`／`round.towardzero` 与 `fpexcept.ignore`／`fpexcept.maytrap`／`fpexcept.strict`。元数据按精确 ID+参数位置进入既有 `CallDescriptor` MetadataArguments（`MDString` 包一层跟踪 `MDNode`），源调用删除后重发射完全相同 `MetadataAsValue` `MDString`，不伪造、不默认、不折叠。`read_register` 的 `!{!"pc"}` 限制不放宽。调用点必须是 `CallingConv::C`、非可变参数、无 FastMathFlags、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。不新增操作码。不改变刚完成的 constrained 算术／比较或普通非 constrained 转换。`vmp-constrained-fp-convert-semantic.ll` 覆盖六族、`f32`／`f64` 与 `i1`／`i8`／`i16`／`i32`／`i64` 代表、全部 round／except 类别、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及 `half`／`bfloat`／`fp128`／`i128`／向量／可伸缩／其它 constrained ID／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不是严格浮点环境，故只走 FileCheck 与 AArch64 `llc`／`readobj`。原先用 C 约定 `constrained.sitofp.f32.i32`／`constrained.fptosi.i8.f32` 作跳过哨兵的测试已改为同类型 `fastcc`。
- 拒绝：无特性 `half`（现为 `unsupported target feature`，见 half 转换节）、`bfloat`／`fp128`、`i128`、向量、可伸缩、其它 constrained ID、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量 f32/f64 严格 constrained 舍入支持边界**

- 接受：精确 `llvm.experimental.constrained.ceil`／`floor`／`trunc`／`round`／`roundeven`／`rint`／`nearbyint`，结果与唯一实参为同一标量 IEEE `f32`／`f64`。`ceil`／`floor`／`trunc`／`round`／`roundeven` 仅带一个合法 `fpexcept` `MDString`（LLVM 15 无 rounding 操作数）。`rint`／`nearbyint` 带合法 `round` 与 `fpexcept`。合法 token 与既有严格算术相同。元数据按精确 ID+参数位置进入既有 `CallDescriptor` MetadataArguments，源调用删除后重发射完全相同 `MetadataAsValue` `MDString`。`read_register` 的 `!{!"pc"}` 限制不放宽。调用点必须是 `CallingConv::C`、非可变参数、无 FastMathFlags、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。不新增操作码。不改变 constrained 算术／比较／转换或普通非 constrained 舍入。`vmp-constrained-fp-round-semantic.ll` 覆盖七 ID 的 `f32`／`f64`、全部 round／except 类别、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及 `half`／`bfloat`／`fp128`／向量／其它 constrained ID／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不是严格浮点环境，故只走 FileCheck 与 AArch64 `llc`／`readobj`。原先用 C 约定 `constrained.ceil.f32` 作跳过哨兵的测试已改为同类型 `fastcc`。
- 拒绝：`half`／`bfloat`／`fp128`、向量、其它 constrained ID、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量 f32/f64 严格 constrained 整数结果舍入支持边界**

- 接受：精确 `llvm.experimental.constrained.lrint`／`llrint`／`lround`／`llround`。目的宽度遵循本仓库 AArch64 ISel 约定：`lrint`／`lround` 为标量 `i32` 或 `i64`（对应 `AArch64InstrInfo.td` 的 W／X 模式）；`llrint`／`llround` 仅为标量 `i64`。唯一浮点实参必须是标量 IEEE `f32`／`f64`。`lrint`／`llrint` 带合法 `round` 与 `fpexcept` `MDString`；`lround`／`llround` 仅带一个合法 `fpexcept`（LLVM 15 无 rounding 操作数）。合法 token 与既有严格算术相同。元数据按精确 ID+参数位置进入既有 `CallDescriptor` MetadataArguments，源调用删除后重发射完全相同 `MetadataAsValue` `MDString`。`read_register` 的 `!{!"pc"}` 限制不放宽。调用点必须是 `CallingConv::C`、非可变参数、无 FastMathFlags、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。不新增操作码。不改变 constrained 算术／比较／转换／同类型舍入、普通非 constrained `lround`／`llround`／`lrint`／`llrint`（仍仅 `i64`）。`half` 源见独立 last-token `+fullfp16` 表面；无特性 half 负例现为 `unsupported target feature`。`vmp-constrained-fp-int-round-semantic.ll` 覆盖四 ID 的 `f32`／`f64`、`lrint`／`lround` 的 `i32`／`i64` 目的、全部 round／except 类别、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及 `llrint`／`llround` 的 `i32` 目的／`i16`／`i128` 结果／`half`／`bfloat`／`fp128`／向量（LLVM 15 验证器拒绝这四 ID 的向量重载，故用形态良好的 `constrained.ceil.v2f32`）／其它 constrained ID／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不是严格浮点环境，故只走 FileCheck 与 AArch64 `llc`／`readobj`。
- 拒绝：`llrint`／`llround` 的 `i32` 目的、`i16`／`i128` 或其它结果宽度、`half`／`bfloat`／`fp128`、向量、其它 constrained ID、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量 f32/f64 严格 constrained 最值支持边界**

- 接受：精确 `llvm.experimental.constrained.minnum`／`maxnum`／`minimum`／`maximum`，结果与两个实参为同一标量 IEEE `f32`／`f64`，外加一个合法 `fpexcept` `MDString`（LLVM 15 无 rounding 操作数）。合法 token 与既有严格算术相同。元数据按精确 ID+参数位置进入既有 `CallDescriptor` MetadataArguments，源调用删除后重发射完全相同 `MetadataAsValue` `MDString`。`read_register` 的 `!{!"pc"}` 限制不放宽。调用点必须是 `CallingConv::C`、非可变参数、无 FastMathFlags、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。不新增操作码。不改变 constrained 算术／比较／转换／舍入或普通非 constrained `minnum`／`maxnum`／`minimum`／`maximum`。`vmp-constrained-fp-minmax-semantic.ll` 覆盖四 ID 的 `f32`／`f64`、全部 except 类别、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及无特性 `half`（现为 `unsupported target feature`，见 half 最值节）／`bfloat`／`fp128`／向量／其它 constrained ID／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不是严格浮点环境，故只走 FileCheck 与 AArch64 `llc`／`readobj`。既有向量 `constrained.minnum.v4f32` 跳过哨兵已改为向量 `constrained.sin.v2f16`。
- 拒绝：`half`／`bfloat`／`fp128`、向量、其它 constrained ID、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量 f32/f64 严格 constrained 超越函数支持边界**

- 接受：精确 `llvm.experimental.constrained.sin`／`cos`／`exp`／`exp2`／`log`／`log2`／`log10`，结果与唯一实参为同一标量 IEEE `f32`／`f64`，外加合法 `round` 与 `fpexcept` `MDString`。合法 token 与既有严格算术相同。元数据按精确 ID+参数位置进入既有 `CallDescriptor` MetadataArguments，源调用删除后重发射完全相同 `MetadataAsValue` `MDString`。`read_register` 的 `!{!"pc"}` 限制不放宽。调用点必须是 `CallingConv::C`、非可变参数、无 FastMathFlags、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。不新增操作码。不改变 constrained 算术／比较／转换／舍入／最值或普通非 constrained `sin`／`cos`／`exp`／`log`。`constrained.pow`／`powi` 见下一节。`vmp-constrained-fp-transcendental-semantic.ll` 覆盖七 ID 的 `f32`／`f64`、全部 round／except 类别、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及 `half`／`bfloat`／`fp128`／向量／其它 constrained ID／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不是严格浮点环境，故只走 FileCheck 与 AArch64 `llc`／`readobj`。原先用 C 约定 `constrained.cos.f32` 作“其它 constrained ID”哨兵的测试已改为同约定 `constrained.fmuladd.f32`。
- 拒绝：`half`／`bfloat`／`fp128`、向量、其它 constrained ID、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量 f32/f64 严格 constrained 幂运算支持边界**

- 接受：精确 `llvm.experimental.constrained.pow`／`powi`。`pow`：结果与两个实参为同一标量 IEEE `f32`／`f64`，外加合法 `round` 与 `fpexcept`。`powi`：标量 `f32`／`f64` 底数、普通标量 `i32` 指数（LLVM 15 固定 `llvm_i32_ty`，可为运行时值，不是 ImmArg）、外加合法 `round` 与 `fpexcept`。合法 token 与既有严格算术相同。元数据按精确 ID+参数位置进入既有 `CallDescriptor` MetadataArguments，源调用删除后重发射完全相同 `MetadataAsValue` `MDString`。`read_register` 的 `!{!"pc"}` 限制不放宽。调用点必须是 `CallingConv::C`、非可变参数、无 FastMathFlags、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。不新增操作码。不改变 constrained 算术／比较／转换／舍入／最值／超越函数或普通非 constrained `pow`／`powi`。`constrained.fmuladd` 见下一节。`vmp-constrained-fp-pow-semantic.ll` 覆盖两 ID 的 `f32`／`f64`、运行时与立即 `i32` 指数、全部 round／except 类别、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及 `half`／`bfloat`／`fp128`／向量／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不是严格浮点环境，故只走 FileCheck 与 AArch64 `llc`／`readobj`。原先用 C 约定 `constrained.pow` 作跳过哨兵的测试已改为同类型 `fastcc`。
- 拒绝：`half`／`bfloat`／`fp128`、向量、其它 constrained ID、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量 f32/f64 严格 constrained fmuladd 支持边界**

- 接受：精确 `llvm.experimental.constrained.fmuladd`，结果与三个实参为同一标量 IEEE `f32`／`f64`，外加合法 `round` 与 `fpexcept` `MDString`。形态与既有 `constrained.fma` 相同。合法 token 与既有严格算术相同。元数据按精确 ID+参数位置进入既有 `CallDescriptor` MetadataArguments，源调用删除后重发射完全相同 `MetadataAsValue` `MDString`。`read_register` 的 `!{!"pc"}` 限制不放宽。调用点必须是 `CallingConv::C`、非可变参数、无 FastMathFlags、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。不新增操作码。不改变 constrained `fma` 或普通非 constrained `fmuladd`／`fma`。LLVM 15 `ConstrainedOps.def` 所列标量数学 ID 至此全部进入既有严格表面。`vmp-constrained-fp-fmuladd-semantic.ll` 覆盖 `f32`／`f64`、全部 round／except 类别、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及 `half`／`bfloat`／`fp128`／向量／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不是严格浮点环境，故只走 FileCheck 与 AArch64 `llc`／`readobj`。原先用 C 约定 `constrained.fmuladd.f32` 作“其它 constrained ID”哨兵的测试已删除该哨兵。
- 拒绝：`half`／`bfloat`／`fp128`、向量、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量 half 严格 constrained 浮点支持边界**

- 接受：精确 `llvm.experimental.constrained.fadd`／`fsub`／`fmul`／`fdiv`／`frem`／`fma`／`fmuladd`／`sqrt`／`fcmp`／`fcmps`，标量 IEEE `half` 仅。操作数与元数据槽与既有 `f32`／`f64` 严格算术相同。所在函数 `target-features` 最后一次出现的精确 `+fullfp16`／`-fullfp16` 令牌须为 `+fullfp16`（不读取命令行 `-mattr`）。缺少或最后令牌为 `-fullfp16` 的形态良好调用报告 `unsupported target feature`，保留 `hikari.vmp.selected`。元数据按精确 ID+参数位置进入既有 `CallDescriptor` MetadataArguments。调用点必须是 `CallingConv::C`、非可变参数、无 FastMathFlags、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。不新增操作码。不改变 `f32`／`f64` 严格算术、普通 half SSA、或其它 constrained ID。`constrained.fmuladd.f16` 见下一节。`vmp-constrained-half-fp-semantic.ll` 覆盖九 ID、全部 round／except 代表、`fcmp`／`fcmps`、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及无特性／最终 `-fullfp16`／`sin`／其它 constrained ID／向量／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。`constrained.fmuladd.f16` 走同一形态／资格／feature-gate 检查器，专用覆盖见下一节。宿主 `lli` 不能假定会选择半精度严格内建，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。原先用 C 约定 `constrained.fmul.f16` 作其它表面跳过哨兵的测试已改为 `constrained.ceil.f16`。无 `+fullfp16` 的 `constrained.fadd.f16` 哨兵改为 `unsupported target feature`。
- 拒绝：缺少或最后令牌为 `-fullfp16` 的形态良好调用（`unsupported target feature`）；其它 constrained ID、向量、`bfloat`／`fp128`、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量 half 严格 constrained fmuladd 支持边界**

- 接受：精确 `llvm.experimental.constrained.fmuladd`，结果与三个实参为同一标量 IEEE `half`，外加合法 `round` 与 `fpexcept`。不再使用独立形态／资格检查器：与 half `constrained.fma` 共用 `isConstrainedScalarHalfArithShapeSupported`／`isSupportedConstrainedScalarHalfArithIntrinsic`，以及同一 last-token `+fullfp16` feature-gate。缺少或最后令牌为 `-fullfp16` 的形态良好调用报告 `unsupported target feature`，保留 `hikari.vmp.selected`。`vmp-constrained-fp-fmuladd-semantic.ll` 中无特性 half 负例同步为该字符串。不新增操作码。不改变 `f32`／`f64` 严格 `fmuladd` 正例。`vmp-constrained-half-fmuladd-semantic.ll` 覆盖全部 round／except 类别、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及无特性／最终 `-fullfp16`／其它 constrained ID／向量／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不能假定会选择半精度严格内建，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。
- 拒绝：缺少或最后令牌为 `-fullfp16` 的形态良好调用（`unsupported target feature`）；向量、其它 constrained ID、`bfloat`／`fp128`、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量 half 严格 constrained 同类型舍入支持边界**

- 接受：精确 `llvm.experimental.constrained.ceil`／`floor`／`trunc`／`round`／`roundeven`／`rint`／`nearbyint`，结果与唯一实参为同一标量 IEEE `half`。`ceil`／`floor`／`trunc`／`round`／`roundeven` 仅带一个合法 `fpexcept`；`rint`／`nearbyint` 带合法 `round` 与 `fpexcept`。复用既有 `isSupportedConstrainedScalarFPRoundShape`，不另建重复检查器。所在函数 `target-features` 最后令牌须为 `+fullfp16`。缺少或最后令牌为 `-fullfp16` 的形态良好调用报告 `unsupported target feature`，与 half 严格算术／`fmuladd` 同一 feature-gate。`vmp-constrained-fp-round-semantic.ll` 中无特性 half 负例同步为该字符串。不新增操作码。不改变 `f32`／`f64` 严格舍入正例，也不开放 half 转换。`vmp-constrained-half-fp-round-semantic.ll` 覆盖七 ID、全部 round／except 类别、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及无特性／最终 `-fullfp16`／其它 constrained ID／向量／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不能假定会选择半精度严格内建，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。原先用 C 约定 `constrained.ceil.f16` 作其它表面跳过哨兵的测试已改为 `constrained.sin.f16` 或 `constrained.minnum.f16`。
- 拒绝：缺少或最后令牌为 `-fullfp16` 的形态良好调用（`unsupported target feature`）；向量、其它 constrained ID、`bfloat`／`fp128`、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量 half 严格 constrained 整数结果舍入支持边界**

- 接受：精确 `llvm.experimental.constrained.lrint`／`llrint`／`lround`／`llround`，唯一实参为标量 IEEE `half`。目的宽度遵循本仓库 AArch64 ISel：`lrint`／`lround` 为 `i32` 或 `i64`；`llrint`／`llround` 仅为 `i64`。复用既有 `isSupportedConstrainedScalarFPIntRoundShape`，不另建重复检查器，也不放宽普通非 constrained half `lround` 的仅 `i64` 白名单。所在函数 `target-features` 最后令牌须为 `+fullfp16`。缺少或最后令牌为 `-fullfp16` 的形态良好调用报告 `unsupported target feature`。`vmp-constrained-fp-int-round-semantic.ll` 中无特性 half 负例同步为该字符串。不新增操作码。half 转换见下一节。`vmp-constrained-half-fp-int-round-semantic.ll` 覆盖四 ID、`lrint`／`lround` 的 `i32`／`i64` 目的、全部 round／except 类别、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及 `ll*` 的 `i32` 目的／`i16`／`i128`／无特性／最终 `-fullfp16`／其它 constrained ID／向量（`constrained.sin.v2f16`）／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不能假定会选择半精度严格内建，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。原先 `vmp-half-lround-semantic.ll` 的 C 约定 `constrained.lround.i64.f16` 哨兵已改为 `constrained.sin.f16`。
- 拒绝：缺少或最后令牌为 `-fullfp16` 的形态良好调用（`unsupported target feature`）；`llrint`／`llround` 的 `i32` 目的、`i16`／`i128` 或其它结果宽度、向量、其它 constrained ID、`bfloat`／`fp128`、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量 half 严格 constrained 转换支持边界**

- 接受：精确 `llvm.experimental.constrained.fptosi`／`fptoui`／`sitofp`／`uitofp`／`fptrunc`／`fpext`。`fptosi`／`fptoui`：标量 IEEE `half` 源、标量 `i1`／`i8`／`i16`／`i32`／`i64` 结果、合法 `fpexcept`。`sitofp`／`uitofp`：标量 `i1`／`i8`／`i16`／`i32`／`i64` 源、标量 `half` 结果、合法 `round` 与 `fpexcept`。`fptrunc`：`f32`／`f64`→`half`，带合法 `round` 与 `fpexcept`。`fpext`：`half`→`f32`／`f64`，带合法 `fpexcept`。复用既有 `isSupportedConstrainedScalarFPConvertShape`（`f32`↔`f64` 宽度转换仍走原路径且不要求 `+fullfp16`）。所在函数 `target-features` 最后令牌须为 `+fullfp16`。缺少或最后令牌为 `-fullfp16` 的形态良好调用报告 `unsupported target feature`。`vmp-constrained-fp-convert-semantic.ll` 中无特性 half 负例同步为该字符串。不新增操作码。不改变 half 算术／`fmuladd`／同类型舍入／整数结果舍入。`vmp-constrained-half-fp-convert-semantic.ll` 覆盖六族、整数宽度代表、`half`↔`f32`／`f64` 宽度转换、全部 round／except 类别、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及无特性／最终 `-fullfp16`／`i128`／其它 constrained ID／向量／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不能假定会选择半精度严格内建，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。
- 拒绝：缺少或最后令牌为 `-fullfp16` 的形态良好调用（`unsupported target feature`）；`i128`、向量、`bfloat`／`fp128`、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量 half 严格 constrained 最值支持边界**

- 接受：精确 `llvm.experimental.constrained.minnum`／`maxnum`／`minimum`／`maximum`，结果与两个实参为同一标量 IEEE `half`，外加一个合法 `fpexcept`（LLVM 15 无 rounding 操作数）。复用既有 `isSupportedConstrainedScalarFPMinMaxShape`，不另建重复检查器。所在函数 `target-features` 最后令牌须为 `+fullfp16`。缺少或最后令牌为 `-fullfp16` 的形态良好调用报告 `unsupported target feature`。`vmp-constrained-fp-minmax-semantic.ll` 中无特性 half 负例同步为该字符串。不新增操作码。不改变 half 算术／`fmuladd`／舍入／转换。`vmp-constrained-half-fp-minmax-semantic.ll` 覆盖四 ID、全部 except 类别、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及无特性／最终 `-fullfp16`／其它 constrained ID／向量／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不能假定会选择半精度严格内建，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。原先 `vmp-constrained-half-fp-semantic.ll` 的 `constrained.minnum.f16` 哨兵已改为 `constrained.pow.f16`。
- 拒绝：缺少或最后令牌为 `-fullfp16` 的形态良好调用（`unsupported target feature`）；向量、其它 constrained ID、`bfloat`／`fp128`、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量 half 严格 constrained 超越函数支持边界**

- 接受：精确 `llvm.experimental.constrained.sin`／`cos`／`exp`／`exp2`／`log`／`log2`／`log10`，结果与唯一实参为同一标量 IEEE `half`，外加合法 `round` 与 `fpexcept`。复用既有 `isSupportedConstrainedScalarFPTranscendentalShape`，不另建重复检查器。所在函数 `target-features` 最后令牌须为 `+fullfp16`。缺少或最后令牌为 `-fullfp16` 的形态良好调用报告 `unsupported target feature`。`vmp-constrained-fp-transcendental-semantic.ll` 中无特性 half 负例同步为该字符串。不新增操作码。不改变 half 算术／`fmuladd`／舍入／转换／最值。`vmp-constrained-half-fp-transcendental-semantic.ll` 覆盖七 ID、全部 round／except 类别、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及无特性／最终 `-fullfp16`／其它 constrained ID／向量／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不能假定会选择半精度严格内建，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。原先用 C 约定 `constrained.sin.f16` 作其它表面跳过哨兵的测试已改为 `constrained.pow.f16`，随后在 pow 表面落地后删除该标量其它 ID 哨兵。
- 拒绝：缺少或最后令牌为 `-fullfp16` 的形态良好调用（`unsupported target feature`）；向量、其它 constrained ID、`bfloat`／`fp128`、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际标量 half 严格 constrained 幂运算支持边界**

- 接受：精确 `llvm.experimental.constrained.pow`／`powi`。`pow`：结果与两个实参为同一标量 IEEE `half`，外加合法 `round` 与 `fpexcept`。`powi`：标量 `half` 底数、普通标量 `i32` 指数（LLVM 15 固定 `llvm_i32_ty`，可为运行时值，不是 ImmArg）、外加合法 `round` 与 `fpexcept`。复用既有 `isSupportedConstrainedScalarFPPowShape`，不另建重复检查器。所在函数 `target-features` 最后令牌须为 `+fullfp16`。缺少或最后令牌为 `-fullfp16` 的形态良好调用报告 `unsupported target feature`。`vmp-constrained-fp-pow-semantic.ll` 中无特性 half 负例同步为该字符串。不新增操作码。这是 LLVM 15 `ConstrainedOps.def` 所列标量 half 数学 ID 的最后一项。常量 `i32` 指数经整数虚拟寄存器重放，FileCheck 不得要求字面 `i32 3`。`vmp-constrained-half-fp-pow-semantic.ll` 覆盖两 ID、运行时与立即 `i32` 指数、全部 round／except 代表、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及无特性／最终 `-fullfp16`／向量／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不能假定会选择半精度严格内建，故只走 FileCheck 与函数属性上的 AArch64 `llc`／`readobj`。原先用 C 约定 `constrained.pow.f16` 作其它表面跳过哨兵的测试已删除该标量哨兵，或改为向量 `constrained.sin.v2f16`。
- 拒绝：缺少或最后令牌为 `-fullfp16` 的形态良好调用（`unsupported target feature`）；向量、非 `i32` 指数、`bfloat`／`fp128`、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际固定 f32/f64 向量严格 constrained 算术支持边界**

- 接受：精确 `llvm.experimental.constrained.fadd`／`fsub`／`fmul`／`fdiv`／`frem`／`fma`／`fmuladd`／`sqrt`／`fcmp`／`fcmps`，固定 IEEE `f32`／`f64` 向量、总原始宽度 1..128（含 `<2 x float>`／`<3 x float>`／`<2 x double>`）。操作数与元数据槽与标量严格算术相同。`fcmp`／`fcmps` 结果为同车道 `<N x i1>`。合法 token 与既有严格算术相同。元数据进入既有 `CallDescriptor` MetadataArguments。调用点必须是 `CallingConv::C`、非可变参数、无 FastMathFlags、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。不新增操作码。不要求 `+fullfp16`。不改变标量严格算术或 half 向量路径。`vmp-constrained-fp-vector-semantic.ll` 覆盖十 ID、64／96／128 位代表、全部 round／except／谓词代表、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及 `half` 向量／超过 128 位／可伸缩／其它 constrained ID／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不是严格浮点环境，故只走 FileCheck 与 AArch64 `llc`／`readobj`。原先用 C 约定 `constrained.fadd.v2f32`／`fmul.v4f32`／`fma.v4f32`／`sqrt.v4f32`／`fmuladd.v2f32` 作跳过哨兵的测试已改为向量 `constrained.sin.v2f16`。无 `+fullfp16` 的形态良好 `constrained.fadd.v2f16` 负例现为 `unsupported target feature`。
- 拒绝：`half` 向量（现见独立 last-token `+fullfp16` 表面）、可伸缩、超过 128 位、其它 constrained ID、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际固定 half 向量严格 constrained 算术支持边界**

- 接受：精确 `llvm.experimental.constrained.fadd`／`fsub`／`fmul`／`fdiv`／`frem`／`fma`／`fmuladd`／`sqrt`／`fcmp`／`fcmps`，固定 IEEE `half` 向量、总原始宽度 1..128（含 `<2 x half>`／`<3 x half>`／`<8 x half>`）。操作数与元数据槽与标量 half 严格算术相同。`fcmp`／`fcmps` 结果为同车道 `<N x i1>`。所在函数 `target-features` 最后令牌须为 `+fullfp16`。缺少或最后令牌为 `-fullfp16` 的形态良好调用报告 `unsupported target feature`。元数据进入既有 `CallDescriptor` MetadataArguments。调用点必须是 `CallingConv::C`、非可变参数、无 FastMathFlags、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。不新增操作码。不改变 `f32`／`f64` 向量严格算术、标量 half 严格算术。同类型舍入、转换、最值、超越函数与 `pow`／`powi` 见独立表面。`vmp-constrained-half-fp-vector-semantic.ll` 覆盖十 ID、32／48／128 位代表、全部 round／except／谓词代表、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及无特性／最终 `-fullfp16`／超过 128 位／可伸缩／其它 constrained ID／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不能假定会选择半精度向量严格内建，故只走 FileCheck 与 AArch64 `llc`／`readobj`。原先用 C 约定且带 `+fullfp16` 的 `constrained.fadd.v2f16`／`fmuladd.v2f16`／`fmul.v4f16`／`sqrt.v4f16` 以及本表面其它 ID 哨兵已改为向量 `constrained.sin.v2f16`／`sin.v4f16`。
- 拒绝：缺少或最后令牌为 `-fullfp16` 的形态良好调用（`unsupported target feature`）；可伸缩、超过 128 位、其它 constrained ID、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际固定 half 向量严格 constrained 同类型舍入支持边界**

- 接受：精确 `llvm.experimental.constrained.ceil`／`floor`／`trunc`／`round`／`roundeven`／`rint`／`nearbyint`，结果与唯一实参为同一固定 IEEE `half` 向量、总原始宽度 1..128（含 `<2 x half>`／`<3 x half>`／`<8 x half>`）。`ceil`／`floor`／`trunc`／`round`／`roundeven` 仅带一个合法 `fpexcept`；`rint`／`nearbyint` 带合法 `round` 与 `fpexcept`。操作数与元数据槽与标量 half 严格舍入相同。所在函数 `target-features` 最后令牌须为 `+fullfp16`。缺少或最后令牌为 `-fullfp16` 的形态良好调用报告 `unsupported target feature`。`vmp-constrained-fp-vector-round-semantic.ll` 中无特性 half 负例同步为该字符串。元数据进入既有 `CallDescriptor` MetadataArguments。调用点必须是 `CallingConv::C`、非可变参数、无 FastMathFlags、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。不新增操作码。不改变 `f32`／`f64` 向量严格舍入、标量 half 严格舍入，也不开放 half 向量整数取整。转换、最值、超越函数与 `pow`／`powi` 见独立表面。`vmp-constrained-half-fp-vector-round-semantic.ll` 覆盖七 ID、32／48／128 位代表、全部 round／except 代表、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及无特性／最终 `-fullfp16`／超过 128 位／可伸缩／其它 constrained ID／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不能假定会选择半精度向量严格内建，故只走 FileCheck 与 AArch64 `llc`／`readobj`。原先用 C 约定且带 `+fullfp16` 的 `constrained.ceil.v2f16`／`ceil.v4f16`／`round.v2f16` 作跳过哨兵的测试已改为向量 `constrained.sin.v2f16`／`sin.v4f16`。
- 拒绝：缺少或最后令牌为 `-fullfp16` 的形态良好调用（`unsupported target feature`）；可伸缩、超过 128 位、其它 constrained ID、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际固定 half 向量严格 constrained 最值支持边界**

- 接受：精确 `llvm.experimental.constrained.minnum`／`maxnum`／`minimum`／`maximum`，结果与两个实参为同一固定 IEEE `half` 向量、总原始宽度 1..128（含 `<2 x half>`／`<3 x half>`／`<8 x half>`），外加一个合法 `fpexcept`（LLVM 15 无 rounding 操作数）。操作数与元数据槽与标量 half 严格最值相同。所在函数 `target-features` 最后令牌须为 `+fullfp16`。缺少或最后令牌为 `-fullfp16` 的形态良好调用报告 `unsupported target feature`。`vmp-constrained-fp-vector-minmax-semantic.ll` 中无特性 half 负例同步为该字符串。元数据进入既有 `CallDescriptor` MetadataArguments。调用点必须是 `CallingConv::C`、非可变参数、无 FastMathFlags、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。不新增操作码。不改变 `f32`／`f64` 向量严格最值、标量 half 严格最值。转换、超越函数与 `pow`／`powi` 见独立表面。`vmp-constrained-half-fp-vector-minmax-semantic.ll` 覆盖四 ID、32／48／128 位代表、全部 except 代表、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及无特性／最终 `-fullfp16`／超过 128 位／可伸缩／其它 constrained ID／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不能假定会选择半精度向量严格内建，故只走 FileCheck 与 AArch64 `llc`／`readobj`。原先用 C 约定且带 `+fullfp16` 的 `constrained.minnum.v2f16` 作跳过哨兵的测试已改为向量 `constrained.sin.v2f16`。
- 拒绝：缺少或最后令牌为 `-fullfp16` 的形态良好调用（`unsupported target feature`）；可伸缩、超过 128 位、其它 constrained ID、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际固定 half 向量严格 constrained 转换支持边界**

- 接受：精确 `llvm.experimental.constrained.fptosi`／`fptoui`／`sitofp`／`uitofp`／`fptrunc`／`fpext`。`fptosi`／`fptoui`：固定 IEEE `half` 向量源、同车道固定 `i1`／`i8`／`i16`／`i32`／`i64` 向量结果、合法 `fpexcept`。`sitofp`／`uitofp`：固定 `i1`／`i8`／`i16`／`i32`／`i64` 向量源、同车道固定 `half` 向量结果、合法 `round` 与 `fpexcept`。`fptrunc` 为同车道 `v*f32`／`v*f64`→`v*half`，带合法 `round` 与 `fpexcept`。`fpext` 为同车道 `v*half`→`v*f32`／`v*f64`，带合法 `fpexcept`。两端各自总原始宽度独立为 1..128（含 `<2 x half>`→`<2 x i32>`／`<2 x i1>`／`<2 x i64>`、`<3 x half>`→`<3 x i16>`、`<8 x half>`→`<8 x i16>`、`<2 x i32>`→`<2 x half>`、`<2 x float>`／`<2 x double>`↔`<2 x half>`）。所在函数 `target-features` 最后令牌须为 `+fullfp16`。缺少或最后令牌为 `-fullfp16` 的形态良好调用报告 `unsupported target feature`。`vmp-constrained-fp-vector-convert-semantic.ll` 中无特性 half 负例同步为该字符串。元数据进入既有 `CallDescriptor` MetadataArguments。调用点必须是 `CallingConv::C`、非可变参数、无 FastMathFlags、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。不新增操作码。不改变 `f32`／`f64` 向量严格转换、标量 half 严格转换。超越函数与 `pow`／`powi` 见独立表面。`vmp-constrained-half-fp-vector-convert-semantic.ll` 覆盖六族、整数宽度代表、32／48／128 位、全部 round／except 代表、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及无特性／最终 `-fullfp16`／超过 128 位／可伸缩／其它 constrained ID／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不能假定会选择半精度向量严格内建，故只走 FileCheck 与 AArch64 `llc`／`readobj`。原先用 C 约定且带 `+fullfp16` 的 `constrained.fptosi.v2i16.v2f16` 作跳过哨兵的测试已改为向量 `constrained.pow.v2f16`。
- 拒绝：缺少或最后令牌为 `-fullfp16` 的形态良好调用（`unsupported target feature`）；可伸缩、任一侧超过 128 位、车道数不匹配、`i128` 元素、其它 constrained ID、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际固定 half 向量严格 constrained 超越函数支持边界**

- 接受：精确 `llvm.experimental.constrained.sin`／`cos`／`exp`／`exp2`／`log`／`log2`／`log10`，结果与唯一实参为同一固定 IEEE `half` 向量、总原始宽度 1..128（含 `<2 x half>`／`<3 x half>`／`<8 x half>`），外加合法 `round` 与 `fpexcept`。操作数与元数据槽与标量 half 严格超越函数相同。所在函数 `target-features` 最后令牌须为 `+fullfp16`。缺少或最后令牌为 `-fullfp16` 的形态良好调用报告 `unsupported target feature`。`vmp-constrained-fp-vector-transcendental-semantic.ll` 中无特性 half 负例同步为该字符串。元数据进入既有 `CallDescriptor` MetadataArguments。调用点必须是 `CallingConv::C`、非可变参数、无 FastMathFlags、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。不新增操作码。不改变 `f32`／`f64` 向量严格超越函数、标量 half 严格超越函数。`pow`／`powi` 见独立表面。`vmp-constrained-half-fp-vector-transcendental-semantic.ll` 覆盖七 ID、32／48／128 位代表、全部 round／except 代表、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及无特性／最终 `-fullfp16`／超过 128 位／可伸缩／其它 constrained ID／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不能假定会选择半精度向量严格内建，故只走 FileCheck 与 AArch64 `llc`／`readobj`。原先用 C 约定且带 `+fullfp16` 的 `constrained.sin.v2f16`／`sin.v4f16` 作跳过哨兵的测试已改为向量 `constrained.pow.v2f16`／`pow.v4f16` 并挂 deopt bundle。
- 拒绝：缺少或最后令牌为 `-fullfp16` 的形态良好调用（`unsupported target feature`）；可伸缩、超过 128 位、`pow`／`powi`（现见独立表面）、其它 constrained ID、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际固定 half 向量严格 constrained 幂运算支持边界**

- 接受：精确 `llvm.experimental.constrained.pow`／`powi`。`pow`：结果与两个实参为同一固定 IEEE `half` 向量、总原始宽度 1..128（含 `<2 x half>`／`<3 x half>`／`<8 x half>`），外加合法 `round` 与 `fpexcept`。`powi`：固定 `half` 向量底数、普通标量 `i32` 指数（LLVM 15 固定 `llvm_i32_ty`，可为运行时值，不是 ImmArg）、外加合法 `round` 与 `fpexcept`。复用既有 `isConstrainedScalarFPPowID` 与普通 `i32` 指数检查。所在函数 `target-features` 最后令牌须为 `+fullfp16`。缺少或最后令牌为 `-fullfp16` 的形态良好调用报告 `unsupported target feature`。`vmp-constrained-fp-vector-pow-semantic.ll` 中无特性 half 负例同步为该字符串。元数据进入既有 `CallDescriptor` MetadataArguments。调用点必须是 `CallingConv::C`、非可变参数、无 FastMathFlags、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。不新增操作码。不改变 `f32`／`f64` 向量严格 `pow`／`powi`、标量 half 严格 `pow`／`powi`。这是 LLVM 15 `ConstrainedOps.def` 所列固定 half 向量数学 ID 的最后一项。常量 `i32` 指数经整数虚拟寄存器重放，FileCheck 不得要求字面 `i32 3`。`vmp-constrained-half-fp-vector-pow-semantic.ll` 覆盖两 ID、运行时与立即 `i32` 指数、32／48／128 位代表、全部 round／except 代表、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及无特性／最终 `-fullfp16`／超过 128 位／可伸缩／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不能假定会选择半精度向量严格内建，故只走 FileCheck 与 AArch64 `llc`／`readobj`。原先用 C 约定且带 `+fullfp16` 的形态良好 `constrained.pow.v*f16` 跳过哨兵现挂 deopt bundle，以免落入本表面；此后不再有所列其它 well-formed listed ID 可作 `+fullfp16` 其它 ID 哨兵。
- 拒绝：缺少或最后令牌为 `-fullfp16` 的形态良好调用（`unsupported target feature`）；可伸缩、超过 128 位、非 `i32` 指数、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际固定 f32/f64 向量严格 constrained 同类型舍入支持边界**

- 接受：精确 `llvm.experimental.constrained.ceil`／`floor`／`trunc`／`round`／`roundeven`／`rint`／`nearbyint`，结果与唯一实参为同一固定 IEEE `f32`／`f64` 向量、总原始宽度 1..128（含 `<2 x float>`／`<3 x float>`／`<2 x double>`）。`ceil`／`floor`／`trunc`／`round`／`roundeven` 仅带一个合法 `fpexcept`；`rint`／`nearbyint` 带合法 `round` 与 `fpexcept`。合法 token 与既有严格算术相同。元数据进入既有 `CallDescriptor` MetadataArguments。调用点必须是 `CallingConv::C`、非可变参数、无 FastMathFlags、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。不新增操作码。不要求 `+fullfp16`。不改变标量严格舍入。half 向量同类型舍入见独立 last-token `+fullfp16` 表面；无 `+fullfp16` 的形态良好 `constrained.ceil.v2f16` 负例现为 `unsupported target feature`。`vmp-constrained-fp-vector-round-semantic.ll` 覆盖七 ID、64／96／128 位代表、全部 round／except 代表、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及 `half` 向量／超过 128 位／可伸缩／其它 constrained ID／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不是严格浮点环境，故只走 FileCheck 与 AArch64 `llc`／`readobj`。原先用 C 约定 `constrained.ceil.v2f32`／`ceil.v4f32` 作跳过哨兵的测试已改为向量 `constrained.sin.v2f16`。
- 拒绝：`half` 向量（现见独立 last-token `+fullfp16` 表面）、可伸缩、超过 128 位、其它 constrained ID、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际固定 f32/f64 向量严格 constrained 转换支持边界**

- 接受：精确 `llvm.experimental.constrained.fptosi`／`fptoui`／`sitofp`／`uitofp`／`fptrunc`／`fpext`。`fptosi`／`fptoui`：固定 IEEE `f32`／`f64` 向量源、同车道固定 `i1`／`i8`／`i16`／`i32`／`i64` 向量结果、合法 `fpexcept`。`sitofp`／`uitofp`：固定 `i1`／`i8`／`i16`／`i32`／`i64` 向量源、同车道固定 `f32`／`f64` 向量结果、合法 `round` 与 `fpexcept`。`fptrunc` 仅同车道 `v*f64`→`v*f32`，带合法 `round` 与 `fpexcept`。`fpext` 仅同车道 `v*f32`→`v*f64`，带合法 `fpexcept`。两端各自总原始宽度独立为 1..128（含 `<2 x float>`→`<2 x i32>`／`<2 x i1>`／`<2 x i64>`、`<3 x float>`→`<3 x i32>`、`<2 x double>`→`<2 x i64>`、`<2 x i32>`→`<2 x float>`、`<2 x double>`↔`<2 x float>`）。合法 token 与既有严格算术相同。元数据进入既有 `CallDescriptor` MetadataArguments。调用点必须是 `CallingConv::C`、非可变参数、无 FastMathFlags、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。不新增操作码。不要求 `+fullfp16`。不改变标量严格转换。half 向量转换见独立 last-token `+fullfp16` 表面；无 `+fullfp16` 的形态良好 `constrained.fptosi.v2i16.v2f16` 负例现为 `unsupported target feature`。`vmp-constrained-fp-vector-convert-semantic.ll` 覆盖六族、整数宽度代表、64／96／128 位、全部 round／except 代表、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及 `half` 向量／超过 128 位／可伸缩／其它 constrained ID／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不是严格浮点环境，故只走 FileCheck 与 AArch64 `llc`／`readobj`。原先用 C 约定 `constrained.fptosi.v2i32.v2f32`／`v4i32.v4f32` 作跳过哨兵的测试已改为向量 `constrained.sin.v2f16`。
- 拒绝：`half` 向量（现见独立 last-token `+fullfp16` 表面）、可伸缩、任一侧超过 128 位、车道数不匹配、`i128` 元素、其它 constrained ID、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际固定 f32/f64 向量严格 constrained 最值支持边界**

- 接受：精确 `llvm.experimental.constrained.minnum`／`maxnum`／`minimum`／`maximum`，结果与两个实参为同一固定 IEEE `f32`／`f64` 向量、总原始宽度 1..128（含 `<2 x float>`／`<3 x float>`／`<2 x double>`），外加一个合法 `fpexcept`（LLVM 15 无 rounding 操作数）。合法 token 与既有严格算术相同。元数据进入既有 `CallDescriptor` MetadataArguments。调用点必须是 `CallingConv::C`、非可变参数、无 FastMathFlags、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。不新增操作码。不要求 `+fullfp16`。不改变标量严格最值。half 向量最值见独立 last-token `+fullfp16` 表面；无 `+fullfp16` 的形态良好 `constrained.minnum.v2f16` 负例现为 `unsupported target feature`。`vmp-constrained-fp-vector-minmax-semantic.ll` 覆盖四 ID、64／96／128 位代表、全部 except 代表、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及 `half` 向量／超过 128 位／可伸缩／其它 constrained ID／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不是严格浮点环境，故只走 FileCheck 与 AArch64 `llc`／`readobj`。原先用 C 约定 `constrained.minnum.v2f32`／`v4f32` 作跳过哨兵的测试已改为向量 `constrained.sin.v2f16`。
- 拒绝：`half` 向量（现见独立 last-token `+fullfp16` 表面）、可伸缩、超过 128 位、其它 constrained ID、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际固定 f32/f64 向量严格 constrained 超越函数支持边界**

- 接受：精确 `llvm.experimental.constrained.sin`／`cos`／`exp`／`exp2`／`log`／`log2`／`log10`，结果与唯一实参为同一固定 IEEE `f32`／`f64` 向量、总原始宽度 1..128（含 `<2 x float>`／`<3 x float>`／`<2 x double>`），外加合法 `round` 与 `fpexcept`。合法 token 与既有严格算术相同。元数据进入既有 `CallDescriptor` MetadataArguments。调用点必须是 `CallingConv::C`、非可变参数、无 FastMathFlags、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。不新增操作码。不要求 `+fullfp16`。不改变标量严格超越函数。half 向量超越函数见独立 last-token `+fullfp16` 表面；无 `+fullfp16` 的形态良好 `constrained.sin.v2f16` 负例现为 `unsupported target feature`。也不开放 `pow`／`powi`。`vmp-constrained-fp-vector-transcendental-semantic.ll` 覆盖七 ID、64／96／128 位代表、全部 round／except 代表、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及 `half` 向量／超过 128 位／可伸缩／其它 constrained ID／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不是严格浮点环境，故只走 FileCheck 与 AArch64 `llc`／`readobj`。原先用 C 约定 `constrained.sin.v2f32`／`v4f32` 作跳过哨兵的测试已改为向量 `constrained.pow.v2f16`。
- 拒绝：`half` 向量（现见独立 last-token `+fullfp16` 表面）、可伸缩、超过 128 位、`pow`／`powi`、其它 constrained ID、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际固定 f32/f64 向量严格 constrained 幂运算支持边界**

- 接受：精确 `llvm.experimental.constrained.pow`／`powi`。`pow`：结果与两个实参为同一固定 IEEE `f32`／`f64` 向量、总原始宽度 1..128（含 `<2 x float>`／`<3 x float>`／`<2 x double>`），外加合法 `round` 与 `fpexcept`。`powi`：固定 `f32`／`f64` 向量底数、普通标量 `i32` 指数（LLVM 15 固定 `llvm_i32_ty`，可为运行时值，不是 ImmArg）、外加合法 `round` 与 `fpexcept`。常量 `i32` 指数经整数虚拟寄存器重放，FileCheck 不得要求字面 `i32 3`。合法 token 与既有严格算术相同。元数据进入既有 `CallDescriptor` MetadataArguments。调用点必须是 `CallingConv::C`、非可变参数、无 FastMathFlags、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 不在本表面形状里拒绝，由通用接受门收下并按非 tail `CreateCall`（`TCK_None`）重放。不新增操作码。不要求 `+fullfp16`。不改变标量严格 `pow`／`powi`。half 向量 `pow`／`powi` 见独立 last-token `+fullfp16` 表面；无 `+fullfp16` 的形态良好 `constrained.pow.v2f16` 负例现为 `unsupported target feature`。带 `+fullfp16` 的 C 约定 `pow.v*f16` 哨兵现挂 deopt bundle，以免落入新表面。LLVM 15 `ConstrainedOps.def` 所列固定 `f32`／`f64` 向量数学 ID 至此全部进入既有严格表面。`vmp-constrained-fp-vector-pow-semantic.ll` 覆盖两 ID、运行时与立即 `i32` 指数、64／96／128 位代表、全部 round／except 代表、`strictfp` 正例、普通 `tail`、钻石 `phi` 与回边循环，以及 `half` 向量／超过 128 位／可伸缩／其它 constrained ID／`fastcc`／`musttail`／operand bundle／`poison`／`undef` 负例。宿主 `lli` 不是严格浮点环境，故只走 FileCheck 与 AArch64 `llc`／`readobj`。原先用 C 约定 `constrained.pow.v2f32`／`v4f32` 作跳过哨兵的测试已改为向量 `constrained.sin.v2f16`。
- 拒绝：`half` 向量（现见独立 last-token `+fullfp16` 表面）、可伸缩、超过 128 位、非 `i32` 指数、其它 constrained ID、非法／错位／非 `MDString` 元数据、FastMathFlags、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际固定 f32/f64 向量 fma/fmuladd 支持边界**

- 接受：精确 `llvm.fma`／`llvm.fmuladd`，结果与三个实参为同一固定 `f32`／`f64` 向量（总原始宽度 1..128，含 `<2 x float>`／`<3 x float>`／`<4 x float>`／`<2 x double>`）。调用点必须是 `CallingConv::C`、非可变参数。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，保存／恢复 FastMathFlags。不新增操作码，不要求 `+fullfp16`，不经过 `half` 向量数学门控，也不改变标量 `f16`／`f32`／`f64` `fma`／`fmuladd` 或 listed `half` 向量数学。`vmp-fp-vector-fma-semantic.ll` 覆盖两 ID 与 64／96／128 位形态、FMF，以及 `half`（无 `+fullfp16` 仍为 `unsupported target feature`）／`bfloat`／可伸缩／超过 128 位／constrained／`fastcc`／`musttail`／operand bundle 负例；有限位模式可在宿主 `lli` 上与参考函数差分。
- 拒绝：`half` 向量（仍走既有最后令牌 `+fullfp16` 路径）、标量（仍走既有标量 `fma` 路径）、`bfloat`／`fp128`、可伸缩、超过 128 位、constrained、非 C（如 `fastcc`）、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际固定 f32/f64 向量 sqrt/fabs 支持边界**

- 接受：精确 `llvm.sqrt`／`llvm.fabs`，结果与单一实参为同一固定 `f32`／`f64` 向量（总原始宽度 1..128，含 `<2 x float>`／`<3 x float>`／`<4 x float>`／`<2 x double>`）。调用点必须是 `CallingConv::C`、非可变参数。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，保存／恢复 FastMathFlags。不新增操作码，不要求 `+fullfp16`，不经过 `half` 向量数学门控，也不改变标量 `f16`／`f32`／`f64` `sqrt`／`fabs` 或 listed `half` 向量数学。`vmp-fp-vector-sqrt-fabs-semantic.ll` 覆盖两 ID 与 64／96／128 位形态、FMF，以及 `half`（无 `+fullfp16` 仍为 `unsupported target feature`）／`bfloat`／可伸缩／超过 128 位／constrained／`fastcc`／`musttail`／operand bundle 负例；有限位模式可在宿主 `lli` 上与参考函数差分。
- 拒绝：`half` 向量（仍走既有最后令牌 `+fullfp16` 路径）、标量（仍走既有标量 `sqrt`／`fabs` 路径）、`bfloat`／`fp128`、可伸缩、超过 128 位、constrained、非 C（如 `fastcc`）、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际固定 f32/f64 向量 minnum/maxnum/minimum/maximum 支持边界**

- 接受：精确 `llvm.minnum`／`maxnum`／`minimum`／`maximum`，结果与两个实参为同一固定 `f32`／`f64` 向量（总原始宽度 1..128，含 `<2 x float>`／`<3 x float>`／`<4 x float>`／`<2 x double>`）。调用点必须是 `CallingConv::C`、非可变参数。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，保存／恢复 FastMathFlags。不新增操作码，不要求 `+fullfp16`，不经过 `half` 向量数学门控，也不改变标量 `minnum`／`minimum` 或整数 `smin`／`smax`。`vmp-fp-vector-minmax-semantic.ll` 覆盖四 ID 与 64／96／128 位形态、FMF，以及 `half`（无 `+fullfp16` 仍为 `unsupported target feature`）／`bfloat`／可伸缩／超过 128 位／constrained／`fastcc`／`musttail`／operand bundle 负例；`minnum`／`maxnum` 有限位模式可在宿主 `lli` 上与参考函数差分。宿主 x86 无法选择 `llvm.minimum`／`maximum` 向量，故这两项只走 FileCheck 与 AArch64 `llc`。
- 拒绝：`half` 向量（仍走既有最后令牌 `+fullfp16` 路径）、标量（仍走既有标量路径）、`bfloat`／`fp128`、可伸缩、超过 128 位、constrained、非 C（如 `fastcc`）、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际固定 f32/f64 向量舍入／符号复制／幂运算支持边界**

- 接受：精确 `llvm.ceil`／`floor`／`trunc`／`round`／`rint`／`nearbyint`／`roundeven`（单一同型向量实参）、`llvm.copysign`／`llvm.pow`（两个同型向量实参）、`llvm.powi`（同型向量底数加严格标量 `i32` 指数）。结果与全部向量实参为同一固定 `f32`／`f64` 向量（总原始宽度 1..128，含 `<2 x float>`／`<3 x float>`／`<4 x float>`／`<2 x double>`）。调用点必须是 `CallingConv::C`、非可变参数。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，保存／恢复 FastMathFlags。不新增操作码，不要求 `+fullfp16`，不经过 `half` 向量数学门控，也不改变标量舍入／`copysign`／`pow`／`powi` 或 listed `half` 向量数学。不开放向量 `sin`／`cos`／`exp`／`log` 等其它超越函数。普通 `tail` 现拒绝；`musttail` 仍排除。`vmp-fp-vector-rounding-sign-power-semantic.ll` 覆盖舍入族代表、`copysign`、`pow`、`powi` 与 64／96／128 位形态、FMF，以及 `bfloat`／可伸缩／超过 128 位／`fastcc`／`musttail`／operand bundle／非 `i32` `powi` 负例；有限位模式可在宿主 `lli` 上与参考函数差分。
- 拒绝：`half` 向量（仍走既有最后令牌 `+fullfp16` 路径）、标量（仍走既有标量路径）、向量超越函数、`bfloat`／`fp128`、可伸缩、超过 128 位、constrained、非 C（如 `fastcc`）、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、非 `i32` 指数的 `powi`、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际固定 f32/f64 向量超越函数支持边界**

- 接受：精确 `llvm.sin`／`cos`／`exp`／`exp2`／`log`／`log2`／`log10`，结果与唯一实参为同一固定 `f32`／`f64` 向量（总原始宽度 1..128，含 `<2 x float>`／`<3 x float>`／`<4 x float>`／`<2 x double>`）。调用点必须是 `CallingConv::C`、非可变参数。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，保存／恢复 FastMathFlags。不新增操作码，不要求 `+fullfp16`，不经过 `half` 向量数学门控，也不改变标量超越路径或 listed `half` 向量数学。本 helper 不扩展向量 `pow`／`powi` 或其它未列入 ID（`f32`／`f64` 向量 `canonicalize` 见独立 `VectorFMul` 表面）。普通 `tail` 现拒绝；`musttail` 仍排除。`vmp-fp-vector-transcendental-semantic.ll` 覆盖七 ID、64／96／128 位形态、FMF，以及 `bfloat`／可伸缩／超过 128 位／`fastcc`／`musttail`／operand bundle／constrained／half 向量 `canonicalize` 负例；宿主 `lli` 只对有限、log 定义域内输入与参考函数做位差分。
- 拒绝：`half` 向量（仍走既有最后令牌 `+fullfp16` 路径）、标量（仍走既有标量路径）、向量 `pow`／`powi` 及其它未列入 ID、`bfloat`／`fp128`、可伸缩、超过 128 位、constrained、非 C（如 `fastcc`）、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际固定 f32/f64 向量饱和转换支持边界**

- 接受：精确 `llvm.fptosi.sat`／`llvm.fptoui.sat`，源为固定 `f32`／`f64` 向量，目的为同车道固定 `i8`／`i16`／`i32`／`i64` 向量。两端各自总原始宽度独立为 1..128，车道数完全相同（含 `<4 x float>`→`<4 x i8>`／`<4 x float>`→`<4 x i16>`／`<2 x float>`→`<2 x i32>`／`<2 x float>`→`<2 x i64>`／`<2 x double>`→`<2 x i8>`／`<2 x double>`→`<2 x i32>`／`<3 x float>`→`<3 x i8>`／`<3 x float>`→`<3 x i32>` 等；`<3 x float>`→`<3 x i64>` 因目的 192 位而仍拒绝）。调用点必须是 `CallingConv::C`、非可变参数。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放。不新增操作码，不要求 `+fullfp16`，不改变普通向量 `fptosi`／`fptoui` 指令、标量饱和转换或 half 门控。普通 `tail` 现拒绝；`musttail` 仍排除。`vmp-fp-vector-sat-convert-semantic.ll` 覆盖每个源浮点宽度到 `i8`／`i16`／`i32`／`i64` 的有符号／无符号代表、非二幂车道、负值 unsigned 归零、上下溢饱和、钻石 `phi` 与回边循环，以及无 `+fullfp16` 的 `half`（走独立 last-token 表面，跳过原因 `unsupported target feature`）／`bfloat`／`fp128`／可伸缩／超过 128 位／`i1` 或其它不支持目标宽度／`fastcc`／`musttail`／operand bundle／constrained 负例；形态良好的 LLVM 内建不会出现车道数不匹配。宿主 `lli` 只对有限位模式与参考函数差分。
- 拒绝：`half` 源（走独立 last-token `+fullfp16` 表面）、`bfloat`／`fp128` 源、可伸缩、任一侧超过 128 位、`i1` 或其它目标元素宽度、车道数不匹配、标量（仍走既有标量 sat 路径）、普通非 sat `fptosi`／`fptoui` 指令（仍走既有向量转换操作码）、constrained 及其它未列入 ID、非 C（如 `fastcc`）、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际标量 f32/f64 饱和转换支持边界**

- 接受：精确 `llvm.fptosi.sat`／`llvm.fptoui.sat`，单一标量 `f32`／`f64` 实参，结果为标量 `i8`／`i16`／`i32`／`i64`。经既有 `CallDescriptor` 与整数／浮点虚拟寄存器原样重放。不新增操作码，不改变普通非饱和 `fptosi`／`fptoui`、向量饱和转换或 half 门控。`i32`／`i64` 仍走原包装器；`i8`／`i16` 走窄宽度助手，调用点须为 `CallingConv::C`、非可变参数。普通 `tail` 现与 i32／i64 包装器一样拒绝；`musttail` 仍排除。`vmp-fptosi-sat-intrinsic-semantic.ll`／`vmp-fptoui-sat-intrinsic-semantic.ll` 与对应 `i64` 文件覆盖 `f32`→`i32`／`i64`；`vmp-fptoint-sat-double.ll` 覆盖 `f64`→`i32`／`i64`；`vmp-fptoint-sat-narrow-semantic.ll` 覆盖 `f32`／`f64`→`i8`／`i16` 的有符号／无符号、负值 unsigned 归零、上下溢饱和、普通 `tail`、钻石 `phi` 与回边循环，以及 `i1`／无 `+fullfp16` 的 `half`（走独立 last-token 表面，跳过原因 `unsupported target feature`）／`fastcc`／`musttail`／operand bundle／constrained／`poison` 负例。宿主 `lli` 对有限位模式与参考函数差分。
- 拒绝：`i1` 或其它结果宽度、`half` 源（走独立 last-token `+fullfp16` 表面）、`bfloat`／`fp128` 源、向量（走独立向量 sat 表面）、可伸缩、constrained、非 C（窄宽度助手）、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际固定 f32/f64 向量 canonicalize 支持边界**

- 接受：精确 `llvm.canonicalize`，结果与唯一实参为同一固定 `f32`／`f64` 向量（总原始宽度 1..128，含 `<2 x float>`／`<3 x float>`／`<4 x float>`／`<2 x double>`）。调用点必须是 `CallingConv::C`、非可变参数。规划时不得经 `CallDescriptor` 重发射 `llvm.canonicalize`（AArch64／X86 不能选择该内建）；模仿标量路径，用既有 `VectorFMul` 降为每车道 `fmul x, 1.0`，同型向量 `1.0` 常量进向量虚拟寄存器，`packVectorVariant` 携带精确 FastMathFlags。不新增操作码，不要求 `+fullfp16`，不改变标量 `canonicalize` 或 half 门控。普通 `tail` 现拒绝；`musttail` 仍排除。`vmp-fp-vector-canonicalize-semantic.ll` 覆盖有限值、`+0`／`-0`、`+inf`、quiet NaN payload、非二幂车道与 `fast` FMF，以及 `half`／`bfloat`／可伸缩／超过 128 位／`fastcc`／`musttail`／operand bundle／constrained 负例；参考函数使用同一 `fmul x, 1.0` 展开，宿主 `lli` 做位差分。
- 拒绝：`half` 向量（走独立最后令牌 `+fullfp16` 的 `VectorFMul` 表面）、标量（仍走既有标量 `FMul` 降级）、`bfloat`／`fp128`、可伸缩、超过 128 位、constrained、非 C（如 `fastcc`）、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际固定 f32/f64 向量 is.fpclass 支持边界**

- 接受：精确 `llvm.is.fpclass`，唯一浮点实参为固定 `f32`／`f64` 向量（总原始宽度 1..128，含 `<2 x float>`／`<3 x float>`／`<4 x float>`／`<2 x double>`），结果为同车道数的 `<N x i1>`。第二参数必须是 `i32` `ConstantInt` 且保持 LLVM `ImmArg`，经既有 `CallDescriptor` 的 ImmediateArguments 原样重发射，不进虚拟寄存器。调用点必须是 `CallingConv::C`、非可变参数。经既有 `CallDescriptor` 与向量虚拟寄存器重放，不新增操作码，不要求 `+fullfp16`，不改变标量 `is.fpclass`。固定 `half` 向量 `is.fpclass` 见独立表面。普通 `tail` 现拒绝；`musttail` 仍排除。`vmp-fp-vector-fpclass-semantic.ll` 覆盖普通／正负零／正负无穷／NaN（不固定 payload）、64／96／128 位形态，以及无 `+fullfp16` 的 `half`（`unsupported target feature`）／`bfloat`／可伸缩／超过 128 位／`fastcc`／`musttail`／operand bundle／constrained 负例；宿主 `lli` 与参考函数做位差分。动态或非 `i32` 的 mask、以及车道数不匹配的结果，由资格检查拒绝（后者在形态良好的 LLVM 内建重载上不会出现）。
- 拒绝：无 `+fullfp16` 的 `half` 源（走独立 half 表面的 feature gate）、`bfloat`／`fp128` 源、可伸缩、超过 128 位、结果不是同车道 `<N x i1>`、动态或错误宽度 mask、标量（仍走既有标量 `is.fpclass` 路径）、constrained 及其它未列入 ID、非 C（如 `fastcc`）、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 浮点实参。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际 sret 支持边界**

- 接受：直接、非 intrinsic、非可变参数、`CallingConv::C`、返回 void 的 `CallInst`，以及满足同一保守 `sret` 合同且带不可达最小 landingpad 岛的 nounwind `InvokeInst`；第 0 个实参是地址空间零指针并带 `StructRet`/`sret`；被调 `Function` 也是 C、void、固定参数，且第 0 个形式参数带相同 `StructRet` 类型。`invoke` 另要求调用点与被调 `FunctionType` 完全同一。经既有 `CallDescriptor` 重放，完整保留 `FunctionType`、调用约定、`AttributeList`（含对齐）与内存／调试元数据；nounwind `invoke` 再把虚拟机 PC 转到正常后继，不执行展开边。被虚拟化函数自身的 `sret` 参数只进入既有地址空间零指针虚拟寄存器，不把返回聚合物化为普通聚合虚拟寄存器。
- 拒绝：间接、`noreturn`、intrinsic、可变参数、非 C（调用点或被调任一侧）、`tail`/`musttail`、操作数捆绑、`returns_twice`、会抛出或可达 EH 上的 `sret`；`sret` 不在第 0 参数；`sret` 与 `byval`/`byref`/`preallocated`/`inalloca`/`nest`/`swifterror`/`swiftasync`/`elementtype` 组合。`hasUnsupportedCallABIAttributes` 仍拒绝全部 `StructRet`；仅普通直接 `CallInst` 路径与 nounwind 直接 `invoke` 路径用 `isSupportedDirectSretCall` 放行上述精确形态。`CallInst` 资格失败保持原函数与 `hikari.vmp.selected`；不合格 `invoke` 仍为早期 `exception handling`。

**当前实际 byval 支持边界**

- 接受：直接、非 intrinsic、非可变参数、`CallingConv::C`、非 `tail`/`musttail`、无操作数捆绑／`noreturn`／`returns_twice` 的 `CallInst`；至少一个地址空间零指针实参带 `ByVal`；调用点与被调对应形式参数均带相同 `ByVal` 指向类型。经既有 `CallDescriptor` 重放，完整保留 `FunctionType`、调用约定、`AttributeList`（含 `byval` 类型与对齐）与元数据。被虚拟化函数自身的 `byval` 参数只进入既有地址空间零指针虚拟寄存器，不把 ABI 副本物化为普通聚合虚拟寄存器。`vmp-byval-semantic.ll` 覆盖 VMP 调用者到 VMP `byval` 被调、参考／保护差分，以及属性保留。
- 拒绝：间接、intrinsic、可变参数、`noreturn`、非 C、`sret`+`byval`、`byval`+`byref`、`preallocated`/`inalloca`/`nest`/`swifterror`/`swiftasync`/`elementtype`、以及 `ByVal` 类型或位置与被调不匹配的形态。`hasUnsupportedCallABIAttributes` 仍拒绝全部 `ByVal`；仅普通直接调用路径用 `isSupportedDirectByvalCall` 放行上述精确形态。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际 byref 支持边界**

- 接受：直接、非 intrinsic、非可变参数、`CallingConv::C`、非 `tail`/`musttail`、无操作数捆绑／`noreturn`／`returns_twice` 的 `CallInst`；至少一个地址空间零指针实参带 `ByRef`；调用点与被调对应形式参数均带相同 `ByRef` 指向类型。经既有 `CallDescriptor` 重放，完整保留 `FunctionType`、调用约定、`AttributeList`（含 `byref` 类型与对齐）与元数据。被虚拟化函数自身的 `byref` 参数只进入既有地址空间零指针虚拟寄存器，不把所指对象物化为普通聚合虚拟寄存器。`vmp-byref-semantic.ll` 覆盖 VMP 调用者到 VMP `byref` 被调、参考／保护的可见写入与返回值，以及属性保留。
- 拒绝：间接、intrinsic、可变参数、`noreturn`、非 C、`sret`+`byref`、`byval`+`byref`、`preallocated`/`inalloca`/`nest`/`swifterror`/`swiftasync`/`elementtype`、以及 `ByRef` 类型或位置与被调不匹配的形态。`hasUnsupportedCallABIAttributes` 仍拒绝全部 `ByRef`；仅普通直接调用路径用 `isSupportedDirectByrefCall` 放行上述精确形态。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际 AArch64 NEON tbl/tbx 支持边界**

- 接受：精确 `llvm.aarch64.neon.tbl1`..`tbl4` 与 `tbx1`..`tbx4`，签名与 `IntrinsicsAArch64.td` 一致：表寄存器恒为 `<16 x i8>`；结果与索引为已支持的 `<8 x i8>` 或 `<16 x i8>`（`tbx` 的 passthru 与结果同型）；调用点必须是 `CallingConv::C`、非可变参数。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码，不要求 `+neon`／`+fullfp16`（基线 AArch64 AdvSIMD）。一般已放行内建路径不再复查调用约定，故该检查放在 `isSupportedAArch64NeonTblTbxIntrinsic` 内。`vmp-aarch64-neon-tbl-tbx-semantic.ll` 覆盖八个族、两种查找宽度、`fastcc`／`musttail`／operand bundle 负例、FileCheck 结构与 AArch64 `llc`；宿主 `lli` 无法选择这些内建，故不做宿主差分。
- 拒绝：非 C（如 `fastcc`）、可变参数、非 `i8` 查找类型（如 `tbl1.v8i16`）、`<4 x i8>`、可伸缩、`llvm.arm.neon.vtbl*`、其它 NEON 洗牌／长乘（`smull`／`umull`；已放行的 `pmull.v8i16`／`pmull64`／`pmul` 见独立表面）、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际 AArch64 NEON 多项式长乘 pmull／pmull64 支持边界**

- 接受：仅 AArch64。精确 ISel 已覆盖的形态：`llvm.aarch64.neon.pmull.v8i16`（`AdvSIMD_2VectorArg_Long_Intrinsic` 实例化为 `<8 x i16>(<8 x i8>, <8 x i8>)`，对应 `PMULLv8i8`，基线 HasNEON）；以及 `llvm.aarch64.neon.pmull64`（固定 `<16 x i8>(i64, i64)`，对应 `PMULLv1i64`）。后者要求函数 `target-features` 最后一次出现的精确令牌为 `+aes`（`HasAES`／`FEAT_AES`；`+aes2`／`+crypto`／缺失／最后 `-aes` 不算）。调用点必须是 `CallingConv::C`、非可变参数、实参类型等于形参。经既有 `CallDescriptor` 重放：`v8i8`／`v8i16`／`v16i8` 走向量虚拟寄存器，`pmull64` 的 `i64` 实参走整数虚拟寄存器。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-pmull-semantic.ll`（O0/O2×97/7）覆盖两种正例、多特征最后 `+aes`、AArch64 `llc`／`readobj`／汇编（`pmull`），以及 `pmull.v4i32`／SVE `pmullb`／`fastcc`／`musttail`／bundle／无 `+aes`／最后 `-aes`／仅 `+aes2`／仅 `+crypto` 负例。形态良好的 `smull`／`umull` 见独立表面。同宽 `pmul` 见下一节。形态良好的 crypto AES 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：其它 Long 宽度（`v16i16`／`v4i32`／`v2i64`）、SVE `pmullb`／`pmullt`、无关 SHA／SM4／SVE AES（形态良好的 `llvm.aarch64.crypto.aese`／`aesd`／`aesmc`／`aesimc` 见独立表面）、`half`／`bfloat`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。形态良好但缺少 `+aes` 的 `pmull64` 为 `unsupported target feature`。同宽 `pmul` 与整数长乘 `smull`／`umull` 见独立表面。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 同宽多项式乘 pmul 支持边界**

- 接受：仅 AArch64。精确 ISel 已覆盖的 `llvm.aarch64.neon.pmul`（`AdvSIMD_2VectorArg_Intrinsic`）：`<8 x i8>(<8 x i8>, <8 x i8>)` 对应 `PMULv8i8`，`<16 x i8>(<16 x i8>, <16 x i8>)` 对应 `PMULv16i8`（`SIMDThreeSameVectorB`，基线 HasNEON）。结果与两实参同型。调用点必须是 `CallingConv::C`、非可变参数、实参类型等于形参。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码，不要求 `+aes`／`+neon` 最后令牌。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-pmul-semantic.ll`（O0/O2×97/7）覆盖两种正例、AArch64 `llc`／`readobj`／汇编（`pmul`），以及 `pmul.v8i16`／`v4i8`／`v4i32`／SVE `pmul`／`fastcc`／`musttail`／bundle 负例。形态良好的 `sqdmulh`／`sqrdmulh` 与 `smull`／`umull` 见独立表面。形态良好的 crypto AES 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：非 `i8` 同宽形态、`<4 x i8>`、`pmull`（独立表面）、整数长乘 `smull`／`umull`（独立表面）、饱和加倍乘高（见后续节）、浮点、SVE `pmul`、无关 SHA／SM4／SVE AES（形态良好的 crypto AES 见独立表面）、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 饱和加倍乘高 sqdmulh／sqrdmulh 支持边界**

- 接受：仅 AArch64。精确 ISel 已覆盖的非累加饱和加倍乘高：`llvm.aarch64.neon.sqdmulh`／`sqrdmulh`（`AdvSIMD_2IntArg`）以及 `.lane`／`.laneq`（`AdvSIMD_2VectorArg_Lane`：`anyint`、`match`、`anyint`、`i32`）。同宽向量形态对应 `SIMDThreeSameVectorHS`：`<4 x i16>`／`<8 x i16>`／`<2 x i32>`／`<4 x i32>`，结果与两实参同型。车道形态对应 `SIMDIndexedHSPatterns`：`.lane` 的索引向量为同元素 64 位（`<4 x i16>`／`<2 x i32>`），`.laneq` 的索引向量为同元素 128 位（`<8 x i16>`／`<4 x i32>`）；结果仍为上述 ISel `i16`／`i32` 向量，可与索引不同宽度（如 `v4i16` 结果 + `v8i16` 索引的 laneq，或 `v8i16` 结果 + `v4i16` 索引的 lane）。车道为范围内非负 `i32` `ConstantInt`（`[0, Nsrc)`），经 `CallDescriptor` 立即数保存（TD 未标 `ImmArg`，指令选择只匹配 `VectorIndex*`／`ConstantSDNode`）。调用点必须是 `CallingConv::C`、非可变参数、实参类型等于形参。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码，不要求 `+rdm`／`+neon`／`+aes` 最后令牌。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-sqdmulh-semantic.ll`（O0/O2×97/7）覆盖四宽 `sqdmulh`、`sqrdmulh`、最后 `-rdm` 与 `+rdm` 正例、同宽与混宽 lane／laneq 及立即数保留，以及 `v8i8`／`v2i64`／动态或越界或负车道／lane 与 128 位索引／laneq 与 64 位索引／非 ISel `pmul`／SVE／`fastcc`／`musttail`／bundle 负例。形态良好的标量 `i32` 见下一节。形态良好的累加 `sqrdmlah`／`sqrdmlsh` 与整数长乘 `smull`／`umull` 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：标量 `i32`（独立表面）、标量 `i16`（无 ISel；clang `vqdmulhh_s16`／`vqrdmulhh_s16` 走 `Use64BitVectors`）、`i8`／`i64` 向量、`half`／`bfloat`、累加 `sqrdmlah`／`sqrdmlsh`（走独立 last-token `+rdm` 表面）、动态／越界／负／非 `i32` 车道、lane／laneq 与索引向量宽度不匹配、`smull`／`umull`／`pmul`／`pmull`（走独立表面）、SVE、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 标量饱和加倍乘高 sqdmulh／sqrdmulh 支持边界**

- 接受：仅 AArch64。同一 IR ID `llvm.aarch64.neon.sqdmulh`／`sqrdmulh` 的标量形态。clang `vqdmulhs_s32`／`vqrdmulhs_s32`。ISel `SIMDThreeScalarHS`（基线 HasNEON）**只匹配** `i32(i32, i32)`。`i16` 编码存在但 pattern 为空。`i8`／`i64` 无标量 ISel。**不得**降为普通 `mul`。`INT_MIN*INT_MIN` 饱和到 `INT_MAX`（`2*SMin*SMin` 溢出高半）。舍入边：`3*(1<<30)` 的 Q31 高半为 1.5，`sqdmulh` 截断、`sqrdmulh` 加 `1<<(esize-1)` 后再右移；LLVM 15 不折叠，汇编保持 `sqdmulh s`／`sqrdmulh s`。无独立标量 `.lane` IR：clang `vqdmulhs_lane_s32` 是 `extractelement` 后再走同一 ID（ISel `sqdmulh s, s, v.s[n]`）。无最后令牌（`+/-rdm` 不改变非累加表面），资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、两 `i32` 实参且形参／结果精确匹配。经既有 `CallDescriptor` 与整数虚拟寄存器原样重放。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-scalar-sqdmulh-semantic.ll`（O0/O2×97/7）覆盖两 ID 的 `i32`、最后 `-rdm`／`+rdm`、`INT_MIN` 饱和常量、车道 extract、AArch64 `llc`／`readobj`／汇编（`sqdmulh`／`sqrdmulh`），以及 `i16`／`i8`／`i64`／`<1 x i32>`／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的向量 `sqdmulh`／`sqrdmulh` 见上一节。累加 `sqrdmlah`／`sqrdmlsh` 标量仍拒绝（独立 `+rdm` 表面）。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`i16`／`i8`／`i64`、`<1 x i32>`、向量饱和加倍乘高（独立表面）、累加 `sqrdmlah`／`sqrdmlsh`、普通 `mul`、SVE、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON RDM 累加／累减乘高 sqrdmlah／sqrdmlsh 支持边界**

- 接受：仅 AArch64。精确 ISel 已覆盖的累加／累减饱和加倍乘高：`llvm.aarch64.neon.sqrdmlah`／`sqrdmlsh`（`AdvSIMD_3IntArg`），对应 `SIMDThreeSameVectorSQRDMLxHTiedHS`（`HasNEON`+`HasRDM`）：`<4 x i16>`／`<8 x i16>`／`<2 x i32>`／`<4 x i32>`，结果与三个实参同型。NEON 无独立 `.lane`／`.laneq` IR ID（clang 把 `vqrdmlah_lane` 降为 `splat_lane` + 三实参；ISel `SIMDIndexedSQRDMLxHSDTied` 匹配该三实参的 `duplane`）。所在函数 `target-features` 最后一次出现的精确 `+rdm`／`-rdm` 令牌须为 `+rdm`（`HasRDM`／`FEAT_RDM`；缺失／最后 `-rdm`／仅 `+neon`／`+v8.1a` 不算）。调用点必须是 `CallingConv::C`、非可变参数、实参类型等于形参。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码。不改变无门控的 `sqdmulh`／`sqrdmulh` 表面。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-sqrdmlah-semantic.ll`（O0/O2×97/7）覆盖四宽 `sqrdmlah`、两宽 `sqrdmlsh`、多特征最后 `+rdm`、AArch64 `llc`／`readobj`／汇编（`sqrdmlah`／`sqrdmlsh`），以及无 `+rdm`／最后 `-rdm`／仅 `+neon`／标量／`v8i8`／`v2i64`／`smull`／SVE 与 SVE `sqrdmlah_lane`／`fastcc`／`musttail`／bundle 负例。宿主不能选择这些内建，故无 `lli`。
- 拒绝：缺少或最后令牌为 `-rdm` 的形态良好调用（跳过原因 `unsupported target feature`，保留 `hikari.vmp.selected`）；标量 `i16`／`i32`、`i8`／`i64` 向量、`half`／`bfloat`、SVE（含 `sqrdmlah_lane`／`sqrdmlsh_lane`）、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 整数长乘 smull／umull 支持边界**

- 接受：仅 AArch64。精确 ISel 已覆盖的 `llvm.aarch64.neon.smull`／`umull`（`AdvSIMD_2VectorArg_Long`）：`<8 x i16>(<8 x i8>, <8 x i8>)` 对应 `SMULLv8i8_v8i16`／`UMULLv8i8_v8i16`；`<4 x i32>(<4 x i16>, <4 x i16>)` 对应 `SMULLv4i16_v4i32`／`UMULLv4i16_v4i32`；`<2 x i64>(<2 x i32>, <2 x i32>)` 对应 `SMULLv2i32_v2i64`／`UMULLv2i32_v2i64`（`SIMDLongThreeVectorBHS` 低半，基线 HasNEON）。结果与两实参车道数相同，元素位宽加倍。高半 `smull2`／`umull2` 不是独立 IR ID（clang／ISel 对 128 位源做 `extract_high` 后再走上述 64 位输入形态）。调用点必须是 `CallingConv::C`、非可变参数、实参类型等于形参。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码，不要求 `+rdm`／`+aes`／`+neon` 最后令牌。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-smull-semantic.ll`（O0/O2×97/7）覆盖三宽 `smull` 与三宽 `umull`、AArch64 `llc`／`readobj`／汇编（`smull`／`umull`），以及 256 位 `v16i16`／`v4i8`／`pmull.v4i32`／SVE `smullb`／`smullt`／`fastcc`／`musttail`／bundle 负例。形态良好的 `sqdmull` 见下一节。宿主不能选择这些内建，故无 `lli`。
- 拒绝：256 位 Long 实例（`v16i16`／`v8i32`／`v4i64`）、`<4 x i8>` 等非 ISel 宽度、`i64` 源元素、标量、饱和加倍长乘 `sqdmull`（独立表面）、多项式 `pmull`／`pmul`（后两者走独立表面）、SVE `smullb`／`smullt` 及 lane、`half`／`bfloat`／浮点、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 饱和加倍长乘 sqdmull 支持边界**

- 接受：仅 AArch64。精确 ISel 已覆盖的 `llvm.aarch64.neon.sqdmull`（`AdvSIMD_2VectorArg_Long`）：`<4 x i32>(<4 x i16>, <4 x i16>)` 对应 `SQDMULLv4i16_v4i32`；`<2 x i64>(<2 x i32>, <2 x i32>)` 对应 `SQDMULLv2i32_v2i64`（`SIMDLongThreeVectorHS` 低半，基线 HasNEON）。仅 HS：无 `i8` 源。高半 `sqdmull2` 不是独立 IR ID（clang／ISel 对 128 位源做 `extract_high` 后再走上述 64 位输入形态）。NEON 无 `.lane` IR ID（`SIMDIndexedLongSD` 匹配该二实参的 `duplane`）。调用点必须是 `CallingConv::C`、非可变参数、实参类型等于形参。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码，不要求 `+rdm`／`+aes`／`+neon` 最后令牌。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-sqdmull-semantic.ll`（O0/O2×97/7）覆盖两宽正例、AArch64 `llc`／`readobj`／汇编（`sqdmull`），以及 `v8i8`／256 位 `v8i32`／非 ISel `pmul`／SVE `sqdmullb`／`sqdmullt`／`fastcc`／`musttail`／bundle 负例。形态良好的标量 `sqdmulls.scalar` 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：标量 `sqdmulls.scalar`（独立表面）、`i8`／`i64` 源、256 位 Long 实例、高半独立 ID（不存在）、lane IR（不存在）、`smull`／`umull`／`pmull`／`sqdmulh`（走独立表面）、饱和加倍长乘累加／减 `sqdmlal`／`sqdmlsl`（无 IR ID，独立锁定表面）、SVE `sqdmullb`／`sqdmullt`、`half`／`bfloat`／浮点、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 标量饱和加倍长乘 sqdmulls.scalar 支持边界**

- 接受：仅 AArch64。精确非重载 LLVM 15 ID `llvm.aarch64.neon.sqdmulls.scalar`（固定 `i64(i32, i32)`）。clang `vqdmulls_s32`。ISel `SIMDThreeScalarMixedHS` `SQDMULL`（基线 HasNEON）**只匹配**该对：汇编 `sqdmull d, s, s`。`i16` 编码存在但 pattern 为空。车道形态是 `extractelement` 后再走同一 ID（ISel `SQDMULLv1i64_indexed`）。语义为有符号加倍长乘 `2*a*b` 饱和到 `i64`。**不得**改写成 `smull`、普通 `mul` 或向量 `sqdmull`。无立即数、无最后令牌。调用点必须是 `CallingConv::C`、非可变参数、两 `i32` 实参且形参／结果精确匹配。经既有 `CallDescriptor` 与整数虚拟寄存器原样重放，不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-sqdmulls-scalar-semantic.ll`（O0/O2×97/7）覆盖标量正例、车道 extract、`SMAX`／`SMIN` 饱和常量、AArch64 `llc`／`readobj`／汇编（`sqdmull d`），以及 SVE `sqdmullb`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的向量 `sqdmull` 见独立表面。`sqdmulls.scalar`+标量 `sqadd.i64` 仍因标量 `sqadd` 拒绝（无独立标量 `sqdmlal` ID）。宿主不能选择该内建，故无 `lli`。
- 拒绝：`i16` 标量对（空 ISel）、向量 `sqdmull`（独立表面）、普通 `mul`／`smull`、SVE `sqdmullb`／`sqdmullt`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 饱和加倍长乘累加／减 sqdmlal／sqdmlsl 支持边界**

- 接受：仅 AArch64。LLVM 15 **没有** `llvm.aarch64.neon.sqdmlal`／`sqdmlsl` IR ID（`IntrinsicsAArch64.td` 仅有 `sqdmull`／`sqdmulls.scalar` 与 SVE `sqdmlalb`／`sqdmlalt`／`sqdmlslb`／`sqdmlslt`；clang `vqdmlal`／`vqdmlsl` 是 `NEONMAP2` 到 `sqdmull`+`sqadd`／`sqsub`）。不得发明 CallDescriptor ID、高半 `sqdmlal2` ID 或 `.lane` ID。ISel `SIMDLongThreeVectorSQDMLXTiedHS`（基线 HasNEON）匹配的低半 IR 为：`sqadd(acc, sqdmull(a,b))` → `sqdmlal`；`sqsub(acc, sqdmull(a,b))` → `sqdmlsl`。累加器与结果为加宽类型，窄源元素位宽减半、车道数相同：`<4 x i32>`＋两个 `<4 x i16>`，`<2 x i64>`＋两个 `<2 x i32>`。仅 HS：无 `i8` 源。高半是 `extract_high`／`shufflevector` 后再走上述低半形态。车道／`laneq` 是常量掩码 `shufflevector` 再走同一形态；ISel `SIMDIndexedLongSQDMLXSDTied` 匹配 `duplane`（`i16` 为 `VectorIndexH` 0..7，`i32` 为 `VectorIndexS` 0..3）。无 ImmArg，故无立即数路由。覆盖走既有 `sqdmull` 与 `sqadd`／`sqsub` `CallDescriptor` 及向量虚拟寄存器，**不得**降为普通 `mul`／`add`／`sub`，不新增操作码。调用点必须是 `CallingConv::C`、非可变参数、实参类型等于形参。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-sqdmlal-semantic.ll`（O0/O2×97/7）覆盖两族两宽低半、高半、车道与有符号最小饱和正例、AArch64 `llc`／`readobj`／汇编（虚拟寄存器重放把 ISel `sqdmlal`／`sqdmlsl` 惯用法拆成 `sqdmull` 再 `sqadd`／`sqsub`），以及标量 `sqdmulls.scalar`+`sqadd.i64`／标量 `sqadd.i32`／`i8` 源／256 位／`fabd`／`half`／无 `+bf16` 的 `bfloat` 形参／SVE `sqdmlalb`／`sqdmlslb`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好的 `sqdmull` 与向量 `sqadd`／`sqsub` 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：发明的 `llvm.aarch64.neon.sqdmlal` 等名字（非法 IR）、高半／车道独立 ID（不存在）、标量（无独立 ID；`sqdmulls.scalar`+标量 `sqadd` 由既有门拒绝）、`i8`／`i64` 窄源、256 位 Long 实例、`half`／`bfloat`／浮点、SVE `sqdmlalb`／`sqdmlalt`／`sqdmlslb`／`sqdmlslt` 及 lane、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 加宽整数加减 saddl／uaddl／ssubl／usubl 支持边界**

- 接受：仅 AArch64。LLVM 15 **没有** `llvm.aarch64.neon.saddl`／`uaddl`／`ssubl`／`usubl` IR ID（`IntrinsicsAArch64.td` 未定义；clang `vaddl`／`vsubl` 是 `SOpInst`，展开为 `vmovl`+`add`／`sub`）。不得发明 CallDescriptor ID。ISel `SIMDLongThreeVectorBHS`（基线 HasNEON）匹配的低半 IR 为：`add (sext, sext)` → `saddl`；`add (zext, zext)` → `uaddl`；`sub (sext, sext)` → `ssubl`；`sub (zext, zext)` → `usubl`。宽度仅 `v8i8`→`v8i16`、`v4i16`→`v4i32`、`v2i32`→`v2i64`。覆盖走既有 `VectorSExt`／`VectorZExt`／`VectorAdd`／`VectorSub` 与向量虚拟寄存器，不新增操作码。高半 `saddl2` 等不是独立 IR（`extract_high` 后再走上述形态）。`vmp-aarch64-neon-saddl-semantic.ll`（O0/O2×97/7）覆盖四族三宽正例、AArch64 `llc`／`readobj`／汇编（虚拟寄存器重放把 ISel `saddl` 惯用法拆成 `sshll`／`ushll` 再 `add`／`sub`），以及 SVE `saddlb`／`ssublb` 负例。形态良好的变窄 `addhn`、成对 `saddlp`／`uaddlp` 与同宽 `sabd`／`uabd` 见后续独立表面。宿主 `lli` 可执行已虚拟化的整数向量 IR。
- 拒绝：发明的 `llvm.aarch64.neon.saddl` 等名字（非法 IR）、宽加 `saddw`／`ssubw`（ISel 另一族，本表面不单列）、绝对差长 `sabdl`／`uabdl`（无 IR ID，独立锁定表面 `vmp-aarch64-neon-sabdl-semantic.ll`）、同宽绝对差 `sabd`／`uabd`（独立表面）、变窄 `addhn`／`subhn`／`raddhn`／`rsubhn`（独立表面）、成对加宽 `saddlp`／`uaddlp`（独立表面）、跨车道 `saddlv`、饱和 `sqadd`、SVE `saddlb`／`saddlt`／`ssublb`、`half`／`bfloat`／浮点、可伸缩。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际 AArch64 NEON 变窄高半加减 addhn／subhn／raddhn／rsubhn 支持边界**

- 接受：仅 AArch64。精确 ISel 已覆盖的 `llvm.aarch64.neon.addhn`／`subhn`／`raddhn`／`rsubhn`（`AdvSIMD_2VectorArg_Narrow`）：`<8 x i8>(<8 x i16>, <8 x i16>)`、`<4 x i16>(<4 x i32>, <4 x i32>)`、`<2 x i32>(<2 x i64>, <2 x i64>)`，对应 `SIMDNarrowThreeVectorBHS` 低半（基线 HasNEON）。结果与两实参车道数相同，元素位宽减半。高半 `addhn2` 不是独立 IR ID（两次低半调用再 `concat_vectors`／`INSERT_SUBREG`）。调用点必须是 `CallingConv::C`、非可变参数、实参类型等于形参。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码，不要求最后令牌。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-addhn-semantic.ll`（O0/O2×97/7）覆盖四 ID 三宽正例、AArch64 `llc`／`readobj`／汇编（`addhn`／`subhn`／`raddhn`／`rsubhn`），以及 256 位 `v16i8`／`v4i8`／SVE `addhnb`／`fastcc`／`musttail`／bundle 负例。形态良好的 `saddlp` 见下一节。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`i8` 源、`i64` 结果、`<4 x i8>` 等非 ISel 宽度、256 位实参、高半独立 ID（不存在）、加宽 `saddl`／`smull`／饱和 `sqadd`（走独立或既有表面）、SVE `addhnb`／`addhnt`、`half`／`bfloat`／浮点、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 成对加宽加减 saddlp／uaddlp 支持边界**

- 接受：仅 AArch64。精确 ISel 已覆盖的 `llvm.aarch64.neon.saddlp`／`uaddlp`（`AdvSIMD_1VectorArg_Expand`，一元）：`<4 x i16>(<8 x i8>)`、`<8 x i16>(<16 x i8>)`、`<2 x i32>(<4 x i16>)`、`<4 x i32>(<8 x i16>)`、`<1 x i64>(<2 x i32>)`、`<2 x i64>(<4 x i32>)`，对应 `SIMDLongTwoVector`（基线 HasNEON）。结果车道数为实参一半，元素位宽加倍。`SADALP`／`UADALP` 是 `add`+`saddlp` 的 ISel，不是独立 IR ID。调用点必须是 `CallingConv::C`、非可变参数、实参类型等于形参。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-saddlp-semantic.ll`（O0/O2×97/7）覆盖两 ID 六宽正例、AArch64 `llc`／`readobj`／汇编（`saddlp`／`uaddlp`），以及车道不匹配／`i64` 源／非 ISel `v4i8`／SVE `sadalp`／`fastcc`／`musttail`／bundle 负例。形态良好的同宽 `addp` 见下一节；形态良好的跨车道 `saddlv`／`uaddlv` 见独立表面；形态良好的成对整数极值 `smaxp`／`umaxp`／`sminp`／`uminp` 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：车道数不是一半、`i64` 源、标量、同宽成对 `addp`（独立表面）、跨车道 `saddlv`（独立表面）、累加 `sadalp`（SVE）、变窄 `addhn`、加宽 `saddl`（无 IR ID）、SVE、`half`／`bfloat`／浮点、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 同宽成对加 addp 支持边界**

- 接受：仅 AArch64。精确 ISel 已覆盖的 `llvm.aarch64.neon.addp`（`AdvSIMD_2VectorArg`，二元同型）：`<8 x i8>`、`<16 x i8>`、`<4 x i16>`、`<8 x i16>`、`<2 x i32>`、`<4 x i32>`、`<2 x i64>`，对应 `SIMDThreeSameVector` `ADDP`（基线 HasNEON）。clang `vpadd`／`vpaddq`。不是元素级 `add`，也不是一元加宽 `saddlp`／`uaddlp`。调用点必须是 `CallingConv::C`、非可变参数、实参类型等于形参。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-addp-semantic.ll`（O0/O2×97/7）覆盖七宽正例、AArch64 `llc`／`readobj`／汇编（`addp`），以及 `v1i64`／`v4i8`／256 位 `v4i64`／SVE `addp`／`fastcc`／`musttail`／bundle 负例。形态良好的成对整数极值 `smaxp`／`umaxp`／`sminp`／`uminp` 见下一节；形态良好的浮点 `faddp` 见独立表面；形态良好的跨车道 `saddlv`／`uaddlv` 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：标量 `ADDP`（`SIMDPairwiseScalarD`）、`<1 x i64>`、`<4 x i8>`、256 位、浮点 `faddp`（独立表面）、成对 `smaxp`／`sminp`／`umaxp`／`uminp`（独立表面）、加宽 `saddlp`（独立表面）、跨车道 `saddlv`（独立表面）／`saddv`、SVE `addp`、`half`／`bfloat`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 同宽成对浮点加 faddp 支持边界**

- 接受：仅 AArch64。精确 `llvm.aarch64.neon.faddp`（`AdvSIMD_2VectorArg`，二元同型）。clang `vpadd`／`vpaddq` 浮点形态。ISel `SIMDThreeSameVectorFP` `FADDP`（基线 HasNEON）：`<2 x float>`、`<4 x float>`、`<2 x double>`；半精度 `<4 x half>`／`<8 x half>` 另需 `HasFullFP16`。半精度函数 `target-features` 最后一次出现的精确令牌必须为 `+fullfp16`；缺失、无令牌、最后 `-fullfp16`、仅 `+fp16fml` 不算。`f32`／`f64` 无额外令牌。资格不看命令行 `-mattr`。**不得**改写成元素级 `fadd` 或整数 `addp`。调用点必须是 `CallingConv::C`、非可变参数、两实参同型。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放。既有浮点调用 FMF 门拒绝带 `FastMathFlags` 的形态。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-faddp-semantic.ll`（O0/O2×97/7）覆盖五宽、多特征最后 `+fullfp16`、AArch64 `llc`／`readobj`／汇编（`faddp`，`llc` 另加 `-mattr=+fullfp16`），以及 `nnan ninf`／`<1 x double>`／`<2 x half>`／`bfloat`／无令牌／最后 `-fullfp16`／仅 `+fp16fml`／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的整数 `addp` 与成对浮点 `fmaxp`／`fminp` 见独立表面。宿主不能选择该内建，故无 `lli`。
- 拒绝：带 `FastMathFlags` 的调用、标量成对、`<1 x double>`、`<2 x half>`、`bfloat`、元素级 `fadd`、整数 `addp`（独立表面）、成对浮点最值（独立表面）、SVE `faddp`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。形态良好但半精度缺令牌为 `unsupported target feature`。

**当前实际 AArch64 NEON 绝对浮点比较 facge／facgt 支持边界**

- 接受：仅 AArch64。精确 `llvm.aarch64.neon.facge`／`facgt`（`AdvSIMD_2Arg_FloatCompare_Intrinsic`：`anyint (anyfloat, match)`）。clang `vcage`／`vcagt`；`vcale`／`vcalt` 是交换实参后的同一 ID。ISel `SIMDThreeSameVectorFPCmp`（基线 HasNEON）：`<2 x i32>`←`<2 x float>`、`<4 x i32>`←`<4 x float>`、`<2 x i64>`←`<2 x double>`；半精度 `<4 x i16>`←`<4 x half>`／`<8 x i16>`←`<8 x half>` 另需 `HasFullFP16`。半精度函数 `target-features` 最后一次出现的精确令牌必须为 `+fullfp16`；缺失、无令牌、最后 `-fullfp16`、仅 `+fp16fml` 不算。`f32`／`f64` 无额外令牌。资格不看命令行 `-mattr`。ARM `FACGE`／`FACGT` 比较 `|a|` 与 `|b|`，结果为全 1／全 0 整数车道。`sNaN` 报 Invalid；`qNaN` 为无序假（零掩码）；`|+0|==|-0|`。**不得**改写成 `fabs`+`fcmp` 或通用 `fcmp`：ARM `FABS` 对 `sNaN` 不报 Invalid，复合会丢掉异常，且结果宽度是整数掩码而非 `i1`。调用点必须是 `CallingConv::C`、非可变参数、两实参同型、结果车道数与元素位宽匹配源浮点。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放（浮点向量入、整数掩码向量出）。整数结果不是 `FPMathOperator`，LLVM 15 禁止在其上写 `FastMathFlags`（解析非法，不是跳过哨兵）。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-facge-facgt-semantic.ll`（O0/O2×97/7）覆盖两 ID、五 ISel 宽、多特征最后 `+fullfp16`、AArch64 `llc`／`readobj`／汇编（`facge`／`facgt`，`llc` 另加 `-mattr=+fullfp16`），以及 `<1 x double>`／`<2 x half>`／`bfloat`／结果宽度不匹配／无令牌／最后 `-fullfp16`／仅 `+fp16fml`／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。标量 `i32(f32)`／`i64(f64)`／`i32(f16)` 见下一节。宿主不能选择这些内建，故无 `lli`。
- 拒绝：标量（独立表面）、`<1 x double>`、`<2 x half>`、`bfloat`、结果与源浮点车道／位宽不匹配、`fabs`+`fcmp`／通用 `fcmp`、SVE `facge`／`facgt`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。形态良好但半精度缺令牌为 `unsupported target feature`。

**当前实际 AArch64 NEON 标量绝对浮点比较 facge／facgt 支持边界**

- 接受：仅 AArch64。同一 IR ID `llvm.aarch64.neon.facge`／`facgt` 的标量形态。clang `vcages_f32`／`vcaged_f64`／`vcageh_f16`（半精度在此 ID 之后把 `i32` 截成 `i16`）。ISel `SIMDThreeScalarFPCmp`（基线 HasNEON）：`i32(float)`／`i64(double)`；`i32(half)` 另需 `HasFullFP16`（`FACGE16`／`FACGT16` 再 `INSERT_SUBREG`）。**没有** `i16(half)` ISel。半精度函数 `target-features` 最后一次出现的精确令牌必须为 `+fullfp16`；缺失、无令牌、最后 `-fullfp16`、仅 `+fp16fml` 不算。`f32`／`f64` 无额外令牌。资格不看命令行 `-mattr`。**不得**改写成 `fabs`+`fcmp`。调用点必须是 `CallingConv::C`、非可变参数、两实参同型、结果为上述配对。经既有 `CallDescriptor` 重放：标量浮点虚拟寄存器入、标量整数虚拟寄存器出。整数结果不是 `FPMathOperator`。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-scalar-facge-facgt-semantic.ll`（O0/O2×97/7）覆盖 `i32(f32)`／`i64(f64)`／`i32(f16)`、交换实参的 `vcale`、多特征最后 `+fullfp16`、AArch64 `llc`／`readobj`／汇编（`facge`／`facgt` 的 `s`／`d`／`h`，`llc` 另加 `-mattr=+fullfp16`），以及 `i16(f16)`／无令牌／最后 `-fullfp16`／仅 `+fp16fml`／`<1 x double>`／`bfloat`／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的向量 `facge` 见上一节。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`i16(half)`、`<1 x double>`、`bfloat`、向量 `facge`（独立表面）、`fabs`+`fcmp`、SVE、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。形态良好但半精度缺令牌为 `unsupported target feature`。

**当前实际 AArch64 NEON 普通浮点比较 fcmeq／fcmge／fcmgt（无独立 IR ID）**

- 审查结论：**不实现、不发明 ID**。LLVM 15 `IntrinsicsAArch64.td` **没有** `int_aarch64_neon_fcmeq`／`fcmge`／`fcmgt`。`AArch64InstrInfo.td` 的 `defm FCMEQ`／`FCMGE`／`FCMGT`（`SIMDThreeSameVectorFPCmp`）绑定的是 `AArch64fcmeq`／`AArch64fcmge`／`AArch64fcmgt`（`AArch64ISD::FCMEQ` 等 SDNode），由通用 `fcmp` 选择，不是 target intrinsic。clang `vceqq_f32`／`vcgeq_f32`／`vcgtq_f32` 前端是 `__p0 == __p1`／比较运算符，降为 `fcmp` 再 `sext` 到整数掩码。既有 VMP `isSupportedFloatVectorCompare` 已覆盖固定 `half`／`f32`／`f64` 向量 `FCmpInst`（谓词完整、`<N x i1>` 结果）。绝对比较 `facge`／`facgt` 才有独立 IR ID（见上一节），因为 `fabs`+`fcmp` 会丢掉 `sNaN` Invalid。零比较 `FCMEQZ` 等同理是 `fcmp` 对零。不得把通用 `fcmp` 改写成不存在的 `neon.fcmeq`。

**当前实际 AArch64 NEON 成对整数极值 smaxp／umaxp／sminp／uminp 支持边界**

- 接受：仅 AArch64。精确 ISel 已覆盖的 `llvm.aarch64.neon.smaxp`／`umaxp`／`sminp`／`uminp`（`AdvSIMD_2VectorArg`，二元同型）：`<8 x i8>`、`<16 x i8>`、`<4 x i16>`、`<8 x i16>`、`<2 x i32>`、`<4 x i32>`，对应 `SIMDThreeSameVectorBHS`（基线 HasNEON）。clang `vpmax`／`vpmin` 整数形态。无 `i64` 元素（BHS 不含 D）。不是元素级 `smax`／`smin`／`umax`／`umin`，不是相邻对洗牌后再 `smax`／`smin`，也不是跨车道 `smaxv`／`sminv` 或通用 `llvm.smax`／`llvm.vector.reduce.smax`。调用点必须是 `CallingConv::C`、非可变参数、实参类型等于形参。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码，不改写成元素级 min/max。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-int-pairwise-minmax-semantic.ll`（O0/O2×97/7）覆盖四 ID 与 BHS 多宽正例、AArch64 `llc`／`readobj`／汇编（`smaxp`／`umaxp`／`sminp`／`uminp`），以及 `v2i64`／`v1i32`／`v4i8`／256 位 `v8i32`／`i1`／SVE `smaxp`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的元素级 `smax`／`smin`／`umax`／`umin` 见下一节。形态良好的同宽 `addp`、成对浮点 `fmaxp`／`fminp` 与跨车道 `smaxv`／`sminv` 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：标量、`<1 x i32>`／`<1 x i64>`、`i64` 元素、`<4 x i8>`、256 位、`i1`、元素级 `llvm.aarch64.neon.smax`／`smin`／`umax`／`umin`（独立表面）、通用 `llvm.smax`／`llvm.smin`（既有整数向量表面）、洗牌合成、跨车道 `smaxv`／`sminv`（独立表面）、同宽 `addp`（独立表面）、浮点 `fmaxp`／`fminp`（独立表面）、SVE `smaxp`／`sminp`／`umaxp`／`uminp`、`half`／`bfloat`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 元素级整数最值 smax／smin／umax／umin 支持边界**

- 接受：仅 AArch64。精确 `llvm.aarch64.neon.smax`／`smin`／`umax`／`umin`（`AdvSIMD_2VectorArg`：`anyvector (match, match)`）。clang `vmax`／`vmin` 整数形态（`CGBuiltin.cpp` 仍发这些 ID，不是 `llvm.smax`）。ISel `SIMDThreeSameVectorBHS` 匹配 `smax`／`smin`／`umax`／`umin` ISD：`<8 x i8>`／`<16 x i8>`／`<4 x i16>`／`<8 x i16>`／`<2 x i32>`／`<4 x i32>`。`AArch64ISelLowering` 把这些 ID 降为 `ISD::SMAX`／`SMIN`／`UMAX`／`UMIN`，与 `llvm.smax` 同 ISD，但 **IR ID 不同**，不得改写成 `llvm.smax`。无 `i64`（BHS 不含 D）。调用点必须是 `CallingConv::C`、非可变参数、两实参同型。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放。不新增操作码。无最后令牌。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-int-extrema-semantic.ll`（O0/O2×97/7）覆盖四 ID 与 BHS 多宽正例、AArch64 `llc`／`readobj`／汇编（`smax`／`smin`／`umax`／`umin`），以及 `v2i64`／`v1i32`／`v4i8`／256 位／`i1`／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的成对 `smaxp` 与通用 `llvm.smax` 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：标量、`i64` 元素、`<1 x i32>`、`<4 x i8>`、256 位、`i1`、成对 `smaxp`／`sminp`（独立表面）、通用 `llvm.smax`／`llvm.smin`（既有整数向量表面，另含 `i64`／`v1`／非 ISel 宽）、跨车道 `smaxv`、SVE、`half`／`bfloat`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 同宽绝对差 sabd／uabd 支持边界**

- 接受：仅 AArch64。精确 ISel 已覆盖的 `llvm.aarch64.neon.sabd`／`uabd`（`AdvSIMD_2VectorArg`，二元同型）：`<8 x i8>`、`<16 x i8>`、`<4 x i16>`、`<8 x i16>`、`<2 x i32>`、`<4 x i32>`，对应 `SIMDThreeSameVectorBHS`（基线 HasNEON）。clang `vabd`。无 `i64` 元素（BHS 不含 D）。`SABA`／`UABA` 是 `add`+`sabd` 的 ISel，不是独立 IR ID（见下一节）。`SABDL`／`UABDL` 在 LLVM 15 无 IR ID。调用点必须是 `CallingConv::C`、非可变参数、实参类型等于形参。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-abd-semantic.ll`（O0/O2×97/7）覆盖两 ID 六宽正例、AArch64 `llc`／`readobj`／汇编（`sabd`／`uabd`），以及 `v2i64`／`v4i8`／256 位 `v8i32`／SVE `sabd`／`fastcc`／`musttail`／bundle 负例。形态良好的浮点 `fabd` 与标量 `sisd.fabd` 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`i64` 元素、`<4 x i8>`、256 位、浮点 `fabd`（独立表面）、标量 `sisd.fabd`（独立表面）、累加 `saba`（无独立 IR ID，见下一节）、加宽 `sabdl`／`uabdl`（无 IR ID，独立锁定表面）、SVE `sabd`／`sabdlb`、`half`／`bfloat`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 浮点绝对差 fabd 支持边界**

- 接受：仅 AArch64。精确 `llvm.aarch64.neon.fabd`（`AdvSIMD_2VectorArg`，二元同型）。clang `vabd` 浮点形态。ISel `SIMDThreeSameVectorFP`（基线 HasNEON）：`<2 x float>`、`<4 x float>`、`<2 x double>`；半精度 `<4 x half>`／`<8 x half>` 另需 `HasFullFP16`。半精度函数 `target-features` 最后一次出现的精确令牌必须为 `+fullfp16`；缺失、无令牌、最后 `-fullfp16`、仅 `+fp16fml` 不算。`f32`／`f64` 无额外令牌。资格不看命令行 `-mattr`。**不得**改写成 `fabs(fsub)`（ISel 虽可把后者选成 `fabd`，但 NaN／有符号零／异常语义与内建重放不同）。调用点必须是 `CallingConv::C`、非可变参数、两实参同型。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放。既有浮点调用 FMF 门拒绝带 `FastMathFlags` 的形态（`unsupported float call instruction`），故本表面只接受无 FMF 的目标语义。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-fabd-semantic.ll`（O0/O2×97/7）覆盖五宽、多特征最后 `+fullfp16`、有符号零／NaN／±Inf 常量、AArch64 `llc`／`readobj`／汇编（`fabd`，`llc` 另加 `-mattr=+fullfp16`），以及 `nnan ninf`／`<1 x double>`／`<2 x half>`／`bfloat`／无令牌／最后 `-fullfp16`／仅 `+fp16fml`／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的整数 `sabd` 见上一节。形态良好的标量 `sisd.fabd` 见下一节。宿主不能选择该内建，故无 `lli`。
- 拒绝：带 `FastMathFlags` 的调用、标量 `sisd.fabd`（独立表面）、`<1 x double>`、`<2 x half>`、`bfloat`、`fabs`+`fsub` 合成、SVE `fabd`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。形态良好但半精度缺令牌为 `unsupported target feature`。

**当前实际 AArch64 NEON 标量绝对差 sisd.fabd 支持边界**

- 接受：仅 AArch64。精确 `llvm.aarch64.sisd.fabd`（`AdvSIMD_2Scalar_Float`：`anyfloat (match, match)`）。clang `vabds_f32`／`vabdd_f64`／`vabdh_f16`。ISel `SIMDFPThreeScalar` `FABD`（基线 HasNEON）：标量 `float`／`double`；半精度标量 `half` 另需 `HasFullFP16`。半精度函数 `target-features` 最后一次出现的精确令牌必须为 `+fullfp16`；缺失、无令牌、最后 `-fullfp16`、仅 `+fp16fml` 不算。`f32`／`f64` 无额外令牌。资格不看命令行 `-mattr`。**不得**改写成 `fabs(fsub)`。`<1 x double>` 是 `neon.fabd` 的 ISel 回退，不是本 ID。调用点必须是 `CallingConv::C`、非可变参数、两实参同型。经既有 `CallDescriptor` 与标量浮点虚拟寄存器原样重放。既有浮点调用 FMF 门拒绝带 `FastMathFlags` 的形态（`unsupported float call instruction`）。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-sisd-fabd-semantic.ll`（O0/O2×97/7）覆盖标量 `f32`／`f64`／`half`、多特征最后 `+fullfp16`、有符号零常量、AArch64 `llc`／`readobj`／汇编（`fabd` 的 `s`／`d`／`h`，`llc` 另加 `-mattr=+fullfp16`），以及 `nnan ninf`／`<1 x double>`／`bfloat`／无令牌／最后 `-fullfp16`／仅 `+fp16fml`／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的向量 `neon.fabd` 见上一节。宿主不能选择该内建，故无 `lli`。
- 拒绝：带 `FastMathFlags` 的调用、向量 `neon.fabd`（独立表面）、`<1 x double>`、`bfloat`、`fabs`+`fsub` 合成、SVE `fabd`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。形态良好但半精度缺令牌为 `unsupported target feature`。

**当前实际 AArch64 NEON 绝对差累加 saba／uaba 支持边界**

- 接受：仅 AArch64。LLVM 15 **没有** `llvm.aarch64.neon.saba`／`uaba` IR ID（`IntrinsicsAArch64.td` 仅有 SVE `saba`／`uaba`；clang `vaba` 是 `SOpInst` `OP_ABA`，展开为 `acc + vabd(a,b)`）。不得发明 CallDescriptor ID。ISel `SIMDThreeSameVectorBHSTied`（基线 HasNEON）匹配的三操作数同宽 IR 为：`add (acc, sabd(a,b))` → `saba`；`add (acc, uabd(a,b))` → `uaba`。宽度仅 `v8i8`／`v16i8`／`v4i16`／`v8i16`／`v2i32`／`v4i32`（BHS，无 `i64`）。覆盖走既有 `sabd`／`uabd` `CallDescriptor` 与 `VectorAdd` 及向量虚拟寄存器，不新增操作码。`vmp-aarch64-neon-saba-semantic.ll`（O0/O2×97/7）覆盖两族六宽正例、AArch64 `llc`／`readobj`／汇编（虚拟寄存器重放把 ISel `saba` 惯用法拆成 `sabd`／`uabd` 再 `add`），以及 `v2i64`／`v4i8`／256 位／`fabd`／SVE `saba`／`sabalb`／`fastcc`／`musttail`／bundle 负例。宿主不能选择 `sabd`，故无 `lli`。
- 拒绝：发明的 `llvm.aarch64.neon.saba` 等名字（非法 IR）、加宽累加 `sabal`／`uabal`（无 IR ID）、`i64` 元素、`<4 x i8>`、256 位、浮点 `fabd`+`fadd`、SVE `saba`／`sabalb`、`half`／`bfloat`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 加宽绝对差 sabdl／uabdl 支持边界**

- 接受：仅 AArch64。LLVM 15 **没有** `llvm.aarch64.neon.sabdl`／`uabdl` IR ID（`IntrinsicsAArch64.td` 仅有同宽 `sabd`／`uabd` 与 SVE `sabdlb`／`sabdlt`／`uabdlb`／`uabdlt`；clang `vabdl` 是 `SOpInst` `OP_ABDL`，展开为 `vmovl(unsigned cast, vabd)`，即 `zext(sabd/uabd)`）。不得发明 CallDescriptor ID 或高半 `sabdl2` ID。ISel `SIMDLongThreeVectorBHSabdl`（基线 HasNEON）匹配的低半 IR 为：`zext(sabd)` → `sabdl`；`zext(uabd)` → `uabdl`。另有 `abs(sub(zext,zext))` 仅匹配 `uabdl`。宽度仅 `v8i8`→`v8i16`、`v4i16`→`v4i32`、`v2i32`→`v2i64`。窄输入有符号性在 `sabd`／`uabd` 上；加宽结果一律零扩展（绝对差非负）。覆盖走既有 `sabd`／`uabd` `CallDescriptor` 与 `VectorZExt` 及向量虚拟寄存器，不新增操作码。高半 `sabdl2` 不是独立 IR（`extract_high` 后再走上述形态）。调用点必须是 `CallingConv::C`、非可变参数、实参类型等于形参。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-sabdl-semantic.ll`（O0/O2×97/7）覆盖两族三宽正例、AArch64 `llc`／`readobj`／汇编（虚拟寄存器重放把 ISel `sabdl`／`uabdl` 惯用法拆成 `sabd`／`uabd` 再 `ushll`），以及 `v2i64`／`v4i8`／256 位 `v16i16`／`fabd`／`half`／SVE `sabdlb`／`sabdlt`／`uabdlb`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好的同宽 `sabd`／`uabd` 与累加 `saba` 见独立表面；形态良好的累加长 `sabal`（`add`+`zext(sabd)`）会走既有操作虚拟化，不得当作本表面 skip 负例。宿主不能选择 `sabd`，故无 `lli`。
- 拒绝：发明的 `llvm.aarch64.neon.sabdl` 等名字（非法 IR）、高半独立 ID（不存在）、累加长 `sabal`／`uabal`（无 IR ID，不单列本表面）、同宽 `sabd`／`uabd`（独立表面）、标量、`i64` 输入、`v16i8`＋`zext` 到 `v16i16` 等非 ISel 长型、`v4i8`、浮点 `fabd`／`half`／`bfloat`、SVE `sabdlb`／`sabdlt`／`uabdlb`／`uabdlt`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 整数跨车道归约 saddv／uaddv／sminv／uminv／smaxv／umaxv 支持边界**

- 接受：仅 AArch64。精确 `llvm.aarch64.neon.saddv`／`uaddv`／`sminv`／`uminv`／`smaxv`／`umaxv`（`AdvSIMD_1VectorArg_Int_Across`）。clang／ISel 真形态为标量 `i32` 结果加 BHS／`v2i32` 实参：`<8 x i8>`／`<16 x i8>`／`<4 x i16>`／`<8 x i16>`／`<2 x i32>`／`<4 x i32>`。`v2i32` 走 `ADDP`／`*MAXP`／`*MINP` 回退，不是 `ADDV.4s`。另接受 `saddv`／`uaddv` 的 `i64`（`<2 x i64>`），ISel `ADDPv2i64p`。clang 对 `i8`／`i16` 先发 `i32` 再截断。调用点必须是 `CallingConv::C`、非可变参数、实参类型等于形参。经既有 `CallDescriptor` 重放：向量虚拟寄存器入、整数虚拟寄存器出。不并入通用 `llvm.vector.reduce.*`。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-int-across-semantic.ll`（O0/O2×97/7）覆盖六 ID、BHS／`v2i32`／`v2i64` 正例、AArch64 `llc`／`readobj`／汇编（`addv`／`sminv`／`uminv`／`smaxv`／`umaxv`），以及 `i8` 结果／`sminv` 于 `v2i64`／`v4i8`／256 位／SVE `saddv`／`fastcc`／`musttail`／bundle 负例。形态良好的浮点 `faddv` 与加宽 `saddlv`／`uaddlv` 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：加宽 `saddlv`／`uaddlv`（独立表面）、`i8`／`i16` 结果、`sminv`／`smaxv`／`uminv`／`umaxv` 于 `<2 x i64>`、`<4 x i8>`、256 位、浮点 `faddv`／`fminv`（独立表面）、通用 `llvm.vector.reduce.*`（既有路径）、SVE `saddv`／`sminv`、`half`／`bfloat`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 加宽跨车道加 saddlv／uaddlv 支持边界**

- 接受：仅 AArch64。精确 `llvm.aarch64.neon.saddlv`／`uaddlv`（`AdvSIMD_1VectorArg_Int_Across`：`anyint (anyvector)`）。clang `vaddlv`。ISel `SIMDAcrossLanesSigned/UnsignedLong`（基线 HasNEON）：`i32`←`<8 x i8>`／`<16 x i8>`／`<4 x i16>`／`<8 x i16>`，`i64`←`<4 x i32>`。clang `vaddlv_s32`／`vaddlv_u32` 的 `<2 x i32>`→`i64` 是同一 IR ID，ISel 走 `SADDLP`／`UADDLP`（无硬件 `SADDLV.2s`），仍按内建原样重放。`i8`／`i16` 结果是 clang 对 `i32` 的截断，不是本表面。**不得**改写成 `saddv`、成对 `saddlp` 或 `llvm.vector.reduce.add`。调用点必须是 `CallingConv::C`、非可变参数、形参等于实参。经既有 `CallDescriptor` 重放：向量虚拟寄存器入、整数虚拟寄存器出。不新增操作码。无最后令牌。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-addlv-semantic.ll`（O0/O2×97/7）覆盖两 ID、六 ISel 宽（含 `v2i32`）、AArch64 `llc`／`readobj`／汇编（`saddlv`／`uaddlv`），以及 `i16` 结果／`i32`←`v4i32`／`v2i64`／`v4i8`／256 位／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的同宽 `saddv` 与成对 `saddlp` 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`i8`／`i16` 结果、`i32`←`<4 x i32>`（那是 `saddv`）、`<2 x i64>`、`<4 x i8>`、256 位、同宽 `saddv`（独立表面）、成对 `saddlp`（独立表面）、通用 `llvm.vector.reduce.add`、SVE、`half`／`bfloat`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 饱和可变移位 sqshl／uqshl／sqrshl／uqrshl 支持边界**

- 接受：仅 AArch64。精确 `llvm.aarch64.neon.sqshl`／`uqshl` 以及相邻且 ISel 合同完全相同的饱和舍入对 `sqrshl`／`uqrshl`（`AdvSIMD_2IntArg`，二元同型）。ISel `SIMDThreeSameVector`（基线 HasNEON）：`<8 x i8>`／`<16 x i8>`／`<4 x i16>`／`<8 x i16>`／`<2 x i32>`／`<4 x i32>`／`<2 x i64>`。两实参为同型向量（按车道移位，可为负）。常量移位量不是 `ImmArg`，重放后 ISel 可能折成 `SQSHL_I`。调用点必须是 `CallingConv::C`、非可变参数、实参类型等于形参。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-satshift-semantic.ll`（O0/O2×97/7）覆盖四 ID 与 64／128 位正例、AArch64 `llc`／`readobj`／汇编（`sqshl`／`uqshl`／`sqrshl`／`uqrshl`），以及 `v1i64`／`v4i8`／256 位／SVE `sqshl`／`fastcc`／`musttail`／bundle 负例。形态良好的标量 `i32`／`i64` 见下一节。形态良好的立即 `sqshlu` 见独立表面。形态良好的非饱和 `sshl`／`srshl` 见后续节。宿主不能选择这些内建，故无 `lli`。
- 拒绝：标量 `i32`／`i64`（独立表面）、标量 `i8`／`i16`（无 ISel；clang `vqshlb_s8`／`vqshlh_s16` 走 `Use64BitVectors`）、`<1 x i64>`、非饱和 `sshl`／`ushl`（独立表面）、舍入非饱和 `srshl`／`urshl`（独立表面）、立即有符号转无符号 `sqshlu`（独立表面）、饱和变窄移位 `sqshrn`／`uqshrn` 等（独立表面）、SVE `sqshl`、`<4 x i8>`、256 位、`half`／`bfloat`／浮点、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 标量饱和可变移位 sqshl／uqshl／sqrshl／uqrshl 支持边界**

- 接受：仅 AArch64。同一 IR ID `llvm.aarch64.neon.sqshl`／`uqshl`／`sqrshl`／`uqrshl` 的标量形态。clang `vqshls_s32`／`vqshld_s64`／`vqshls_u32`／`vqrshls_s32`／`vqrshld_s64`。ISel `SIMDThreeScalarBHSD`（基线 HasNEON）**只匹配** `i32(i32, i32)` 与 `i64(i64, i64)`。`i8`／`i16` 编码存在但 pattern 为空。**不得**降为普通 `shl` 或 `llvm.sshl.sat`。移位量为有符号（负值＝右移）。`sqrshl`／`uqrshl` 右移时舍入。移位量不是 ImmArg；裸 IR 上正立即数可 ISel 成 `sqshl s, s, #imm`（`INT_MAX<<1` 保持 `sqshl #1`，不折成常量），负移位量保持寄存器形态 `sqshl s, s, s`。`3>>1`：`sqshl` 截断、`sqrshl` 舍入，LLVM 15 均不折叠。经虚拟寄存器重放后正立即数也保持寄存器形态 `sqshl s, s, s`。无最后令牌，资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、两实参同型。经既有 `CallDescriptor` 与整数虚拟寄存器原样重放。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-scalar-satshift-semantic.ll`（O0/O2×97/7）覆盖四 ID 的 `i32`／`i64`、`INT_MAX<<1` 饱和常量、负移位舍入常量、AArch64 `llc`／`readobj`／汇编（`sqshl`／`uqshl`／`sqrshl`／`uqrshl`），以及 `i8`／`i16`／`<1 x i64>`／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的向量饱和移位见上一节。形态良好的标量立即 `sqshlu` 见独立表面。非饱和标量 `sshl`／`srshl` 仅 `i64` 有 ISel，仍拒绝。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`i8`／`i16`、`<1 x i64>`、向量饱和移位（独立表面）、非饱和 `sshl`／`ushl`／`srshl`／`urshl`、立即 `sqshlu`（独立表面）、SVE、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 立即有符号转无符号饱和左移 sqshlu 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.neon.sqshlu`（`AdvSIMD_2IntArg`：一元同型 `anyint` 加同型移位向量）。clang `vqshlu_n`／`vqshluq_n`。ISel **只有**立即形态：`tryCombineShiftImm` 把常量 splat 折成 `AArch64ISD::SQSHLU_I`，再由 `SIMDVectorLShiftBHSD` `SQSHLU`（基线 HasNEON）匹配 `vecshiftL*`（`Imm < EltBits`，即 `0..EltBits-1`）：`<8 x i8>`／`<16 x i8>`／`<4 x i16>`／`<8 x i16>`／`<2 x i32>`／`<4 x i32>`／`<2 x i64>`。**没有**寄存器形态 `SQSHLU`。第 1 实参不是 ImmArg，但必须是元素宽常量 splat 且落在 ImmLeaf 内，经 `CallDescriptor` ImmediateArguments 原样重放（不得进向量虚拟寄存器，否则 ISel 看不到立即数）。**不得**改写成 `sqshl`（有符号饱和）、`ushl` 或普通 `shl`：负数车道饱和到 0。调用点必须是 `CallingConv::C`、非可变参数、两实参与结果同型。不新增操作码，无最后令牌。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-sqshlu-semantic.ll`（O0/O2×97/7）覆盖七宽 BHSD、立即数 0／7、`i8` 有符号最小／负常量输入、AArch64 `llc`／`readobj`／汇编（`sqshlu` 的 `.8b`／`.16b`／`.4h`／`.8h`／`.2s`／`.4s`／`.2d` 与 `#0`／`#1`／`#7`），以及 `<1 x i64>`／`<4 x i8>`／动态移位／非 splat／越界 `#8`／负立即／SVE `sqshlu`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的标量 `i32`／`i64` 见下一节。形态良好的 `sqshl`／`sshl` 见独立表面。宿主不能选择该内建，故无 `lli`。
- 拒绝：标量 `i32`／`i64`（独立表面）、`<1 x i64>`、动态／非 splat／越界／负立即移位、饱和可变 `sqshl`／`uqshl`（独立表面）、非饱和 `sshl`／`ushl`（独立表面）、普通 `shl`、SVE `sqshlu`、`<4 x i8>`／256 位、`half`／`bfloat`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 标量立即有符号转无符号饱和左移 sqshlu 支持边界**

- 接受：仅 AArch64。同一 IR ID `llvm.aarch64.neon.sqshlu` 的标量形态。clang `vqshlus_n_s32`／`vqshlud_n_s64`。ISel `tryCombineShiftImm`→`AArch64ISD::SQSHLU_I`，再由 `SIMDScalarLShiftBHSD`（基线 HasNEON）**只匹配** `i32`＋`vecshiftL32`（`0..31`）与 `i64`＋`vecshiftL64`（`0..63`）。汇编 `sqshlu s, s, #imm`／`sqshlu d, d, #imm`。`i8`／`i16` 编码存在但 pattern 为空。**没有**寄存器形态：动态／越界／负立即 `CannotYetSelect`。**不得**降为 `sqshl`／`ushl`／普通 `shl`。负数饱和到 0（`-1<<1` 与 `INT_MIN<<1` 均保持 `sqshlu s, s, #1`，不折成 0；`INT_MAX<<1` 同样保持立即形态）。第 1 实参不是 ImmArg，但必须是同型 `ConstantInt` 且落在 ImmLeaf 内，经既有 `isCallImmediateArgument`（`sqshlu` arg1 为 `Constant`）走 ImmediateArguments。无最后令牌，资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、两实参与结果同型。经既有 `CallDescriptor` 与整数虚拟寄存器原样重放。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-scalar-sqshlu-semantic.ll`（O0/O2×97/7）覆盖 `i32`／`i64`、立即数 0／1／31／63、`INT_MIN` 饱和常量、AArch64 `llc`／`readobj`／汇编（`sqshlu`），以及 `i8`／`i16`／`<1 x i64>`／动态／越界 `#32`／负立即／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的向量 `sqshlu` 见上一节。宿主不能选择该内建，故无 `lli`。
- 拒绝：`i8`／`i16`、`<1 x i64>`、动态／越界／负立即、向量 `sqshlu`（独立表面）、饱和可变 `sqshl`、非饱和 `sshl`／`ushl`、普通 `shl`、SVE、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 非饱和可变移位 sshl／ushl／srshl／urshl 支持边界**

- 接受：仅 AArch64。精确 `llvm.aarch64.neon.sshl`／`ushl`／`srshl`／`urshl`（`AdvSIMD_2IntArg`，二元同型）。ISel `SIMDThreeSameVector`（基线 HasNEON）：`<8 x i8>`／`<16 x i8>`／`<4 x i16>`／`<8 x i16>`／`<2 x i32>`／`<4 x i32>`／`<2 x i64>`。两实参为同型向量（按车道有符号移位量，负值表示右移）。`srshl`／`urshl` 舍入；`sshl`／`ushl` 不饱和。常量移位量不是 `ImmArg`，重放后 ISel 可能折成 `VSHL`／`SRSHR_I`。调用点必须是 `CallingConv::C`、非可变参数、实参类型等于形参。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-varshift-semantic.ll`（O0/O2×97/7）覆盖四 ID 与 64／128 位正例、AArch64 `llc`／`readobj`／汇编（`sshl`／`ushl`／`srshl`／`urshl`），以及 `v1i64`／`v4i8`／256 位／SVE `srshl`／`fastcc`／`musttail`／bundle 负例。形态良好的标量 `i64` 见下一节。形态良好的立即 `sqshlu` 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：标量 `i64`（独立表面）、标量 `i32`（`CannotYetSelect`）、`<1 x i64>`、饱和 `sqshl`／`uqshl`／`sqrshl`／`uqrshl`（独立表面）、立即有符号转无符号 `sqshlu`（独立表面）、饱和变窄移位 `sqshrn`／`uqshrn` 等（独立表面）、SVE `srshl`／`lsl`、`<4 x i8>`、256 位、`half`／`bfloat`／浮点、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 标量非饱和可变移位 sshl／ushl／srshl／urshl 支持边界**

- 接受：仅 AArch64。同一四 ID 的标量形态。clang `vshld_s64`／`vshld_u64`／`vrshld_s64`／`vrshld_u64`。ISel 额外 `Pat`（基线 HasNEON）**只匹配** `i64(i64, i64)`→`SSHLv1i64`／`USHLv1i64`／`SRSHLv1i64`／`URSHLv1i64`。汇编 `sshl d, d, d` 等。`SIMDThreeScalarD` 本身只匹配 `v1i64`（本表面不收）。`i32` `CannotYetSelect`。**不得**降为普通 `shl` 或饱和 `sqshl`。移位量为有符号（负值＝右移）。`srshl`／`urshl` 舍入。移位量不是 ImmArg。审计：`SMAX<<1`／全 1 `ushl` 裸 IR 折成 `shl d, d, #1`（不饱和，环绕）；`3>>1` 时 `sshl` 保持 `sshl d, d, d`，`srshl`／`urshl` 折成 `srshr`／`urshr #1`。经虚拟寄存器重放后一律保持寄存器形态。无最后令牌，资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、两 `i64` 实参同型。经既有 `CallDescriptor` 与整数虚拟寄存器原样重放。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-scalar-varshift-semantic.ll`（O0/O2×97/7）覆盖四 ID、`3>>1` 截断／舍入常量、AArch64 `llc`／`readobj`／汇编（`sshl`／`ushl`／`srshl`／`urshl`），以及 `i32`／`<1 x i64>`／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的向量可变移位见上一节。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`i32`（无 ISel）、`<1 x i64>`、向量可变移位（独立表面）、饱和 `sqshl`／`sqrshl`、立即 `sqshlu`、SVE、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 饱和变窄移位 sqshrn／uqshrn／sqrshrn／uqrshrn／sqshrun／sqrshrun 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.neon.sqshrn`／`uqshrn`／`sqrshrn`／`uqrshrn`／`sqshrun`／`sqrshrun`（`AdvSIMD_2Arg_Scalar_Narrow`：结果 `anyint`，源为 `LLVMExtendedType`，移位为 `i32`）。**没有** `sqshrn2` IR ID。clang 高半 `vqshrn_high` 是同 ID 再 `concat_vectors`／洗牌。ISel `SIMDVectorRShiftNarrowBHS`（基线 HasNEON）仅低半：`<8 x i8>(<8 x i16>, i32)` 立即数 `vecshiftR16Narrow` **1..8**；`<4 x i16>(<4 x i32>, i32)` 为 `vecshiftR32Narrow` **1..16**；`<2 x i32>(<2 x i64>, i32)` 为 `vecshiftR64Narrow` **1..32**。`i32` 移位**不是** ImmArg，但 ISel 只匹配 ImmLeaf，故经 `CallDescriptor` ImmediateArguments 原样重放，不得进整数虚拟寄存器。向量源／结果走既有向量虚拟寄存器。不新增操作码，无最后令牌。调用点必须是 `CallingConv::C`、非可变参数、形参／实参／结果精确匹配。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-sat-narrow-shift-semantic.ll`（O0/O2×97/7）覆盖六 ID 与三宽正例（含边界立即数 1／8／16／32）、同 ID 加 `shufflevector` 的高半序列、AArch64 `llc`／`readobj`／汇编（`sqshrn`／`uqshrn`／`sqrshrn`／`uqrshrn`／`sqshrun`／`sqrshrun` 的 `.8b`／`.4h`／`.2s`），以及动态移位／立即数 0／越界／`<4 x i8>`／SVE `sqshrnb`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好的标量 `i32(i64)` 见下一节。虚拟化后调用与洗牌拆开，不要求汇编一定出现 `sqshrn2`。宿主不能选择这些内建，故无 `lli`。
- 拒绝：非饱和 `rshrn`（独立表面）／`shrn`（无 IR ID）、同宽饱和／可变移位 `sqshl`／`sshl`（独立表面）、立即 `sqshlu`、标量 `i32(i64)`（独立表面）、`i16(i32)`／`i8(i16)`（无 ISel）、动态或非 `i32` 常量移位、立即数 0 或超出 ImmLeaf、`<4 x i8>`／256 位、`half`／`bfloat`／浮点、SVE `sqshrnb`／`sqshrnt`／`sqrshrnb` 等、发明的 `sqshrn2` ID、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 标量饱和变窄移位 sqshrn／uqshrn／sqrshrn／uqrshrn／sqshrun／sqrshrun 支持边界**

- 接受：仅 AArch64。同一六 ID 的标量形态。clang `vqshrnd_n_s64`／`vqshrnd_n_u64`／`vqrshrnd_n_s64`／`vqrshrnd_n_u64`／`vqshrund_n_s64`／`vqrshrund_n_s64`。ISel `SIMDScalarRShiftBHS`（基线 HasNEON）**只匹配** `i32(i64, i32)`，立即数 `vecshiftR32` **1..32**。汇编 `sqshrn s, d, #imm` 等。`i16(i32)`／`i8(i16)` 编码存在但 pattern 为空（ISel fatal）；clang `vqshrns_n_s32`／`vqshrnh_n_s16` 走 `Use64BitVectors`。**不得**降为 `scalar.sqxtn` 或普通 `lshr`＋`trunc`。`SMAX>>1` 保持 `sqshrn s, d, #1`（不折成常量）。`3>>1`：`sqshrn` 截断、`sqrshrn` 舍入，LLVM 15 均不折叠。`sqshrun`／`sqrshrun` 为有符号→无符号：负数饱和到 0（`-1` 保持 `sqshrun s, d, #1`，不折成 0）。标量 `rshrn.i32` **无 ISel**（`CannotYetSelect`）。`i32` 移位不是 ImmArg，经既有 `isCallImmediateArgument` 走 ImmediateArguments。无最后令牌，资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、形参／实参／结果精确匹配。经既有 `CallDescriptor` 与整数虚拟寄存器原样重放。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-scalar-sat-narrow-shift-semantic.ll`（O0/O2×97/7）覆盖六 ID、立即数 1／3／32、`sqshrun` 负数饱和、AArch64 `llc`／`readobj`／汇编（`sqshrn`／`uqshrn`／`sqrshrn`／`uqrshrn`／`sqshrun`／`sqrshrun`），以及 `i16`／动态／立即数 0／越界 `#33`／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的向量饱和变窄移位见上一节。非饱和标量 `rshrn` 无 ISel，仍拒绝。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`i16(i32)`／`i8(i16)`、向量饱和变窄移位（独立表面）、非饱和 `rshrn`（标量 `CannotYetSelect`）、无移位的 `scalar.sqxtn`、动态／0／越界立即、SVE、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 非饱和舍入变窄移位 rshrn 支持边界**

- 接受：仅 AArch64。精确且唯一的非饱和舍入变窄 IR ID `llvm.aarch64.neon.rshrn`（`AdvSIMD_2Arg_Scalar_Narrow`：结果 `anyint`，源为 `LLVMExtendedType`，移位为 `i32`）。**没有** `llvm.aarch64.neon.shrn`（`SHRN` 是 `trunc`+`ashr` ISel）也**没有** `rshrn2` ID。clang `vrshrn_high_n` 是 `IOpInst`：同 ID 再 `concat_vectors`／洗牌。ISel `SIMDVectorRShiftNarrowBHS`（基线 HasNEON）仅低半：`<8 x i8>(<8 x i16>, i32)` 立即数 `vecshiftR16Narrow` **1..8**；`<4 x i16>(<4 x i32>, i32)` 为 `vecshiftR32Narrow` **1..16**；`<2 x i32>(<2 x i64>, i32)` 为 `vecshiftR64Narrow` **1..32**。源元素半宽立即数（8／16／32）另有 `RADDHN` 匹配，仍是同一 IR ID。`i32` 移位**不是** ImmArg，经 `CallDescriptor` ImmediateArguments 原样重放。向量源／结果走既有向量虚拟寄存器。不新增操作码，无最后令牌。调用点必须是 `CallingConv::C`、非可变参数、形参／实参／结果精确匹配。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-rshrn-semantic.ll`（O0/O2×97/7）覆盖三宽正例（立即数 3／9／19／1）、同 ID 加 `shufflevector` 的高半序列、AArch64 `llc`／`readobj`／汇编（`rshrn` `.8b`／`.4h`／`.2s`），以及动态移位／立即数 0／越界／标量 `i32(i64)`／`<4 x i8>`／SVE `rshrnb`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好的饱和 `sqshrn`／`uqshrn` 等见独立表面，不得留在本 lit 当负例。虚拟化后调用与洗牌拆开，不要求汇编一定出现 `rshrn2`／`raddhn`。宿主不能选择该内建，故无 `lli`。
- 拒绝：饱和 `sqshrn`／`uqshrn`／`sqrshrn`／`uqrshrn`／`sqshrun`／`sqrshrun`（独立表面）、发明的 `shrn`／`rshrn2` ID、标量（无向量 `RSHRN` 以外的 ISel 合同）、动态或非 `i32` 常量移位、立即数 0 或超出 ImmLeaf、`<4 x i8>`／256 位、`half`／`bfloat`／浮点、SVE `rshrnb`／`rshrnt`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 移位插入 vsli／vsri 支持边界**

- 审查：clang `vsli_n`／`vsri_n` 是 `WInst`，`CGBuiltin` 发精确 ID `llvm.aarch64.neon.vsli`／`vsri`，**不是** 通用 `shl`／`lshr`／`or`。ARM32 才走 `arm_neon_vshiftins`。ISel 另可把 `or(and(x,C1), shl/lshr(y,C2))` 折成 `AArch64ISD::VSLI`／`VSRI`，那条前端形态已由既有整数向量 `and`／`or`／移位覆盖，不得再发明 ID 或复制一条调用表面。
- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.neon.vsli`／`vsri`（`AdvSIMD_3VectorArg_Scalar`：两个同型向量 + `i32`）。ISel `LowerINTRINSIC_WO_CHAIN` 要求常量立即数且 `<=` 元素位宽，再匹配 `SIMDVectorLShiftBHSDTied`／`SIMDVectorRShiftBHSDTied`（基线 HasNEON）：`<8 x i8>`／`<16 x i8>`／`<4 x i16>`／`<8 x i16>`／`<2 x i32>`／`<4 x i32>`／`<2 x i64>`。`vsli` 立即数为 `vecshiftL`：**0 .. EltBits-1**；`vsri` 为 `vecshiftR`：**1 .. EltBits**。`i32` 移位**不是** ImmArg，经 `CallDescriptor` ImmediateArguments 原样重放。两向量走既有向量虚拟寄存器。不新增操作码，无最后令牌。调用点必须是 `CallingConv::C`、非可变参数、形参／实参／结果精确匹配。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-sli-sri-semantic.ll`（O0/O2×97/7）覆盖两 ID 与 64／128 位正例（含边界立即数 0／7／63／1／16／32）、AArch64 `llc`／`readobj`／汇编（`sli`／`sri` 的 `.8b`／`.16b`／`.4h`／`.2d`／`.8h`／`.4s`／`.2s`），以及动态移位／`vsli` 立即数 8／`vsri` 立即数 0／`<1 x i64>`／`<4 x i8>`／SVE `sli`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。宿主不能选择这些内建，故无 `lli`。
- 拒绝：发明的 `sli`／`sri` 无 `v` 前缀 ID、标量／`<1 x i64>`（`SLId`／`SRId`）、动态或非 `i32` 常量移位、`vsli` 立即数 `>= EltBits`、`vsri` 立即数 0 或 `> EltBits`、`<4 x i8>`／256 位、`half`／`bfloat`／浮点、SVE `sli`／`sri`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。通用 `or`+移位不是本族负例。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 饱和变窄 sqxtn／uqxtn／sqxtun 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 向量 ID `llvm.aarch64.neon.sqxtn`／`uqxtn`／`sqxtun`（`AdvSIMD_1VectorArg_Narrow`：结果 `anyint`，源为 `LLVMExtendedType`）。符号性在 ID 上：`sqxtn` 有符号→有符号，`uqxtn` 无符号→无符号，`sqxtun` 有符号→无符号。**没有** `sqxtn2`／`uqxtn2`／`sqxtun2` IR ID。clang `vqmovn_high` 是 `SOpInst` `OP_QXTN`：`vcombine` + 同一 ID。ISel `SIMDMixedTwoVector`（基线 HasNEON）仅低半：`<8 x i8>(<8 x i16>)`、`<4 x i16>(<4 x i32>)`、`<2 x i32>(<2 x i64>)`。非饱和 `XTN` 是 `trunc` ISel，不是 IR ID。标量 `scalar.sqxtn`／`scalar.uqxtn`／`scalar.sqxtun` 见独立表面。无立即数。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码，无最后令牌。调用点必须是 `CallingConv::C`、非可变参数、一实参且形参／实参／结果精确匹配。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-sat-narrow-semantic.ll`（O0/O2×97/7）覆盖三 ID 三宽正例、同 ID 加 `shufflevector` 的高半序列、AArch64 `llc`／`readobj`／汇编（`sqxtn`／`uqxtn`／`sqxtun` 的 `.8b`／`.4h`／`.2s`），以及 `<4 x i8>`／SVE `sqxtnb`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。虚拟化后调用与洗牌拆开，不要求汇编一定出现 `sqxtn2`。既有整数向量 `trunc` 不是本族负例。形态良好的标量饱和变窄见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：标量 `scalar.sqxtn`／`scalar.uqxtn`／`scalar.sqxtun`（独立表面）、发明的高半 ID、非饱和 `trunc`／`XTN`（既有向量截断表面）、饱和／舍入变窄移位 `sqshrn`／`rshrn`（独立表面）、`<4 x i8>`／256 位、`half`／`bfloat`／浮点、SVE `sqxtnb`／`sqxtnt`／`uqxtnb`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 标量饱和变窄 scalar.sqxtn／scalar.uqxtn／scalar.sqxtun 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.neon.scalar.sqxtn`／`scalar.uqxtn`／`scalar.sqxtun`（`AdvSIMD_1IntArg_Narrow`：`anyint (anyint)`）。clang `vqmovnd_s64`／`vqmovnd_u64`／`vqmovund_s64`。`vqmovnh`／`vqmovns` 走向量 `sqxtn`，不是本 ID。ISel `SIMDTwoScalarMixedBHS`（基线 HasNEON）**只匹配** `i32(i64)`：汇编 `sqxtn`／`uqxtn`／`sqxtun s, d`。`i16(i32)`／`i8(i16)` 编码存在但 pattern 为空。**不得**改写成 `trunc` 或向量 `sqxtn`。无立即数、无最后令牌。调用点必须是 `CallingConv::C`、非可变参数、一实参且形参／实参／结果精确匹配。经既有 `CallDescriptor` 与整数虚拟寄存器原样重放，不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-scalar-sat-narrow-semantic.ll`（O0/O2×97/7）覆盖三 ID 正例与边界常量（`sqxtn`：`INT32_MIN-1`／`INT32_MAX+1`；`uqxtn`：`2^32`／`i64 -1`＝`UINT64_MAX`；`sqxtun`：`-1`／`2^32`），AArch64 `llc`／`readobj`／汇编（`sqxtn`／`uqxtn`／`sqxtun` 的 `s, d`），以及 `i16(i32)`／`i8(i16)`／SVE `sqxtnb`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的向量 `sqxtn` 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`i16(i32)`／`i8(i16)`（空 ISel pattern）、向量 `sqxtn`／`uqxtn`／`sqxtun`（独立表面）、非饱和 `trunc`、SVE `sqxtnb`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 乘累加 smlal／umlal／mla（无独立 IR ID）**

- 审查结论：**不实现、不发明 ID**。LLVM 15 `IntrinsicsAArch64.td` **没有** `int_aarch64_neon_smlal`／`umlal`／`smlsl`／`umlsl`／`mla`／`mls`。clang `vmla` 是 `IOpInst` `OP_MLA`＝`add(acc, mul(a,b))`；`vmlal` 是 `SOpInst` `OP_MLAL`＝`add(acc, vmull(a,b))`，其中 `vmull` 已是既有 `llvm.aarch64.neon.smull`／`umull`。ISel `SMLAL`／`UMLAL` 匹配 `add(acc, AArch64smull/umull)`，`MLA`／`MLS` 由 MachineCombine 从通用 `mul`+`add`／`sub` 形成。饱和加倍长乘累加已由 `sqdmull`+`sqadd`／`sqsub` 独立锁定。既有 VMP 整数向量 `add`／`mul` 与 `smull`／`umull` `CallDescriptor` 已覆盖 clang `vmla`／`vmlal`。不得发明 `smlal` CallDescriptor ID。

**当前实际 AArch64 NEON 加宽左移 shll／sshll／ushll 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.neon.shll`（`AdvSIMD_2VectorArg_Scalar_Wide_BySize`：结果加宽向量，单一截断源，无立即数）与 `llvm.aarch64.neon.sshll`／`ushll`（`AdvSIMD_2VectorArg_Scalar_Wide`：同一加宽结果＋`i32` 移位，**不是** ImmArg）。**只接受低半三对**：`<8 x i16>(<8 x i8>)`、`<4 x i32>(<4 x i16>)`、`<2 x i64>(<2 x i32>)`。`shll` 的移位量为源元素宽（8／16／32）。`sshll`／`ushll` 的 `i32` 必须是范围内 `ConstantInt`：`vecshiftL8`＝`0..7`、`vecshiftL16`＝`0..15`、`vecshiftL32`＝`0..31`（含 0）。经既有 `isCallImmediateArgument` 走 `ImmediateArguments`，否则 ISel 看不到 ImmLeaf。**没有** `shll2`／`sshll2`／`ushll2` IR ID：高半是 `extract_high`／`shufflevector` 后再走同一低半 ID，VMP 不接受宽源（`v16i8`／`v8i16`／`v4i32`）直接挂在这些 ID 上。clang `vshll_n` 仍是 `NEONMAP0` 的 `sext`／`zext`＋`shl`（既有向量表面），**不得**把通用移位改写成这些 ID，也**不得**在 VMP 里把这些 ID 降成 `sext`／`zext`＋`shl`。调用点必须是 `CallingConv::C`、非可变参数、形参／实参／结果精确匹配。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放。不新增操作码。无最后令牌。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-shll-semantic.ll`（O0/O2×97/7）覆盖三宽 `shll`、三宽 `sshll`／`ushll` 的 0／上界立即数、高半 extract＋同一 ID、AArch64 `llc`／`readobj`／汇编（`shll`／`sshll`／`ushll`），以及动态／越界 `#8`／`#16`／`#32`／负立即／宽源 `v16i16`／`v4i8`／SVE `sshllb`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。宿主不能选择这些内建，故无 `lli`。
- 拒绝：宽源／高半直接形态、`v4i8`、动态／越界／负立即、SVE `sshllb`／`ushllb`／`sshllt`、标量、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。不得发明 `shll2` CallDescriptor ID。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 扩展浮点乘 fmulx 支持边界**

- 接受：仅 AArch64。精确且唯一的 IR ID `llvm.aarch64.neon.fmulx`（`AdvSIMD_2FloatArg`：两个同型 `anyfloat`，`Commutative`）。**不得**降为普通 `fmul`（`±0`×`±Inf` 与 NaN 语义不同）。**没有** `fmulx.lane`／`laneq` IR ID。clang `vmulx_lane`／`vmulx_laneq` 是 `IOpInst` `OP_MULX_LN`：splat 后再走同一 ID；标量车道是 extract + 标量 `fmulx`。ISel `SIMDThreeSameVectorFP`（基线 HasNEON）：`<2 x float>`／`<4 x float>`／`<2 x double>`；半精度 `<4 x half>`／`<8 x half>` 另需 `HasFullFP16`。索引形态 `SIMDFPIndexed` 匹配第三操作数 `duplane`（`v4f16`／`v8f16` 的 `VectorIndexH` `[0,8)`，`v2f32`／`v4f32` 的 `VectorIndexS` `[0,4)`，`v2f64` 的 `VectorIndexD` `[0,2)`）。半精度函数 `target-features` 最后一次出现的精确令牌必须为 `+fullfp16`；缺失、无令牌、最后 `-fullfp16`、仅 `+fp16fml` 不算。`f32`／`f64` 无额外令牌。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、两实参且结果／形参／实参同型。经既有 `CallDescriptor` 与浮点向量或标量浮点虚拟寄存器原样重放，不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-fmulx-semantic.ll`（O0/O2×97/7）覆盖五向量布局正例、多特征最后 `+fullfp16`、clang 惯用 splat＋`fmulx`、AArch64 `llc`／`readobj`／汇编（`fmulx` `.4h`／`.8h`／`.2s`／`.4s`／`.2d`，`llc` 另加 `-mattr=+fullfp16`），以及半精度无 `+fullfp16`／最后 `-fullfp16`／仅 `+fp16fml`／`v1f64`／`v2f16`／`bfloat`／SVE `fmulx`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。标量 `f16`／`f32`／`f64` 见下一节。形态良好但半精度缺令牌为 `unsupported target feature`。不得发明 `fmulx.lane` CallDescriptor ID。宿主不能选择该内建，故无 `lli`。
- 拒绝：`<1 x double>`、发明的车道 ID、普通 `fmul`、`vcmla`／`vcadd`（独立复数表面）、`bfloat`、`<2 x half>`、SVE `fmulx`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。标量形态见下一节。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 标量扩展浮点乘 fmulx 支持边界**

- 接受：仅 AArch64。同一 IR ID `llvm.aarch64.neon.fmulx` 的标量形态。clang `vmulxs_f32`／`vmulxd_f64`／`vmulxh_f16`；标量车道是 `extractelement` 再走同一 ID。ISel `SIMDFPThreeScalar` `FMULX`（`HasNEONorSME`）：标量 `float`／`double`；半精度标量 `half` 另需 `HasFullFP16`。无舍入 ImmArg：`FMULX` 不是 `FPCR.RMode` 转换，而是特殊乘（`±0`×`±Inf` 为 `±2.0`，不是 NaN）。**不得**降为普通 `fmul`。半精度函数 `target-features` 最后一次出现的精确令牌必须为 `+fullfp16`；缺失、无令牌、最后 `-fullfp16`、仅 `+fp16fml` 不算。`f32`／`f64` 无额外令牌。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、两实参同型。经既有 `CallDescriptor` 与标量浮点虚拟寄存器原样重放。既有浮点调用 FMF 门拒绝带 `FastMathFlags` 的形态。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-scalar-fmulx-semantic.ll`（O0/O2×97/7）覆盖标量 `f32`／`f64`／`half`、多特征最后 `+fullfp16`、extract 车道、`0`×`+Inf` 常量、AArch64 `llc`／`readobj`／汇编（`fmulx` 的 `s`／`d`／`h`，`llc` 另加 `-mattr=+fullfp16`），以及 `nnan ninf`／`<1 x double>`／`bfloat`／无令牌／最后 `-fullfp16`／仅 `+fp16fml`／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的向量 `fmulx` 见上一节。宿主不能选择该内建，故无 `lli`。
- 拒绝：带 `FastMathFlags` 的调用、`<1 x double>`、`bfloat`、普通 `fmul`、向量 `fmulx`（独立表面）、SVE `fmulx`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。形态良好但半精度缺令牌为 `unsupported target feature`。

**当前实际 AArch64 NEON 显式舍入浮点转整数 fcvt\{a,m,n,p\}\{s,u\} 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.neon.fcvtas`／`fcvtau`（就近、平局远离零）、`fcvtms`／`fcvtmu`（向 −∞）、`fcvtns`／`fcvtnu`（就近、平局偶数）、`fcvtps`／`fcvtpu`（向 +∞）。签名 `AdvSIMD_FPToIntRounding`：`anyint(anyfloat)`，一元。**不得**降为普通 `fptosi`／`fptoui`（那是向零截断）。向零的 `fcvtzs`／`fcvtzu` 见下一节。ISel `SIMDTwoVectorFPToInt`（基线 HasNEON）：`<2 x float>`→`<2 x i32>`、`<4 x float>`→`<4 x i32>`、`<2 x double>`→`<2 x i64>`；半精度 `<4 x half>`→`<4 x i16>`／`<8 x half>`→`<8 x i16>` 另需 `HasFullFP16`。半精度函数 `target-features` 最后一次出现的精确令牌必须为 `+fullfp16`；缺失、无令牌、最后 `-fullfp16`、仅 `+fp16fml` 不算。`f32`／`f64` 无额外令牌。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、一实参、结果／源为上述同车道配对。经既有 `CallDescriptor` 重放：浮点向量虚拟寄存器入、整数向量虚拟寄存器出。不新增操作码，无 ImmArg。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-fcvt-rounding-semantic.ll`（O0/O2×97/7）覆盖八 ID 的 `v4f32`、`fcvtns` 五宽、多特征最后 `+fullfp16`、AArch64 `llc`／`readobj`／汇编（`fcvtas`／`fcvtau`／`fcvtms`／`fcvtmu`／`fcvtns`／`fcvtnu`／`fcvtps`／`fcvtpu` 与 `.4s`／`.2s`／`.2d`／`.4h`／`.8h`，`llc` 另加 `-mattr=+fullfp16`），以及半精度无令牌／最后 `-fullfp16`／仅 `+fp16fml`／`v1f64`／`v2f16`／`bfloat`／车道不匹配／SVE `fcvtzs`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好的向零 `fcvtzs`／`fcvtzu` 见独立节。形态良好的标量见下一节（已从本 lit 撤走，以免虚拟化后负例失效）。形态良好但半精度缺令牌为 `unsupported target feature`。宿主不能选择这些内建，故无 `lli`。
- 拒绝：向零 `fcvtzs`／`fcvtzu`（独立表面）、变窄 `fcvtxn`／`sisd.fcvtxn`（独立表面）、定点 `vcvtfp2fxs`／`vcvtfp2fxu`／`vcvtfxs2fp`／`vcvtfxu2fp`（独立表面）、标量 `f16`／`f32`／`f64`（独立表面）、`<1 x double>`、`<2 x half>`、`bfloat`、车道或元素位宽不匹配、SVE `fcvt`／`fcvtzs`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 标量显式舍入浮点转整数 fcvt\{a,m,n,p\}\{s,u\} 支持边界**

- 接受：仅 AArch64。同一八个 IR ID `llvm.aarch64.neon.fcvtas`／`fcvtau`（就近、平局远离零）、`fcvtms`／`fcvtmu`（向 −∞）、`fcvtns`／`fcvtnu`（就近、平局偶数）、`fcvtps`／`fcvtpu`（向 +∞）的标量形态。clang `vcvtas_s32_f32`／`vcvtad_s64_f64`／`vcvtah_s32_f16`。ISel `FPToIntegerUnscaled`（基线 HasNEON）：`i32`／`i64`←`f32`／`f64`；`i32`／`i64`←`f16` 另需 `HasFullFP16`。汇编是标量浮点到通用寄存器：`fcvtas w0, s0`／`fcvtau w0, s0`／`fcvtms`／`fcvtns`／`fcvtps` 以及混宽 `fcvtas x0, d0`／`w0, d0`／`x0, s0`／`w0, h0`，**不是** SIMD `fcvtas s, s`（`SIMDFPTwoScalar` pattern 为空）。**不得**降为普通 `fptosi`／`fptoui`（舍入模式不同；越界饱和，NaN 变为 0）。`i16(f16)` 无标量 Pat（legalizer 崩溃）。`<1 x double>` 走 SIMD `FCVTASv1i64`（`fcvtas d, d`），本表面不收。半精度函数 `target-features` 最后一次出现的精确令牌必须为 `+fullfp16`；缺失、无令牌、最后 `-fullfp16`、仅 `+fp16fml` 不算。`f32`／`f64` 无额外令牌。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、一实参、结果／源为上述配对。经既有 `CallDescriptor` 重放：标量浮点虚拟寄存器入、标量整数虚拟寄存器出。整数结果不是 `FPMathOperator`。不新增操作码，无 ImmArg。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-scalar-fcvt-rounding-semantic.ll`（O0/O2×97/7）覆盖八 ID 的 `i32(f32)`、`fcvtns` 混宽、`i32(f16)`／`i64(f16)`、多特征最后 `+fullfp16`、+Inf 保持活 `fcvtas`、AArch64 `llc`／`readobj`／汇编（`fcvtas`／`fcvtau`／`fcvtms`／`fcvtmu`／`fcvtns`／`fcvtnu`／`fcvtps`／`fcvtpu` 的 `w`／`x`，`llc` 另加 `-mattr=+fullfp16`），以及 `i16(f16)`／无令牌／最后 `-fullfp16`／仅 `+fp16fml`／`<1 x double>`／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的向量舍入见上一节。向零标量 `fcvtzs`／`fcvtzu` 见独立节。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`i16(f16)`（无 Pat）、向量舍入（独立表面）、向零 `fcvtzs`／`fcvtzu`（独立表面）、普通 `fptosi`／`fptoui`、定点 `vcvtfp2fxs`、`<1 x double>`、`bfloat`、SVE、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。形态良好但半精度缺令牌为 `unsupported target feature`。

**当前实际 AArch64 NEON 向零饱和浮点转整数 fcvtzs／fcvtzu 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.neon.fcvtzs`／`fcvtzu`（`AdvSIMD_FPToIntRounding`：`anyint(anyfloat)`，一元）。clang `vcvt*_s`／`vcvt*_u` 发这两 ID，不是普通 `fptosi`／`fptoui`。语义为向零截断且 **AArch64 饱和**：越界饱和到整数极值，NaN 变为 0；LangRef `fptosi`／`fptoui` 越界为 poison，故 **不得**改写。ISel 与舍入族相同的 `SIMDTwoVectorFPToInt` 配对（基线 HasNEON）：`<2 x float>`→`<2 x i32>`、`<4 x float>`→`<4 x i32>`、`<2 x double>`→`<2 x i64>`；半精度 `<4 x half>`→`<4 x i16>`／`<8 x half>`→`<8 x i16>` 另需 `HasFullFP16`。半精度函数 `target-features` 最后一次出现的精确令牌必须为 `+fullfp16`；缺失、无令牌、最后 `-fullfp16`、仅 `+fp16fml` 不算。`f32`／`f64` 无额外令牌。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、一实参、结果／源为上述同车道配对。经既有 `CallDescriptor` 重放：浮点向量虚拟寄存器入、整数向量虚拟寄存器出。不新增操作码，无 ImmArg。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-fcvtz-semantic.ll`（O0/O2×97/7）覆盖两 ID 与五宽、多特征最后 `+fullfp16`、向零／+Inf／qNaN 常量、AArch64 `llc`／`readobj`／汇编（`fcvtzs`／`fcvtzu`，`llc` 另加 `-mattr=+fullfp16`），以及无令牌／最后 `-fullfp16`／仅 `+fp16fml`／`<1 x double>`／`<2 x half>`／`bfloat`／车道不匹配／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的舍入 `fcvtas` 等见独立节。形态良好的标量见下一节（已从本 lit 撤走，以免虚拟化后负例失效）。宿主不能选择这些内建，故无 `lli`。
- 拒绝：标量 `f16`／`f32`／`f64`（独立表面）、`<1 x double>`、`<2 x half>`、`bfloat`、车道或元素位宽不匹配、普通 `fptosi`／`fptoui`（既有向量转换表面，语义不同）、舍入 `fcvtas` 等（独立表面）、定点 `vcvtfp2fxs`（独立表面）、SVE `fcvtzs`／`fcvtzu`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。形态良好但半精度缺令牌为 `unsupported target feature`。

**当前实际 AArch64 NEON 标量向零饱和浮点转整数 fcvtzs／fcvtzu 支持边界**

- 接受：仅 AArch64。同一 IR ID `llvm.aarch64.neon.fcvtzs`／`fcvtzu` 的标量形态。clang `vcvts_s32_f32`／`vcvtd_s64_f64`／`vcvth_s32_f16` 及混宽 `vcvts_s32_f64`／`vcvtd_s64_f32`。ISel `FPToIntegerIntPats`（基线 HasNEON）：`i32`／`i64`←`f32`／`f64`；`i32`／`i64`←`f16` 另需 `HasFullFP16`。汇编是标量浮点到通用寄存器：`fcvtzs w0, s0`／`fcvtzu w0, s0`／`fcvtzs x0, d0`／`fcvtzs w0, d0`／`fcvtzs x0, s0`／`fcvtzs w0, h0`，**不是** SIMD `fcvtzs s, s`（`SIMDFPTwoScalar` 的 FCVTZS pattern 为空）。**不得**降为普通 `fptosi`／`fptoui`（越界饱和到整数极值，NaN 变为 0；LangRef 越界为 poison）。`i16(f16)` 无标量 Pat（legalizer 崩溃）。半精度函数 `target-features` 最后一次出现的精确令牌必须为 `+fullfp16`；缺失、无令牌、最后 `-fullfp16`、仅 `+fp16fml` 不算。`f32`／`f64` 无额外令牌。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、一实参、结果／源为上述配对。经既有 `CallDescriptor` 重放：标量浮点虚拟寄存器入、标量整数虚拟寄存器出。整数结果不是 `FPMathOperator`，不得写 FastMathFlags。不新增操作码，无 ImmArg。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-scalar-fcvtz-semantic.ll`（O0/O2×97/7）覆盖两 ID、同宽与混宽 `i32`／`i64`←`f32`／`f64`、`i32(f16)`／`i64(f16)`、多特征最后 `+fullfp16`、+Inf 保持活 `fcvtzs`、AArch64 `llc`／`readobj`／汇编（`fcvtzs`／`fcvtzu` 的 `w`／`x`，`llc` 另加 `-mattr=+fullfp16`），以及 `i16(f16)`／无令牌／最后 `-fullfp16`／仅 `+fp16fml`／`<1 x double>`／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的向量 `fcvtzs`／`fcvtzu` 见上一节。形态良好的标量显式舍入见独立节。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`i16(f16)`（无 Pat）、向量 `fcvtzs`／`fcvtzu`（独立表面）、普通 `fptosi`／`fptoui`、舍入 `fcvtas` 等（独立表面）、定点 `vcvtfp2fxs`、`<1 x double>`、`bfloat`、SVE `fcvtzs`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。形态良好但半精度缺令牌为 `unsupported target feature`。

**当前实际 AArch64 NEON 定点转换 vcvtfp2fxs／vcvtfp2fxu／vcvtfxs2fp／vcvtfxu2fp 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.neon.vcvtfp2fxs`／`vcvtfp2fxu`（`AdvSIMD_CvtFPToFx`：`anyint(anyfloat, i32)`，浮点→有／无符号定点）与 `vcvtfxs2fp`／`vcvtfxu2fp`（`AdvSIMD_CvtFxToFP`：`anyfloat(anyint, i32)`，定点→浮点）。**不得**降为 `sitofp`／`uitofp`／`fptosi`／`fptoui` 或无立即数的 `fcvtzs`。`i32` 小数位数在 TD 上不是 `ImmArg`，但 ISel 只匹配 `vecshiftR16`／`R32`／`R64` ImmLeaf，故经既有 `CallDescriptor` ImmediateArguments 原样重发射。ISel `SIMDVectorRShiftSD`／`SIMDVectorRShiftToFP`（基线 HasNEON）：`<2 x float>`↔`<2 x i32>` 立即数 1..32、`<4 x float>`↔`<4 x i32>` 立即数 1..32、`<2 x double>`↔`<2 x i64>` 立即数 1..64；半精度 `<4 x half>`↔`<4 x i16>`／`<8 x half>`↔`<8 x i16>` 立即数 1..16，另需 `HasFullFP16`。半精度函数 `target-features` 最后一次出现的精确令牌必须为 `+fullfp16`；缺失、无令牌、最后 `-fullfp16`、仅 `+fp16fml` 不算。`f32`／`f64` 无额外令牌。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、两实参、结果／源为上述同车道配对、立即数为范围内 `i32` `ConstantInt`。经既有 `CallDescriptor` 重放：浮点／整数向量虚拟寄存器加立即数。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-fixedpoint-cvt-semantic.ll`（O0/O2×97/7）覆盖四 ID、五宽、立即数端点、多特征最后 `+fullfp16`、AArch64 `llc`／`readobj`／汇编（带 `#fbits` 的 `fcvtzs`／`fcvtzu`／`scvtf`／`ucvtf`，`llc` 另加 `-mattr=+fullfp16`），以及半精度无令牌／最后 `-fullfp16`／仅 `+fp16fml`／立即数 0／越界／动态立即数／`v1f64`／`v2f16`／`bfloat`／车道不匹配／SVE `fcvtzs`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好但半精度缺令牌为 `unsupported target feature`。显式舍入 `fcvtas` 等见独立表面，不得当作本表面 skip 负例。形态良好的标量见下一节（已从本 lit 撤走，以免虚拟化后负例失效）。宿主不能选择这些内建，故无 `lli`。
- 拒绝：标量 `f16`／`f32`／`f64`（独立表面）、`<1 x double>`、立即数 0／越界／动态、`<2 x half>`、`bfloat`、车道或元素位宽不匹配、无立即数的 `fcvtzs`／`fcvtzu` 与舍入 `fcvtas` 等（独立或既有表面）、SVE、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 标量定点转换 vcvtfp2fxs／vcvtfp2fxu／vcvtfxs2fp／vcvtfxu2fp 支持边界**

- 接受：仅 AArch64。同一四 ID 的标量形态。clang `vcvts_n_s32_f32`／`vcvtd_n_s64_f64`／`vcvth_n_s32_f16`／`vcvts_n_f32_s32`。ISel `SIMDFPScalarRShift` 额外 `Pat`（基线 HasNEON）：`i32(f32)`／`f32(i32)` 立即数 1..32（`vecshiftR32`）、`i64(f64)`／`f64(i64)` 立即数 1..64（`vecshiftR64`）；半精度 `i32(f16)` 1..32、`i64(f16)` 1..64、`f16(i32／i64)` 1..16（`vecshiftR16`），另需 `HasFullFP16`。汇编是 SIMD 标量 `fcvtzs s, s, #n`／`scvtf d, d, #n`／`fcvtzs h, h, #n`，**不是** GPR `fcvtzs w, s, #n`。**不得**降为 `sitofp`／`uitofp`／`fptosi`／`fptoui` 或无立即数的 `fcvtzs`。混宽 `i32(f64)` `CannotYetSelect`；`i16(f16)` legalizer 崩溃。`i32` 小数位不是 ImmArg，经既有 `isCallImmediateArgument` 走 ImmediateArguments。半精度函数 `target-features` 最后一次出现的精确令牌必须为 `+fullfp16`；缺失、无令牌、最后 `-fullfp16`、仅 `+fp16fml` 不算。`f32`／`f64` 无额外令牌。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、两实参、结果／源为上述配对、立即数为范围内 `i32` `ConstantInt`。经既有 `CallDescriptor` 重放：标量浮点／整数虚拟寄存器加立即数。`fx2fp` 浮点结果上的 FastMathFlags 由既有浮点调用门拒绝（`unsupported float call instruction`）。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-scalar-fixedpoint-cvt-semantic.ll`（O0/O2×97/7）覆盖四 ID、`#1`／`#32`／`#64`／`#16` 端点、多特征最后 `+fullfp16`、`1.0` 保持活 `fcvtzs s,#1`、AArch64 `llc`／`readobj`／汇编（`fcvtzs`／`fcvtzu`／`scvtf`／`ucvtf` 的 `s`／`d`／`h` 加 `#n`，`llc` 另加 `-mattr=+fullfp16`），以及 `i16(f16)`／混宽／立即数 0／33／17／动态／无令牌／最后 `-fullfp16`／仅 `+fp16fml`／`<1 x double>`／`nnan`／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的向量定点见上一节。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`i16(f16)`、混宽 `i32(f64)`、立即数 0／越界／动态、向量定点（独立表面）、`<1 x double>`、带 `FastMathFlags` 的 `fx2fp`、无立即数的 `fcvtzs`／舍入 `fcvtas`、SVE、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。形态良好但半精度缺令牌为 `unsupported target feature`。

**当前实际 AArch64 NEON 不精确变窄 fcvtxn／sisd.fcvtxn 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.neon.fcvtxn`（`AdvSIMD_1VectorArg_Expand`）与 `llvm.aarch64.sisd.fcvtxn`（`float(double)`）。语义是 IEEE 754 **就近到奇数**（inexact narrowing），**不得**降为 `fptrunc`／`FCVTN`。LLVM 15 **没有** `neon.fcvtn` IR ID（普通变窄是 `fptrunc`）。ISel `SIMDFPInexactCvtTwoVector`（基线 HasNEON）仅 `<2 x float>(<2 x double>)` → `fcvtxn v.2s, v.2d`。高半 `fcvtxn2` 不是独立 IR ID（`concat_vectors`／`shufflevector` 低半结果）。标量 `sisd.fcvtxn` 是真实 ID，ISel `SIMDInexactCvtTwoScalar` → `fcvtxn s, d`。无最后令牌，资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、一实参且结果／形参／实参类型精确匹配。经既有 `CallDescriptor` 重放：向量走浮点向量虚拟寄存器，标量走标量浮点虚拟寄存器。不新增操作码，无 ImmArg。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-fcvtxn-semantic.ll`（O0/O2×97/7）覆盖向量、标量、高半 concat 正例、AArch64 `llc`／`readobj`／汇编（`fcvtxn` `.2s` 与标量 `s`），以及 `v2i32` 目的／直接 `v4f32`／`half`／`bfloat`／SVE `fcvtxnt`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。普通 `fptrunc` 会虚拟化，不得当作 skip 负例。宿主不能选择这些内建，故无 `lli`。
- 拒绝：发明的 `fcvtxn2` ID、直接 `<4 x float>(<2 x double>)`、整数目的、`half`／`bfloat`、普通 `fptrunc`／`FCVTN`、SVE `fcvtx`／`fcvtxnt`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 半精度／单精度转换 vcvtfp2hf／vcvthf2fp 支持边界**

- 接受：仅 AArch64。精确非重载 LLVM 15 ID `llvm.aarch64.neon.vcvtfp2hf`（固定 `<4 x i16>(<4 x float>)`）与 `llvm.aarch64.neon.vcvthf2fp`（固定 `<4 x float>(<4 x i16>)`）。clang `vcvt_f16_f32`／`vcvt_f32_f16`。结果的 `i16` 是历史半精度位模式，**不是** `<4 x half>`。ISel（基线 HasNEON）：`FCVTNv4i16` → `fcvtn v.4h, v.4s`；`FCVTLv4i16` → `fcvtl v.4s, v.4h`。高半 `fcvtn2`／`fcvtl2` 不是独立 IR ID（`concat`／`extract_subvector` 后再走同一 ID）。**不得**改写成 `fptrunc`／`fpext`。无 `+fullfp16`（操作数是 `i16`，不是 `half`）。无舍入 ImmArg：`FCVTN` 使用当前 `FPCR.RMode`（复位默认 RN、就近偶数）；`FCVTL` 精确（IEEE binary16 是 binary32 子集）。无最后令牌，资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、一实参且形参／结果精确匹配。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码。`vcvthf2fp` 上的 FastMathFlags 由既有浮点调用 FMF 门拒绝。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-fp16-cvt-semantic.ll`（O0/O2×97/7）覆盖两 ID、高半 shuffle、AArch64 `llc`／`readobj`／汇编（`fcvtn`／`fcvtl`），以及 `arm.neon.vcvthf2fp`／SVE `fcvt.f32f16`／FMF／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。普通 `fptrunc`／`fpext` 会虚拟化，不得当作 skip 负例。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`<4 x half>` 结果（该 ID 固定为 `v4i16`）、其它宽度、`llvm.arm.neon.vcvt*`、普通 `fptrunc`／`fpext`、SVE `fcvt`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI、`vcvthf2fp` 上的 FastMathFlags。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON FRINT32／64 向量 frint32z／frint32x／frint64z／frint64x 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.neon.frint32z`／`frint32x`／`frint64z`／`frint64x`（`AdvSIMD_1FloatArg`：一元同型 `anyfloat`）。这是 v8.5-A 把浮点舍入到 32／64 位整数范围仍以浮点表示：`z` 向零，`x` 用当前舍入模式。**不得**降为 `llvm.ceil`／`floor`／`trunc`／`rint`／`round`／`nearbyint`（那些是基线 `FRINTA`／`M`／`P`／`I`／`N`／`X`／`Z`，走既有通用舍入表面）。标量 `llvm.aarch64.frint32z` 等 `.f32` 是既有 `+fptoint` 路径，不并入本表面。ISel `FRIntNNTVector`／`SIMDTwoVectorSD` 在 `HasFRInt3264`／`FeatureFRInt3264`／令牌 `"fptoint"` 下：`<2 x float>`／`<4 x float>`／`<2 x double>`。无半精度 ISel。函数 `target-features` 最后一次出现的精确令牌必须为 `+fptoint`；缺失、无令牌、最后 `-fptoint`、仅 `+v8.5a` 不算。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、一实参且结果／形参／实参同型。经既有 `CallDescriptor` 与浮点向量虚拟寄存器原样重放，不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-frint3264-semantic.ll`（O0/O2×97/7）覆盖四 ID 与三宽、多特征最后 `+fptoint`、AArch64 `llc`／`readobj`／汇编（`frint32z`／`frint32x`／`frint64z`／`frint64x` 的 `.4s`／`.2s`／`.2d`，`llc` 另加 `-mattr=+fptoint`），以及无 `+fptoint`／最后 `-fptoint`／仅 `+v8.5a`／标量 `neon.frint32z.f32`／`half`／`v1f64`／`bfloat`／标量 `aarch64.frint32z.f64`（既有路径仍报 `unsupported frint`）／SVE `frinta`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好但缺 `+fptoint` 为 `unsupported target feature`。通用 `llvm.ceil` 等会虚拟化，不得当作 skip 负例。宿主不能选择这些内建，故无 `lli`。
- 拒绝：标量 `neon.frint*`、标量 `aarch64.frint*.f64`（既有表面未开放）、`half`／`bfloat`／`<1 x double>`、SVE `frinta`／`frintn` 等、通用 `ceil`／`floor`／`rint`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 倒数指数 frecpx 支持边界**

- 接受：仅 AArch64。精确且唯一的 IR ID `llvm.aarch64.neon.frecpx`（`AdvSIMD_1FloatArg`：一元 `anyfloat`）。这是倒数**指数**（保留尾数、对指数取补），**不得**降为 `frecpe`／`fdiv`。clang 只有标量 `vrecpxs`／`vrecpxd`／`vrecpxh`。ISel `SIMDFPTwoScalar` `FRECPX`（`HasNEONorSME`）仅标量 `f32`／`f64`；`FRECPXv1f16` 另需 `HasFullFP16`。**没有** 向量 `FRECPX` 模式（与 `frecpe` 的 `SIMDTwoVectorFP` 不同）。半精度函数 `target-features` 最后一次出现的精确令牌必须为 `+fullfp16`；缺失、无令牌、最后 `-fullfp16`、仅 `+fp16fml` 不算。`f32`／`f64` 无额外令牌。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、一实参且结果／形参／实参同型。经既有 `CallDescriptor` 与标量浮点虚拟寄存器原样重放，不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-frecpx-semantic.ll`（O0/O2×97/7）覆盖标量 `f32`／`f64`／`half` 正例、多特征最后 `+fullfp16`、AArch64 `llc`／`readobj`／汇编（`frecpx` 的 `s`／`d`／`h`，`llc` 另加 `-mattr=+fullfp16`），以及半精度无 `+fullfp16`／最后 `-fullfp16`／仅 `+fp16fml`／向量 `<2 x float>`／`<1 x double>`／`bfloat`／SVE `frecpx`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好但半精度缺令牌为 `unsupported target feature`。形态良好的向量 `frecpe` 见独立估算表面；形态良好的标量 `frecpe`／`frecps` 见独立标量估算／迭代表面。宿主不能选择该内建，故无 `lli`。
- 拒绝：任意定长向量（含 `<2 x float>`／`<4 x float>`／`<2 x double>`／`<4 x half>`，无 ISel）、`<1 x double>`、`bfloat`、`frecpe`／`frecps`／`urecpe`（独立或其它表面）、SVE `frecpx`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 整数倒数估算 urecpe／ursqrte 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.neon.urecpe`／`ursqrte`（`AdvSIMD_1VectorArg`：一元同型 `anyvector`）。这是无符号整数倒数／倒平方根**估算**，**不得**降为 `udiv`／`frecpe`／`frecpx`。clang `vrecpe`／`vrsqrte` 的 `Ui`／`QUi` 走这两 ID；浮点形态走 `frecpe`／`frsqrte`。ISel `SIMDTwoVectorS`（基线 HasNEON）仅 `<2 x i32>`／`<4 x i32>`。**没有** 标量整数 ID。调用点必须是 `CallingConv::C`、非可变参数、一实参且结果／形参／实参同型。经既有 `CallDescriptor` 与整数向量虚拟寄存器原样重放，不新增操作码，无最后令牌。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-int-reciprocal-semantic.ll`（O0/O2×97/7）覆盖两 ID 两宽正例、AArch64 `llc`／`readobj`／汇编（`urecpe`／`ursqrte` 的 `.2s`／`.4s`），以及 `<8 x i16>`／`<2 x i64>`／SVE `urecpe`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好的浮点 `frecpe`／`frecpx` 见独立表面，不得留在本 lit。宿主不能选择这些内建，故无 `lli`。
- 拒绝：标量整数（无此 ID）、`<8 x i16>`／`<8 x i8>`／`<2 x i64>` 等非 ISel 宽度、`frecpe`／`frsqrte`／`frecpx`（独立浮点表面）、SVE `urecpe`／`ursqrte`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 点积 sdot／udot 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.neon.sdot`／`udot`（`AdvSIMD_Dot`）。**没有** `sdot.lane`／`sdot.laneq` IR ID。clang `vdot_lane`／`vdot_laneq` 是 `SOpInst` `OP_DOT_LN`：把 32 位组 splat 后再走 `vdot`。ISel `SIMDThreeSameVectorDot` 在 `HasDotProd`／`FEAT_DotProd` 下：`<2 x i32>(<2 x i32>, <8 x i8>, <8 x i8>)` 对应 `SDOT`／`UDOT` 的 `v8i8`；`<4 x i32>(<4 x i32>, <16 x i8>, <16 x i8>)` 对应 `v16i8`。索引形态 `SDOTlane`／`UDOTlane` 匹配 `sdot(acc, a, bitcast(duplane32))`，`VectorIndexS` 为 `[0,4)`。函数 `target-features` 最后一次出现的精确令牌必须为 `+dotprod`；缺失、无令牌、最后 `-dotprod`、仅 `+i8mm` 不算。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、累加器／结果同型、两字节向量同型且与累加器宽度配对。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-dot-semantic.ll`（O0/O2×97/7）覆盖两宽 `sdot`／`udot`、clang 惯用 splat＋`sdot` 车道形态、多特征最后 `+dotprod`、AArch64 `llc`／`readobj`／汇编（`sdot`／`udot`，`llc` 另加 `-mattr=+dotprod`），以及无 `+dotprod`／最后 `-dotprod`／仅 `+i8mm`／混宽／SVE `sdot`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好的 `usdot` 见独立表面。形态良好的 `smmla`／`ummla`／`usmmla` 见独立 `+i8mm` 表面。形态良好但缺少 `+dotprod` 为 `unsupported target feature`。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`usdot`／`smmla`／`ummla`／`usmmla`（独立 `+i8mm` 表面）／`bfdot`、SVE `sdot`／`sdot.lane`、标量、混宽累加器／字节向量、`half`／`bfloat`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。不得发明 `sdot.lane` CallDescriptor ID。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON i8mm 矩阵乘累加 smmla／ummla／usmmla 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.neon.smmla`／`ummla`／`usmmla`（`AdvSIMD_MatMul`：累加器／结果同型，两源同型）。符号性在 ID 上，不在 LLVM 类型上：`smmla` 为有符号×有符号（ISel `B=0,U=0`），`ummla` 为无符号×无符号（`B=0,U=1`），`usmmla` 为无符号×有符号（`B=1,U=0`）。ISel `SIMDThreeSameVectorMatMul` 在 `HasMatMulInt8`／`FeatureMatMulInt8`／`FEAT_I8MM` 下，令牌为精确 `"i8mm"`，汇编谓词 `"i8mm"`：仅 `<4 x i32>(<4 x i32>, <16 x i8>, <16 x i8>)`，汇编 `Rd.4s, Rn.16b, Rm.16b`。**没有** 64 位 `v2i32`／`v8i8` MMLA ISel（与 `sdot` 的两宽不同）。`usdot` 同属 `HasMatMulInt8` 但是 `AdvSIMD_Dot` 混符号点积，不在本表面。函数 `target-features` 最后一次出现的精确令牌必须为 `+i8mm`；缺失、无令牌、最后 `-i8mm`、仅 `+dotprod`／`+f32mm`／`+f64mm` 不算。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、三实参且形参／实参／结果类型精确匹配。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-i8mm-semantic.ll`（O0/O2×97/7）覆盖三 ID 正例、多特征最后 `+i8mm`、AArch64 `llc`／`readobj`／汇编（`smmla`／`ummla`／`usmmla`，`llc` 另加 `-mattr=+i8mm`），以及无 `+i8mm`／最后 `-i8mm`／仅 `+dotprod`／仅 `+f32mm`／`bfdot`／`bfmmla`（`bfloat` 形参为 `unsupported argument type`）／`half` 布局／`v8i8`／SVE `smmla`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好但缺少 `+i8mm` 为 `unsupported target feature`。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`usdot`（独立 `+i8mm` 点积表面）／`sdot`／`udot`（独立 `+dotprod` 表面）／`bfdot`／`bfmmla`（独立 `+bf16` 表面）、SVE `smmla`／`ummla`／`usmmla`、标量、64 位 `v2i32`＋`v8i8`、`half`／`bfloat`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 混符号点积 usdot 支持边界**

- 接受：仅 AArch64。精确 `llvm.aarch64.neon.usdot`（`AdvSIMD_Dot`）。clang `vusdot`。**没有** `usdot.lane` IR ID；clang `vusdot_lane` 是 splat 后再走同一 ID。ISel `SIMDThreeSameVectorDot` `USDOT` 在 `HasMatMulInt8`／`FEAT_I8MM` 下：`<2 x i32>`＋两个 `<8 x i8>`，`<4 x i32>`＋两个 `<16 x i8>`。混符号：无符号×有符号累加到 `i32`。**不得**改写成 `sdot`／`udot` 或通用乘加。函数 `target-features` 最后一次出现的精确令牌必须为 `+i8mm`；缺失、无令牌、最后 `-i8mm`、仅 `+dotprod` 不算。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、三实参且形参／实参／结果类型精确匹配。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-usdot-semantic.ll`（O0/O2×97/7）覆盖两宽、多特征最后 `+i8mm`、AArch64 `llc`／`readobj`／汇编（`usdot`，`llc` 另加 `-mattr=+i8mm`），以及无令牌／最后 `-i8mm`／仅 `+dotprod`／混宽／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的 `sdot`／`udot` 与 `smmla` 见独立表面。形态良好但缺 `+i8mm` 为 `unsupported target feature`。宿主不能选择该内建，故无 `lli`。
- 拒绝：`sdot`／`udot`（独立 `+dotprod` 表面）、`smmla`／`ummla`／`usmmla`（独立矩阵表面）、SVE `usdot`、标量、混宽、`half`／`bfloat`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。不得发明 `usdot.lane` CallDescriptor ID。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON BF16 点积／矩阵乘累加 bfdot／bfmmla 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.neon.bfdot`（`AdvSIMD_Dot`，重载）与 `llvm.aarch64.neon.bfmmla`（非重载固定 `<4 x float>(<4 x float>, <8 x bfloat>, <8 x bfloat>)`）。**没有** `bfdot.lane` IR ID。clang `vbfdot_lane`／`vbfdot_laneq` 是 `SOpInst` `OP_BFDOT_LN`：把 32 位组 splat 后再走 `vbfdot`。ISel 在 `HasNEON`＋`HasBF16`／`FeatureBF16`／`FEAT_BF16` 下，令牌为精确 `"bf16"`：`SIMDThreeSameVectorBFDot` 为 `<2 x float>`＋两个 `<4 x bfloat>`（`bfdot Rd.2s, Rn.4h, Rm.4h`）与 `<4 x float>`＋两个 `<8 x bfloat>`（`bfdot Rd.4s, Rn.8h, Rm.8h`）；`SIMDThreeSameVectorBF16MatrixMul` 仅 128 位 `bfmmla Rd.4s, Rn.8h, Rm.8h`。函数 `target-features` 最后一次出现的精确令牌必须为 `+bf16`；缺失、无令牌、最后 `-bf16`、仅 `+bf16fml`／`+i8mm`／`+dotprod` 不算。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、三实参且形参／实参／结果类型精确匹配。经既有 `CallDescriptor` 重放：`f32` 累加器／结果走既有定长向量虚拟寄存器，`bfloat` 源走既有 `bfloat` 向量虚拟寄存器（`isSupportedFixedVectorFrameType`），不把 `bfloat` 并入 `isSupportedFixedVectorType`，也不走 `LegalizeBFloatMath`。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-bf16-semantic.ll`（O0/O2×97/7）覆盖两宽 `bfdot`、`bfmmla`、多特征最后 `+bf16`、AArch64 `llc`／`readobj`／汇编（`bfdot` `.2s`／`.4s` 与 `bfmmla`，`llc` 另加 `-mattr=+bf16`），以及无 `+bf16`／最后 `-bf16`／仅 `+bf16fml`／仅 `+i8mm`／仅 `+dotprod`／`smmla`／`sdot`／`usdot`／`half`／`f32` 源／混宽／SVE `bfdot`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好的 `bfmlalb`／`bfmlalt` 见独立 FML 表面。clang 车道 IR 需要 `bfloat`↔`float` 向量 `bitcast`，本表面不开放该转换。形态良好但缺少 `+bf16` 为 `unsupported target feature`（`bfloat` 形参无令牌仍先为 `unsupported argument type`）。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`bfmlalb`／`bfmlalt`（独立 `+bf16` FML 表面）、`sdot`／`udot`／`usdot`、`smmla`／`ummla`／`usmmla`、SVE `bfdot`／`bfmmla`、标量、`half`／`f32` 源替换、混宽累加器／`bfloat` 向量、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。不得发明 `bfdot.lane` CallDescriptor ID。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON BF16 融合乘加长 bfmlalb／bfmlalt 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.neon.bfmlalb`／`bfmlalt`（`AdvSIMD_BF16FML`，非重载固定 `<4 x float>(<4 x float>, <8 x bfloat>, <8 x bfloat>)`）。**没有** `bfmlalb.lane`／`bfmlalt.lane` IR ID。clang `vbfmlalb_lane`／`vbfmlalt_lane` 是 `SOpInst`：把 `bfloat` 车道 splat 后再走同一 ID。ISel `SIMDBF16MLAL`／`SIMDBF16MLALIndex` 在 `HasNEON`＋`HasBF16`／`FeatureBF16` 下，汇编谓词为精确 `"bf16"`：`bfmlalb`／`bfmlalt Rd.4s, Rn.8h, Rm.8h`；索引形态匹配 `bfmlal*(acc, a, duplane16)`，`VectorIndexH` 为 `[0,8)`。**LLVM 15 AArch64 没有** `FeatureBF16FML`／`+bf16fml` 令牌（那只是 TableGen 类名 `AdvSIMD_BF16FML`）；`+fp16fml` 是半精度 FML，与本表面无关。函数 `target-features` 最后一次出现的精确令牌必须为 `+bf16`；缺失、无令牌、最后 `-bf16`、仅 `+bf16fml`／`+i8mm`／`+dotprod` 不算。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、三实参且形参／实参／结果类型精确匹配。经既有 `CallDescriptor` 重放：`f32` 累加器／结果走既有定长向量虚拟寄存器，`bfloat` 源走既有 `bfloat` 向量虚拟寄存器，不把 `bfloat` 并入 `isSupportedFixedVectorType`，也不走 `LegalizeBFloatMath`。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-bf16-fml-semantic.ll`（O0/O2×97/7）覆盖两 ID 正例、clang 惯用 splat＋`bfmlalb`、多特征最后 `+bf16`、AArch64 `llc`／`readobj`／汇编（`bfmlalb`／`bfmlalt`，`llc` 另加 `-mattr=+bf16`），以及无 `+bf16`／最后 `-bf16`／仅 `+bf16fml`／仅 `+i8mm`／仅 `+dotprod`／`smmla`／`sdot`／`usdot`／SVE `bfmlalb`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好的 `bfdot`／`bfmmla` 见独立表面。`half`／`f32` 源替换与 64 位形态在非重载 ID 上为验证器非法。形态良好但缺少 `+bf16` 为 `unsupported target feature`。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`bfdot`／`bfmmla`（独立表面）、`sdot`／`udot`／`usdot`、`smmla`／`ummla`／`usmmla`、SVE `bfmlalb`／`bfmlalt`／`.lane`、标量、单独车道 IR ID（不存在）、`half`／`f32` 源替换（验证器非法）、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。不得发明 `bfmlalb.lane` CallDescriptor ID。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON BF16 转换 bfcvt／bfcvtn／bfcvtn2 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 非重载 ID `llvm.aarch64.neon.bfcvt`（`bfloat(float)`）、`llvm.aarch64.neon.bfcvtn`（`<8 x bfloat>(<4 x float>)`）、`llvm.aarch64.neon.bfcvtn2`（`<8 x bfloat>(<8 x bfloat>, <4 x float>)`）。clang `vcvth_bf16_f32`／`__a64_vcvtq_low_bf16_v`／`vcvtq_high_bf16_v`。ISel：`BFCVT` 在 `HasNEONorSME`＋`HasBF16` 下为 `bfcvt h, s`；`BFCVTN`／`BFCVTN2` 在 `HasNEON`＋`HasBF16` 下为 `bfcvtn v.4h, v.4s`（低 4 道，高半清零）与绑定目的 `bfcvtn2 v.8h, v.4s`（第 0 实参为保留的低半）。ARM `BFCVT` 为 round-to-odd 且 `mayRaiseFPException=1`，**不得**改写成 `fptrunc`／`fpext`、bitcast 或通用向量，也不得走 `LegalizeBFloatFloat`。函数 `target-features` 最后一次出现的精确令牌必须为 `+bf16`；缺失、无令牌、最后 `-bf16`、仅 `+bf16fml`／`+i8mm`／`+dotprod`／`+fullfp16` 不算。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、形参／实参／结果类型精确匹配。经既有 `CallDescriptor` 重放：标量 `bfloat` 结果走既有标量 `bfloat` 虚拟寄存器，`v8bf16` 走既有 `bfloat` 向量虚拟寄存器（`isSupportedFixedVectorFrameType`），`v4f32` 走既有定长向量虚拟寄存器；`bfcvtn2` 必须原样重放绑定低半输入。不把 `bfloat` 并入 `isSupportedFixedVectorType`。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。既有浮点调用 FMF 门拒绝带 `FastMathFlags` 的形态（`unsupported float call instruction`）。`vmp-aarch64-neon-bfcvt-semantic.ll`（O0/O2×97/7）覆盖三 ID 正例、`bfcvtn2` 绑定低半重放、多特征最后 `+bf16`、AArch64 `llc`／`readobj`／汇编（`bfcvt`／`bfcvtn`／`bfcvtn2`，`llc` 另加 `-mattr=+bf16`），以及无 `+bf16`／最后 `-bf16`／仅 `+bf16fml`／poison 低半／SVE `fcvt.bf16f32`／`nnan`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。`half`／`f64`／`v8f32` 替换在非重载 ID 上为验证器非法。形态良好的 `bfdot`／`bfmlalb` 见独立表面。形态良好但缺少 `+bf16` 为 `unsupported target feature`（`bfloat` 形参无令牌仍先为 `unsupported argument type`）。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`bfdot`／`bfmmla`／`bfmlalb`／`bfmlalt`（独立 `+bf16` 表面）、SVE `fcvt.bf16f32`／`fcvtnt_bf16f32`、通用 `fptrunc`／`fpext`（独立 `LegalizeBFloatFloat`，舍入不同）、poison／undef 绑定低半、`half`／`f64`／错误宽度（验证器非法）、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI、带 `FastMathFlags` 的调用。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON FP16 融合乘加／减长 fmlal／fmlsl／fmlal2／fmlsl2 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.neon.fmlal`／`fmlsl`／`fmlal2`／`fmlsl2`（`AdvSIMD_FP16FML`，重载）。`fmlal`／`fmlsl` 为低半，`fmlal2`／`fmlsl2` 为高半。**没有** `fmlal.lane` IR ID。clang `vfmlal_lane_*` 是 splat 后再走同一 ID。ISel `SIMDThreeSameVectorFML`／`SIMDThreeSameVectorFMLIndex` 在 `HasNEON`＋`HasFP16FML`／`FeatureFP16FML`／`FEAT_FHM` 下，令牌为精确 `"fp16fml"`（`FeatureFP16FML` 蕴含 `FeatureFullFP16`，但资格只看 `+fp16fml`）：`<2 x float>`＋两个 `<4 x half>`（`Rd.2s, Rn.2h, Rm.2h`）与 `<4 x float>`＋两个 `<8 x half>`（`Rd.4s, Rn.4h, Rm.4h`）。函数 `target-features` 最后一次出现的精确令牌必须为 `+fp16fml`；缺失、无令牌、最后 `-fp16fml`、仅 `+fullfp16`／`+bf16`／`+i8mm`／`+dotprod` 不算。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、三实参且形参／实参／结果类型精确匹配。经既有 `CallDescriptor` 重放：`f32` 累加器／结果与 `half` 源均走既有定长向量虚拟寄存器（`half` 已在 `isSupportedFixedVectorType`），不把 `bfloat` 并入该谓词。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-fp16-fml-semantic.ll`（O0/O2×97/7）覆盖四 ID 两宽正例、clang 惯用 splat＋`fmlal`、多特征最后 `+fp16fml`、AArch64 `llc`／`readobj`／汇编（`fmlal`／`fmlsl`／`fmlal2`／`fmlsl2` 的 `.2s` 与 `.4s`，`llc` 另加 `-mattr=+fp16fml`），以及无 `+fp16fml`／最后 `-fp16fml`／仅 `+fullfp16`／仅 `+bf16`／仅 `+i8mm`／仅 `+dotprod`／`smmla`／`sdot`／`usdot`／`bfloat` 源／混宽／SVE `fmlalb`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好的 `bfmlalb`／`bfdot` 见独立表面。形态良好但缺少 `+fp16fml` 为 `unsupported target feature`。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`bfmlalb`／`bfmlalt`／`bfdot`／`bfmmla`（独立 `+bf16` 表面）、`sdot`／`udot`／`usdot`、`smmla`／`ummla`／`usmmla`、SVE `fmlalb`／`fmlalt`／`.lane`、标量、单独车道 IR ID（不存在）、`bfloat` 源替换、混宽累加器／半精度向量、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。不得发明 `fmlal.lane` CallDescriptor ID。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 复数融合乘加 vcmla／fcmla 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.neon.vcmla.rot0`／`rot90`／`rot180`／`rot270`（`AdvSIMD_3VectorArg`：三个同型向量）。**旋转在 ID 上**，不是 ImmArg；ISel 把它们映到汇编立即数 `#0`／`#90`／`#180`／`#270`。**没有** `vcmla.lane`／`laneq` IR ID。clang `vcmla_*_lane` 是 splat 后再走同一 ID。ISel `FCMLA` 在 `HasComplxNum`／`FeatureComplxNum`／`FEAT_FCMA` 下，令牌为精确 `"complxnum"`：`<2 x float>`／`<4 x float>`／`<2 x double>`；半精度 `<4 x half>`／`<8 x half>` 另需 `HasFullFP16`。索引形态 `FCMLA_LANE_PATS` 仅匹配第三操作数的 splat：`v4f16` 的 `VectorIndexD` `[0,2)`、`v8f16` 的 `VectorIndexS` `[0,4)`、`v4f32` 的 `VectorIndexD` `[0,2)`；**没有** `v2f32`／`v2f64` 索引 ISel。函数 `target-features` 最后一次出现的精确令牌必须为 `+complxnum`；半精度另需最后 `+fullfp16`。缺失、无令牌、最后 `-complxnum`、仅 `+fullfp16`／`+fp16fml`／`+i8mm`／`+dotprod` 不算。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、三实参且结果／形参／实参同型。经既有 `CallDescriptor` 与浮点向量虚拟寄存器原样重放，旋转留在被调 ID 上，不进 ImmediateArguments。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-fcmla-semantic.ll`（O0/O2×97/7）覆盖五布局 `rot0`、四旋转于 `<4 x float>`、clang 惯用 splat、多特征最后 `+complxnum`、AArch64 `llc`／`readobj`／汇编（`fcmla` `.4h`／`.8h`／`.2s`／`.4s`／`.2d` 与 `#0`／`#90`／`#180`／`#270`，`llc` 另加 `-mattr=+complxnum,+fullfp16`），以及无 `+complxnum`／最后 `-complxnum`／仅 `+fullfp16`／半精度无 `+fullfp16`／仅 `+fp16fml`／仅 `+i8mm`／仅 `+dotprod`／`smmla`／`sdot`／`usdot`／`bfloat`／`v1f64`／SVE `fcmla`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好的 `vcadd` 见独立表面。形态良好但缺令牌为 `unsupported target feature`。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`vcadd`（独立复数加表面）、`bfdot`／`bfmmla`／`bfmlalb`、`sdot`／`udot`／`usdot`、`smmla`、SVE `fcmla`／`fcmla.lane`、标量、单独车道 IR ID（不存在）、`bfloat`、`<1 x double>`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。不得发明 `vcmla.lane` CallDescriptor ID。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 复数加 vcadd／fcadd 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.neon.vcadd.rot90`／`rot270`（`AdvSIMD_2VectorArg`：两个同型向量）。**只有** 90／270；**没有** `rot0`／`rot180` ID。旋转在 ID 上，不是 ImmArg；ISel `complexrotateopodd` 映到汇编 `#90`／`#270`。**没有** `vcadd.lane` IR ID，也没有索引 FCADD ISel。ISel `FCADD` 在 `HasComplxNum`／`FeatureComplxNum`／`FEAT_FCMA` 下，令牌为精确 `"complxnum"`：`<2 x float>`／`<4 x float>`／`<2 x double>`；半精度 `<4 x half>`／`<8 x half>` 另需 `HasFullFP16`。函数 `target-features` 最后一次出现的精确令牌必须为 `+complxnum`；半精度另需最后 `+fullfp16`。缺失、无令牌、最后 `-complxnum`、仅 `+fullfp16`／`+fp16fml`／`+i8mm`／`+dotprod` 不算。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、两实参且结果／形参／实参同型。经既有 `CallDescriptor` 与浮点向量虚拟寄存器原样重放，旋转留在被调 ID 上。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-vcadd-semantic.ll`（O0/O2×97/7）覆盖五布局 `rot90`、`<4 x float>` 的 `rot270`、多特征最后 `+complxnum`、AArch64 `llc`／`readobj`／汇编（`fcadd` `.4h`／`.8h`／`.2s`／`.4s`／`.2d` 与 `#90`／`#270`，`llc` 另加 `-mattr=+complxnum,+fullfp16`），以及无 `+complxnum`／最后 `-complxnum`／仅 `+fullfp16`／半精度无 `+fullfp16`／仅 `+fp16fml`／仅 `+i8mm`／仅 `+dotprod`／`smmla`／`sdot`／`usdot`／`bfloat`／`v1f64`／SVE `fcadd`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好的 `vcmla` 见独立表面。形态良好但缺令牌为 `unsupported target feature`。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`vcmla`（独立表面）、`bfdot`／`bfmmla`／`bfmlalb`、`sdot`／`udot`／`usdot`、`smmla`、SVE `fcadd`、标量、车道 IR ID（不存在）、`bfloat`、`<1 x double>`、`rot0`／`rot180`（无此 ID）、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。不得发明 `vcadd.lane` CallDescriptor ID。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 字节反转／重排 vrev16／vrev32／vrev64 支持边界**

- 审查结论：LLVM 15 **没有** `llvm.aarch64.neon.rev16`／`rev32`／`rev64`（`IntrinsicsAArch64.td` 无此 ID；全树亦无 `llvm.aarch64.neon.rev*`）。clang `arm_neon.td` 的 `VREV16`／`VREV32`／`VREV64`／`VREV64H` 是 `WOpInst`，操作 `OP_REV16`／`OP_REV32`／`OP_REV64` = `shuffle $p0, $p0, (rev N, mask0)`。生成的 `arm_neon.h` 对 `vrev16*`／`vrev32*`／`vrev64*` 发 `__builtin_shufflevector`（普通 `shufflevector`），不是目标内建。AArch64 ISel `isREVMask(M, VT, 16/32/64)` 把这些常量掩码降为 `AArch64ISD::REV16`／`REV32`／`REV64`，再选 `rev16`／`rev32`／`rev64`（例如 `rev16 Vd.8b`／`.16b`，`rev32 Vd.8b`／`.8h`，`rev64 Vd.8b`／`.4h`／`.4s`）。树内唯一 rev 风格 ID 是 SVE：`llvm.aarch64.sve.rev`／`revb`／`revh`／`revw`／`revd`（以及 `sve.rbit`），本表面拒绝。不得发明 NEON rev CallDescriptor ID，也不得为已覆盖的 `shufflevector` 再开一条调用重放路径。
- 接受：仅既有常量掩码 `shufflevector`（源／结果为总原始宽度 1..128 的既有固定向量，元素 `i1`／`i8`／`i16`／`i32`／`i64`／`half`／`f32`／`f64`；`bfloat` 走独立 last-token `+bf16` 洗牌表面）。clang 小端 `vrev16_u8`／`vrev16q_u8`／`vrev32_u8`／`vrev32q_u16`／`vrev64_u8`／`vrev64_u16`／`vrev64q_u32` 的掩码已由该路径虚拟化，解释器 `CreateShuffleVector` 后 AArch64 `llc` 仍选 `rev16`／`rev32`／`rev64`。不新增操作码，不经 `CallDescriptor`。`vmp-aarch64-neon-rev-semantic.ll`（O0/O2×97/7）覆盖上述七个前端整数掩码正例、AArch64 `llc`／`readobj`／汇编，以及 SVE `rev`／`revb`／`revd`／256 位加宽洗牌／无 `+bf16` 的 `bfloat` 洗牌（形参／返回先于洗牌门，跳过原因为 `unsupported return type`）／`musttail`／操作数捆绑负例。clang `vrev64_f32`／`vrev64_f16` 同样是普通 `shufflevector`，已由既有浮点／`half` 向量洗牌覆盖，不另开 ID。宿主 `lli` 不跑本 lit（ISel 锁的是 AArch64 `REV*`；整数洗牌行为见 `vmp-fixed-vector-semantic.ll`）。
- 拒绝：发明的 `llvm.aarch64.neon.rev*`、SVE `rev`／`revb`／`revh`／`revw`／`revd`／`rbit`、标量 `bswap`／`bitreverse`（独立位内建表面）、可伸缩、超过 128 位、无 `+bf16` 的 `bfloat`、指针向量、非 C、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。zip／uzp／trn 等其它前端洗牌仍走通用 `shufflevector`，不是本族负例。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。本审查未改 `Virtualization.cpp`。

**当前实际 AArch64 crypto AES aese／aesd／aesmc／aesimc 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.crypto.aese`／`aesd`／`aesmc`／`aesimc`（不是 `neon.*`）。`IntrinsicsAArch64.td`：`aese`／`aesd` 为 `Crypto_AES_DataKey`，固定 `<16 x i8>(<16 x i8>, <16 x i8>)`；`aesmc`／`aesimc` 为 `Crypto_AES_Data`，固定 `<16 x i8>(<16 x i8>)`。四者均非重载，故标量／`v8i8`／`half`／`bfloat`／256 位不是合法 IR。ISel `AESTiedInst`／`AESInst`（`AESErr`／`AESDrr`／`AESMCrr`／`AESIMCrr`）在 `HasAES`／`FEAT_AES` 下。函数 `target-features` 最后一次出现的精确令牌必须为 `+aes`；缺失、无令牌、最后 `-aes`、仅 `+aes2`、仅 `+crypto` 不算。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、结果与全部实参／形参均为同一 `<16 x i8>`。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-crypto-aes-semantic.ll`（O0/O2×97/7）覆盖四 ID 正例、多特征最后 `+aes`、AArch64 `llc`／`readobj`／汇编（`aese`／`aesd`／`aesmc`／`aesimc`，`llc` 另加 `-mattr=+aes`），以及无 `+aes`／最后 `-aes`／仅 `+aes2`／仅 `+crypto`／SVE `aese`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好的 SHA-1／SHA-256／SM4 见独立表面。形态良好但缺少 `+aes` 为 `unsupported target feature`。宿主不能选择这些内建，故无 `lli`。
- 拒绝：SVE `aese`／`aesd`／`aesmc`／`aesimc`、标量／非 `<16 x i8>` 宽度／`half`／`bfloat`（非重载 ID 上为验证器非法）、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。形态良好的 SHA-1／SHA-256／SM4 见独立表面。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 crypto SHA-1 sha1h／sha1c／sha1p／sha1m／sha1su0／sha1su1 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.crypto.sha1h`／`sha1c`／`sha1p`／`sha1m`／`sha1su0`／`sha1su1`（不是 `neon.*`）。`IntrinsicsAArch64.td`：`sha1h` 为 `Crypto_SHA_1Hash`，固定 `i32(i32)`，ISel `SHAInstSS`；`sha1c`／`sha1p`／`sha1m` 为 `Crypto_SHA_5Hash4Schedule`，固定 `<4 x i32>(<4 x i32>, i32, <4 x i32>)`，ISel `SHATiedInstQSV`；`sha1su0` 为 `Crypto_SHA_12Schedule`，三个 `<4 x i32>`，ISel `SHATiedInstVVV`；`sha1su1` 为 `Crypto_SHA_8Schedule`，两个 `<4 x i32>`，ISel `SHATiedInstVV`。六者均非重载。全部在 `HasSHA2`／`FEAT_SHA1`＋`SHA256` 下（`FeatureSHA2` 描述为 Enable SHA1 and SHA256）。函数 `target-features` 最后一次出现的精确令牌必须为 `+sha2`；缺失、无令牌、最后 `-sha2`、仅 `+sha3`、仅 `+crypto` 不算。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、实参类型等于形参。经既有 `CallDescriptor` 重放：`sha1h` 的 `i32` 走整数虚拟寄存器，`<4 x i32>` 状态走向量虚拟寄存器。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-crypto-sha1-semantic.ll`（O0/O2×97/7）覆盖六 ID 正例、多特征最后 `+sha2`、AArch64 `llc`／`readobj`／汇编（`sha1h`／`sha1c`／`sha1p`／`sha1m`／`sha1su0`／`sha1su1`，`llc` 另加 `-mattr=+sha2`），以及无 `+sha2`／最后 `-sha2`／仅 `+sha3`／仅 `+crypto`／SVE `aese`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好但缺少 `+sha2` 为 `unsupported target feature`。形态良好的 AES、SHA-256 与 SM4 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：AES 等无关 crypto、SVE、标量／非 ISel 宽度／`half`／`bfloat`（非重载 ID 上为验证器非法）、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。形态良好的 SHA-256／SM4 见独立表面。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 crypto SHA-256 sha256h／sha256h2／sha256su0／sha256su1 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.crypto.sha256h`／`sha256h2`／`sha256su0`／`sha256su1`（不是 `neon.*`）。`IntrinsicsAArch64.td`：`sha256h`／`sha256h2` 为 `Crypto_SHA_8Hash4Schedule`，固定三个 `<4 x i32>`，ISel `SHATiedInstQQV`；`sha256su0` 为 `Crypto_SHA_8Schedule`，两个 `<4 x i32>`，ISel `SHATiedInstVV`；`sha256su1` 为 `Crypto_SHA_12Schedule`，三个 `<4 x i32>`，ISel `SHATiedInstVVV`。四者均非重载。全部在 `HasSHA2`／`FEAT_SHA1`＋`SHA256` 下。函数 `target-features` 最后一次出现的精确令牌必须为 `+sha2`；缺失、无令牌、最后 `-sha2`、仅 `+sha3`、仅 `+crypto` 不算。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、结果与全部实参／形参均为同一 `<4 x i32>`。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-crypto-sha256-semantic.ll`（O0/O2×97/7）覆盖四 ID 正例、多特征最后 `+sha2`、AArch64 `llc`／`readobj`／汇编（`sha256h`／`sha256h2`／`sha256su0`／`sha256su1`，`llc` 另加 `-mattr=+sha2`），以及无 `+sha2`／最后 `-sha2`／仅 `+sha3`／仅 `+crypto`／SVE `aese`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好但缺少 `+sha2` 为 `unsupported target feature`。形态良好的 SHA-1、SHA-512、AES 与 SM4 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：SHA-1／AES 等无关 crypto、SVE、标量／非 `<4 x i32>` 宽度／`half`／`bfloat`（非重载 ID 上为验证器非法）、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。形态良好的 SHA-512／SM4 见独立表面。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 crypto SHA-512 sha512h／sha512h2／sha512su0／sha512su1 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.crypto.sha512h`／`sha512h2`／`sha512su0`／`sha512su1`（不是 `neon.*`）。`IntrinsicsAArch64.td`：`sha512h`／`sha512h2`／`sha512su1` 为 `Crypto_SHA512_3Arg`，固定三个 `<2 x i64>`；`sha512su0` 为 `Crypto_SHA512_2Arg`，两个 `<2 x i64>`。ISel `SHA512H`／`SHA512H2`／`SHA512SU0`／`SHA512SU1` 在 `let Predicates = [HasSHA3]` 下（`AArch64.td` `FeatureSHA3` 令牌为 `"sha3"`，描述 Enable SHA512 and SHA3，并蕴含 `FeatureSHA2`）。四者均非重载。函数 `target-features` 最后一次出现的精确令牌必须为 `+sha3`；缺失、无令牌、最后 `-sha3`、仅 `+sha2`、仅 `+crypto` 不算。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、结果与全部实参／形参均为同一 `<2 x i64>`。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-crypto-sha512-semantic.ll`（O0/O2×97/7）覆盖四 ID 正例、多特征最后 `+sha3`、AArch64 `llc`／`readobj`／汇编（`sha512h`／`sha512h2`／`sha512su0`／`sha512su1`，`llc` 另加 `-mattr=+sha3`），以及无 `+sha3`／最后 `-sha3`／仅 `+sha2`／仅 `+crypto`／SVE `aese`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好但缺少 `+sha3` 为 `unsupported target feature`。形态良好的 SHA-1／SHA-256／SHA-3／AES／SM3／SM4 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：SHA-1／SHA-256／AES／SM3／SM4／SVE、标量／非 `<2 x i64>` 宽度／`half`／`bfloat`（非重载 ID 上为验证器非法）、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。形态良好的 SHA-3 `eor3`／`bcax`／`rax1`／`xar` 见独立表面。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 crypto SHA-3 eor3／bcax／rax1／xar 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.crypto.eor3s`／`eor3u`／`bcaxs`／`bcaxu`／`rax1`／`xar`。`IntrinsicsAArch64.td`：`eor3s`／`eor3u`／`bcaxs`／`bcaxu` 为重载 `Crypto_SHA3_3Arg`；本表面只接受 `<2 x i64>`（ISel 另有 `v16i8`／`v8i16`／`v4i32`，仍拒绝）。`rax1` 为 `Crypto_SHA3_2Arg`，固定两个 `<2 x i64>`。`xar` 为 `Crypto_SHA3_2ArgImm`，两个 `<2 x i64>` 加 `i64` `ImmArg`，ISel `XAR` 匹配 `timm0_63`（`[0,64)`）。ISel `EOR3`／`BCAX`／`RAX1`／`XAR` 在 `HasSHA3`／`FEAT_SHA3` 下。函数 `target-features` 最后一次出现的精确令牌必须为 `+sha3`；缺失、无令牌、最后 `-sha3`、仅 `+sha2`、仅 `+crypto` 不算。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、状态实参与结果均为同一 `<2 x i64>`。`xar` 旋转量必须是范围内非负 `i64` `ConstantInt`，经既有 `CallDescriptor` ImmediateArguments 保存。状态走向量虚拟寄存器。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-crypto-sha3-semantic.ll`（O0/O2×97/7）覆盖 `eor3u`／`eor3s`／`bcaxu`／`rax1`／`xar`（立即数 0 与 63）正例、多特征最后 `+sha3`、AArch64 `llc`／`readobj`／汇编（`eor3`／`bcax`／`rax1`／`xar`，`llc` 另加 `-mattr=+sha3`），以及无 `+sha3`／最后 `-sha3`／仅 `+sha2`／仅 `+crypto`／`eor3u.v16i8`／越界或负 `xar`／SVE `rax1`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好但缺少 `+sha3` 为 `unsupported target feature`。形态良好的 SHA-512／SHA-1／SHA-256／AES／SM3／SM4 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：SHA-1／SHA-256／SHA-512／AES／SM3／SM4／SVE、`eor3`／`bcax` 的非 `<2 x i64>` 宽度、越界或负／非 `i64`／非立即 `xar` 旋转量、标量／`half`／`bfloat`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 crypto SM4 sm4e／sm4ekey 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.crypto.sm4e`／`sm4ekey`（不是 `neon.*`，不是 SVE）。`IntrinsicsAArch64.td`：二者均为 `Crypto_SM4_2Vector`，固定 `<4 x i32>(<4 x i32>, <4 x i32>)`。ISel `SM4E`／`SM4ENCKEY`（`SM4_pattern`）在 `HasSM4`／`FEAT_SM4` 下。二者均非重载。函数 `target-features` 最后一次出现的精确令牌必须为 `+sm4`；缺失、无令牌、最后 `-sm4`、仅 `+sha2`、仅 `+crypto` 不算。资格不看命令行 `-mattr`。`FeatureSM4` 在子目标图中同时打开 SM3，但本表面不接受任何 `sm3*` ID。调用点必须是 `CallingConv::C`、非可变参数、结果与两实参／形参均为同一 `<4 x i32>`。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-crypto-sm4-semantic.ll`（O0/O2×97/7）覆盖两 ID 正例、多特征最后 `+sm4`、AArch64 `llc`／`readobj`／汇编（`sm4e`／`sm4ekey`，`llc` 另加 `-mattr=+sm4`），以及无 `+sm4`／最后 `-sm4`／仅 `+sha2`／仅 `+crypto`／SVE `sm4e`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好但缺少 `+sm4` 为 `unsupported target feature`。形态良好的 AES／SHA-1／SHA-256／SM3 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：AES／SHA／SVE `sm4e`／`sm4ekey`、标量／非 `<4 x i32>` 宽度／`half`／`bfloat`（非重载 ID 上为验证器非法）、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。形态良好的 SM3 见独立表面。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 crypto SM3 sm3ss1／partw1／partw2／tt1a／tt1b／tt2a／tt2b 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.crypto.sm3ss1`／`sm3partw1`／`sm3partw2`／`sm3tt1a`／`sm3tt1b`／`sm3tt2a`／`sm3tt2b`（不是 `neon.*`，无 SVE SM3 ID）。`IntrinsicsAArch64.td`：`sm3ss1`／`partw1`／`partw2` 为 `Crypto_SM3_3Vector`，固定三个 `<4 x i32>`，ISel `SM3SS1`／`SM3PARTW1`／`SM3PARTW2`；`tt*` 为 `Crypto_SM3_3VectorIndexed`，三个 `<4 x i32>` 加 `i64` `ImmArg`，ISel `SM3TT*` 匹配 `VectorIndexS`（`[0,4)`）。七者均非重载。全部在 `HasSM4`／`FEAT_SM4` 下（`FeatureSM4` 描述为 Enable SM3 and SM4）。函数 `target-features` 最后一次出现的精确令牌必须为 `+sm4`；缺失、无令牌、最后 `-sm4`、仅 `+sha2`、仅 `+crypto` 不算。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、三状态实参与结果均为同一 `<4 x i32>`。`tt*` 车道必须是范围内非负 `i64` `ConstantInt`，经既有 `CallDescriptor` ImmediateArguments 保存（TD 已标 `ImmArg`）。状态走向量虚拟寄存器；没有独立标量 `i32` SM3 状态。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-crypto-sm3-semantic.ll`（O0/O2×97/7）覆盖七 ID 正例（`tt*` 车道 0／1／2／3）、多特征最后 `+sm4`、AArch64 `llc`／`readobj`／汇编（`sm3ss1`／`sm3partw1`／`sm3partw2`／`sm3tt1a`／`sm3tt1b`／`sm3tt2a`／`sm3tt2b`，`llc` 另加 `-mattr=+sm4`），以及无 `+sm4`／最后 `-sm4`／仅 `+sha2`／仅 `+crypto`／越界或负车道／SVE `sm4e`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。形态良好但缺少 `+sm4` 为 `unsupported target feature`。形态良好的 SM4／AES／SHA 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：SM4／AES／SHA／SVE、越界或负／非 `i64`／非立即 `tt*` 车道、标量／非 `<4 x i32>` 宽度／`half`／`bfloat`（非重载 ID 上为验证器非法）、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 多向量存储支持边界**

- 接受：精确 `llvm.aarch64.neon.st1x2`／`st1x3`／`st1x4` 与 `st2`／`st3`／`st4`，签名与 `IntrinsicsAArch64.td` 的 `AdvSIMD_NVec_Store_Intrinsic` 一致：`void`，N 个同型向量，最后一参为地址空间零指针。向量类型限于 AArch64 指令选择已覆盖的整数与 `f32`／`f64` 实用重载（64／128 位 `i8`／`i16`／`i32`／`i64`／`f32`／`f64`）。调用点必须是 `CallingConv::C`、非可变参数。经既有 `CallDescriptor` 用向量虚拟寄存器与指针虚拟寄存器原样重放，不新增操作码，不要求 `+neon`／`+fullfp16`。`vmp-aarch64-neon-st-semantic.ll` 覆盖两族、2／3／4 路与 64／128 位，以及 `half`／`fastcc`／`musttail`／operand bundle／动态车道 `st2lane`／非 AS0／`arm.neon.vst*`／非 ISel 宽度负例；宿主 `lli` 无法选择这些内建，故不做宿主差分。
- 拒绝：`half` 向量、SVE `st*`、`llvm.arm.neon.vst*`、非地址空间零指针、`bfloat`、`<4 x i8>` 等非 ISel 宽度、可伸缩、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。`st2lane`／`st3lane`／`st4lane` 见下一节。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际 AArch64 NEON 多向量车道存储支持边界**

- 接受：精确 `llvm.aarch64.neon.st2lane`／`st3lane`／`st4lane`，签名与 `AdvSIMD_NVec_Store_Lane_Intrinsic` 一致：`void (vec x N, i64 lane, ptr AS0)`。向量类型与多向量存储／整向量加载相同（64／128 位 `i8`／`i16`／`i32`／`i64`／`f32`／`f64`）。车道为范围内非负 `i64` `ConstantInt`，经 `CallDescriptor` 立即数保存（TD 未标 `ImmArg`，指令选择只匹配立即数）。调用点必须是 `CallingConv::C`、非可变参数。经既有 `CallDescriptor` 用向量虚拟寄存器、车道立即数与指针虚拟寄存器原样重放，不新增操作码，不要求 `+neon`／`+fullfp16`。不改通用聚合帧。`vmp-aarch64-neon-stlane-semantic.ll` 覆盖 2／3／4 路与 64／128 位，以及动态、越界或负车道／`half`／`bfloat`／`fastcc`／`musttail`／operand bundle／非 AS0／`arm.neon.vst2lane`／非 ISel 宽度负例；宿主 `lli` 无法选择这些内建，故不做宿主差分。
- 拒绝：SVE、`llvm.arm.neon.vst*lane`、`half`／`bfloat`、动态、越界或负车道、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。单向量 `st1lane` 见下一节。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际 AArch64 NEON 多向量加载支持边界**

- 接受：精确 `llvm.aarch64.neon.ld1x2`／`ld1x3`／`ld1x4` 与 `ld2`／`ld3`／`ld4`，签名与 `IntrinsicsAArch64.td` 的 `AdvSIMD_NVec_Load_Intrinsic` 一致：返回 N 个同型向量的结构体，单参为地址空间零指针。向量类型与多向量存储相同（64／128 位 `i8`／`i16`／`i32`／`i64`／`f32`／`f64`）。调用点必须是 `CallingConv::C`、非可变参数。每个字段 0..N-1 必须恰好一次单下标 `extractvalue`；元组本身不得进入 `phi`／`select`／`store`／`ret`／`insertvalue`，包括恰好 16 字节的 `{<8 x i8>,<8 x i8>}` 与 `{<4 x i16>,<4 x i16>}`。按保留聚合生产者处理：不给调用分配聚合虚拟寄存器；经既有 `CallDescriptor` 重放后在 `emitCallHandler` 中把全部字段展开到独立向量虚拟寄存器。不新增操作码，不要求 `+neon`／`+fullfp16`。不得把 16 字节或 32–64 字节 NEON 元组塞进既有最多 64 字节的普通扁平聚合帧。`vmp-aarch64-neon-ld-semantic.ll` 覆盖两族、2／3／4 路与 64／128 位（含第二种 16 字节 pair），以及整体元组／`select`／部分或重复 `extractvalue`／死加载／partial `ld2lane`／`half`／`bfloat`／`fastcc`／`musttail`／operand bundle／非 AS0／`arm.neon.vld*`／非 ISel 宽度负例；宿主 `lli` 无法选择这些内建，故不做宿主差分。
- 拒绝：`half` 向量、SVE `ld*`、`llvm.arm.neon.vld*`、非地址空间零指针、`bfloat`、`<4 x i8>` 等非 ISel 宽度、可伸缩、整体使用元组、缺字段或重复提取、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。`ld2r`／`ld3r`／`ld4r` 见下一节；`ld2lane`／`ld3lane`／`ld4lane` 见再下一节。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际 AArch64 NEON 多向量复制加载支持边界**

- 接受：精确 `llvm.aarch64.neon.ld2r`／`ld3r`／`ld4r`，与 `ld1xN`／`ldN` 同一 `AdvSIMD_NVec_Load_Intrinsic`：`{vec x N} (ptr AS0)`。语义是复制加载（每路一个标量再广播），资格与展开与 `ld1xN`／`ldN` 相同：64／128 位 `i8`／`i16`／`i32`／`i64`／`f32`／`f64`，每个字段 0..N-1 恰好一次单下标 `extractvalue`，元组不得进入 `phi`／`select`／`store`／`ret`／`insertvalue`（含 16 字节 pair）。按保留聚合生产者经既有 `CallDescriptor` 展开到独立向量虚拟寄存器，不进 64 字节聚合帧。调用点必须是 `CallingConv::C`、非可变参数。`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI 拒绝（普通 `tail` 按 `TCK_None` 重放）。不新增操作码，不要求 `+neon`／`+fullfp16`。`vmp-aarch64-neon-ldr-semantic.ll`（O0/O2×97/7）覆盖 2／3／4 路与 64／128 位整数及 `f32`／`f64` 正例，以及 `musttail`／operand bundle／`fastcc`／`half`／`bfloat`／`<4 x i8>`／非 AS0／部分或重复或死提取／整体元组／`arm.neon.vld2` 负例；AArch64 `llc`／`readobj`／汇编确认 `ld2r`／`ld3r`／`ld4r`。宿主 `lli` 无法选择这些内建，故不做宿主差分。
- 拒绝：`half`／`bfloat`、SVE、`llvm.arm.neon.vld*`、非 AS0、非 ISel 宽度、整体使用元组、缺字段或重复提取、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际 AArch64 NEON 多向量车道加载支持边界**

- 接受：精确 `llvm.aarch64.neon.ld2lane`／`ld3lane`／`ld4lane`，签名与 `AdvSIMD_NVec_Load_Lane_Intrinsic` 一致：`{vec x N} (vec x N, i64 lane, ptr AS0)`。向量类型与多向量存储／整向量加载相同（64／128 位 `i8`／`i16`／`i32`／`i64`／`f32`／`f64`）。车道为范围内非负 `i64` `ConstantInt`，经 `CallDescriptor` 立即数保存（TD 未标 `ImmArg`，指令选择只匹配立即数）。调用点必须是 `CallingConv::C`、非可变参数。每个字段 0..N-1 必须恰好一次单下标 `extractvalue`；元组本身不得进入 `phi`／`select`／`store`／`ret`／`insertvalue`，包括 16 字节 `{<8 x i8>,<8 x i8>}` 与 `{<4 x i16>,<4 x i16>}`。按保留聚合生产者展开到独立向量虚拟寄存器，不进 64 字节聚合帧。不新增操作码，不要求 `+neon`／`+fullfp16`。`vmp-aarch64-neon-ldlane-semantic.ll` 覆盖 2／3／4 路与 64／128 位（含第二种 16 字节 pair），以及部分／重复／死提取／整体元组／`phi`／`select`／`store`／`insertvalue`／动态、越界或负车道／`half`／`bfloat`／`fastcc`／`musttail`／operand bundle／非 AS0／`arm.neon.vld2lane` 负例；宿主 `lli` 无法选择这些内建，故不做宿主差分。
- 拒绝：SVE、`llvm.arm.neon.vld*lane`、`half`／`bfloat`、动态、越界或负车道、整体使用元组、缺字段或重复提取、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。复制加载 `ld2r`／`ld3r`／`ld4r` 见上一节。单向量 `ld1lane` 见下一节。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际 AArch64 NEON 单向量车道加载／存储支持边界**

- 接受：精确 `llvm.aarch64.neon.ld1lane` 与 `llvm.aarch64.neon.st1lane`，签名与 `AdvSIMD_1Vec_Load_Lane_Intrinsic`／既有 `AdvSIMD_1Vec_Store_Lane_Intrinsic` 一致：`vec (vec, i64 lane, ptr AS0)` 与 `void (vec, i64 lane, ptr AS0)`。结果是更新后的单一向量，不是元组，不走 `extractvalue` 展开，也不进 64 字节聚合帧。向量类型与 `ld2/3/4lane`／`st2/3/4lane` 相同（64／128 位 `i8`／`i16`／`i32`／`i64`／`f32`／`f64`）。车道为范围内非负 `i64` `ConstantInt`，经 `CallDescriptor` 立即数保存。调用点必须是 `CallingConv::C`、非可变参数。`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI 拒绝。经既有 `CallDescriptor` 用向量虚拟寄存器、车道立即数与指针虚拟寄存器原样重放，不新增操作码，不要求 `+neon`／`+fullfp16`。`vmp-aarch64-neon-ldst1lane-semantic.ll`（O0/O2×97/7）覆盖 64／128 位整数与 `f32`／`f64` 正例，以及动态、越界或负车道／`half`／`bfloat`／`<4 x i8>`／`fastcc`／`musttail`／operand bundle／非 AS0／`arm.neon.vld1lane` 负例；AArch64 `llc`／`readobj`／汇编确认 `ld1`／`st1`。宿主 `lli` 无法选择这些内建，故不做宿主差分。
- 拒绝：`half`／`bfloat`、SVE、`llvm.arm.neon.vld*lane`／`vst*lane`、动态、越界或负车道、非 AS0、非 ISel 宽度、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际 AArch64 NEON 饱和整数加减支持边界**

- 接受：精确 `llvm.aarch64.neon.sqadd`／`uqadd`／`sqsub`／`uqsub`，签名与 `AdvSIMD_2IntArg_Intrinsic` 一致：`anyint (match, match)`，且进一步要求结果与两个实参为同一实用 ISel 整数向量（64 位 `<8 x i8>`／`<4 x i16>`／`<2 x i32>` 或 128 位 `<16 x i8>`／`<8 x i16>`／`<4 x i32>`／`<2 x i64>`）。调用点必须是 `CallingConv::C`、非可变参数。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码，不要求 `+neon`。`vmp-aarch64-neon-satint-semantic.ll` 覆盖四个 ID 与 64／128 位，以及 `<1 x i64>`／`<4 x i8>`／SVE／`fastcc`／`musttail`／operand bundle 负例。标量 `i32`／`i64` 见下一节。形态良好的混符号饱和累加 `suqadd`／`usqadd` 见后续节；形态良好的减半加见后续节。宿主 `lli` 无法选择这些内建，故不做宿主差分。
- 拒绝：标量 `i8`／`i16`（无 ISel）、标量 `i32`／`i64`（独立表面）、`<1 x i64>`、混符号 `suqadd`／`usqadd`（独立表面）、减半加 `shadd`／`uhadd`／`srhadd`／`urhadd`（独立表面）及其它 NEON 算术（饱和加倍乘高 `sqdmulh`／`sqrdmulh`、饱和可变移位 `sqshl`／`uqshl` 与饱和绝对值／取负 `sqabs`／`sqneg` 见独立表面）、SVE、`half`／`bfloat`／浮点、`<4 x i8>` 等非 ISel 宽度、可伸缩、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际 AArch64 NEON 标量饱和加减 sqadd／uqadd／sqsub／uqsub 支持边界**

- 接受：仅 AArch64。同一组 LLVM 15 ID `llvm.aarch64.neon.sqadd`／`uqadd`／`sqsub`／`uqsub` 的标量形态。clang `vqadds_s32`／`vqaddd_s64`／`vqadds_u32`／`vqsubs_*`。ISel `SIMDThreeScalarBHSD`（基线 HasNEON）**只匹配** `i32(i32,i32)` 与 `i64(i64,i64)`。`i8`／`i16` 编码存在但 pattern 为空。clang `vqaddb_s8`／`vqaddh_s16` 会向量化（`Use64BitVectors`）。**不得**降为 `llvm.sadd.sat` 或普通 `add`。无最后令牌，资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、两实参同型。经既有 `CallDescriptor` 与整数虚拟寄存器原样重放。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-scalar-satint-semantic.ll`（O0/O2×97/7）覆盖四 ID 的 `i32`／`i64`、`SMAX+1` 饱和常量、AArch64 `llc`／`readobj`／汇编（`sqadd`／`uqadd`／`sqsub`／`uqsub`），以及 `i8`／`i16`／`<1 x i64>`／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的向量饱和加减见上一节。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`i8`／`i16`、`<1 x i64>`、向量饱和加减（独立表面）、通用 `llvm.sadd.sat`（既有表面）、SVE、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 混符号饱和累加 suqadd／usqadd 支持边界**

- 接受：仅 AArch64。精确 ISel 已覆盖的 `llvm.aarch64.neon.suqadd`／`usqadd`（`AdvSIMD_2IntArg`，二元同型）：`<8 x i8>`、`<16 x i8>`、`<4 x i16>`、`<8 x i16>`、`<2 x i32>`、`<4 x i32>`、`<2 x i64>`，对应 `SIMDTwoVectorBHSDTied`（基线 HasNEON）。clang `vuqadd`→`suqadd`、`vsqadd`→`usqadd`。第 0 实参是绑定累加器（结果寄存器），第 1 实参是另一符号加数：`suqadd` 为有符号累加无符号值并按有符号饱和；`usqadd` 为无符号累加有符号值并按无符号饱和。调用点必须是 `CallingConv::C`、非可变参数、实参类型等于形参。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放（保持操作数顺序），不新增操作码，**不得**改写成 `add`／`min`／`max`／扩展或截断。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-suqadd-semantic.ll`（O0/O2×97/7）覆盖两 ID 七宽正例、饱和边界常量（有符号最大＋1、无符号最大＋1、0＋-1）、AArch64 `llc`／`readobj`／汇编（`suqadd`／`usqadd`），以及 `<1 x i64>`／`v4i8`／256 位 `v4i64`／`i1`／SVE `suqadd`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。标量 `i32`／`i64` 见下一节。形态良好的同符号 `sqadd`／`uqadd` 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：标量 `i8`／`i16`（无 ISel）、标量 `i32`／`i64`（独立表面）、`<1 x i64>`、`<4 x i8>`、256 位、`i1`、同符号 `sqadd`／`uqadd`（独立表面）、`add`／`smin`／`smax`／`sext`／`zext` 合成、SVE `suqadd`／`usqadd`、`half`／`bfloat`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 标量混符号饱和累加 suqadd／usqadd 支持边界**

- 接受：仅 AArch64。同一 IR ID `llvm.aarch64.neon.suqadd`／`usqadd` 的标量形态。clang `vuqadds_s32`／`vuqaddd_s64`→`suqadd`，`vsqadds_u32`／`vsqaddd_u64`→`usqadd`。ISel `SIMDTwoScalarBHSDTied`（基线 HasNEON）**只匹配** `i32(i32,i32)` 与 `i64(i64,i64)`。`i8`／`i16` 编码存在但 pattern 为空。第 0 实参是绑定累加器，必须保持操作数顺序。**不得**改写成 `add`／`min`／`max`。无最后令牌，资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、两实参同型。经既有 `CallDescriptor` 与整数虚拟寄存器原样重放。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-scalar-suqadd-semantic.ll`（O0/O2×97/7）覆盖两 ID 的 `i32`／`i64`、`SMAX+1` 饱和常量、AArch64 `llc`／`readobj`／汇编（`suqadd`／`usqadd`），以及 `i8`／`i16`／`<1 x i64>`／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的向量 `suqadd` 见上一节。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`i8`／`i16`、`<1 x i64>`、向量混符号累加（独立表面）、同符号 `sqadd`（独立表面）、`add`／`smin`／`smax`、SVE、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 减半加 shadd／uhadd／srhadd／urhadd 支持边界**

- 接受：仅 AArch64。精确 ISel 已覆盖的 `llvm.aarch64.neon.shadd`／`uhadd`／`srhadd`／`urhadd`（`AdvSIMD_2VectorArg`，二元同型）：`<8 x i8>`、`<16 x i8>`、`<4 x i16>`、`<8 x i16>`、`<2 x i32>`、`<4 x i32>`，对应 `SIMDThreeSameVectorBHS`（基线 HasNEON）。clang `vhadd`／`vrhadd`。无 `i64` 元素（BHS 不含 D）。AArch64 ISel 把四 ID 降为 `AVGFLOORS`／`AVGFLOORU`／`AVGCEILS`／`AVGCEILU`，再选 `shadd`／`uhadd`／`srhadd`／`urhadd`。`shadd`／`uhadd` 向负无穷取整（截断）；`srhadd`／`urhadd` 向正无穷取整（舍入）。调用点必须是 `CallingConv::C`、非可变参数、实参类型等于形参。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码，**不得**改写成 `add`／移位或通用算术。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-hadd-semantic.ll`（O0/O2×97/7）覆盖四 ID 与全部六宽 BHS 正例、AArch64 `llc`／`readobj`／汇编（`shadd`／`uhadd`／`srhadd`／`urhadd`），以及 `v2i64`／`v1i32`／`v4i8`／256 位 `v8i32`／`i1`／SVE `shadd`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的饱和 `sqadd` 见上一节；形态良好的减半减 `shsub`／`uhsub` 见下一节。宿主不能选择这些内建，故无 `lli`。
- 拒绝：标量、`<1 x i32>`／`<1 x i64>`、`i64` 元素、`<4 x i8>`、256 位、`i1`、减半减 `shsub`／`uhsub`（独立表面）、饱和 `sqadd`／`uqadd`（独立表面）、`add`+移位合成、SVE `shadd`／`uhadd`／`srhadd`／`urhadd`、`half`／`bfloat`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 减半减 shsub／uhsub 支持边界**

- 接受：仅 AArch64。精确 ISel 已覆盖的 `llvm.aarch64.neon.shsub`／`uhsub`（`AdvSIMD_2VectorArg`，二元同型）：`<8 x i8>`、`<16 x i8>`、`<4 x i16>`、`<8 x i16>`、`<2 x i32>`、`<4 x i32>`，对应 `SIMDThreeSameVectorBHS`（基线 HasNEON）。clang `vhsub`。无 `i64` 元素（BHS 不含 D）。LLVM 15 **没有**舍入减半减 `srhsub`／`urhsub` IR ID。ISel 直接匹配内建 ID（不是 `AVG*`）。语义为有符号／无符号减半再截断（`(a-b)/2` 向负无穷／零方向按目标约定），**不得**改写成 `sub`／移位或通用算术。调用点必须是 `CallingConv::C`、非可变参数、实参类型等于形参。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-hsub-semantic.ll`（O0/O2×97/7）覆盖两 ID 与全部六宽 BHS 正例、AArch64 `llc`／`readobj`／汇编（`shsub`／`uhsub`），以及 `v2i64`／`v1i32`／`v4i8`／256 位 `v8i32`／`i1`／SVE `shsub`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的减半加 `shadd`／`uhadd` 与饱和 `sqsub` 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：标量、`<1 x i32>`／`<1 x i64>`、`i64` 元素、`<4 x i8>`、256 位、`i1`、发明的 `srhsub`／`urhsub`、减半加 `shadd`／`uhadd`（独立表面）、饱和 `sqsub`／`uqsub`（独立表面）、`sub`+移位合成、SVE `shsub`／`uhsub`／`shsubr`／`uhsubr`、`half`／`bfloat`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 饱和绝对值／取负 sqabs／sqneg 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.neon.sqabs`／`sqneg`（`AdvSIMD_1IntArg`：一元同型 `anyint`）。这是饱和绝对值／取负，**不得**降为 `llvm.abs` 或普通 `sub`。ISel `SIMDTwoVectorBHSD`（基线 HasNEON）与 `sqadd` 相同：`<8 x i8>`／`<16 x i8>`／`<4 x i16>`／`<8 x i16>`／`<2 x i32>`／`<4 x i32>`／`<2 x i64>`。有符号最小值：`sqabs` 与 `sqneg` 均饱和到有符号最大值（ARM `SignedSatQ`；LLVM 15 不折叠常量 `INT_MIN`，汇编保持 `sqabs`／`sqneg`）。调用点必须是 `CallingConv::C`、非可变参数、一实参且结果／形参／实参同型。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码，无最后令牌。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-sat-absneg-semantic.ll`（O0/O2×97/7）覆盖两 ID 与 64／128 位正例、`i8`／`i16` 有符号最小常量输入、AArch64 `llc`／`readobj`／汇编（`sqabs`／`sqneg` 的 `.8b`／`.16b`／`.4h`／`.4s`／`.2d`／`.8h`／`.2s`），以及 `<1 x i64>`／`<4 x i8>`／SVE `sqabs`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice` 负例。标量 `i32`／`i64` 见下一节。通用 `llvm.abs`／向量取负不是本族负例。形态良好的非饱和 `neon.abs` 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：标量 `i8`／`i16`（无 ISel）、标量 `i32`／`i64`（独立表面）、`<1 x i64>`、发明的无符号 `uqabs`、非饱和 `neon.abs`（独立表面）、`<4 x i8>`／256 位、`half`／`bfloat`／浮点、SVE `sqabs`／`sqneg`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 标量饱和绝对值／取负 sqabs／sqneg 支持边界**

- 接受：仅 AArch64。同一 IR ID `llvm.aarch64.neon.sqabs`／`sqneg` 的标量形态。clang `vqabss_s32`／`vqabsd_s64`／`vqnegs_s32`／`vqnegd_s64`。ISel `SIMDTwoScalarBHSD`（基线 HasNEON）**只匹配** `i32(i32)` 与 `i64(i64)`。`i8`／`i16` 编码存在但 pattern 为空。**不得**降为 `llvm.abs` 或普通 `sub`。`INT_MIN`：`sqabs` 与 `sqneg` 均饱和到 `INT_MAX`（汇编保持 `sqabs s0, s0`／`sqneg s0, s0`，不折成常量）。无最后令牌，资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、一实参同型。经既有 `CallDescriptor` 与整数虚拟寄存器原样重放。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-scalar-sat-absneg-semantic.ll`（O0/O2×97/7）覆盖两 ID 的 `i32`／`i64`、`INT_MIN` 饱和常量、AArch64 `llc`／`readobj`／汇编（`sqabs`／`sqneg`），以及 `i8`／`i16`／`<1 x i64>`／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的向量 `sqabs`／`sqneg` 见上一节。宿主不能选择这些内建，故无 `lli`。
- 拒绝：`i8`／`i16`、`<1 x i64>`、向量饱和绝对值（独立表面）、非饱和 `neon.abs`、通用 `llvm.abs`、SVE、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 整数绝对值 abs 支持边界**

- 接受：仅 AArch64。精确 LLVM 15 ID `llvm.aarch64.neon.abs`（`AdvSIMD_1Arg`：一元同型 `any`）。clang 整数 `vabs`／`vabsq` 发此 ID；浮点 `vabs` 是 `llvm.fabs`（既有表面）。ISel `AArch64ISelLowering` 把合法整数向量自定义降为 `ISD::ABS`，再由 `SIMDTwoVectorBHSD` `ABS`（基线 HasNEON）匹配：`<8 x i8>`／`<16 x i8>`／`<4 x i16>`／`<8 x i16>`／`<2 x i32>`／`<4 x i32>`／`<2 x i64>`。**不得**改写成 `llvm.abs`（另一 ID，另有 `is_int_min_poison` ImmArg）或饱和 `sqabs`。有符号最小值保持为有符号最小值（`ABS` 不饱和；`SQABS` 才饱和到最大值）。调用点必须是 `CallingConv::C`、非可变参数、一实参且结果／形参／实参同型。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码，无最后令牌。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-abs-semantic.ll`（O0/O2×97/7）覆盖全部七宽 BHSD 正例、`i8` 有符号最小常量输入、AArch64 `llc`／`readobj`／汇编（`abs` 的 `.8b`／`.16b`／`.4h`／`.8h`／`.2s`／`.4s`／`.2d`），以及标量 `i32`（ISel fatal）／`<1 x i64>`／`<4 x i8>`／浮点 `v4f32`（ISel 会 fatal）／SVE `abs`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的标量 `i64` 见下一节。形态良好的 `sqabs`／`sqneg` 与通用 `llvm.abs` 见独立表面。宿主不能选择该内建，故无 `lli`。
- 拒绝：标量 `i64`（独立表面）、标量 `i32`（`report_fatal_error`）、`<1 x i64>`、`<4 x i8>`／256 位、浮点／`half`／`bfloat`（clang 浮点 `vabs` 是 `llvm.fabs`；直接写 `neon.abs` 浮点在 LLVM 15 ISel 会 fatal）、饱和 `sqabs`／`sqneg`（独立表面）、通用 `llvm.abs`（既有标量／向量表面）、SVE `abs`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 标量整数绝对值 abs 支持边界**

- 接受：仅 AArch64。同一 IR ID `llvm.aarch64.neon.abs` 的标量 `i64` 形态。clang `vabsd_s64`。ISel 自定义降为 `BITCAST v1i64`＋`ISD::ABS`＋`BITCAST i64`，再由 `SIMDTwoScalarD` `ABS`（基线 HasNEON）匹配：汇编 `abs d, d`。`i32` ISel `report_fatal_error`。**不得**改写成 `llvm.abs` 或饱和 `sqabs`。`INT_MIN` 保持 `INT_MIN`（`abs d, d` 不折叠；`sqabs` 才饱和到 `INT_MAX`）。无最后令牌，资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、一 `i64` 实参同型。经既有 `CallDescriptor` 与整数虚拟寄存器原样重放。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-scalar-abs-semantic.ll`（O0/O2×97/7）覆盖 `i64`、`INT_MIN` 常量、AArch64 `llc`／`readobj`／汇编（`abs`），以及 `i32`／`<1 x i64>`／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的向量 `abs` 见上一节。宿主不能选择该内建，故无 `lli`。
- 拒绝：`i32`（ISel fatal）、`<1 x i64>`、向量 `abs`（独立表面）、饱和 `sqabs`、通用 `llvm.abs`、浮点、SVE、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 整数取负 neon.neg（无独立 IR ID）**

- 审查结论：**不实现、不发明 ID**。LLVM 15 `IntrinsicsAArch64.td` **没有** `int_aarch64_neon_neg`。clang `vneg`／`vnegq` 是 `SOpInst` `OP_NEG`＝`(op "-", $p0)`：整数降为通用 `sub zeroinitializer, %a`，浮点降为通用 `fneg`。ISel `defm NEG : SIMDTwoVectorBHSD` 绑定的是 `UnOpFrag<(sub immAllZerosV, node:$LHS)>`，不是 target intrinsic；标量 `SIMDTwoScalarD` 同样匹配 `sub` 对零。饱和取负才有独立 ID `llvm.aarch64.neon.sqneg`（`INT_MIN` 饱和到 `INT_MAX`；硬件 `NEG`／通用 `sub` 则环绕回 `INT_MIN`）。既有 VMP 定长整数向量 `sub` 与浮点 `fneg` 已覆盖 clang `vneg`。不得把通用 `sub`／`fneg` 改写成不存在的 `neon.neg`，也不得用 `sqneg` 替代（会改变有符号最小值语义）。下一已证明的真实缺口是立即 `sqshlu`。

**当前实际 AArch64 NEON 前导符号位计数 cls 支持边界**

- 接受：仅 AArch64。精确 ISel 已覆盖的 `llvm.aarch64.neon.cls`（`AdvSIMD_1VectorArg`，一元同型）：`<8 x i8>`、`<16 x i8>`、`<4 x i16>`、`<8 x i16>`、`<2 x i32>`、`<4 x i32>`，对应 `SIMDTwoVectorBHS`（基线 HasNEON）。clang `vcls`。无 `i64` 元素（BHS 不含 D）。LLVM 15 **没有** `llvm.aarch64.neon.clz` IR ID（`CLZ` 走通用 `llvm.ctlz`）。调用点必须是 `CallingConv::C`、非可变参数、一实参且结果／形参／实参同型。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码，**不得**改写成 `llvm.ctlz`、`bitreverse`、算术右移或标量 `llvm.aarch64.cls`／`cls64`。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-aarch64-neon-cls-semantic.ll`（O0/O2×97/7）覆盖全部六宽 BHS 正例、前导符号位常量（0／-1／1／SMAX／SMIN／0xC0／0xE0）、AArch64 `llc`／`readobj`／汇编（`cls`），以及 `v2i64`／`v1i32`／`v4i8`／256 位 `v8i32`／`i1`／SVE `cls`／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的标量 `cls`／`cls64` 见 `vmp-aarch64-target-helpers-intrinsic-semantic.ll`；形态良好的向量 `ctlz`／`bitreverse` 见既有整数向量位表面。宿主不能选择该内建，故无 `lli`。
- 拒绝：标量 `llvm.aarch64.cls`／`cls64`（独立表面）、`<1 x i32>`／`<1 x i64>`、`i64` 元素、`<4 x i8>`、256 位、`i1`、发明的 `neon.clz`、通用 `llvm.ctlz`／`bitreverse`／`ashr` 合成、SVE `cls`、`half`／`bfloat`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 浮点向量 min/max 支持边界**

- 接受：精确 `llvm.aarch64.neon.fmin`／`fmax`／`fminnm`／`fmaxnm`，签名与 `AdvSIMD_2FloatArg_Intrinsic` 一致：`anyfloat (match, match)`，且进一步要求结果与两个实参为同一实用 ISel `f32`／`f64` 向量（64 位 `<2 x float>` 或 128 位 `<4 x float>`／`<2 x double>`）。调用点必须是 `CallingConv::C`、非可变参数。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码，不要求 `+neon`／`+fullfp16`。本表面不含成对或跨车道归约 ID（见后续两节）。`vmp-aarch64-neon-fminmax-semantic.ll` 覆盖四个 ID 与三种 ISel 宽度，以及 `half`／`bfloat`／`<1 x double>`／`<3 x float>`／NEON `smin`／SVE／`arm.neon`／`fastcc`／`musttail`／operand bundle 负例。形态良好的标量见下一节（已从本 lit 撤走，以免虚拟化后负例失效）。形态良好的跨车道 `faddv` 见独立表面。形态良好的整数 `sminv`／`smaxv` 与通用 `minnum` 见独立表面。宿主 `lli` 无法选择这些内建，故不做宿主差分。
- 拒绝：标量 `float`／`double`／`half`（独立表面）、`half`／`bfloat` 向量（含 `<4 x half>`／`<8 x half>`）、`<1 x double>`、`<3 x float>` 等非 ISel 宽度、`llvm.aarch64.neon.smin`、通用 `llvm.minnum`／`minimum`、SVE、`llvm.arm.neon.*`、可伸缩、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。成对 `fminp`／`fmaxp`／`fminnmp`／`fmaxnmp` 与跨车道 `fminv`／`fmaxv`／`fminnmv`／`fmaxnmv` 不在本表面。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际 AArch64 NEON 标量浮点 min/max fmin／fmax／fminnm／fmaxnm 支持边界**

- 接受：仅 AArch64。同一四 ID `llvm.aarch64.neon.fmin`／`fmax`／`fminnm`／`fmaxnm` 的标量形态。clang `vmins_f32`／`vmaxs_f32`／`vminnm*_f*`／`vmaxnmh_f16`。ISel 把这四 ID 自定义降为 `ISD::FMINIMUM`／`FMAXIMUM`／`FMINNUM`／`FMAXNUM`，再由 `TwoOperandFPData`（基线 HasNEON）匹配：汇编 `fmin`／`fmax`／`fminnm`／`fmaxnm` 的 `s`／`d`；半精度 `h` 另需 `HasFullFP16`。**不得**改写成 `llvm.minimum`／`llvm.minnum`（另一 ID，即便落到同一 SDNode）。半精度函数 `target-features` 最后一次出现的精确令牌必须为 `+fullfp16`；缺失、无令牌、最后 `-fullfp16`、仅 `+fp16fml` 不算。`f32`／`f64` 无额外令牌。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、两实参与结果同型。经既有 `CallDescriptor` 与标量浮点虚拟寄存器原样重放。既有浮点调用 FMF 门拒绝带 `FastMathFlags` 的形态。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-scalar-fminmax-semantic.ll`（O0/O2×97/7）覆盖四 ID 的 `f32`、`fmin.f64`、`fmin.f16`／`fmaxnm.f16`、多特征最后 `+fullfp16`、AArch64 `llc`／`readobj`／汇编（`fmin`／`fmax`／`fminnm`／`fmaxnm` 的 `s`／`d`／`h`，`llc` 另加 `-mattr=+fullfp16`），以及 `nnan ninf`／无令牌／最后 `-fullfp16`／仅 `+fp16fml`／`<1 x double>`／`bfloat`／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的向量 `fmin` 见上一节。宿主不能选择这些内建，故无 `lli`。
- 拒绝：带 `FastMathFlags` 的调用、向量 `fmin`／`fmax`（独立表面）、成对／跨车道 `fminp`／`fminv`、`<1 x double>`、`bfloat`、通用 `llvm.minimum`／`minnum`、SVE、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。形态良好但半精度缺令牌为 `unsupported target feature`。

**当前实际 AArch64 NEON 成对浮点向量 min/max 支持边界**

- 接受：精确 `llvm.aarch64.neon.fminp`／`fmaxp`／`fminnmp`／`fmaxnmp`，签名与 `AdvSIMD_2VectorArg_Intrinsic` 一致：`anyvector (match, match)`，且进一步要求结果与两个实参为与普通 `fmin`／`fmax` 相同的实用 ISel `f32`／`f64` 向量（64 位 `<2 x float>` 或 128 位 `<4 x float>`／`<2 x double>`）。调用点必须是 `CallingConv::C`、非可变参数。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码，不扩大普通 `fmin`／`fmax` ID 列表，不要求 `+neon`／`+fullfp16`。`vmp-aarch64-neon-fminmax-pairwise-semantic.ll` 覆盖四个 ID 与三种 ISel 宽度，以及 `half`／`bfloat`／`<1 x double>`／`<3 x float>`／整数 `anyvector`／SVE／`arm.neon`／`fastcc`／`musttail`／operand bundle 负例。形态良好的跨车道 `faddv` 见独立表面。形态良好的整数成对 `sminp`／`smaxp`／`uminp`／`umaxp` 见独立表面；形态良好的通用 `llvm.minnum` 见 `vmp-fp-vector-minmax-semantic.ll`。宿主 `lli` 无法选择这些内建，故不做宿主差分。
- 拒绝：`half`／`bfloat`、整数 `anyvector`（如 `fminp.v4i32`）、`<1 x double>`、`<3 x float>` 等非 ISel 宽度、整数成对 `sminp`／`smaxp`（独立表面）、通用 `llvm.minnum`（既有浮点向量表面）、SVE、`llvm.arm.neon.*`、可伸缩、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。跨车道 `fminv`／`fmaxv`／`fminnmv`／`fmaxnmv` 不在本表面。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际 AArch64 NEON 跨车道浮点 min/max 归约支持边界**

- 接受：精确 `llvm.aarch64.neon.fminv`／`fmaxv`／`fminnmv`／`fmaxnmv`，签名与 `AdvSIMD_1VectorArg_Float_Across_Intrinsic` 一致：`anyfloat (anyvector)`，且进一步要求标量 `f32`／`f64` 结果与单一实用 ISel `f32`／`f64` 向量实参的元素类型匹配（64 位 `<2 x float>` 或 128 位 `<4 x float>`／`<2 x double>`，与普通 `fmin`／`fmax` 相同）。调用点必须是 `CallingConv::C`、非可变参数。经既有 `CallDescriptor` 重放：向量虚拟寄存器入、标量浮点虚拟寄存器出。不新增操作码，不扩大普通或成对 ID 列表，不要求 `+neon`／`+fullfp16`。`vmp-aarch64-neon-fminmax-reduce-semantic.ll` 覆盖四个 ID 与三种 ISel 宽度，以及 `half`／`bfloat`／`<1 x double>`／`<3 x float>`／结果宽度不匹配／SVE／`arm.neon`／`fastcc`／`musttail`／operand bundle 负例。形态良好的跨车道 `faddv` 见下一节。形态良好的整数 `sminv`／`smaxv` 见独立表面。宿主 `lli` 无法选择这些内建，故不做宿主差分。
- 拒绝：`half`／`bfloat` 结果或实参、`<1 x double>`／`<3 x float>` 等非 ISel 宽度、结果与向量元素类型不同、整数 `sminv`／`smaxv`（独立表面）、`faddv`（独立表面）、通用 `llvm.vector.reduce.fmin`（仍走既有归约路径）、SVE、`llvm.arm.neon.*`、可伸缩、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际 AArch64 NEON 跨车道浮点加 faddv 支持边界**

- 接受：仅 AArch64。精确 `llvm.aarch64.neon.faddv`（`AdvSIMD_1VectorArg_Float_Across_Intrinsic`：`anyfloat (anyvector)`）。clang `vaddv_f32`／`vaddvq_f32`／`vaddvq_f64`。ISel 仅三对（基线 HasNEON，`AArch64InstrInfo.td` 固定 `FADDP` 树，无独立 NEON `FADDV`）：`float`←`<2 x float>`（`FADDPv2i32p`）、`float`←`<4 x float>`（`FADDPv4f32` 再 `FADDPv2i32p`，即 `(a0+a1)+(a2+a3)`）、`double`←`<2 x double>`（`FADDPv2i64p`）。LLVM 15 无 `neon.faddv` 半精度 ISel；`vaddv_f16` 是 SVE。**不得**改写成 `llvm.vector.reduce.fadd`（带起始值的顺序归约／`reassoc`）、成对 `neon.faddp`（向量结果）、元素级 `fadd` 或无序重结合。调用点必须是 `CallingConv::C`、非可变参数、形参等于实参。经既有 `CallDescriptor` 重放：向量虚拟寄存器入、标量浮点虚拟寄存器出。既有浮点调用 FMF 门拒绝带 `FastMathFlags` 的形态。不新增操作码，不要求 `+neon`／`+fullfp16`。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-faddv-semantic.ll`（O0/O2×97/7）覆盖三宽正例、AArch64 `llc`／`readobj`／汇编（`faddp` 树），以及 `nnan ninf`／`half`（即使最后 `+fullfp16`）／`<2 x half>`／`bfloat`／`<1 x double>`／`<3 x float>`／结果宽度不匹配／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。标量 `faddv` 不是合法 IR（`anyvector`）。形态良好的成对 `faddp` 与通用 `llvm.vector.reduce.fadd` 见独立表面。宿主不能选择该内建，故无 `lli`。
- 拒绝：带 `FastMathFlags` 的调用、`half`／`bfloat`（含 `+fullfp16` 的 `<4 x half>`／`<2 x half>`）、标量 `float`／`double`、`<1 x double>`／`<3 x float>` 等非 ISel 宽度、结果与向量元素类型不同、成对 `faddp`（独立表面）、通用 `llvm.vector.reduce.fadd`（既有归约路径）、跨车道 `fminv`／`fmaxv`（独立表面）、SVE `faddv`、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际 AArch64 NEON 倒数／倒平方根估算与迭代支持边界**

- 接受：精确 `llvm.aarch64.neon.frecpe`／`frsqrte`（`AdvSIMD_1FloatArg_Intrinsic`：`anyfloat (match)`）与 `frecps`／`frsqrts`（`AdvSIMD_2FloatArg_Intrinsic`：`anyfloat (match, match)`），且进一步要求结果与全部实参为同一实用 ISel `f32`／`f64` 向量（64 位 `<2 x float>` 或 128 位 `<4 x float>`／`<2 x double>`，与普通 `fmin`／`fmax` 相同）。估算为一元，迭代为两同型实参。调用点必须是 `CallingConv::C`、非可变参数。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不新增操作码，不要求 `+neon`／`+fullfp16`。`vmp-aarch64-neon-reciprocal-semantic.ll` 覆盖四个 ID 与三种 ISel 宽度，以及向量 `half`／`bfloat`／`<1 x double>`／`<3 x float>`／SVE／`arm.neon`／`fastcc`／`musttail`／operand bundle 负例；形态良好的标量估算／迭代见下一节；形态良好的 `frecpx` 与整数 `urecpe`／`ursqrte` 见独立表面，不得留在本 lit。宿主 `lli` 无法选择这些内建，故不做宿主差分。
- 拒绝：标量 `float`／`double`／`half`（独立标量表面）、向量 `half`／`bfloat`、`<1 x double>`、`<3 x float>` 等非 ISel 宽度、`frecpx`（独立标量指数表面）、整数 `urecpe`／`ursqrte`（独立整数估算表面）、SVE、`llvm.arm.neon.vrecpe`／`vrsqrte` 等、可伸缩、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际 AArch64 NEON 标量倒数／倒平方根估算与迭代 frecpe／frsqrte／frecps／frsqrts 支持边界**

- 接受：仅 AArch64。同一组 LLVM 15 ID 的标量形态：`llvm.aarch64.neon.frecpe`／`frsqrte`（一元 `AdvSIMD_1FloatArg`）与 `frecps`／`frsqrts`（二元 `AdvSIMD_2FloatArg`）。clang `vrecpes_f32`／`vrecped_f64`／`vrecpeh_f16`／`vrecpss_f32`／`vrsqrt*`。ISel：估算为 `SIMDFPTwoScalar` 加显式 `f16`／`f32`／`f64` 匹配（`HasNEONorSME`）；迭代为 `SIMDFPThreeScalar`。半精度另需 `HasFullFP16`。无舍入 ImmArg：估算是表查找，迭代是 `2-ab`／`(3-ab)/2`，**不得**降为 `fdiv`／`fmul`／`fsub`。半精度函数 `target-features` 最后一次出现的精确令牌必须为 `+fullfp16`；缺失、无令牌、最后 `-fullfp16`、仅 `+fp16fml` 不算。`f32`／`f64` 无额外令牌。资格不看命令行 `-mattr`。调用点必须是 `CallingConv::C`、非可变参数、形参／实参／结果同型。经既有 `CallDescriptor` 与标量浮点虚拟寄存器原样重放。既有浮点调用 FMF 门拒绝带 `FastMathFlags` 的形态。不新增操作码。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。`vmp-aarch64-neon-scalar-reciprocal-semantic.ll`（O0/O2×97/7）覆盖四 ID 的标量 `f32`／`f64`／`half`、多特征最后 `+fullfp16`、AArch64 `llc`／`readobj`／汇编（`frecpe`／`frsqrte`／`frecps`／`frsqrts` 的 `s`／`d`／`h`，`llc` 另加 `-mattr=+fullfp16`），以及 `nnan ninf`／`<1 x double>`／`bfloat`／无令牌／最后 `-fullfp16`／仅 `+fp16fml`／SVE／`fastcc`／`musttail`／bundle／`noreturn`／`returns_twice`／`sret`+`byval` 负例。形态良好的向量估算／迭代见上一节；`frecpx` 见独立表面。宿主不能选择这些内建，故无 `lli`。
- 拒绝：带 `FastMathFlags` 的调用、向量估算／迭代（独立表面）、`<1 x double>`、`bfloat`、`frecpx`、整数 `urecpe`、普通 `fdiv`、SVE、可伸缩、非 C、可变参数、`musttail`、操作数捆绑、`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。形态良好但半精度缺令牌为 `unsupported target feature`。

**当前实际 AArch64 NEON vcopy.lane 支持边界**

- 接受：精确 `llvm.aarch64.neon.vcopy.lane`，签名与 `AdvSIMD_2Vector2Index_Intrinsic` 一致，且进一步要求 dest／src／结果为同一 128 位整数向量（`<16 x i8>`／`<8 x i16>`／`<4 x i32>`／`<2 x i64>`，对应 AArch64 `INSvi*lane` 实用重载）。两个车道下标为范围内 `i64` `ConstantInt`，经 `CallDescriptor` 立即数保存（TD 未标 `ImmArg`，但指令选择只匹配立即数）。调用点必须是 `CallingConv::C`、非可变参数。经既有向量虚拟寄存器重放，不新增操作码，不要求 `+neon`。`vmp-aarch64-neon-vcopy-lane-semantic.ll` 覆盖四种整数宽度，以及 64 位／`half`／`f32`／宽度不匹配／动态或越界车道／`fastcc`／`musttail`／operand bundle 负例；宿主 `lli` 无法选择该内建，故不做宿主差分。
- 拒绝：64 位形态、dest／src 宽度不同、`half`／`f32`／`f64`／`bfloat`、动态或越界车道、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际整数 min/max 支持边界**

- 接受：精确 `llvm.smin`／`smax`／`umin`／`umax`。类型为标量 `i1`..`i64`、受限标量 `i128`，或既有定长整数向量（总宽 1..128，元素 `i1`／`i8`／`i16`／`i32`／`i64`）。两实参与结果同型。调用点必须是 `CallingConv::C`、精确非可变参数 `FunctionType`、形参类型相等。经既有 `CallDescriptor` 重放：标量走整数／`i128` 帧，向量走向量帧。不新增操作码。本表面不含浮点 `minnum`／`minimum`（见独立 `f32`／`f64` 向量表面）。`vmp-minmax-intrinsic-semantic.ll` 覆盖标量 `i8`；`vmp-i128-abs-minmax-sat-semantic.ll` 覆盖标量 `i128`；`vmp-minmax-vector-semantic.ll` 覆盖 64／128 位整数向量，以及 `half` `minnum`／constrained／宽 `maxnum`／NEON `smin`／`i128` 向量／可伸缩／普通 `tail`／`fastcc`／`noreturn`／`returns_twice`／`musttail`／operand bundle／`poison` 负例；整数向量可在宿主 `lli` 上与参考函数差分。
- 拒绝：本表面拒绝浮点 `minnum`／`maxnum`／`minimum`／`maximum`（合法宽度 `f32`／`f64` 向量由独立表面接受）、`llvm.aarch64.neon.smin` 等目标内建、`llvm.vector.reduce.smin` 等归约（仍走既有归约路径）、`i128` 向量、可伸缩向量、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。畸形形态的专用跳过原因为 `unsupported smin`／`smax`／`umin`／`umax`。

**当前实际固定整数向量位运算／漏斗移位支持边界**

- 接受：精确 `llvm.ctpop`／`llvm.bswap`／`llvm.bitreverse`／`llvm.ctlz`／`llvm.cttz`／`llvm.fshl`／`llvm.fshr`，类型为固定整数向量，元素仅 `i8`／`i16`／`i32`／`i64`，总原始宽度 1..128（含 64／96／128 位与非二幂车道，如 `<8 x i8>`／`<3 x i32>`／`<16 x i8>`／`<2 x i64>`）。`ctpop`／`bswap`／`bitreverse` 为一参且结果同型；`bswap` 另要求元素为偶数字节（`i16`／`i32`／`i64`；`i8` `bswap` 不是形态良好的 LLVM IR）。`ctlz`／`cttz` 为一参同型加 `i1` `ConstantInt` `ImmArg` 零定义标志，原样保留到 `CallDescriptor` ImmediateArguments。`fshl`／`fshr` 为三个相同的支持向量类型，开放普通双操作数漏斗（两个拼接操作数不必是同一 SSA 值），不收紧为旋转。调用点必须是 `CallingConv::C`、精确非可变参数 `FunctionType`、形参类型相等。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不引入 FastMathFlags 路径，不新增操作码。不改变标量位内建、受限标量 `i128` 位内建、标量漏斗、受限标量 `i128` 同操作数旋转、普通向量算术、half 门控，或 VMP 内部字节码解码所用的 `llvm.fshl`。`musttail` 拒绝；普通 `tail` 接受并按 `TCK_None` 重放。`vmp-int-vector-bit-semantic.ll` 覆盖七个 ID、`i8`／`i16`／`i32`／`i64`、64／96／128 位形态、`ctlz`／`cttz` 的 `false`／`true` 立即数（`true` 仅用非零输入）、钻石 `phi` 与回边循环，以及 `i1`／`i128` 元素／`half`／`bfloat`／`fp128`／可伸缩／超宽／`i1` 漏斗签名／向量定点乘／`fastcc`／`noreturn`／`returns_twice`／`musttail`／operand bundle／constrained／`poison` 负例；宿主 `lli` 与参考函数做位差分。形态良好的这些 ID 不会出现半精度／`bfloat`／浮点元素或跨类型签名。
- 拒绝：`i1`／`i128` 元素、浮点／`half`／`bfloat`／指针／聚合、可伸缩、任一侧超过 128 位、`i8` `bswap`、标量（仍走既有标量位／漏斗路径）、受限标量 `i128`（仍走既有 `i128` 位／同操作数旋转路径）、签名不匹配、constrained 及其它未列入 ID、非 C（如 `fastcc`）、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。畸形形态的专用跳过原因为 `unsupported ctpop`／`bswap`／`bitreverse`／`ctlz`／`cttz`／`fshl`／`fshr`。

**当前实际固定整数向量 abs／饱和加减移位支持边界**

- 接受：精确 `llvm.abs`／`llvm.sadd.sat`／`llvm.uadd.sat`／`llvm.ssub.sat`／`llvm.usub.sat`／`llvm.sshl.sat`／`llvm.ushl.sat`，类型为固定整数向量，元素仅 `i8`／`i16`／`i32`／`i64`，总原始宽度 1..128（含 64／96／128 位与非二幂车道）。`abs` 为同型一向量实参加 `i1` `ConstantInt` `ImmArg` `is_int_min_poison`，立即参数保留到 `CallDescriptor`。饱和加减与饱和左移为两个同一支持向量类型实参及同型结果。调用点必须是 `CallingConv::C`、精确非可变参数 `FunctionType`、形参类型相等。经既有 `CallDescriptor` 与向量虚拟寄存器原样重放，不引入 FastMathFlags 路径，不新增操作码。不改变标量 abs／饱和、受限标量 `i128` abs／饱和、AArch64 NEON `sqadd`／`uqadd`／`sqsub`／`uqsub` 或普通向量算术。`musttail` 拒绝；普通 `tail` 接受并按 `TCK_None` 重放。`vmp-int-vector-abs-sat-semantic.ll` 覆盖七个 ID、`i8`／`i16`／`i32`／`i64`、64／96／128 位形态、`abs` 的 `false`／`true` 立即数（`true` 不传有符号最小值）、有符号最小／最大饱和、无符号下溢归零／上溢饱和、饱和移位的范围内与会使值溢出的计数（不使用 `>=` 位宽的毒计数）、钻石 `phi` 与回边循环，以及 `i1`／`i128` 元素／`half`／`bfloat`／`fp128`／可伸缩／超宽／`i1` 饱和签名／向量定点乘／`fastcc`／`noreturn`／`returns_twice`／`musttail`／operand bundle／constrained／`poison` 负例；宿主 `lli` 与参考函数做位差分。
- 拒绝：`i1`／`i128` 元素、浮点／`half`／`bfloat`／指针／聚合、可伸缩、任一侧超过 128 位、标量（仍走既有标量 abs／饱和路径）、受限标量 `i128`（仍走既有 `i128` abs／饱和路径）、NEON `sqadd`／`uqadd`／`sqsub`／`uqsub`（仍走既有 NEON 表面）、签名不匹配、constrained 及其它未列入 ID、非 C（如 `fastcc`）、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。畸形形态的专用跳过原因为 `unsupported abs`／`sadd.sat`／`uadd.sat`／`ssub.sat`／`usub.sat`／`sshl.sat`／`ushl.sat`。

**当前实际固定整数向量定点乘除支持边界**

- 接受：精确 `llvm.smul.fix`／`llvm.umul.fix`／`llvm.sdiv.fix`／`llvm.udiv.fix` 与 `llvm.smul.fix.sat`／`llvm.umul.fix.sat`／`llvm.sdiv.fix.sat`／`llvm.udiv.fix.sat`。结果与前两个操作数必须是同一固定向量类型，元素仅 `i8`／`i16`／`i32`／`i64`，总原始宽度必须严格为 64 或 128（如 `<8 x i8>`／`<16 x i8>`／`<4 x i16>`／`<8 x i16>`／`<2 x i32>`／`<4 x i32>`／`<1 x i64>`／`<2 x i64>`）。第三实参必须是 `i32` `ConstantInt` `ImmArg` scale，经既有 `CallDescriptor` ImmediateArguments 原样重发射，不进虚拟寄存器。调用点必须是 `CallingConv::C`、精确非可变参数 `FunctionType`、形参类型相等、无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI。经既有 `CallDescriptor` 与向量虚拟寄存器重放，不新增操作码。不改变标量定点、固定整数向量位运算／abs／饱和加减移位、half 或其它路径。宽度收紧到 64／128 的原因：本地原生 AArch64 `llc` 探测确认八个 ID 仅在此范围稳定；`<3 x i32>` 等非标准总宽会令后端崩溃，故绝不可接受。`vmp-int-vector-fixpoint-semantic.ll` 覆盖八个 ID、`i8`／`i16`／`i32`／`i64` 的 64／128 位代表、scale 0 与非零、钻石 `phi` 与回边循环，以及 `i1`／`i128`／`half`／`bfloat`／可伸缩／96 位／32 位／超过 128 位／`fastcc`／`noreturn`／`returns_twice`／`musttail`／operand bundle／constrained／`poison` 负例；宿主 `lli` 不能执行这些向量 ID（x86 指令选择崩溃，且 LLVM 15 解释器无法降低 `llvm.smul.fix.v*`），故只走 FileCheck 与 AArch64 `llc`／`readobj`，不伪造宿主语义。形态良好的 LLVM 内建不会出现非 `i32` 常量 scale。
- 拒绝：`i1`／`i128` 元素、浮点／`half`／`bfloat`、可伸缩、总宽不是 64／128（含 `<3 x i32>` 96 位与 `<4 x i8>` 32 位）、超过 128 位、非 `i32` `ConstantInt` scale、标量（仍走既有标量定点路径）、constrained、非 C、可变参数、`musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI、`poison`／`undef` 实参。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。畸形形态的专用跳过原因为 `unsupported smul.fix`／`umul.fix`／`sdiv.fix`／`udiv.fix`／`smul.fix.sat`／`umul.fix.sat`／`sdiv.fix.sat`／`udiv.fix.sat`。

**当前实际间接调用支持边界**

- 接受：真间接 `CallInst`（`getCalledFunction()` 为空、不是 inline asm）；`CallingConv::C`；非可变参数；0..8 个固定参数；无 `musttail`／操作数捆绑／`noreturn`／`returns_twice`／复杂 ABI；被调操作数是已有地址空间零指针（普通虚拟寄存器：函数指针参数、全局表 `load`、`select` 或 `phi`）；调用点 `FunctionType` 与每个实参／返回类型精确匹配。每个固定参数以及非 `void` 返回为既有标量（`i1`/`i8`/`i16`/`i32`/`i64`、地址空间零指针、IEEE `half`/`f32`/`f64`）、既有定长向量（总宽 1..128，元素 `i1`/`i8`/`i16`/`i32`/`i64`/`half`/`f32`/`f64`）或既有扁平聚合（1..8 字段、保守分配 1..64 字节）。标量 `half` 走既有浮点虚拟寄存器；定长向量走既有向量虚拟寄存器；小型扁平聚合走既有聚合虚拟寄存器；均经 `CallDescriptor` 重放。标量或向量浮点返回上的任意有效 FastMathFlags 原样保存并恢复。纯调用 ABI 不要求 `+fullfp16`，也不改变 listed `half` 数学／超越／向量数学内建的既有 feature gate。既有整数／地址空间零指针／`f32`/`f64`／标量 `half`／定长向量规则不收紧。last-token `+bf16` 标量／1..128 `bfloat` 向量间接调用仍走独立 `isBFloatIndirectCallShape`（普通 `tail` 同样按 `TCK_None` 重放）。`vmp-indirect-call-eligibility-semantic.ll`（O0/O2×97/7）覆盖 i32 回调／`select` 被调宿主差分、`+bf16` bfloat 重放，以及非 C／可变参数／`musttail`／bundle／`noreturn`／`returns_twice`／`byval`／`poison`／无 feature 负例。既有 `vmp-indirect-call-i32-semantic.ll`／`vmp-indirect-call-mixed-scalar-semantic.ll`／`vmp-indirect-call-half-semantic.ll`／`vmp-indirect-call-vector-semantic.ll`／`vmp-indirect-call-aggregate-semantic.ll`／`vmp-bfloat-indirect-call-semantic.ll` 覆盖原表面。
- 拒绝：可伸缩向量、指针向量、超过 128 位的固定向量、两层及以上嵌套／超过 8 字段／超过 64 字节的聚合、无 `+bf16` 的 `bfloat`/`fp128`、非地址空间零被调指针、可变参数、非 C、超过八个固定参数、`sret`/`byval`/`byref` 等复杂 ABI、`i128`、`musttail`／操作数捆绑／`noreturn`／`returns_twice`。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。超出既有定长向量或小型扁平聚合表面的形态在函数级以 `unsupported return type`／`unsupported argument type` 跳过；其余间接形态跳过原因为 `indirect call`。形态良好但缺少 `+bf16` 的 bfloat 间接仍为 `unsupported target feature`。

**当前实际可变参数支持边界**

- 接受：直接定义为 `CallingConv::C`、可变参数、无异常处理的函数；具名参数与 `va_arg` 结果仅为 `i32`/`i64`/`double`/地址空间零指针。`llvm.va_start`／`llvm.va_end` 须为精确非可变参数 `void(ptr)`、`C`、实参类型等于形参、AS0 普通指针；`llvm.va_copy` 须为精确 `void(ptr, ptr)`、`C`、两个不同的 AS0 普通指针且类型等于形参。普通 `tail` 接受并按非 tail `CreateCall`／`TCK_None` 重放。一种形态是至多一个入口静态 `va_list`，`va_start`/`va_end`/`va_arg` 只操作该对象。另一种受限形态是恰好两个入口静态地址空间零 `va_list`：一次 `va_start` 初始化源列表；一次 `void llvm.va_copy(ptr dest, ptr src)`，`dest` 为另一列表且 `src` 为已初始化列表；两个列表各一次 `va_end`；`va_arg` 可分别从两个列表读取。资格检查验证复制、列表别名、用户集合、`alloca` 归属，以及真实控制流生命周期：`va_start` 支配源列表的 `va_copy`／`va_arg`／`va_end`；`va_copy` 支配目标列表的 `va_arg`／`va_end`；每个 `va_end` 后支配该列表全部 `va_arg`，源 `va_end` 还后支配 `va_copy`；任一 `va_end` 支配 `va_copy` 或后续 `va_arg` 则安全跳过。解释器从指针虚拟寄存器恢复原生 `va_list` 地址，用专用 `VAArg` 操作码原生创建 `va_arg`；`va_start`/`va_end`/`va_copy` 经既有 `CallDescriptor` 重放。保留原函数调用约定、`va_list` 内存更新与调试信息。无 `va_*` 的纯具名参数 C 可变参数函数也可虚拟化。不扩大可变参数调用者，也不接受动态／未知 `va_list`。`vmp-vaarg-semantic.ll` 覆盖单列表与 `va_copy` 生命周期、`va_arg` 差分，以及畸形／非 C／AS1／`musttail`／bundle／`sret`／生命周期负例。
- 非可变参数调用者仍可按既有规则对另一 C 可变参数函数发起受限直接可变参数调用（尾部 `i32`/`i64`/`double`/地址空间零指针）。
- 拒绝：可变参数函数中再发起原生可变参数尾部调用；非 C 调用约定；vector/aggregate/`half`/`bfloat`/`float` `va_arg`；动态或非入口 `va_list`；多个不匹配 `va_list`；重复或链式 `va_copy`、`dest` 等于 `src`、缺失 `va_end`、源列表结束后再 `va_copy`、任一列表结束后再 `va_arg`；`va_arg` 之外的未知使用；`musttail`、操作数捆绑、`noreturn`、`returns_twice`、异常；非 AArch64 目标。形状失败用专用 `unsupported va_start`／`va_end`／`va_copy`；生命周期失败仍为 `unsupported va_arg instruction`。资格失败保持原函数与 `hikari.vmp.selected`，不加 `hikari.vmp.virtualized`。

**当前实际标量位内建（ctpop／bswap／bitreverse／ctlz／cttz／fshl／fshr）支持边界**

- 接受：精确标量 `llvm.ctpop`／`bswap`／`bitreverse`（`FunctionType` `T(T)`）、`ctlz`／`cttz`（`T(T, i1)`，`is_zero_poison` 为 `ImmArg` `ConstantInt`，走描述符立即数）、`fshl`／`fshr`（`T(T,T,T)`）。`T` 为既有 `i1`..`i64`，另加受限标量 `i128` 位内建与受限标量 `i128` 漏斗。均为 `CallingConv::C`、非可变参数、实参类型等于形参。经既有 `CallDescriptor` 重放，不新增操作码。普通 `tail` 接受并按非 tail `CreateCall`／`TCK_None` 重放。不放宽到向量（向量位内建仍为独立表面，普通 `tail` 在向量侧仍可不拒绝）。`vmp-scalar-bit-intrinsic-semantic.ll`（O0/O2×97/7）覆盖重放、`ctlz` 的 `i1 true` 立即数保留、宿主 `ctpop`／`bswap`／`ctlz`／`cttz` 差分，以及 `noreturn`／`musttail`／bundle／`fastcc`／`poison`／`sret`／AS1 负例。
- 拒绝：`musttail`、操作数捆绑、`fastcc`／非 C、可变参数、`poison`、`noreturn`／`returns_twice`、复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际标量 abs／饱和加减移位／定点乘除支持边界**

- 接受：精确标量 `llvm.abs`（`T(T, i1)`，`is_int_min_poison` 为 `ImmArg` `ConstantInt`）、`sadd`／`uadd`／`ssub`／`usub`／`sshl`／`ushl.sat`（`T(T,T)`）、`smul`／`umul`／`sdiv`／`udiv.fix` 与 `.fix.sat`（`T(T,T,i32)`，scale 为 `ImmArg` `ConstantInt`）。`T` 为既有 `i1`..`i64` 另加受限标量 `i128`。均为 `CallingConv::C`、精确非可变参数 `FunctionType`、实参类型等于形参。经既有 `CallDescriptor` 重放，不新增操作码。普通 `tail` 接受并按非 tail `CreateCall`／`TCK_None` 重放。不改变向量 abs／饱和／定点助手或 `smin`／`smax`／`umin`／`umax`。`vmp-scalar-abs-sat-fix-semantic.ll`（O0/O2×97/7）覆盖 i8 全 ID 重放与宿主差分、i32 代表，以及 `noreturn`／`musttail`／bundle／`fastcc`／`poison`／`returns_twice`／`sret`／AS1 负例。`vmp-i128-abs-minmax-sat-semantic.ll`／`vmp-i128-fixpoint-semantic.ll` 覆盖受限 `i128`（定点无宿主 `lli`）。
- 拒绝：`musttail`、操作数捆绑、`fastcc`／非 C、可变参数、`poison`、`noreturn`／`returns_twice`、复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。畸形形态的专用跳过原因为 `unsupported abs`／`sadd.sat`／…／`smul.fix`／…。

**当前实际标量 half／float／double 数学内建支持边界**

- 接受：精确标量 `llvm.fma`／`llvm.fmuladd`（`T(T,T,T)`）、`sqrt`／`sin`／`cos`／`exp`／`exp2`／`log`／`log2`／`log10`／`fabs`（`T(T)`）、`pow`／`copysign`／`minnum`／`maxnum`／`minimum`／`maximum`（`T(T,T)`）、`powi`（`T(T, i32)`，指数必须是 `i32`）。`T` 为既有标量 `half`／`float`／`double`。`half` listed 数学仍要求函数属性最后一次出现的 `+fullfp16`。均为 `CallingConv::C`、精确非可变参数 `FunctionType`、实参类型等于形参。经既有 `CallDescriptor` 重放，不新增操作码。普通 `tail` 接受并按非 tail `CreateCall`／`TCK_None` 重放。任意有效 FastMathFlags 仍由既有 intrinsic FMF 白名单保存并恢复。不改变 `fp128`、`bfloat`、向量、constrained、rounding／fpclass 家族。`vmp-scalar-fp-math-semantic.ll`（O0/O2×97/7）覆盖 f32 宿主可靠混合（sqrt／fabs／fma／fmuladd／minnum／maxnum／copysign）、f64／minimum／maximum／超越／`+fullfp16` half 重放，以及非 C／`powi` i64 形参／`musttail`／bundle／`noreturn`／`returns_twice`／`poison`／复杂 ABI／无 `+fullfp16` half／constrained 负例。
- 拒绝：`musttail`、操作数捆绑、`fastcc`／非 C、可变参数、形参类型不等（含 `powi` 非 `i32` 指数）、`poison`、`noreturn`／`returns_twice`、复杂 ABI。形态良好但缺少或最后一次为 `-fullfp16` 的标量 half 仍为 `unsupported target feature`。向量／`fp128`／`bfloat` 不走本族专用跳过。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。畸形形态的专用跳过原因为 `unsupported fma`／`fmuladd`／`sqrt`／`sin`／`cos`／`exp`／`exp2`／`log`／`log2`／`log10`／`powi`／`pow`／`fabs`／`copysign`／`minnum`／`maxnum`／`minimum`／`maximum`。

**当前实际标量浮点助手内建（rounding／canonicalize／lround／is.fpclass／sat）支持边界**

- 接受：精确标量 `llvm.ceil`／`floor`／`trunc`／`round`／`rint`／`nearbyint`／`roundeven`／`canonicalize`（`T(T)`，`T` 为既有 `half`／`float`／`double`；`canonicalize` 仍在规划期降为 `FMul(x, 1.0)`，不重发射该内建）、`lround`／`llround`／`lrint`／`llrint`（`i64(f32/f64)`）、`is.fpclass`（`i1(f32/f64, i32)`，类别掩码为真正 `ImmArg` `ConstantInt`）、`fptosi.sat`／`fptoui.sat`（`i8`／`i16`／`i32`／`i64` 自 `f32`／`f64`）。`half` listed rounding／canonicalize 仍要求最后一次 `+fullfp16`。均为 `CallingConv::C`、精确非可变参数 `FunctionType`、实参类型等于形参。经既有 `CallDescriptor` 重放（canonicalize 除外），不新增操作码。普通 `tail` 接受并按非 tail `CreateCall`／`TCK_None` 重放。任意有效 FastMathFlags 仍由既有 intrinsic FMF 白名单保存并恢复。不改变 `fp128`、`bfloat`、向量、constrained，也不放宽 half sat／half lround／half is.fpclass 的既有 type／feature 表面。`vmp-scalar-fp-helper-semantic.ll`（O0/O2×97/7）覆盖 f32 宿主可靠混合（ceil／floor／trunc／round／lround／is.fpclass／fptosi.sat.i32）、其余 ID 重放，以及非 C／`lround.i32` 形参／`musttail`／bundle／`noreturn`／`returns_twice`／`poison`／复杂 ABI／无 `+fullfp16` half／constrained 负例。
- 拒绝：`musttail`、操作数捆绑、`fastcc`／非 C、可变参数、形参类型不等、非立即数 `is.fpclass` 掩码、`poison`、`noreturn`／`returns_twice`、复杂 ABI。形态良好但缺少或最后一次为 `-fullfp16` 的标量 half 仍为 `unsupported target feature`。向量／`fp128`／`bfloat` 不走本族专用跳过。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。畸形形态的专用跳过原因为 `unsupported ceil`／`floor`／`trunc`／`round`／`rint`／`nearbyint`／`roundeven`／`canonicalize`／`lround`／`llround`／`lrint`／`llrint`／`is.fpclass`／`fptosi.sat`／`fptoui.sat`。

**当前实际固定 f32／f64 向量数学内建支持边界**

- 接受：精确固定总宽 1..128 的 `f32`／`f64` 向量 `llvm.fma`／`fmuladd`（`T(T,T,T)`）、`sqrt`／`fabs`／`ceil`／`floor`／`trunc`／`round`／`rint`／`nearbyint`／`roundeven`／`sin`／`cos`／`exp`／`exp2`／`log`／`log2`／`log10`（`T(T)`）、`minnum`／`maxnum`／`minimum`／`maximum`／`copysign`／`pow`（`T(T,T)`）、`powi`（向量底 + 标量 `i32` 指数，允许 SSA）、`canonicalize`（规划期仍降为 `VectorFMul(x, splat 1.0)`）、`is.fpclass`（同道 `<N x i1>`，`i32` ImmArg 掩码）、`fptosi.sat`／`fptoui.sat`（同道 `i8`／`i16`／`i32`／`i64` 向量，两边总宽各自 1..128）。均为 `CallingConv::C`、精确非可变参数 `FunctionType`、实参类型等于形参。经既有 `CallDescriptor` 与向量虚拟寄存器重放（canonicalize 除外），不新增操作码。普通 `tail` 接受并按非 tail `CreateCall`／`TCK_None` 重放。任意有效 FastMathFlags 仍由既有 intrinsic FMF 白名单保存并恢复。不改变 half／`bfloat`／`fp128` 向量、vector-reduce、constrained 或标量路径。`vmp-fp-vector-math-semantic.ll`（O0/O2×97/7）覆盖 v2f32 宿主可靠混合（fma／sqrt／fabs／minnum）与全 ID 重放，以及非 C／`powi` i64／`musttail`／bundle／`noreturn`／`poison`／无 `+fullfp16` half 向量／constrained 负例。既有 `vmp-fp-vector-*-semantic.ll` 覆盖各子表面。
- 拒绝：`musttail`、操作数捆绑、`fastcc`／非 C、可变参数、形参类型不等、非立即数 `is.fpclass` 掩码、`poison`、`noreturn`／`returns_twice`、复杂 ABI。half／`bfloat`／`fp128`／可伸缩不走本族专用跳过。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。畸形形态的专用跳过原因为与标量共用的 `unsupported fma`／`sqrt`／`ceil`／`powi`／`is.fpclass`／`fptosi.sat`／…。

**当前实际 llvm.vector.reduce 支持边界**

- 接受：精确 `llvm.vector.reduce.add`／`mul`／`and`／`or`／`xor`／`smin`／`smax`／`umin`／`umax`（`T(<N x T>)`）与 `fadd`／`fmul`（`T(T start, <N x T>)`）／`fmin`／`fmax`（`T(<N x T>)`）。`T` 为既有 `i1`／`i8`／`i16`／`i32`／`i64`／`half`／`float`／`double`，固定总宽 1..128。`half` 浮点归约仍要求最后一次 `+fullfp16`。均为 `CallingConv::C`、精确非可变参数 `FunctionType`、实参类型等于形参（含 `fadd`／`fmul` 起始值）。经既有 `CallDescriptor` 重放，不新增操作码。普通 `tail` 接受并按非 tail `CreateCall`／`TCK_None` 重放。任意有效 FastMathFlags 仍由既有 intrinsic FMF 白名单保存并恢复。固定 1..128 `bfloat` 的 `fadd`／`fmul`／`fmin`／`fmax` 仍走独立 last-token `+bf16` `LegalizeBFloatMath`（同样 C／FTy／形参／普通 `tail` 按 `TCK_None` 重放，任何 FMF 仍拒绝）。不接受 `llvm.vp.reduce.*` 或可伸缩。`vmp-vector-reduce-eligibility-semantic.ll`（O0/O2×97/7）覆盖整数 add／mul／bit／minmax、`fadd` 起始值、f64、`+fullfp16` half、`+bf16` 合法化，以及非 C／`musttail`／bundle／`noreturn`／`returns_twice`／过宽／起始值 `poison`／无 feature／last-token 关闭／`+bf16` FMF／VP 负例。既有 `vmp-vector-reduce-semantic.ll`／`vmp-half-vector-masked-reduce-semantic.ll`／`vmp-bfloat-vector-reduce-semantic.ll` 覆盖原表面。
- 拒绝：`musttail`、操作数捆绑、`fastcc`／非 C、可变参数、形参类型不等、`poison`、`noreturn`／`returns_twice`、复杂 ABI、VP、可伸缩、超过 128 位。形态良好但缺少 `+fullfp16`／`+bf16` 的 half／bfloat 仍为 `unsupported target feature`。`+bf16` 且带 FMF 仍为 `unsupported float call instruction`。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。畸形形态的专用跳过原因为 `unsupported vector reduce instruction`。

**当前实际 llvm.masked 内存支持边界**

- 接受：精确 `llvm.masked.load`／`store`／`gather`／`scatter`／`expandload`／`compressstore`。数据为既有固定总宽 1..128 的 `i1`／`i8`／`i16`／`i32`／`i64`／`half`／`f32`／`f64`。指针为地址空间零标量指针（gather／scatter 为同车道 `<N x ptr>`，N=1..8）。掩码为同车道 `<N x i1>`，passthru／value 与结果／存值同型。load／store／gather／scatter 另有真正 `i32` 幂二 `ImmediateArguments` 对齐。均为 `CallingConv::C`、精确非可变参数 `FunctionType`、实参类型等于形参。经既有 `CallDescriptor` 重放，不新增操作码。普通 `tail` 接受并按非 tail `CreateCall`／`TCK_None` 重放。固定 1..128 `bfloat` 仍走独立 last-token `+bf16` `LegalizeBFloatMaskedMemory`（同样 C／FTy／形参／普通 `tail` 按 `TCK_None` 重放；逐车道 `condbr` 标量访存，不得重放 `llvm.masked.*.bf16`）。不接受 `llvm.vp.load`／可伸缩，也不放宽类型。`vmp-masked-memory-eligibility-semantic.ll`（O0/O2×97/7）覆盖 v4i32 六种形态宿主差分、half 重放、`+bf16` 合法化，以及非 C／`musttail`／bundle／`noreturn`／`returns_twice`／poison 掩码与 passthru／无 feature／VP／AS1 负例。既有 `vmp-masked-memory-semantic.ll`／`vmp-masked-expand-compress-semantic.ll`／`vmp-masked-gather-scatter-semantic.ll`／`vmp-half-vector-masked-reduce-semantic.ll`／`vmp-bfloat-masked-memory-semantic.ll`／`vmp-bfloat-gather-scatter-semantic.ll`／`vmp-bfloat-expand-compress-semantic.ll` 覆盖原表面。
- 拒绝：`musttail`、操作数捆绑、`fastcc`／非 C、可变参数、形参类型不等、非立即数或非幂二对齐、`poison`／`undef`、`noreturn`／`returns_twice`、复杂 ABI、VP、可伸缩、超过 128 位、非 AS0。形态良好但缺少 `+bf16` 的 bfloat 仍为 `unsupported target feature`。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。畸形形态的专用跳过原因为 `unsupported masked memory instruction`。

**当前实际标量 i128 支持边界**

- 接受：标量 `i128` 走独立 `i128` 虚拟寄存器帧（不与 `i64` 整数槽、向量 `i128` 槽或聚合 `i128` 槽混用）。覆盖 `add`/`sub`/`mul`/`udiv`/`sdiv`/`urem`/`srem`、`and`/`or`/`xor`、`shl`/`lshr`/`ashr`（保留 `nuw`/`nsw`/`exact`）、`icmp`、`select`、`phi`、`freeze`、非原子地址空间零 `load`/`store`、`trunc`/`zext`/`sext`（两端均为既有 `i1`/`i8`/`i16`/`i32`/`i64` 或 `i128`）、函数参数与返回、非内建非可变参数且调用点与被调函数均为 `CallingConv::C` 的直接调用的 `i128` 实参与返回、静态 `alloca i128`、纯位内建 `ctpop`/`bswap`/`bitreverse`/`ctlz`/`cttz`（经既有 `Call` 重放，`ctlz`/`cttz` 的 `is_zero_poison` 保持 `i1` ImmArg）、`fshl`/`fshr` 普通漏斗（含同操作数旋转；`vmp-i128-rotate-semantic.ll` 对旋转量 0/1/63/64/127/128/129、两端不同的漏斗做参考／保护差分；普通 `tail` 现拒绝）、标量 `i128` `smul`／`umul`／`sdiv`／`udiv.fix` 与 `.fix.sat`（`i32` ImmArg scale，见 `vmp-i128-fixpoint-semantic.ll`；普通 `tail` 现拒绝）、以及标量 `i128` `abs`／`smin`/`smax`/`umin`/`umax`／`sadd.sat`/`uadd.sat`/`ssub.sat`/`usub.sat`/`sshl.sat`/`ushl.sat`（经既有 `CallDescriptor` 原生重放，`abs` 的 `is_int_min_poison` 保持 `i1` ImmArg；`abs`／饱和普通 `tail` 现拒绝）、以及标量 `i128` `sadd`/`uadd`/`ssub`/`usub`/`smul`/`umul.with.overflow`（仅经既有专用 `AddWithOverflow` 路径：单层 `extractvalue` 字段 0 至多一次、字段 1 至多一次；字段 0 的 `i128` 结果写独立 `i128` 帧，字段 1 的 `i1` 标志写普通整数帧；规划器保持 bitwidth 128；不并入普通聚合或普通调用路径；`vmp-i128-overflow-semantic.ll`）。`i128` 不并入 `isSupportedValueType` / 普通调用类型谓词，只在上述指令与精确内建门控、寄存器帧上显式打开。`ptrtoint`/`inttoptr` 仍仅限既有 `i1`..`i64`。
- 拒绝：`i128` 原子 `load`/`store`/`atomicrmw`/`cmpxchg`、`insertvalue`／整体使用／多重或非 `ExtractValue` 的 `i128` `*with.overflow` 聚合、`i128` `va_arg`、以 `i128` 为条件的 `switch`、`i128` 作为 GEP 下标、动态 `alloca` 长度或动态 `alloca i128` 元素、`i128` `ptrtoint`/`inttoptr`、可变参数具名／尾部 `i128`、间接 `i128` 调用、非 C 约定（如 `fastcc`）的 `i128` 直接调用（调用点或被调函数任一侧非 C 均拒绝）、`i128` 向量元素与扁平聚合字段、以及既有仅 `i1`..`i64` 的其它未列入整数内建。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际动态 alloca 支持边界**

- 接受：入口块、地址空间零、已定大小的标量元素（`i1`/`i8`/`i16`/`i32`/`i64`、地址空间零指针、`half`/`f32`/`f64`；不含 `i128`／聚合／向量）、显式合法对齐、非 `inalloca`/`swifterror`。面向普通 C VLA／`__builtin_alloca`。数组长度是同一函数的直接 `i32`/`i64` 参数，或入口块中位于该 `alloca` 之前、最终产生 `i32`/`i64` 的纯整数表达式（参数、常量、`add`/`sub`/`mul`、`udiv`/`sdiv`/`urem`/`srem`、`and`/`or`/`xor`、`shl`/`lshr`/`ashr`、`trunc`/`zext`/`sext`、`icmp`、`select`；保留 `nuw`/`nsw`/`exact`）。资格仍用该 DAG 封闭长度来源；规划后长度走既有整数虚拟寄存器。
- 专用 `VMOpcode::DynamicAlloca` 解释器处理器：从整数虚拟寄存器 load 运行时长度，按原分配类型／地址空间 0／对齐／`DebugLoc` 在**执行路径**上 `CreateAlloca`，把指针写入指针虚拟寄存器。不占用静态 `alloca` 序言路径。结果可进入既有 `GEP`、`load`／`store`、`ptrtoint`／`inttoptr`、`select`／`phi` 与已放行调用。运行时长度为 0 时按 LLVM 允许的空分配处理。
- 常量/静态 `alloca` 保持原序言路径，可与 `llvm.stacksave` / `llvm.stackrestore` 组合。
- 拒绝：非入口动态 `alloca`；循环／重复分配；`load`/`store`/`call`/`alloca` 作为长度依赖、`freeze`、`phi`、原子、内建、指针/浮点/向量/聚合长度、位于目标 `alloca` 之后的定义、`undef`/`poison`、非 `i32`/`i64` 最终长度、动态 `i128`／聚合／向量元素、非 AS0。表达式分析或节点预算失败时保持原函数与 `hikari.vmp.selected`，跳过原因 `unsupported stack allocation`。
- 拒绝：同一函数同时包含上述入口动态 `alloca` 与 `llvm.stacksave`、`llvm.stackrestore` 或 `llvm.get.dynamic.area.offset`（动态栈状态交互）。资格检查以 `dynamic stack state` 安全跳过并保留 `hikari.vmp.selected`。
- `vmp-dynamic-alloca-semantic.ll`（O0/O2×97/7）覆盖参数／整数 DAG 长度、i32 计数、零长度、转换后访存、宿主 `lli` 差分、AArch64 `llc`／`readobj`、非 AArch64 跳过，以及 load／call／非入口／聚合／向量／AS1／`stacksave`／`get.dynamic.area.offset` 负例。

**当前实际普通 tail CallInst 支持边界**

- 接受：仅 AArch64。已支持表面上的普通 `CallInst` `tail`（`isTailCall() && !isMustTailCall()`）视为优化提示，不是新 ABI。资格与非 tail 相同。形状助手只检查 `musttail`／捆绑／ABI／类型，**不得**再因普通 `tail` 拒绝（含已支持的直接／间接 C 调用、`sret`／`byval`／`byref`、专用 `noreturn`、listed 数学／memory／assume／frame 类内建、NEON st1lane／ld1lane／ld2r）。解释器 `emitCallHandler` 一律 `CreateCall` 后 `setTailCallKind(TCK_None)`。`vmp-direct-call-tail-eligibility-semantic.ll`（O0/O2×97/7）覆盖 i32／向量／可变参数／constrained／bfloat／fp128 普通 tail 与非 tail 重放、`musttail` 仍为早期 `musttail call`、无 `+bf16` 的形态良好 bfloat tail 仍为 `unsupported target feature`；`vmp-tail-call-semantic.ll` 覆盖已支持 `fastcc` 普通 tail 差分；`vmp-call-abi-tail-semantic.ll`（O0/O2×97/7）覆盖受限 `sret`／`byval`／`byref`／专用 `noreturn`／受限间接／last-token `+bf16` 间接 bfloat 的普通 tail 按 `TCK_None` 重放，以及直接 `musttail call`、间接 `musttail` 的 `indirect call`、无 `+bf16` 形态良好 bfloat 间接 tail 仍为 `unsupported target feature`。族 lit 里用形态良好 ID 作普通 tail 负例的哨兵已删除。族节与族 lit 题头不再把普通 `tail` 写成拒绝；负例只保留 `musttail`。
- 拒绝：`musttail`（早期 `musttail call`；间接 `musttail` 仍为晚期 `indirect call`）。`InvokeInst` 仍拒绝 `tail`／`musttail`。`callbr`／超出受限直接表面的内联汇编／操作数捆绑／未支持复杂 ABI 或调用约定不因本项放开。可变参数尾上的非法实参类型仍为 `unsupported call argument`。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际受限直接 InlineAsm CallInst 支持边界**

- 接受：仅 AArch64。直接 `CallInst`（被调为 `InlineAsm*`，不是 `Function*`，也不是间接指针）。`CallingConv::C`，`FunctionType` 精确等于该 `InlineAsm` 且非可变参数，0..8 个实参。结果与实参仅为 `void`（仅结果）／`i1`／`i8`／`i16`／`i32`／`i64`／地址空间零指针；无 `poison`／`undef`。ATT 方言，`canThrow` 为假，非 `alignstack`。汇编串长度 ≤256。约束解析后：输入／输出码仅为 `r`，破坏仅为 `{memory}`／`{cc}`；无标签、匹配、提前破坏、间接或多种备选。输出个数与结果一致（`void` 为 0，标量为 1），输入个数等于实参个数。经既有 `CallDescriptor` 重放：描述符保存 `InlineAsm*`（上下文去重常量，不进虚拟寄存器），解释器 `CreateCall(FunctionType*, InlineAsm*, Args)` 后 `setTailCallKind(TCK_None)`。普通 `tail` 是优化提示。不新增操作码。宿主不能执行 AArch64 内联汇编，故 `vmp-inline-asm-semantic.ll`（O0/O2×97/7）只走 FileCheck 与 AArch64 `llc`／`readobj`／汇编（`nop`／`dmb ish`／`mov`）。覆盖 `nop`、空串 `=r,r` 的 `i32`／`i64`／AS0 指针拷贝、`mov`、`dmb ish`、`~{cc}` 与普通 `tail`；`alignstack`／`unwind`／匹配 `=r,0`／提前破坏 `=&r` 负例锁定为 `inline assembly`。族 lit 里用形态良好 `i32 asm "", "=r,r"` 作跳过哨兵的负例已删除。AS0 指针作 `"r"` 寻址的真实访存（`ldr`／`str`，可选 `~{memory}`）已由同一助手接受，但不得再用空串指针拷贝代替；见 `vmp-inline-asm-pointer-memory-semantic.ll`（O0/O2×97/7 FileCheck＋AArch64 `llc`／`readobj`／汇编）。仍拒绝 `"m"`／`"*m"`／`"=*r"`／`"=*m"`／非零地址空间，跳过原因 `inline assembly`。
- 拒绝：`InvokeInst`（非 `CallInst` 的内联汇编仍为早期 `inline assembly`）、`musttail`（早期 `musttail call`；`musttail` 直接作用于 `InlineAsm` 会被 LLVM 验证器拒绝，故 lit 用同原型 helper 加形态良好 `nop`）、操作数捆绑、`fastcc`／非 C、可变参数、`noreturn`／`returns_twice`、复杂 ABI（含调用点 `sret`）、`fp128`／`bfloat`／浮点／向量／聚合、破坏 `sp`／其它寄存器、Intel 方言、`alignstack`、可抛。形态失败跳过原因 `inline assembly`，保留 `hikari.vmp.selected`（直接 `musttail` 除外）。受限 `CallBr` 见下一节。

**当前实际受限直接 InlineAsm CallBr 支持边界**

- 接受：仅 AArch64。直接 `CallBrInst`（被调为 `InlineAsm*`，不是 `Function*`，也不是间接指针）。Linux 内核 `asm goto` 表面。`CallingConv::C`，`FunctionType` 精确等于该 `InlineAsm` 且非可变参数，0..8 个实参。结果与实参仅为 `void`（仅结果）／`i1`／`i8`／`i16`／`i32`／`i64`／地址空间零指针；无 `poison`／`undef`／`blockaddress` 实参。ATT 方言，`canThrow` 为假，非 `alignstack`。汇编串长度 ≤256。1..8 个同函数间接目的，默认目的必有。目的块允许 PHI（`getEdgeTarget` 物化边拷贝）。约束解析后：输入／输出码仅为 `r`，标签码仅为 `i`／`X`，破坏仅为 `{memory}`／`{cc}`；无匹配、提前破坏、间接或多种备选。标签个数等于间接目的个数，输入个数等于实参个数。新操作码 `VMOpcode::CallBr`：描述符保存 `InlineAsm*` 与目的 PC；解释器 `CreateCallBr` 到各目的跳板，跳板写入目的虚拟机 PC 后回到调度器；非 `void` 结果只在默认目的写入虚拟寄存器。资格失败或预算失败保持原函数与 `hikari.vmp.selected`。宿主不能执行 AArch64 内联汇编，故 `vmp-inline-asm-callbr-semantic.ll`（O0/O2×97/7）只走 FileCheck 与 AArch64 `llc`／`readobj`／汇编。AS0 指针 `"r"` 寻址的 `str`＋`~{memory}` 已作为该表面的访存锁，不得只用无指针的 `nop`＋`~{memory}` 代替。`hikari_fla` 会请求 VMP post-CFF：void `asm goto` 解释器因 `callbr` 终结符以 `candidate flattening or verification failed` 事务式跳过；带结果的形态因默认跳板跨块使用 handler 的 `CurrentPC` 以 `cross-block SSA requires fixStack` 事务式跳过。两种都保留已虚拟化函数与 `hikari.vmp.post.cff`，不加 `hikari.vmp.post.cff.applied`。不因此放宽 CFF。
- 拒绝：`Function*`／间接被调、`InvokeInst`、Intel 方言、`alignstack`、可抛、`musttail`、操作数捆绑、`fastcc`／非 C、可变参数、`noreturn`／`returns_twice`、复杂 ABI、`fp128`／浮点／向量／聚合、破坏 `sp`、0 或超过 8 个间接目的、非 `i`／`X` 标签、立即数 `i` 输入、`blockaddress` 实参。形态失败跳过原因 `callbr`，保留 `hikari.vmp.selected`（直接 `musttail` 除外）。`vmp-nounwind-invoke-semantic.ll` 中原形态良好 `callbr` 哨兵已删除，见本 lit。

**当前实际受限 SVE 基础整数 SSA 支持边界**

- 接受：仅 AArch64。函数 `target-features` 最后一次出现的精确 `+sve`／`-sve` 须为 `+sve`（不读取命令行 `-mattr`；`+sve2`／`+sme` 不算）。类型仅全寄存器整数可伸缩：`<vscale x 16 x i8>`／`<vscale x 8 x i16>`／`<vscale x 4 x i32>`／`<vscale x 2 x i64>`。指令：同型 `add`／`sub`／`mul`／`shl`／`lshr`／`ashr`／`udiv`／`sdiv`／`urem`／`srem`／`and`／`or`／`xor`（独立 `Scalable*` 操作码；SVE Variant `[10:8]` 按操作只编码／重放允许的 BinaryFlag：`add`／`sub`／`mul`／`shl` 为 `nuw`／`nsw`，`lshr`／`ashr`／`udiv`／`sdiv` 为 `exact`，`urem`／`srem`／`and`／`or`／`xor` 为 0，绝不能跨操作重放。元素种类在 `[3:0]`；`freeze` SourceKind 在 `[15:12]`）。LLVM 15 `llc -mattr=+sve -fast-isel=false` 对上述四宽均能选择：`mul`／移位走谓词 `MUL`／`LSL`／`LSR`／`ASR`（`ptrue`）；`udiv`／`sdiv` 的 `.s`／`.d` 原生，`.b`／`.h` 由 ISel 加宽到 32 位再截断；`urem`／`srem` 为 Expand（`x-x/y*y`），因 `div` 为 Custom 而不会 unroll 可伸缩。另有标量 `i1` `select`，`phi`，`freeze`（含直接冻 `undef`／`poison`），非原子地址空间零 `load`／`store`，以及这些类型作函数实参／返回。独立可伸缩帧：`alloca <vscale x 16 x i8>, i64 N`（`1<<56`），其它三宽经同大小 `bitcast`。操作码 `ScalableMove`／`Freeze`／`Select`／`Add`／`Sub`／`And`／`Or`／`Xor`／`Mul`／`Shl`／`LShr`／`AShr`／`UDiv`／`SDiv`／`URem`／`SRem`／`Ret`；`load`／`store` 复用既有内存描述符 `IsScalable`。不经 `CallDescriptor` 传递 SVE。既有 `llvm.vscale` 仍走整数 `CallDescriptor`。宿主不能执行 AArch64 SVE，故 `vmp-sve-integer-ssa-semantic.ll`（O0/O2×97/7）只走 FileCheck 与 AArch64 `llc`／`readobj`／汇编（`llc` 用 `-mattr=+sve -fast-isel=false`：LLVM 15 FastISel 对可伸缩 `store` 的 `TypeSize` 会 fatal；FileCheck 锁定虚拟化后原生带标志的 `mul`／`shl`／`lshr`／`ashr`／`udiv`／`sdiv`）。预算失败保留原函数。
- 拒绝：缺少或最后令牌为 `-sve`／仅 `+sve2` 的形态良好全寄存器 ABI（函数先检查返回类型，跳过原因为 `unsupported return type`；无 SVE 返回、仅 SVE 形参则为 `unsupported argument type`。既有 `vmp-fixed-vector-semantic.ll` `@unsupported_scalable` 哨兵仍是后者）。函数 ABI 已是标量、体内形态良好 SVE 算术则为 `unsupported target feature`。半寄存器／浮点可伸缩、extract／insert／shuffle、SVE 内建、经调用传递 SVE、原子可伸缩访存。体内半寄存器访存／二元（如全寄存器 ABI 下对 `<vscale x 8 x i8>` 的 `load`／`add`）为 `unsupported vector load instruction` 或 `unsupported vector binary instruction`。向量 `icmp`／谓词见下一节。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际受限 SVE 谓词 SSA 支持边界**

- 接受：仅 AArch64，最后令牌 `+sve`。数据侧仍是四种全寄存器整数。`icmp` 仅同型整数十谓词（`eq`／`ne`／`ugt`／`uge`／`ult`／`ule`／`sgt`／`sge`／`slt`／`sle`），结果谓词必须与数据车道数匹配：`nxv16i8`→`nxv16i1`，`nxv8i16`→`nxv8i1`，`nxv4i32`→`nxv4i1`，`nxv2i64`→`nxv2i1`。LLVM 15 `llc -mattr=+sve -fast-isel=false` 对这些对选择 `ptrue`+`cmpeq`／`cmpne`／`cmphi`／`cmphs`／`cmpgt`／`cmpge`（交换实参覆盖 `ult`／`ule`／`slt`／`sle`）。车道匹配的 `select <vscale x N x i1>, data, data` 选择 `sel z`。同型谓词 `and`／`or`／`xor` 经独立操作码 `ScalablePredAnd`／`Or`／`Xor`：解释器先 `convert.from.svbool` 回到正确视图再 `CreateAnd`／`Or`／`Xor` 再打包；ISel 为 `and p`／`sel p`（`or`）／`eor p`。同型谓词 `freeze`（`ScalablePredFreeze`）支持普通操作数以及直接 `undef`／`poison`（SourceKind 在 Variant `[15:12]`，与数据 freeze 相同，不进 VReg）。同型谓词／谓词／谓词 `select` 用独立 `ScalablePredValueSelect`，**不得**复用 data 的 `ScalablePredSelect`；标量 `i1` 条件的谓词 `select` 仍拒绝。谓词 `phi` 与本函数谓词实参／返回（AAPCS `variant_pcs`，P 寄存器）。独立谓词帧：`alloca <vscale x 16 x i1>, i64 N`（`1<<55`，`vmp.sve.preds`），**不得**与 `nxv16i8` 数据帧或标量整数帧混用。LLVM 15 不能选择 `load`／`store` `nxv8i1`／`nxv4i1`／`nxv2i1`（`Cannot select`）；解释器把解包谓词经 `llvm.aarch64.sve.convert.to/from.svbool` 变成 `nxv16i1` 再 `str`／`ldr p`（不是用户 `CallDescriptor` 表面）。既有标量 `i1` `ScalableSelect` 不变。`vmp-sve-predicate-ssa-semantic.ll`（O0/O2×97/7）覆盖四宽 `icmp`＋谓词返回、四宽 data `select`、十谓词、谓词 `phi`／实参／恒等、四宽 `and`／`or`／`xor`、`freeze` 普通／`poison`／`undef`、同型谓词 `select`，以及无 `+sve`／`nxv1i1`／半寄存器比较／谓词 `shl`／`load`／标量 `i1` 谓词 `select`／谓词调用。
- 拒绝：`nxv1i1`（ISel 对 `nxv1i64` icmp 会 `punpklo`，不是本表面）；`nxv32i1` 及更宽（ISel 拆成两个 P 寄存器）；半寄存器数据上的 `icmp`；谓词 `shl`／`mul` 等非逻辑二元；标量 `i1` 条件的谓词 `select`；用户谓词 `load`／`store`；经 `CallDescriptor` 传递谓词；SVE 浮点比较。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际受限 SVE masked.load／masked.store 支持边界**

- 接受：仅 AArch64，最后令牌 `+sve`。仅 `llvm.masked.load`／`llvm.masked.store`（不是 gather／scatter／expand／compress）。数据为四种全寄存器整数，谓词必须车道匹配（`nxv16i8`↔`nxv16i1` 等），指针为地址空间零标量 `ptr`，对齐为 `i32` 二幂 `ConstantInt` ImmArg（LangRef／Verifier；0 非法），load 的 passthru 与结果同型。`poison`／`undef` 操作数拒绝。操作码 `ScalableMaskedLoad`／`ScalableMaskedStore` 加独立描述符（类型、对齐、`DebugLoc`、安全元数据）；解释器 `CreateMaskedLoad`／`CreateMaskedStore`，数据／谓词／指针走各自虚拟寄存器帧。LLVM 15 `llc -mattr=+sve -fast-isel=false` 对解释器外壳（volatile `nxv16i8`／`nxv16i1` 帧＋`convert.to/from.svbool`）选择谓词 `ld1b`／`ld1h`／`ld1w`／`ld1d` 与 `st1*`，passthru 用 `mov z, p/m` 合并，**不是**无条件 `ldr`＋`select`，也不是逐车道循环。全假 store 可被 ISel 删掉。`vmp-sve-masked-memory-semantic.ll`（O0/O2×97/7）覆盖动态／全真／全假谓词、passthru、四宽 load／store、GEP、无 `+sve` ABI（`unsupported return type`）、体内无令牌（标量 ABI，`unsupported target feature`）、非四宽／半寄存器 mismatch／`poison`／非 AS0／gather／scatter（标量 ABI 下均为 `unsupported masked memory instruction`）。既有 `vmp-masked-memory-semantic.ll` `@unsupported_masked_scalable` 无 `+sve`，跳过改为 `unsupported target feature`。
- 拒绝：expand／compress；非 AS0；`nxv4i64`／`nxv8i8` 等非四宽；对齐非二幂或非 `i32` 常量；`poison`／`undef`；经 `CallDescriptor` 传递 SVE masked。gather／scatter 见下一节。车道数不匹配的 mask／data 是非法 IR，不作为 lit 哨兵（半寄存器 `nxv8i8`＋匹配 `nxv8i1` 是合法替代哨兵）。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际受限 SVE masked.gather／masked.scatter 支持边界**

- 接受：仅 AArch64，最后令牌 `+sve`。仅 `llvm.masked.gather`／`llvm.masked.scatter`。数据为四种全寄存器整数，谓词与地址向量必须同车道（`nxv16i8`↔`nxv16i1`↔`nxv16ptr` 等）。对齐为 `i32` 二幂 `ConstantInt` ImmArg，gather 的 passthru 与结果同型。`poison`／`undef` 拒绝。地址向量不是通用 pointer-vector ABI：函数实参／返回、用户 `load`／`store`、常量全 null 地址仍拒绝。接受来源仅限受限可伸缩 GEP 及其 `phi`。GEP：标量 AS0 基址＋匹配四宽整数向量下标（1 下标），以及与固定 gather 相同的 2／3 下标 AoS／数组／Wrap 形态。独立地址帧：`alloca <vscale x 2 x ptr>, i64 N`（`1<<54`，`vmp.sve.pvregs`），`nxv4/8/16 ptr` 经 `llvm.vector.extract`／`insert` 按 2 指针分块打包，**不得** `ptrtoint`／`inttoptr`。操作码 `ScalableMaskedGather`／`ScalableMaskedScatter` 加既有 `ScalableMaskedMemoryDescriptor`；解释器 `CreateMaskedGather`／`CreateMaskedScatter`，数据／谓词／地址走各自虚拟寄存器帧。LLVM 15 `llc -mattr=+sve -fast-isel=false` 对解释器外壳选择谓词 `ld1w {z}, p/z, [x, z.s, sxtw #2]`（GEP）或 `ld1w {z}, p/z, [z.d]`（地址向量）与对应 `st1*`；`nxv16i8`／`nxv8i16` 会 `sunpk`／`punpk` 拆成多次谓词 gather，**不是**无条件标量 `ldr` 或 `whilelo` 逐车道循环。passthru 用 `sel`。`vmp-sve-gather-scatter-semantic.ll`（O0/O2×97/7）覆盖四宽 gather／scatter、动态／全真／全假掩码、passthru、1 下标 GEP、AoS 2 下标、地址 `phi`、无 `+sve` ABI／体内无令牌、非四宽／半寄存器／`poison`／非 AS0／expand、预算。既有 `vmp-sve-masked-memory-semantic.ll` 的 gather／scatter 哨兵仍是标量 ABI＋`zeroinitializer` 地址，跳过 `unsupported masked memory instruction`。
- 拒绝：指针向量函数 ABI；常量／`poison`／`undef` 地址向量；非 AS0；`nxv4i64`／`nxv8i8` 等非四宽；expand／compress／VP；用户对地址向量的 `load`／`store`／`freeze`／`select`；经 `CallDescriptor` 传递 SVE gather。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际受限 SVE llvm.masked.expandload／compressstore 支持边界**

- **本轮明确拒绝，不实现。** 先用 LLVM 15 `llc -mattr=+sve -fast-isel=false` 对真实 VMP interpreter 外壳（volatile `nxv16i8`／`nxv16i1` 帧＋`convert.to/from.svbool`）以及裸 intrinsic 做对象生成。四宽 `nxv16i8`／`nxv8i16`／`nxv4i32`／`nxv2i64` 的 `expandload`／`compressstore` **都能选中**，但汇编是普通谓词连续访存：`ld1w {z}, p/z, [x]`＋`mov z, p/m`（passthru）与 `st1w {z}, p, [x]`，与 `llvm.masked.load`／`store` 相同。**没有** `compact`、没有按活动车道打包的前缀地址、没有 `whilelo` 逐车道循环。AArch64 `LowerMLOAD` 不区分 `isExpandingLoad`；TTI `isLegalMaskedExpandLoad`／`CompressStore` 默认 `false`（仅 x86 AVX-512 覆盖）。LangRef 要求 expand 从连续紧凑序列展开到活动车道、compress 把活动车道压回连续前缀；按普通 `ld1`／`st1` 重放会读／写错误地址。LLVM 15 `IRBuilder` 也没有 `CreateMaskedExpandLoad`／`CreateMaskedCompressStore`。因此不得用普通 masked load／store、无条件矢量访存或逐车道循环冒充。资格保持既有 `unsupported masked memory instruction`（体内无 `+sve` 的形态良好 SVE 整数 masked 仍是 `unsupported target feature`）。`vmp-sve-gather-scatter-semantic.ll` `@unsupported_expand` 锁定该跳过。固定向量 expand／compress 仍走既有 `CallDescriptor` 表面。

**当前实际受限 SVE 整数数学内建支持边界**

- 接受：仅 AArch64，最后令牌 `+sve`。仅全寄存器整数 `nxv16i8`／`nxv8i16`／`nxv4i32`／`nxv2i64`。listed ID：`llvm.abs`（一向量实参＋`i1` ImmArg `is_int_min_poison`）、`llvm.smin`／`smax`／`umin`／`umax`（两同型向量）、`llvm.ctpop`（一向量）、`llvm.ctlz`／`cttz`（一向量＋`i1` ImmArg `is_zero_undef`）、`llvm.bswap`（元素仅 `i16`／`i32`／`i64`）、`llvm.bitreverse`、`llvm.sadd.sat`／`ssub.sat`／`uadd.sat`／`usub.sat`（两同型向量）、`llvm.fshl`／`fshr`（三同型向量，含旋转 `fshl(a,a,n)`，第三实参走 Source2）。调用点必须是 `CallingConv::C`、非可变参数、无 `musttail`／operand bundle／`noreturn`／`returns_twice`／复杂 ABI。`poison`／`undef` 实参拒绝。独立操作码 `ScalableIntMath`，不走 `CallDescriptor`。Variant：`[3:0]` 元素种类，`[8]` ImmArg，`[23:16]` Kind。解释器在 `nxv16i8` 帧上 `CreateIntrinsic` 重建同一 ID，ImmArg 从 Variant 物化为编译期 `i1`。LLVM 15 `llc -mattr=+sve -fast-isel=false` 选择谓词 `abs`／`smin`／`smax`／`umin`／`umax`／`cnt`／`clz`／`rbit`＋`clz`（`cttz`）／`revb`（`bswap`）／`rbit`（`bitreverse`）与非谓词 `sqadd`／`sqsub`／`uqadd`／`uqsub`；漏斗展开为掩码移位＋`orr`（`and #0x1f`／`#0x7`／`#0x3f` 加 `lsl`／`lsr`），不是逐车道循环。`vmp-sve-int-math-semantic.ll`（O0/O2×97/7）覆盖十六 ID、四宽、`abs`／`ctlz`／`cttz` 两种 ImmArg、漏斗与旋转、无 `+sve` ABI（`unsupported return type`）、体内无令牌（标量 ABI，`unsupported target feature`）、`sshl.sat`／半寄存器／`poison` 负例。宿主不能执行 AArch64 SVE，故无 `lli`。
- 拒绝：`sshl.sat`／`ushl.sat`（ISel `UnrollVectorOp` fatal）、`bswap` 的 `i8` 元素（LangRef／Verifier）、半寄存器／其它可伸缩宽、非 C、可变参数、`musttail`、operand bundle、`noreturn`／`returns_twice`、复杂 ABI、`poison`／`undef`。资格失败保持原函数与 `hikari.vmp.selected`。形态良好但缺 `+sve` 且 ABI 已是标量时为 `unsupported target feature`；listed 形态失败为 `unsupported call instruction`。

**当前实际受限 SVE 向量归约支持边界**

- 接受：仅 AArch64，最后令牌 `+sve`。向量仅全寄存器整数 `nxv16i8`／`nxv8i16`／`nxv4i32`／`nxv2i64` 或全寄存器浮点 `nxv4f32`／`nxv2f64`／`nxv8f16`（半精度另需最后令牌 `+fullfp16`）。listed ID：整数 `llvm.vector.reduce.add`／`and`／`or`／`xor`／`smin`／`smax`／`umin`／`umax`（结果为元素标量）；浮点 `llvm.vector.reduce.fadd`（标量 start＋向量）／`fmin`／`fmax`。调用点必须是 `CallingConv::C`、非可变参数、无 `musttail`／operand bundle／`noreturn`／`returns_twice`／复杂 ABI。`poison`／`undef` 拒绝。独立操作码 `ScalableReduce`：向量走 `nxv16i8` 帧，整数结果走整数帧，浮点结果／`fadd` start 走浮点帧。解释器 `CreateIntrinsic` 重建同一 ID，浮点重放 FastMathFlags。LLVM 15 `llc -mattr=+sve[,+fullfp16] -fast-isel=false` 选择 `uaddv`／`andv`／`orv`／`eorv`／`sminv`／`smaxv`／`uminv`／`umaxv` 与 `fadda`／`fminnmv`／`fmaxnmv`。无 `+sve` 时不抢既有固定向量归约哨兵（仍为 `unsupported vector reduce instruction`）。`vmp-sve-reduce-semantic.ll`（O0/O2×97/7）覆盖十一 ID、四宽整数与三宽浮点、无 `+sve` ABI／体内无令牌、半精度缺 `+fullfp16`、`mul`／`fmul`／`poison` 负例。宿主不能执行 AArch64 SVE，故无 `lli`。
- 拒绝：`mul`／`fmul`（ISel `Expanding reductions for scalable vectors is undefined`）、`vp.reduce.*`、半寄存器、非 C、`musttail`、operand bundle、`poison`／`undef`。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际受限 SVE llvm.is.fpclass 支持边界**

- 接受：仅 AArch64，最后令牌 `+sve`。源仅全寄存器浮点 `nxv4f32`／`nxv2f64`／`nxv8f16`（半精度另需最后令牌 `+fullfp16`）。结果谓词必须车道匹配：`nxv4f32`→`nxv4i1`，`nxv2f64`→`nxv2i1`，`nxv8f16`→`nxv8i1`。测试掩码为 `i32` `ConstantInt` ImmArg（LangRef 10 位类别，完整 32 位写入 Variant）。调用点必须是 `CallingConv::C`、非可变参数、无 `musttail`／operand bundle／`noreturn`／`returns_twice`／复杂 ABI。`poison`／`undef` 拒绝。独立操作码 `ScalableFPClass`：源走 `nxv16i8` 帧，结果走 `nxv16i1` 谓词帧（`convert.to/from.svbool` 打包）。解释器 `CreateIntrinsic` 重建同一 ID。LLVM 15 `llc -mattr=+sve[,+fullfp16] -fast-isel=false` 按掩码选择 `fcmuo`（NaN）、`fcmeq #0.0`（零）、整数 `cmpgt`／`cmpeq`（finite／inf）等，不是逐车道标量循环。无 `+sve` 时不抢既有固定向量 `is.fpclass` 哨兵。`vmp-sve-fpclass-semantic.ll`（O0/O2×97/7）覆盖 NaN／零／正规／无穷／有限掩码、`f32`／`f64`／`f16`、无 `+sve` ABI／体内无令牌、半精度缺 `+fullfp16`、半寄存器／`poison` 负例。宿主不能执行 AArch64 SVE，故无 `lli`。
- 拒绝：半寄存器（含 `nxv4f16`）、非 ImmArg／非 `i32` 掩码、非 C、`musttail`、operand bundle、`poison`／`undef`。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际受限 SVE 饱和浮点转整数支持边界**

- 接受：仅 AArch64，最后令牌 `+sve`。仅 `llvm.fptosi.sat`／`llvm.fptoui.sat`，且源／目的为全寄存器同车道且元素位宽相等：`nxv4f32`→`nxv4i32`、`nxv2f64`→`nxv2i64`、`nxv8f16`→`nxv8i16`（半精度另需最后令牌 `+fullfp16`）。调用点必须是 `CallingConv::C`、非可变参数、无 `musttail`／operand bundle／`noreturn`／`returns_twice`／复杂 ABI。`poison`／`undef` 拒绝。独立操作码 `ScalableSatCast`，两端都走 `nxv16i8` 帧。Variant：`[3:0]` 源元素种类，`[8]` SI／UI。解释器 `CreateIntrinsic` 重建同一 ID（`{DstTy, SrcTy}`），**不得**降为普通 `fptosi`／`fptoui`。LLVM 15 `llc -mattr=+sve[,+fullfp16] -fast-isel=false` 选择谓词 `fcvtzs`／`fcvtzu`。无 `+sve` 时不抢既有固定向量饱和转换哨兵（仍为 `unsupported return type`）。`vmp-sve-sat-convert-semantic.ll`（O0/O2×97/7）覆盖三对宽度、有符号／无符号、无 `+sve` ABI／体内无令牌、半精度缺 `+fullfp16`、元素宽不匹配／`poison` 负例。宿主不能执行 AArch64 SVE，故无 `lli`。
- 拒绝：元素位宽不等、半寄存器、非 C、`musttail`、operand bundle、`poison`／`undef`。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际受限 SVE 直接 C 调用支持边界**

- 接受：仅 AArch64，最后令牌 `+sve`。直接非内建 `Function*`，调用点与被调均为 `CallingConv::C`、非可变参数、`FunctionType` 返回与实参类型匹配。至少有一个全寄存器 SVE 数据实参或结果：`nxv16i8`／`nxv8i16`／`nxv4i32`／`nxv2i64`／`nxv4f32`／`nxv2f64`／`nxv8f16`（半精度另需最后令牌 `+fullfp16`）。其余槽为既有普通直接调用类型（`void`／`i1`..`i64`／AS0 指针／标量 `half`／`f32`／`f64`）。无 `musttail`／operand bundle／`noreturn`／`returns_twice`／复杂 ABI。普通 `tail` 按 `TCK_None` 重放。经既有 `CallDescriptor`：SVE 槽从 `nxv16i8` 帧 `scalableFromSlot`／`scalableToSlot`，浮点 SVE 结果重放 FastMathFlags。LLVM 15 `llc -mattr=+sve[,+fullfp16] -fast-isel=false` 选择 `bl`。谓词调用不走本表面（`vmp-sve-predicate-ssa-semantic.ll` `@unsupported_pred_call` 仍为 `unsupported call return type`）。`vmp-sve-direct-call-semantic.ll`（O0/O2×97/7）覆盖恒等／双 SVE 实参／`i8`／浮点／`nnan`／半精度／void＋SVE 实参／标量混用，以及无 `+sve` ABI／体内无令牌、半精度缺 `+fullfp16`、谓词／间接／`musttail`／`poison` 负例。宿主不能执行 AArch64 SVE，故无 `lli`。
- 拒绝：谓词／指针向量 ABI、间接、`invoke`、`musttail`、operand bundle、`fastcc`、可变参数、`poison`／`undef`。资格失败保持原函数与 `hikari.vmp.selected`（直接 `musttail` 除外）。

**当前实际受限 SVE bitcast／stepvector／reverse／active.lane.mask 支持边界**

- 接受：仅 AArch64，最后令牌 `+sve`。全寄存器数据之间、两侧类型不同的同颗粒 `bitcast`（`nxv16i8`／`nxv8i16`／`nxv4i32`／`nxv2i64`／`nxv4f32`／`nxv2f64`／`nxv8f16`，半精度另需最后令牌 `+fullfp16`）。复用既有 `ScalableCast` 与 `VectorCastDescriptor`，两端走 `nxv16i8` 帧，解释器 `CreateCast(BitCast)`。`llvm.experimental.stepvector` 仅全寄存器整数；`llvm.experimental.vector.reverse` 为全寄存器数据（含浮点）。独立操作码 `ScalableVecHelper`：Variant `[3:0]` 元素种类，`[23:16]` Kind（step／reverse）。解释器 `CreateIntrinsic` 重建同一 ID，反向源从数据帧取出。`llvm.get.active.lane.mask` 结果为全寄存器谓词 `nxv16i1`／`nxv8i1`／`nxv4i1`／`nxv2i1`，起止为同宽标量 `i32`／`i64`。独立操作码 `ScalableActiveLaneMask`：谓词走 `nxv16i1` 帧，下标走整数帧，Variant `[3:0]` 谓词车道、`[15:8]` 下标位宽。调用点必须是 `CallingConv::C`、非可变参数、无 `musttail`／operand bundle／`noreturn`／`returns_twice`／复杂 ABI。`poison`／`undef` 拒绝。LLVM 15 `llc -mattr=+sve[,+fullfp16] -fast-isel=false`：`bitcast` 为空操作，`stepvector` 为 `index z, #0, #1`，`reverse` 为 `rev z`，`get.active.lane.mask` 为 `whilelo`。无 `+sve` 时不抢既有固定向量／可伸缩 zext 哨兵（`vmp-vector-width-cast-semantic.ll` `@unsupported_cast_scalable` 仍是 `unsupported vector cast instruction`）。体内无令牌、仅 step／reverse／alm 调用、标量 ABI 时为 `unsupported call instruction`（没有先被 SVE load 吃掉）。`vmp-sve-vec-helper-semantic.ll`（O0/O2×97/7）覆盖四类 `bitcast`、三宽 `stepvector`、整数／浮点 `reverse`、两宽 `active.lane.mask`，以及无 `+sve` ABI／体内无令牌、半精度缺 `+fullfp16`、半寄存器 `zext`／`poison` 负例。宿主不能执行 AArch64 SVE，故无 `lli`。
- 拒绝：谓词／指针向量 `bitcast` 或 `reverse`、半寄存器、改变颗粒的 `zext`／`sext`／`trunc`／`fpext`／`fptrunc`、`extract`／`insert`／通用 `shufflevector`、非 `i32`／`i64` 下标、非 C、`musttail`、operand bundle、`poison`／`undef`。`experimental.vector.splice` 见下一节。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际受限 SVE llvm.experimental.vector.splice 支持边界**

- 接受：仅 AArch64，最后令牌 `+sve`。仅全寄存器数据 `nxv16i8`／`nxv8i16`／`nxv4i32`／`nxv2i64`／`nxv4f32`／`nxv2f64`／`nxv8f16`（半精度另需最后令牌 `+fullfp16`）。两同型向量实参＋`i32` `ConstantInt` ImmArg。调用点必须是 `CallingConv::C`、非可变参数、无 `musttail`／operand bundle／`noreturn`／`returns_twice`／复杂 ABI。`poison`／`undef` 拒绝。复用 `ScalableVecHelper`（Kind＝splice），两端走 `nxv16i8` 帧。Variant：`[3:0]` 元素种类，`[23:16]` Kind，`[55:24]` 有符号 ImmArg。解释器 `CreateIntrinsic` 重建同一 ID。下标必须同时满足验证器 `[-VL, VL-1]` 与 LLVM 15 AArch64 `LowerVECTOR_SPLICE`：非负走 `EXT`（全寄存器下 `VL-1` 远小于 `2048/elemBits`）；负下标仅当 `abs(idx)` 是 `getSVEPredPatternFromNumElements` 认识的图案（1..8、16、32、64、128、256）时走 `ptrue`＋`rev p`＋`splice`。因此 `nxv16i8` 的 `-16` 与 `-1..-8` 接受，`-9..-15` 拒绝（ISel 返回空 `SDValue`）。LLVM 15 `llc -mattr=+sve[,+fullfp16] -fast-isel=false` 对解释器外壳选择 `ext z.b, #N` 或 `ptrue`＋`rev p`＋`splice`，不是逐车道循环。无 `+sve` 时不抢既有哨兵。`vmp-sve-splice-semantic.ll`（O0/O2×97/7）覆盖正／零／负下标、`i8`／`i32`／`i64`／`f32`／`f16`、`nxv16i8` `-16`，以及无 `+sve` ABI／体内无令牌、半精度缺 `+fullfp16`、谓词／半寄存器／`poison`／`-9` 负例。宿主不能执行 AArch64 SVE，故无 `lli`。
- 拒绝：谓词／指针向量 splice、半寄存器、ISel 无图案的负下标、非 ImmArg、非 C、`musttail`、operand bundle、`poison`／`undef`。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际受限 SVE 谓词 helper 支持边界**

- 接受：仅 AArch64，最后令牌 `+sve`。仅全寄存器谓词 `nxv16i1`／`nxv8i1`／`nxv4i1`／`nxv2i1`。listed ID：`llvm.experimental.vector.reverse`（一谓词）、`llvm.aarch64.sve.ptrue`（`i32` ImmArg 图案，仅 `0..31`，对应 `sve_pred_enum`）、`llvm.aarch64.sve.cntp`（两同型谓词，结果 `i64`）、`llvm.aarch64.sve.ptest.any`／`first`／`last`（两同型谓词，结果 `i1`）。调用点必须是 `CallingConv::C`、非可变参数、无 `musttail`／operand bundle／`noreturn`／`returns_twice`／复杂 ABI。`poison`／`undef` 拒绝。独立操作码 `ScalablePredHelper`：谓词走 `nxv16i1` 帧（`convert.to/from.svbool` 打包），`cntp`／`ptest` 结果走整数帧。Variant：`[3:0]` 谓词车道，`[23:16]` Kind，`[31:24]` ptrue 图案。解释器 `CreateIntrinsic` 重建同一 ID。LLVM 15 `llc -mattr=+sve -fast-isel=false` 选择 `rev p`／`ptrue`／`cntp`／`ptest`＋`cset`（`any`→`ne`，`first`→`mi`，`last`→`lo`；非 `nxv16i1` 先 `ptrue`＋`and` 收成 svbool 视图），不是逐车道循环。无 `+sve` 时不抢既有谓词 ABI 哨兵。`vmp-sve-pred-helper-semantic.ll`（O0/O2×97/7）覆盖三宽 reverse、`ptrue` all／`.s`／`vl1`、两宽 `cntp`、三 ID `ptest`，以及无 `+sve` ABI／体内无令牌、谓词 splice、`poison`、图案 32、`nxv1i1` 负例。宿主不能执行 AArch64 SVE，故无 `lli`。
- 拒绝：谓词 `experimental.vector.splice`（ISel 先把谓词升成整数再 `ext`，不是原生 `splice p`）、`nxv1i1`、ptrue 图案 ≥32、非 C、`musttail`、operand bundle、`poison`／`undef`。用户面 `convert.to/from.svbool` 仍关闭（解释器内部打包不受影响）。`while*` 见下一节。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际受限 SVE whilelo／whilelt／whilele／whilels 支持边界**

- 接受：仅 AArch64，最后令牌 `+sve`。复用既有 `ScalableActiveLaneMask`。`llvm.get.active.lane.mask` 与基础 SVE `llvm.aarch64.sve.whilelo`／`whilelt`／`whilele`／`whilels`：结果为全寄存器谓词，起止为同宽标量 `i32`／`i64`。调用点必须是 `CallingConv::C`、非可变参数、无 `musttail`／operand bundle／`noreturn`／`returns_twice`／复杂 ABI。`poison`／`undef` 拒绝。谓词走 `nxv16i1` 帧，下标走整数帧。Variant：`[3:0]` 谓词车道，`[15:8]` 下标位宽，`[23:16]` Kind（ALM／lo／lt／le／ls）。解释器 `CreateIntrinsic` 重建同一 ID。LLVM 15 `llc -mattr=+sve -fast-isel=false` 选择 `whilelo`／`whilelt`／`whilele`／`whilels`（`i32` 用 `w`，`i64` 用 `x`）。`vmp-sve-while-semantic.ll`（O0/O2×97/7）覆盖四 ID、`i32`／`i64`、`nxv16i1`／`nxv8i1`／`nxv4i1`／`nxv2i1` 与既有 ALM，以及无 `+sve` ABI／体内无令牌、SVE2 `whilege`、`nxv1i1`、`poison` 负例。既有 `vmp-sve-vec-helper-semantic.ll` 的 ALM 仍覆盖。宿主不能执行 AArch64 SVE，故无 `lli`。
- 拒绝：SVE2 `whilege`／`whilegt`／`whilehs`／`whilehi`（无 `+sve2` 时 `Cannot select`；本表面不开放 `+sve2`）、`whilerw`／`whilewr`、`nxv1i1`（`Cannot select`）、非 `i32`／`i64` 下标、非 C、`musttail`、operand bundle、`poison`／`undef`。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际受限 SVE 常见 ACLE 支持边界**

- 接受：仅 AArch64，最后令牌 `+sve`。独立操作码 `ScalableACLE`。数据走 `nxv16i8` 帧，谓词走 `nxv16i1` 帧，标量走既有整数／浮点帧。listed：（1）谓词二元 `aarch64.sve.add`／`sub`／`mul`／`smax`／`umax`／`smin`／`umin`／`and`／`orr`／`eor`／`sel`／`fadd`／`fsub`／`fmul`／`fdiv`，以及仅 `.s`／`.d` 的 `sdiv`／`udiv`；（2）merged `abs`／`neg`；（3）谓词 `and_z`／`orr_z`／`eor_z`／`bic_z`；（4）用户面 `convert.to/from.svbool`；（5）`cntb`／`cnth`／`cntw`／`cntd`（`i32` ImmArg 0..31→`i64`）；（6）`lasta`／`lastb`；（7）`dup.x`／`index`；（8）`zip1`／`zip2`；（9）`compact` 仅 32／64 位元素；（10）`cmpeq`／`cmpne`／`cmpge`／`cmpgt`／`cmphi`／`cmphs`。调用点必须是 `CallingConv::C`、非可变参数、无 `musttail`／operand bundle／`noreturn`／`returns_twice`／复杂 ABI。`poison`／`undef` 拒绝。`nxv8f16` 另需 `+fullfp16`。LLVM 15 `llc -mattr=+sve -fast-isel=false` 选择谓词 `add`／`mul`／`sdiv`／`sel`／`fadd`／`and p/z`／`cntb`／`lasta`／`index`／`zip1`／`compact`／`cmpeq`／`abs p/m`，解释器外壳保持谓词，不是逐车道循环。`vmp-sve-acle-common-semantic.ll`（O0/O2×97/7）覆盖上述代表形态，以及无 `+sve` ABI、`sdiv`／`compact` 的 `i8`、`poison` 负例。宿主不能执行 AArch64 SVE，故无 `lli`。
- 拒绝：`i8`／`i16` `sdiv`／`udiv`／`compact`（`Cannot select`）、SVE2、`tbl`／`sunpk`（改宽）、`fmla` 三操作数、FFR、`whilerw`、非 C、`musttail`、operand bundle、`poison`／`undef`。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际受限跳表 indirectbr 支持边界**

- 接受：仅 AArch64。在既有静态同函数 `indirectbr`（`blockaddress`／`select`／`phi`／`bitcast`，目的块无 PHI）之上，另接受地址为非原子、非 `volatile` 的地址空间零 `load ptr`，其指针来自仅本函数使用的**本地链接**（`private`／`internal`，`hasLocalLinkage`）常量全局 `[1..16 x ptr]` 跳表，或该表上两下标 GEP（首下标为 `0`，第二下标为既有整数操作数）。表项必须全是本函数且列在该 `indirectbr` 目的列表中的 `blockaddress`。规划期把表项改写成既有 `__hikari_vmp_ba.*` 标签全局，使 `deleteBody` 后 `load` 与解释器线性 `icmp eq` 比较的是同一标签，而不是已销毁基本块的 `blockaddress`。转换失败时恢复原初值并丢弃标签。不新增操作码。`vmp-indirectbr-jumptable-semantic.ll`（O0/O2×97/7）覆盖两／三路跳表、动态下标、常量下标 GEP、宿主 `lli` 差分与 AArch64 `llc`／`readobj`；外部链接负例用本函数 `blockaddress` 锁定 `hasLocalLinkage`，不是跨函数表项。既有 `vmp-indirectbr.ll` 仍覆盖 `select` 静态形态与预算回滚。
- 拒绝：`inttoptr`／任意指针实参、目的块带 PHI、`volatile`／原子 `load`、跨函数或非 `blockaddress` 表项、超过 16 项、被其它函数使用的共享表、TLS 表、**外部／weak／linkonce 等非本地链接表**，以及**被非本地 `GlobalVariable`／`GlobalAlias` 代理暴露的本地表**（改写初值仍会破坏跨模块可观察接口）。跳过原因 `unsupported indirectbr instruction`，保留 `hikari.vmp.selected`。

**当前实际语义不抛出直接 invoke 支持边界**

- 接受：仅 AArch64。`InvokeInst` 的调用点或被调函数可证明 `nounwind`（`CallBase::doesNotThrow`），被调为直接非内建 `Function*`，调用点与被调函数都必须是 `CallingConv::C`，且 `invoke` 的 `FunctionType` 必须与被调 `FunctionType` 完全同一（不透明指针下直接 `Function*` 仍可写不同原型）。其余符合既有普通直接调用 ABI／类型规则：结果与实参为 `void`／`i1`..`i64`／地址空间零指针／`half`／`f32`／`f64`／既有定长向量／既有扁平聚合，实参类型等于形参，非可变参数，无 `tail`／`musttail`／操作数捆绑／`noreturn`／`returns_twice`／内联汇编／`byval`／`byref`／其它复杂 ABI。唯一 ABI 例外是既有保守直接 C void `sret`（`isSupportedDirectSretCall`：void、固定参数、第 0 实参为地址空间零指针，调用点与被调第 0 形参带相同 `StructRet` 类型）。输出指针语义经既有 `CallDescriptor` 原样重放，函数 `personality` 写回。`i128`、IEEE `fp128`、`bfloat`、间接 `invoke` 不走本表面。展开目的块必须是不可达的最小 EH 岛：非 `blockaddress`、无 PHI、首条为 `landingpad`（只识别 `cleanup`／`catch`／`filter` 结构以便丢弃，不执行）、其后仅允许对该 landingpad 的块内 `extractvalue`、终结为对本岛值的 `resume` 或 `unreachable`；全部前驱必须是 `invoke` 的 unwind 边（不得有普通 `br`／`switch`／invoke 正常后继）；岛内值不得有块外 live 使用（含 PHI）。共享同一岛的每一条 `invoke` 都必须满足上述 nounwind 形态。不新增操作码：规划为既有 `VMOpcode::Call`（`CallDescriptor` 重放 `CreateCall`，调用约定／属性／`DebugLoc`／FMF 与普通直接调用相同）再跟一条到正常后继的 `VMOpcode::Br`。从不生成或执行展开边；成功变换时丢弃该岛。函数若带 `personality` 则在 `deleteBody` 后写回（不按符号名过滤）。既有 VMP→post-CFF 路径可继续平坦化解释器外壳。
- 拒绝：Windows funclet、间接／可变参数／捆绑／`noreturn`／`i128` `invoke`、非保守 `sret`、非 C、`FunctionType` 不一致。可达 Itanium `invoke`／`landingpad`／`resume` 见下一节，不得再作为本 lit 的形态良好负例。跳过原因保持 `exception handling`。资格失败保持原函数与 `hikari.vmp.selected`。

**当前实际 Itanium 直接 invoke／landingpad／resume 支持边界**

- 接受：仅 AArch64。函数 personality 最后必须是 `__gxx_personality_v0` 或 `__gcc_personality_v0`。直接非内建 `Function*`、`CallingConv::C`（调用点与被调）、`FunctionType` 精确同一、非可变参数、无 `tail`／`musttail`／操作数捆绑／`noreturn`／`returns_twice`／内联汇编／`sret`／`byval`／其它复杂 ABI。结果与实参为 `void`／`i1`..`i64`／地址空间零指针／`half`／`f32`／`f64`／既有定长向量／既有扁平聚合。展开目的块第一非 PHI 必须是 `{ptr,i32}` `LandingPadInst`（`cleanup`／`catch` 类型信息 Constant／`filter` 指针数组），前驱全是 unwind 到该块的合格 `invoke`。`landingpad` 结果写入既有扁平聚合虚拟寄存器；其后 `extractvalue`／`phi`／`resume` 走既有聚合／整数／控制流表面。`llvm.eh.typeid.for` 仍按独立表面重放。解释器对每条合格 `invoke` 发射原生 `InvokeInst`：正常边写入结果虚拟寄存器并把虚拟机 PC 转到正常后继（含该后继 PHI）；展开边发射带原条款的原生 `LandingPadInst`，把 `{ptr,i32}` 写入该 landingpad 的聚合虚拟寄存器，再把 PC 转到虚拟着陆块（含着陆块 PHI）。`ResumeInst` 从聚合虚拟寄存器装回同一类型并发射原生 `resume`。不把 nounwind 不可达岛改成这条路径。资格在 `deleteBody` 前全部判定；发射失败仍是 `report_fatal_error`，因此形态必须保证可验证。post-CFF 见到解释器里的 `invoke`／`landingpad`／跨块 SSA 会事务式跳过并保留已虚拟化函数。`vmp-itanium-invoke-semantic.ll`（O0/O2×97/7）覆盖正常返回、`catch`＋`eh.typeid.for`、cleanup 调用后 `resume`、着陆块 PHI／`extractvalue`、post-CFF 跳过；`@llvm.used` 在 `main` 不调用的情况下仍把五条受保护 EH 外壳留过 `internalize`／`globaldce`，AArch64 `llc`／`readobj` 检查 `.cfi_personality`／`.cfi_lsda`／`.gcc_except_table`／`.eh_frame`、直接 `bl` 调用点与 `_Unwind_Resume`（llc 保留时）。负例为间接／ObjC personality／`fastcc`／bundle／`noreturn`。宿主不能可靠执行真实展开／捕获，故该 lit 无 `lli`。正常边（非 nounwind 直接 `invoke` 到已定义、实际不抛的辅助函数，可达 cleanup `landingpad`／`resume` 保留但不执行）的宿主差分见 `vmp-itanium-invoke-normal-edge-semantic.ll`（O0/O2×97/7：AArch64 上变换并 FileCheck 仍为 `invoke`／`landingpad`／`resume` 且已 `hikari.vmp.virtualized`，辅助函数带 `optnone` 以免 `default<O2>` FunctionAttrs 推断 `nounwind` 后落入 Call+Br 岛；`internalize` 后把已变换模块 triple 换成 `x86_64` 再 `lli -force-interpreter`，与同语义 `call` 参考函数比退出码；AArch64 `llc`／`readobj` 仍检查 personality／LSDA／`bl`／`_Unwind_Resume`）。这只证明正常边结果一致，不声称展开行为。
- 证据分层（不得把下层证据写成展开已在目标机上跑通）：（1）静态 IR／codegen：`vmp-itanium-invoke-semantic.ll`（O0/O2×97/7 FileCheck＋AArch64 `llc`／`readobj`，`main` 不调用受保护外壳，靠 `@llvm.used` 防 DCE）。（2）宿主正常边：`vmp-itanium-invoke-normal-edge-semantic.ll`（`main` 调用受保护函数；`lli -force-interpreter` 只走不抛的正常边）。（3）目标机真实 unwind：**当前没有**、也**不得**添加 `REQUIRES: vmp-aarch64-runner` 对象执行回归。已文档执行器（`llvm/test/lit.cfg.py` 可选 `VMP_AARCH64_RUNNER`、`%vmp_aarch64_runner`）只保证上述 C ABI 链接／部署／运行；仓库无执行器实现，环境变量可缺省，唯一 runner lit 不含 `__cxa_*`／typeinfo。在此契约上写 `__cxa_allocate_exception`／`__cxa_throw`＋catch／cleanup＋`resume` 会被按文档实现的 C 链接器做成未解析符号硬失败，而不是“执行器不可用则跳过”。因此未添加目标展开 lit，也不声称真实捕获／再抛。最小扩展后再写：文档并实现树外执行器按 AArch64 Linux C++ Itanium EH 链接（`libstdc++` 或 `libc++`＋`libc++abi`，外加 `libgcc_s` 或 `libunwind`），解析 personality／`__cxa_*`／`_Unwind_Resume`／标准 typeinfo；若 C 链接仍是默认，增加 `--cxx-eh` 或独立 lit 特性 `vmp-aarch64-runner-cxx-eh`，使未扩展执行器跳过而非失败；对象仍由 `main` 返回退出码且 `main` 必须调用受保护函数以防 DCE；然后才用 `REQUIRES` 覆盖（a）受保护 VMP `invoke` 的 catch 返回约定值，（b）受保护 cleanup `landingpad` 做可观察清理后原生 `ResumeInst`、由外层帧捕获以验证再抛与清理。
- 拒绝：Windows `catchswitch`／`catchpad`／`cleanuppad`、间接／可变参数／捆绑／`musttail`／`noreturn`／`returns_twice`／复杂 ABI、非 Itanium personality、非 `{ptr,i32}` landingpad、地址被取的着陆块、普通前驱进入着陆块、token 逃逸、不合格共享着陆（含不合格 `invoke` 前驱）。资格失败保持原函数与 `hikari.vmp.selected`。
- `vmp-nounwind-invoke-semantic.ll`（O0/O2×aesSeed 97/7）覆盖 C++ 风格 nounwind `invoke` 正常路径、正常后继 PHI、共享岛、块内 `extractvalue`、`unreachable` 岛、void `invoke`、VMP+post-CFF、personality 写回、宿主 `lli` 差分、AArch64 `llc`／`readobj`、非 AArch64 跳过，以及 throwing／可达 handler／跨块 resume／mixed／间接／vararg／非 nounwind `sret`／`i128`／bundle／`noreturn`／调用点 `fastcc`／被调 `fastcc`／精确 `FunctionType` 不一致负例。形态良好 `callbr` 已移到 `vmp-inline-asm-callbr-semantic.ll`，不得再作为本 lit 的 `exception handling` 哨兵。`vmp-nounwind-invoke-sret-semantic.ll` 覆盖通过 nounwind `invoke` 用保守 `sret` 返回小型结构体、自身 `sret` 形参转发、post-CFF、宿主 `lli` 与 AArch64 `llc`／`readobj`，以及 `sret`+`byval`／`byref`／`swifterror`／非首参／`fastcc`／vararg／throwing／可达 handler 负例。

**当前实际 llvm.experimental.noalias.scope.decl 支持边界**

- 接受：按内建 ID 丢弃全部 `llvm.experimental.noalias.scope.decl`（与 `lifetime.start`／`lifetime.end` 相同），包括普通 `tail`、`fastcc`、挂在该标记上的 `musttail`、以及该标记上的 operand bundle。默认 `O2` 内联器在内联带 `noalias` 参数的函数时会插入该内建（`use-noalias-intrinsic-during-inlining` 默认为真）。该内建对晚于优化的 VMP 没有可观察程序语义，不得因为标记的调用约定或捆绑而跳过整个函数；早期 `getUnsupportedReason` 在 `musttail` 检查之前忽略该 ID。不经 `CallDescriptor` 重放，不新增操作码，不占用虚拟寄存器，不调用 `getFastMathFlags()`（`void`，不是 `FPMathOperator`）。与声明配对的 `!alias.scope`／`!noalias` 在共用 `applyMetadata` 路径中被保守剥离，避免重建后的本机 `load`／`store`／原子／调用带上没有对应动态声明的 Scoped-AA 承诺；`!tbaa` 等其它元数据原样保留。可观察语义不变，只是放弃该别名优化。不改变 `assume` 操作数捆绑、TLS、EH 或其它标记内建。`vmp-noalias-scope-decl-semantic.ll` 覆盖单声明、普通 `tail`、两个不同 scope、与 `lifetime` 共存、带 `!alias.scope`／`!noalias`／`!tbaa` 的 `load`／`store`、钻石 `phi`、回边循环、`fastcc`／标记上的 `musttail`／deopt bundle，以及“声明 + 对其它函数的真实 `musttail`”负例。宿主 `lli` 可对整数载荷做差分；同时走 FileCheck 与 AArch64 `llc`／`readobj`。
- 拒绝：该标记本身不再作为跳过原因。函数中其它不受支持的形态（例如对其它函数的 `musttail`）仍按原诊断跳过；资格失败保持原函数与 `hikari.vmp.selected`（真实 `musttail` 仍走既有早期 `musttail call` 诊断）。

**当前实际 llvm.assume 知识操作数捆绑支持边界**

- 接受：精确 `llvm.assume`，`void`，一个普通 `i1` 条件（常量 `true` 或既有整数 SSA）。无捆绑时保持原 plain assume 表面。有捆绑时，每个捆绑必须是下列知识标签之一，且可混用：`"align"`（地址空间零指针 + 2 的幂 `ConstantInt` 对齐或普通非常量 `i64` 对齐，可选 `ConstantInt` 偏移或普通非常量 `i64` 偏移；常量对齐／偏移仍走立即数，动态 `i64` 走既有整数虚拟寄存器载荷；本仓库 `clang -O2` 对 `__builtin_assume_aligned` 的形态）、`"nonnull"`（一个地址空间零普通指针）、`"noundef"`（一个普通 `i1`..`i64` 或地址空间零指针）、`"dereferenceable"`／`"dereferenceable_or_null"`（地址空间零指针 + 非负 `ConstantInt` 字节数）、`"cold"`（零操作数；LLVM 15 `AssumeBundleBuilder::isUsefullToPreserve` 的最后一项）。经既有 `CallDescriptor` 重放：标签名写入描述符，条件与非常量操作数走虚拟寄存器，对齐／偏移／长度保持立即数，`cold` 不占用额外载荷；不新增操作码，发射时按记录的标签重建捆绑而不是写死 `"align"`。要求 `CallingConv::C`、精确非可变参数 `void(i1)`、条件类型等于形参。普通 `tail` 接受并按非 tail `CreateCall`／`TCK_None` 重放。`vmp-assume-align-bundle-semantic.ll` 覆盖无捆绑 assume、原 `align` 形态、动态 `i64` 对齐／偏移、`nonnull`／`noundef`／两种 `dereferenceable`／`cold` 与混用，以及 `"ignore"`／`deopt`／`fastcc`／`noreturn`／`sret`／AS1／对其它函数的真实 `musttail`／`poison`／`undef`／非 2 的幂对齐负例。宿主 `lli` 剥掉 assume 后对整数载荷做差分；同时走 FileCheck 与 AArch64 `llc`／`readobj`。
- 拒绝：其它捆绑标签（`ignore`／`deopt`／`nofree`／`nounwind` 等）、混有不受支持标签、带操作数的 `"cold"`、浮点／向量／聚合／`i128` 的 `noundef`、非 `i64` 的运行时对齐／偏移、非 2 的幂 `ConstantInt` 对齐、负的 `dereferenceable` 长度、`poison`／`undef` 指针或对齐／`noundef` 操作数、`musttail`、`fastcc`／非 C、`noreturn`／`returns_twice`、复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（真实 `musttail` 仍走既有早期 `musttail call` 诊断）。

**当前实际 llvm.stackprotector 支持边界**

- 接受：精确 `llvm.stackprotector`，`CallingConv::C`，非可变参数 `FunctionType` `void(ptr, ptr)`，实参类型等于形参。第 0 个是 cookie（地址空间零普通指针，不是 `poison`／`undef`），经指针虚拟寄存器重放；第 1 个是同函数入口静态 `alloca` 的保护槽，且分配类型为地址空间零指针，发射时必须是解释器入口新建的实际 `AllocaInst`（验证器要求槽是 alloca，不能是虚拟寄存器 load）。不新增操作码，不插入 `__stack_chk_guard` 或 `ssp`／`sspstrong`／`sspreq`；函数上已有的这些属性原样保留，AArch64 `llc` 因此会生成 `__stack_chk_fail`。普通 `tail` 接受并按非 tail `CreateCall`／`TCK_None` 重放。`vmp-stackprotector-semantic.ll` 覆盖 `inttoptr` cookie 写入后再读回、槽为 `%vmp.stack` alloca、带 `sspstrong` 的真实栈保护路径、`@__stack_chk_guard` 地址作 cookie、钻石 `phi`，以及 `noreturn`／bundle／`fastcc`／AS1 形参／动态槽／`sret`／对其它函数的真实 `musttail`／非指针 `alloca` 槽／`undef` cookie 负例。`vmp-stackprotector-skip.ll` 钉住 `alloca i64` 槽仍跳过（`unsupported stackprotector`）。宿主 `lli` 可对整数 cookie 载荷做差分；同时走 FileCheck、AArch64 `llc`／`readobj`／汇编与对象符号 `__stack_chk_fail`。
- 拒绝：非同函数静态指针 `alloca` 槽、动态 `alloca`、`swifterror`／`inalloca`、`poison`／`undef`、`musttail`、operand bundle、`fastcc`／非 C、可变参数、`noreturn`、复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（真实 `musttail` 仍走既有早期 `musttail call` 诊断）。

**当前实际 thread_local 全局变量支持边界**

- 接受：地址空间零的 `thread_local` GlobalValue（含默认通用动态模型与 `initialexec`）作为既有普通指针常量。`clang -O2` 对 `_Thread_local int t; return t+1` 发出 `load i32, ptr @t`；该地址写入指针虚拟寄存器帧，由 AArch64 ISel 降低 TLS，解释器在同一线程上运行。不新增操作码，不引入 `llvm.threadlocal.address`（LLVM 15 无此内建）。`load`／`store`／`GEP`／指针比较／把 TLS 地址作为普通直接 C 调用实参均复用既有路径。早期 `getUnsupportedReason` 不再因操作数是 TLS 全局而跳过整个函数。`vmp-tls-global-semantic.ll` 覆盖默认 TLS 的 load+add、store 后再 load、`initialexec`、数组 `GEP`、TLS 地址与指针参数的 `icmp`、把 TLS 地址传给其它函数、钻石 `phi`、回边循环，以及“TLS 载荷 + 对其它函数的真实 `musttail`”负例。宿主 `lli` 会分配 TLS 存储但不套用初值，用例在每次调用前写入已知值后做整数差分；同时走 FileCheck 与 AArch64 `llc`／`readobj`。
- 拒绝：非零地址空间、不受支持的载荷类型（`fp128`／`bfloat` 等）仍按原类型／指令门禁跳过。不改变 EH、协程或其它 TLS 运行时模型。资格失败保持原函数与 `hikari.vmp.selected`（真实 `musttail` 仍走既有早期 `musttail call` 诊断）。

**当前实际 llvm.is.constant 支持边界**

- 接受：精确 `llvm.is.constant`（`__builtin_constant_p`），结果 `i1`，恰好一个操作数，类型为既有标量整数 `i1`／`i8`／`i16`／`i32`／`i64`、受限标量 `i128`、地址空间零指针、标量 `half`／`f32`／`f64`，或既有固定向量（总原始宽度 1..128，元素 `i1`／`i8`／`i16`／`i32`／`i64`／`half`／`f32`／`f64`）。必须在规划期按原操作数判定，降为把 `i1` `true`／`false` 写入整数虚拟寄存器的 `Move`，**不得**经 `CallDescriptor` 重放：重放操作数是虚拟寄存器 load，永远不是 manifest 常量，原先对常量会得到 `true` 的调用会全部变成 `false`。判定与 `LowerConstantIntrinsics`／`Constant::isManifestConstant` 一致：`ConstantInt`／`ConstantFP`／`null` 以及仅由这些构成的 `ConstantExpr`（含 `inttoptr`、整数 `add` 常量表达式、`i128` 常量与固定向量常量）为 `true`；`GlobalValue`／`BlockAddress` 以及指向全局的 `GEP` 常量虽是 `Constant` 但不是 manifest，为 `false`；运行时 SSA（参数、`phi`、未折叠的 `add`、运行时 `i128`／向量等）为 `false`。普通 `tail` 降为普通 `Move`。不新增操作码，不把操作数装入虚拟寄存器。`vmp-is-constant-semantic.ll` 覆盖 `i32` 字面量、运行时参数、`i1`／`i8`／`i16`／`i64`、`ptr null`、全局地址、`inttoptr` 常量、全局 `GEP` 常量、`half`／`f32`／`f64` 字面量、整数 `ConstantExpr`、易失 load 后的 SSA `add`、普通 `tail`、`select`、钻石 `phi`、回边循环、指针参数、`i128` 字面量／运行时／`ConstantExpr`、`<2 x i32>` 常量／运行时、`<4 x i32>` 与 `<2 x float>` 常量，以及 `poison`／`undef`／对其它函数的真实 `musttail`／operand bundle／`i2`／非零地址空间／超宽 `<8 x i32>` 负例。宿主 `lli` 不能执行该内建，故 `main` 按已知 `0`／`1` 做差分；同时走 FileCheck 与 AArch64 `llc`／`readobj`。
- 拒绝：非 `i1`／`i8`／`i16`／`i32`／`i64`／`i128` 整数宽、聚合、`bfloat`／`fp128`、可伸缩或总宽超过 128 的向量、非零地址空间、`poison`／`undef`、`musttail`、operand bundle、复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（真实 `musttail` 仍走既有早期 `musttail call` 诊断；`i2`／非零地址空间／超宽向量参数仍走既有 `unsupported argument type`）。

**当前实际 llvm.ptrauth 支持边界**

- 接受：精确通用 `llvm.ptrauth.sign`／`llvm.ptrauth.sign_generic`（不是 `llvm.aarch64.*`）。结果与值／鉴别器均为普通 `i64`。`sign` 的密钥是 `i32` `ImmArg` `ConstantInt` 0..3（ASIA／ASIB／ASDA／ASDB），经既有 `CallDescriptor` 立即数重放，不得进虚拟寄存器。`sign_generic` 为两个 `i64` 虚拟寄存器（PACGA）。函数 `target-features` 最后令牌须为精确 `+pauth`（`+pauth2`／缺失／最后 `-pauth` 不算）。经既有 `CallDescriptor` 重放，不新增操作码。本仓库 LLVM 15 AArch64 SDAG 只选择这两个 ID；`auth`／`strip`／`resign`／`blend` 与 `"ptrauth"` 操作数捆绑仍拒绝。普通 `tail` 由通用接受门收下并按 `TCK_None` 重放。资格不看命令行 `-mattr`。`vmp-ptrauth-intrinsic-semantic.ll` 覆盖四把密钥、零鉴别器、`sign_generic`、多特征最后 `+pauth`、普通 `tail`、钻石 `phi`、回边循环，以及缺失／禁用／`+pauth2` 特征负例、`auth`／`strip`／`resign`／`blend`／密钥 4／对其它函数的真实 `musttail`／`poison`／operand bundle 负例。宿主不能执行 PAC，故不做 `lli`；对 `main` 可达子集做 FileCheck、AArch64 `llc`／`readobj`，汇编中须出现 `pacia`／`pacib`／`pacda`／`pacdb`／`pacga`。
- 拒绝：`llvm.ptrauth.auth`／`strip`／`resign`／`blend`、调用点 `"ptrauth"` 捆绑、密钥越界、`poison`／`undef`、`musttail`、operand bundle、复杂 ABI、缺少或最后令牌为 `-pauth`／仅 `+pauth2`。资格失败保持原函数与 `hikari.vmp.selected`（真实 `musttail` 仍走既有早期 `musttail call` 诊断；形态正确但缺 `+pauth` 走 `unsupported target feature`）。

**当前实际 llvm.vector.insert／llvm.vector.extract 支持边界**

- 接受：精确 `llvm.vector.insert`／`llvm.vector.extract`，两端均为既有固定向量（总原始宽度 1..128，元素 `i1`／`i8`／`i16`／`i32`／`i64`／`half`／`f32`／`f64`），元素类型相同。下标是非负 `i64` `ConstantInt` `ImmArg`，必须是子向量长度的倍数且不越界（与验证器一致）。`extract` 降为一次既有 `VectorShuffle`（两个源都是原向量，掩码为连续下标）；`insert` 降为两次 `VectorShuffle`（先把子向量拓宽到目的宽度，再按目的／拓宽结果混合）。**不得**经 `CallDescriptor` 重放：宿主 `lli` 不能执行该内建。普通 `tail` 降为普通 shuffle。不新增操作码。`vmp-vector-insert-extract-semantic.ll` 覆盖从 `<4 x i32>` 提取低／高半、单车道 `<1 x i32>`、整向量恒等提取、向低／高半与整向量插入、`f32` 提取／插入、`<8 x i8>` 插入 `<4 x i8>`、普通 `tail`、钻石 `phi`、回边循环，以及超宽 `<8 x i32>`／`poison`／`undef`／对其它函数的真实 `musttail`／operand bundle／插入 `poison` 子向量负例（可伸缩仍拒绝，但不放进同一 `llc` 模块以免无 `+sve` 的 ISel 无法合法化）。参考函数用等价 `shufflevector` 做宿主 `lli` 差分；同时走 FileCheck 与 AArch64 `llc`／`readobj`。
- 拒绝：可伸缩向量、总宽超过 128、元素类型不同、越界或非倍数下标（验证器也不接受）、`poison`／`undef` 向量操作数、`musttail`、operand bundle、复杂 ABI。资格失败保持原函数与 `hikari.vmp.selected`（真实 `musttail` 仍走既有早期 `musttail call` 诊断；超宽／可伸缩参数仍走既有 `unsupported argument type`）。

## 8. 测试策略

- **IR（中间表示）级**：使用 `opt`（优化工具）运行转换，检查验证器、目标函数筛选、跳过诊断和生成结构；
- **行为级**：为每类指令编写小型 C（编程语言）或 C++（编程语言）程序，对比虚拟化前后的退出码、标准输出与内存结果；
- **组合级**：覆盖 `O0`（无优化）、`O2`（常规优化）、LTO（链接时优化）前后流程，以及与字符串加密、调用点混淆的组合；
- **叠加级**：分别验证仅 VMP（虚拟机保护）、仅 CFF（控制流平坦化）和 VMP（虚拟机保护）→ CFF（控制流平坦化）；检查完整性失败路径、无效操作码路径、预算回退和重复运行；
- **随机级**：固定随机种子进行回归；多种种子循环执行以发现编号或状态初始化问题；
- **负向级**：为每个暂不支持特性建立“安全跳过”测试，确保不会悄然生成错误代码。
- **并发与目标级**：在真实 AArch64（64 位 ARM（精简指令集）架构）环境运行原子 `litmus`（弱内存模型）与递归调用用例；宿主解释器只作为快速差分检查，不能替代目标设备验证；
- **性能级**：收集单函数代码尺寸、编译耗时和运行时基准，并对超过预算的函数验证“安全跳过且保留选择属性”。

## 9. 风险与控制措施

| 风险 | 控制措施 |
| --- | --- |
| 语义偏差 | 先限制指令集；每次扩展前增加差分测试与验证器检查。 |
| 优化消解调度器 | 将 VMP（虚拟机保护）置于优化后期；对关键状态访问使用最小必要的防优化约束。 |
| 体积和性能失控 | 默认关闭、逐函数启用、设置函数大小阈值，并记录转换前后指标。 |
| 随机源初始化顺序 | VMP（虚拟机保护）自身保证随机源可用；独立运行编译遍与完整流水线都必须覆盖。 |
| 模块状态泄漏 | 密钥去重状态限定在模块编译生命周期，模块释放后同步释放；增加多模块压力测试。 |
| 字节码可恢复 | 将异或白化定位为混淆而非加密；后续通过多模板处理器、完整性校验和调度分散提高恢复成本。 |
| 原子并发偏差 | 保留原始内存序和同步域；在真实 AArch64（64 位 ARM（精简指令集）架构）环境补充多线程 `litmus`（弱内存模型）测试。 |
| 测试基础设施失效 | 修复 `llvm-lit`（LLVM 测试运行器）配置，并在持续集成中执行选择性 VMP（虚拟机保护）回归。 |
| LLVM 15 不透明指针兼容性 | 新代码只使用不透明指针兼容接口；不重复切换全局指针模式。 |
| 与既有混淆冲突 | 为虚拟化函数建立排他策略，组合功能按测试结果逐步开放。 |
| 后期平坦化破坏虚拟机状态 | 保留所有易失访问和陷阱路径；在函数克隆上转换并验证，失败时回退到原 VMP（虚拟机保护）外壳。 |
| 解释器叠加导致体积爆炸 | 设置后期 CFF（控制流平坦化）的基本块与指令预算；超过预算时跳过后处理。 |

## 10. 完成定义

当以下条件同时满足时，第一阶段 VMP（虚拟机保护）视为完成：

1. 显式开关和逐函数标记均可可靠启用或禁用；
2. 已声明支持的指令集在 AArch64（64 位 ARM（精简指令集）架构）转换流程中通过行为与验证器测试；宿主 X86（x86 架构）仅用于修改目标三元组后的解释器行为对比；
3. 所有不支持特性均安全跳过且有可读诊断；
4. 默认编译路径与未启用 VMP（虚拟机保护）的当前行为一致；
5. 回归测试、构建说明和已知限制已随代码提交。
6. `llvm-lit`（LLVM 测试运行器）选择性回归、真实 AArch64（64 位 ARM（精简指令集）架构）对象生成与目标设备性能记录均可复现；
7. 发布说明明确 VMP（虚拟机保护）当前提供的是混淆增强，不是密码学保密保证。
8. 同时选择 VMP（虚拟机保护）和 CFF（控制流平坦化）时严格执行 VMP（虚拟机保护）→ CFF（控制流平坦化），组合失败能够安全回退到有效的 VMP（虚拟机保护）函数体。
9. 完成覆盖以“指令形态”为单位度量：每一个声明支持的指令形态都有正向行为测试（`O0`（无优化）与 `O2`（常规优化）验证器、宿主 `lli`（LLVM 即时执行器）差分与 AArch64（64 位 ARM（精简指令集）架构）对象生成），每一个未支持形态都有稳定“安全跳过”测试；不以“所有 LLVM（编译器框架）IR（中间表示）语法都能转换”作为当前完成标准。
